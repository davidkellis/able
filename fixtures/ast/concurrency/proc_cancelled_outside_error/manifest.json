{
  "description": "proc_cancelled raises when called outside of proc/spawn",
  "expect": {
    "errors": [
      "proc_cancelled must be called inside an asynchronous task",
      "typechecker: proc_cancelled must be called inside an asynchronous task"
    ],
    "typecheckDiagnostics": [
      "typechecker: proc_cancelled must be called inside an asynchronous task"
    ]
  },
  "entry": "module.json"
}
