ran := 0
handle := proc {
  ran += 1
  7
}
handle.cancel()
first_raw := handle.value()
second_raw := handle.value()
first := `${first_raw}`
second := `${second_raw}`
ran_string := `${ran}`
[first, second, ran_string]
