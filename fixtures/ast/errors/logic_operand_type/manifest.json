{
  "description": "Logical operators require boolean operands",
  "expect": {
    "errors": [
      "Logical operands must be bool",
      "typechecker: '&&' left operand must be bool (got string)"
    ],
    "typecheckDiagnostics": [
      "typechecker: '&&' left operand must be bool (got string)"
    ]
  },
  "entry": "module.json"
}
