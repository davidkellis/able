The session I want to resume from is 019c45a6-40ef-7223-b8a4-f41ab64d9d6d, and I want to continue iterating on the Compiler AOT work. I am wanting you to supervise the continued iteration on the compiler AOT work in that session. At the conclusion of each prompt response codex will suggest related tasks as next steps, and if the next steps are in line with the task at hand and would help to complete the task at hand, then I want those suggested next steps to be completed. I have been prompting like this: I start a new session and prompt with "read and follow the PLAN file prompts" to have the agent onboard onto a new (non-resumed) session to start work on the next outstanding task in the TODO list. Then when it completes the first response, it will usually tell me what it did and suggest some next steps to continue iterating on the task at hand. Then I will reply "proceed as you have suggested. remember, we want to focus on correctness" or something similar, as long as the proposed next steps would further the progress on the task at hand. If there are no suggested tasks or if the suggested next steps are ancillary to the task at hand and don't need to be done as part of the task at hand, then I will note the important items and put them at the bottom of the task list in the PLAN file as optional follow-up items. The main goal is to drive the work captured in the PLAN file on the Compiler AOT work stream to completion. Can you do that by supervising an agent to complete that work?

Make sure that the agents make corrections as needed to the compiler and test suites to keep the test suites passing and to ensure that the compiler and the test suites conform to the v12 spec. Correctness is key, and test coverage is critical to ensure that all compiler behavior matches the v12 spec.

Also, give each agent full access to the computer so it can run commands in its sandbox without running into permissions errors.
