package exec_06_01_compiler_spawn_await

import able.concurrency.{Await}

fn main() -> void {
  result := "pending"
  handle := spawn {
    result = await [Await.default({ => "idle" })]
    result
  }
  future_flush()
  print(result)
  print(handle.value())
}
