package exec_06_01_compiler_map_literal

import able.collections.hash_map.*

fn build() -> HashMap String i32 {
  return #{ "a": 1, "b": 2 }
}

fn main() -> void {
  values := build()
  print(`map ${values["a"]!} ${values["b"]!}`)
}
