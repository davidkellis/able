package exec_06_01_compiler_for_loop_typed_pattern_mismatch

## Semantics: compiler errors when for-loop typed pattern bindings mismatch.

union Val = i32 | String

fn main() -> void {
  values: Array Val := [1, "hi", 3]
  sum := 0
  for value: i32 in values {
    sum = sum + value
  }
  print(`sum ${sum}`)
}
