package exec_06_01_compiler_assignment_pattern_rest_mismatch

## Semantics: rest pattern mismatches yield an Error value (falsy).

fn main() -> void {
  data := [1]
  if [first, second, ...tail] := data {
    print("ok")
  } else {
    print("error")
  }
}
