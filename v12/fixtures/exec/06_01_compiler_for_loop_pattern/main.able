package exec_06_01_compiler_for_loop_pattern

## Semantics: compiler lowers for loops with pattern destructuring.

fn main() -> void {
  pairs := [[1, 2], [3, 4], [5, 6]]
  sum := 0
  for [x, y] in pairs {
    sum = sum + x + y
  }
  print(`sum ${sum}`)
}
