package exec_06_01_compiler_assignment_pattern_positional_mismatch

## Semantics: compiler errors when positional struct patterns have mismatched arity.

struct Pair { i32, i32 }

fn main() -> void {
  value := Pair { 1, 2 }
  Pair { first, second, third } := value
  print(first + second + third)
}
