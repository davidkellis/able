package exec_06_01_compiler_assignment_pattern_errors

## Semantics: pattern assignment mismatches yield an Error value (falsy).

fn main() -> void {
  data := [1, 2]
  if [first, second, third] := data {
    print("ok")
  } else {
    print("error")
  }
}
