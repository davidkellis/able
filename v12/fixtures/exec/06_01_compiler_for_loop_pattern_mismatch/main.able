package exec_06_01_compiler_for_loop_pattern_mismatch

## Semantics: compiler reports runtime errors for for-loop pattern mismatches.

fn main() -> void {
  pairs := [[1], [2, 3]]
  for [x, y] in pairs {
    print(x + y)
  }
}
