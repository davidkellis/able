ran := 0;
handle := spawn {
  ran += 1;
  7;
};
handle.cancel();
first_raw := handle.value();
second_raw := handle.value();
first := `${first_raw}`;
second := `${second_raw}`;
ran_String := `${ran}`;
[first, second, ran_String];
