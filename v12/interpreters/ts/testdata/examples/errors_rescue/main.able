package errors

fn describe(value: i32) -> String {
  do {
    if value % 2 == 0 {
      raise "even value"
    }
    `odd value ${value}`
  } rescue {
    case _ => "handled even"
  }
}

fn main() {
  print(describe(3))
  print(describe(6))
}
