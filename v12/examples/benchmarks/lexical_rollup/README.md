# Lexical Rollup

This application-shaped benchmark reads the checked-in ENABLE word list,
filters selected words through Array's ordinary public `Iterable` API, maps
weighted scores, collects those scores into an Array, and reduces one
deterministic summary. It is an external iterator/collection control, not
authorization for a String, Array, callback, or word-list-specific fast path.
