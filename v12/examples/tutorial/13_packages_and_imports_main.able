package main

import tutorial.tutorial13_math::math
import tutorial.tutorial13_math.{Pair}
import tutorial.tutorial13_strings.{shout, make_label}

fn main() -> void {
  print(`2 + 3 with bias -> ${math.add(2, 3)}`)

  pair := Pair { left: 4, right: 5 }
  print(`pair_sum -> ${math.pair_sum(pair)}`)

  label := make_label("item", 7)
  print(shout(label))
}
