# Able Project Log

## 2026-07-26 — Largest compiled-miss owner intersection closed

- Retained no production or test code. Across four unlike target misses and
  two parity controls, 60 main-only CPU profiles and 18 exact main-allocation
  measurements found no exact compiler/generated-runtime owner meeting the
  three-unlike-application admission gate.
- Tapelang is native primitive/Array work, K-Nucleotide is the closed
  runtime-backed map-storage route, Sudoku is search/checked arithmetic and
  application Array allocation, and Mutex Work Queue is the rejected
  `currentGID`/`runtime.Stack` context route. Pidigits and Binary Trees instead
  remain native Go arithmetic and allocation/GC controls.
- Six strict builds and all 84 retained evidence processes verified. Every
  96-package graph omits the interpreter. An incomplete Binary Trees
  allocation profile was excluded after its profiling observer exceeded the
  one-minute limit; three exact lightweight MemStats runs replaced it.
- Next run a corpus-wide exact-owner frequency census over all current strict
  misses. This entails consolidating compatible profiles, refreshing missing
  rows, and selecting only a non-closed exact owner material in three unlike
  applications. It is important because a widespread smaller lowering tax may
  exist even though the four largest gaps are unrelated; a negative result
  will close local compiled profiling and move the roadmap to typed bytecode
  slots. See
  `docs/perf-baselines/2026-07-26-compiled-largest-miss-owner-intersection-closure.md`.

## 2026-07-25 — Full compiled scorecard refreshed

- The consolidated strict scorecard now contains 61/61 clean rows:
  305/305 Able and 305/305 fresh Go executions verified. Six rows meet the
  95%-of-Go target, five beat Go, and the geometric-mean ratio is 6.2847x.
- Retained general benchmark support for strict argument forwarding, inner
  artifacts, and isolated timing output. The last prevents Go GC trace writes
  from corrupting `/usr/bin/time` lines.
- Fixed a generated multi-receiver channel race caused by checking `len(ch)`
  before a direct receive. Concurrent Text Index moved from 3/20 stress
  timeouts to 100/100 clean runs; all 14 channel-reaching rows passed 70/70
  post-fix scorecard executions.
- Every retained strict dependency graph omits the interpreter. No application,
  stdlib, interpreter, VM, language, dependency, nominal special case, or WASM
  change was made.
- Next intersect exact main-only CPU/allocation owners across Tapelang,
  K-Nucleotide, Sudoku Masks, and Mutex Work Queue, with Pidigits and Binary
  Trees as controls. This is important because those unlike rows contain the
  four largest absolute gaps. See
  `docs/perf-baselines/2026-07-25-full-compiled-scorecard-refreshed.md`.

## 2026-07-25 — Compiled scorecard availability repaired

- Retained only a general harness correction that independently bounds
  generated Go emission and generated-module compilation and honors a supplied
  disk-backed `GOCACHE`. All six formerly unavailable rows fit the unchanged
  one-minute limit in each genuine phase.
- Two final five-run passes across all eight repair rows produced 80/80 public
  verifier successes with no timeout or runtime failure. Every inspected
  strict dependency graph has 96 packages and omits the interpreter; the two
  formerly flaky concurrency rows each passed 10/10.
- The restored rows remain 7.44x-16.72x slower than current Go references. No
  compiler, generated-code, runtime, stdlib, interpreter, VM, language,
  dependency, or WASM change was made.
- Next refresh the complete compiled scorecard with this phase-split harness,
  five verified runs, fresh Go references, and strict dependency audits. This
  is important because the next general lowering/runtime owner needs one
  complete current feature corpus. See
  `docs/perf-baselines/2026-07-25-compiled-scorecard-availability-repaired.md`.

## 2026-07-25 — Compiled runtime-value boundary census closed

- Retained no production or test code. Seven strict telemetry and seven
  normal generated binaries all have 96-package graphs without the
  interpreter; 196/196 telemetry and main-profile executions verified.
- Dependency Plan and Array Slice Window reach no typed/runtime boundary.
  Option/Result, Sensor Calibration, and Manifest Normalization share one
  material exact leaf: the single 128-byte positional runtime struct instance
  required for 6,432, 1,280, and 7,168 explicit nominal encodes per main.
- The retained positional encoding is already at one escaping semantic object.
  Further elimination requires the excluded broad Result/control ABI or
  caller-owned nominal architecture. Union AST/type construction is material
  only in Option/Result, while `control_from_error` is not a shared CPU owner.
- Next refresh and close the eight failed strict scorecard rows with bounded
  build-stage attribution and repeated verifier-backed execution. This matters
  because correctness and availability across regex, validation, nominal, and
  concurrency features must precede selection of another performance owner.
  See
  `docs/perf-baselines/2026-07-25-compiled-runtime-value-boundary-census-closure.md`.

## 2026-07-25 — Static-i64 checked-addition route closed

- Retained no production or test code. A general primitive-only static-i64
  addition/sign-bit overflow path preserved dynamic and narrower widths,
  diagnostics, and control semantics; all five corrected-candidate strict
  graphs contained 96 packages and omitted the interpreter.
- Across 120 accepted processes per baseline/candidate/Go binary and
  application, Concurrent improved 1.090% and Array 4.972%, but Sensor was
  neutral pooled and reversed from a 2.974% improvement to a 2.550%
  regression. Option/Result and the zero-reach Monte Carlo control were also
  neutral, so the explicit admitted-row gate required complete removal.
- All 1,800 authoritative timing processes plus 360 warmups verified.
  Generated-source, assembly, extrema, overflow, recurrence, dependency, and
  restored `ablec` tests pass. Arithmetic-helper microbranches are now closed.
- Next run a feature-diverse typed runtime-value boundary census over at least
  six strict target misses. This entails exact telemetry/caller attribution
  correlated with main-only CPU and allocation profiles; it matters because
  locating repeated boxing or runtime dispatch is the direct route back to
  native-carrier Go parity. See
  `docs/perf-baselines/2026-07-25-compiled-static-i64-checked-addition-closure.md`.

## 2026-07-25 — Compiled main-phase owner intersection closed

- Retained no code. Checked signed multiplication was the only exact
  compiler-owned CPU leaf material in three of four unlike applications;
  exact allocation ownership split among Array copies, union/type metadata,
  parsing/conversion, and concurrent nominal/interface work.
- A general nonnegative i64 `bits.Mul64` prototype reduced the selected helper
  samples, but 120 accepted processes per baseline/candidate/Go binary left
  Concurrent neutral, Array +0.241% slower, Option/Result -1.350%, and Sensor
  -3.053%. The mixed gate required complete prototype removal.
- All 2,504 accepted diagnostic and measurement processes plus 288 warmups
  verified. Focused source, recurrence, and overflow tests pass after removal;
  all strict dependency graphs omit the interpreter. No compiler, runtime,
  stdlib, interpreter, VM, language, dependency, or WASM change was retained.
- Next gate static-width checked addition across the three material rows with
  two controls. This entails native addition/sign-bit overflow lowering plus
  exact semantic and balanced A/B/Go gates; it matters because this is the
  last exact shared primitive helper before arithmetic microbranch work should
  close. See
  `docs/perf-baselines/2026-07-25-compiled-main-phase-owner-intersection-closure.md`.

## 2026-07-25 — Compiled post-package-cut fixed-cost route closed

- Retained no code. Six unlike strict applications now measure
  3.743-15.915 ms in Able versus 1.025-2.319 ms in Go at high resolution;
  their generated bootstrap/registration phases take only 0.393-0.930 ms.
- No exact package, method, interface, import-seeding, initialization, or
  allocation descendant is material in three unlike applications. Repeated
  AST metadata allocation is semantically rooted, and declaration-only
  parser/driver imports remain an already-closed linker route.
- All 1,974 accepted diagnostic and measurement processes plus 180 warmups
  verified. All six strict graphs contain 96 packages, omit the interpreter,
  and focused `ablec` tests pass. No compiler, runtime, stdlib, interpreter,
  VM, language, dependency, or WASM change was made.
- Next intersect main-phase CPU and allocation owners across the four short
  applications allocating at least about 1 MB after registration. This
  entails exact shared-leaf attribution and a three-unlike-program admission
  gate; it matters because the remaining material gap is in lowered
  application execution, not launch registration. See
  `docs/perf-baselines/2026-07-25-compiled-post-package-cut-fixed-cost-closure.md`.

## 2026-07-25 — Compiled Array range-proof admission closed

- Retained no code. All 46 current complete strict target misses generated;
  the application-owned census found 278 native Array guards across 30
  applications, while 16 applications have none.
- One exact material proof does not repeat in three unlike applications:
  NBody needs sibling parameter-Array length contracts, Sudoku recursive
  fixed-shape facts, Tapelang parallel-Array and jump/Tape invariants, and
  Matrix square nested-Array facts. The local same-Array controls were cold or
  negligible.
- All 100 profile processes verified. Six strict builds passed and all six
  96-package graphs omit the interpreter. No compiler, runtime, stdlib,
  interpreter, VM, language, dependency, or WASM change was made.
- Next decompose the residual fixed launch cost in at least six unlike short
  strict applications and their Go equivalents. This entails alternating
  verified process launches plus init/bootstrap/application attribution; it
  matters because a shared 20-50 ms floor prevents short native compiled tools
  from approaching Go end-to-end. See
  `docs/perf-baselines/2026-07-25-compiled-array-range-proof-admission-closure.md`.

## 2026-07-25 — Compiled compute/control cohort closed

- Retained no production code because Tapelang Alphabet, Matrix Multiply, and
  Fib do not share one material primitive, Array, or control-flow owner.
  Ten verified CPU profiles and three exact allocation runs per application
  separate their costs into Tapelang dispatch/Array/checked mutation, Matrix
  native `f64` plus Array guards, and Fib native recursion/control returns.
- A fresh conservative control/effect census finds Fib control-free, Matrix
  `matmul` and Tapelang `execute`/`Tape.inc` fallible, and zero call-site
  specializations in every row. The Fib-only value ABI remains inadmissible.
- Assembly inspection shows Matrix's large `k++` line sample reflects
  concentration inside the complete tight loop, not evidence of a separate
  lowering helper; direct increment lowering is already present.
- All 3 strict builds passed; all 42 smoke, allocation, and CPU-profile
  processes verified; and every 96-package graph omits the interpreter. No
  compiler, stdlib, runtime, interpreter, VM, language, dependency, or WASM
  change was made.
- Next run a coverage-wide native Array range-proof admission census. This
  entails exact guard inventory, repeated dynamic attribution, and a
  three-unlike-application gate before any proof implementation. It matters
  because redundant semantic guards can prevent native Array carriers from
  reaching equivalent Go machine code. See
  `docs/perf-baselines/2026-07-25-compiled-compute-control-cohort-closure.md`.

## 2026-07-25 — Current compiled scorecard and generic-storage route closed

- Fresh five-run measurements cover all 61 Go references and strict compiled
  Able applications. Go verified 305/305 processes; the latest Able rows
  contain 53 complete comparisons, seven target meets, 46 misses, and eight
  incomplete preparation/timing rows.
- Exact allocation and repeated CPU profiles show the same native-specialized
  key/value to runtime-backed storage transition in K-Nucleotide, Inventory
  Reconciliation, and Word Frequency. Binary Event Log's dominant owner is
  instead record conversion/writeback.
- Retained no code because a narrow fix would name stdlib `HashMap`; the
  general semantic parent is shared typed storage for generic nominal values,
  an architectural design beyond this local lowering tranche. All profile
  outputs verified and all strict dependency graphs omit the interpreter.
- Removed completed Able build/profile trees from RAM-backed `/tmp`, leaving
  3.8 MiB of small `able-*` files; future heavy work uses `/var/tmp`.
- Next profile Tapelang Alphabet, Matrix Multiply, and Fib for one shared
  primitive arithmetic or control-flow owner. This entails exact allocation
  and repeated CPU attribution before guarded A/B work; it matters because the
  cohort isolates native-carrier performance from GC-heavy generic storage.
  See
  `docs/perf-baselines/2026-07-25-compiled-scorecard-generic-storage-closure.md`.

## 2026-07-25 — Nominal-parameter effect admission closed

- Retained no code. The current 51-application source survey found the exact
  runtime-origin nominal recovery/static-call/writeback shape in only Manifest
  Normalization and Binary Event Log.
- Fresh Event Routing and Policy builds use concrete native scorer signatures
  and have no matching application writeback, so the three-unlike-program gate
  rejected a transitive effect system before implementation or A/B work.
- All four outputs verified and their strict dependency graphs omit the
  interpreter. No stdlib, runtime, interpreter, VM, language, dependency, or
  WASM change was made.
- Next refresh the strict compiled external scorecard and profile three unlike
  current material misses. This entails repeated Able/Go measurements and
  exact owner attribution; it matters because the existing scoreboard predates
  the latest retained lowering work. See
  `docs/perf-baselines/2026-07-25-compiled-nominal-parameter-effect-admission-closure.md`.

## 2026-07-25 — Caller-owned struct recovery candidate closed

- Retained no code. A shared failure-atomic `from_into` prototype and
  caller-owned return propagation removed Event Routing's 8,200 selected
  recovery allocations, but three exact runs left Manifest and Binary
  allocation counts unchanged.
- Their mandatory runtime-origin `*_apply` writeback makes the recovered
  pointer escape; Manifest also reaches a record field through a compiled
  closure. The prototype only relocated Manifest's 3,072 and Binary's 53,248
  objects into generated caller locals.
- All prototype outputs verified, strict graphs remained interpreter-free,
  nine exact-allocation processes passed, and focused compiler plus CLI tests
  pass after complete prototype removal. No stdlib, runtime, interpreter, VM,
  language, dependency, or WASM change was retained.
- Next qualify conservative read-only/non-escaping nominal parameter effects
  in at least three unlike programs. This entails transitive mutation/escape
  proof before suppressing writeback; it matters because otherwise
  caller-owned recovery cannot change heap behavior. See
  `docs/perf-baselines/2026-07-25-compiled-caller-owned-struct-recovery-closure.md`.

## 2026-07-25 — Compiled named-struct positional encoding retained

- Generated named nominal structs now use definition-ordered positional
  runtime storage rather than allocating field maps. Shared named-field
  decoding and compiler pattern paths preserve compatibility with positional
  and legacy map-backed sources.
- The retained rule is general to the nominal encoding pipeline. A diagnostic
  direct `Error` carrier prototype was removed after proving that it required
  excluded Result/control ABI changes.
- Twenty order-balanced baseline/candidate/Go cohorts improved Event Routing,
  Manifest Normalization, and Binary Event Log by 15.75%-22.61%, with a 19.58%
  geometric mean. Exact normal/error-heavy allocation bytes fell
  32.70%-49.95%.
- All timed, warmup, exact-allocation, and CPU-profile processes verified.
  Strict graphs remain interpreter-free; focused semantic/source, broader
  interface/pattern, and CLI tests pass. No stdlib, runtime, interpreter, VM,
  language, dependency, or WASM change was needed.
- Next qualify caller-owned generated-struct recovery from runtime values
  across the same applications. This entails proving one safe shared
  destination/lifetime rule and guarding identity, aliases, cycles, failure,
  generic/imported fields, and dynamic fallback; it matters because eager
  native pointer allocation is the inverse boundary owner now repeated in all
  three. See
  `docs/perf-baselines/2026-07-25-compiled-named-struct-positional-encoding-retained.md`.

## 2026-07-25 — Captured-callable cross-family candidate closed

- Retained no code because the exact owner did not repeat across the required
  trio. Manifest Normalization and Binary Event Log invoke erased captured
  callables with converted native records, while current Event Routing's
  `scorer` is already a concrete generated Go callable.
- Three exact allocation profiles per application attribute 49,152 cumulative
  conversion objects to Manifest and 851,968 to Binary, with no matching Event
  callsite. Ten verified CPU profiles per application support the same
  conclusion.
- All 42 strict build/profile processes verified and all dependency graphs
  remain interpreter-free; `go test ./cmd/ablec` also passes. No A/B
  candidate, stdlib, runtime, interpreter, language, dependency, or WASM
  change was made.
- Next qualify the shared nominal-to-static-interface payload conversion in
  the three applications' error construction. This entails proving a general
  semantic-encoding rule under normal and error-heavy modes; it matters
  because the same remaining representation crossing is now demonstrated in
  all three unlike programs. See
  `docs/perf-baselines/2026-07-25-compiled-captured-callable-cross-family-closure.md`.

## 2026-07-25 — Compiled discarded pattern-result liveness retained

- The shared pattern-assignment lowering now omits success-result
  materialization only when the surrounding context discards it. RHS
  evaluation, atomic binding, mismatch errors, rescue propagation, and
  observed expression/condition results remain unchanged.
- Manifest Normalization admitted the route across a third unlike workload:
  its 4,096 five-field patterns owned 19,072 String boxes and 26,752 complete
  Array conversion allocations. A 32-execution Policy Callback occurrence was
  rejected as nonmaterial.
- Twenty order-balanced baseline/candidate/Go cohorts improved Event Routing,
  Sensor Calibration, and Manifest Normalization by 7.57%-27.11%, with a
  17.45% geometric mean. Exact allocation bytes fell 14.42%-36.83% and counts
  fell 22.32%-52.07%.
- All timed, warmup, CPU-profile, and exact-allocation processes verified.
  Strict graphs remain interpreter-free; generated/executable pattern guards,
  the existing assignment fixtures, broader pattern tests, and CLI tests pass.
- No stdlib, runtime, interpreter, VM, language, dependency, or WASM change
  was needed.
- Next qualify the erased captured-callable carrier crossing in Event,
  Manifest, and Binary Event Log before attempting one general closure-capture
  specialization. This entails exact caller proof, compatible/conflicting/
  dynamic capture guards, and repeated A/B/Go measurement; it matters because
  statically compatible closures should keep native arguments on Go carriers.
  See
  `docs/perf-baselines/2026-07-25-compiled-discarded-pattern-result-liveness-retained.md`.

## 2026-07-25 — Compiled native String.split retained

- Guarded exact canonical primitive `String.split` call sites now use native
  Go String carriers and return a fresh native `Array String`. Invalid UTF-8
  retains the Able diagnostic path, while bound-method, erased, dynamic, and
  user-defined calls retain shared lowering.
- Generated and executable guards cover computed arguments, ASCII and
  multibyte delimiters, empty-delimiter codepoints, repeated/edge delimiters,
  result freshness, invalid receiver/delimiter UTF-8, and bound-method
  compatibility.
- Twenty order-balanced baseline/candidate/Go cohorts improved Event Routing,
  Word Frequency, and Sensor Calibration by 14.39%-70.31%, with a 45.85%
  geometric mean. Exact allocation bytes fell 20.96%-37.64% and counts fell
  41.22%-61.40%; the prior slice/decode allocation pipeline disappeared.
- All timed, warmup, CPU-profile, and allocation-profile processes verified.
  Fresh strict graphs omit the interpreter; focused compiler and CLI tests
  pass. No stdlib, runtime, interpreter, VM, language, dependency, or WASM
  change was needed.
- Next qualify successful pattern-result materialization with a third unlike
  strict application before attempting a type-agnostic liveness rule. This
  entails exact profiles, success-value/mismatch/error semantic guards, and
  repeated A/B/Go measurement; it matters because discarded successful
  destructuring should not force native values through `runtime.Value`. See
  `docs/perf-baselines/2026-07-25-compiled-string-split-native-retained.md`.

## 2026-07-25 — Compiled bridge.ToString caller closure

- Retained no production code because `bridge.ToString` has different semantic
  parents in each application: native pattern-result materialization in
  Event/Sensor, runtime-backed generic Map keys in Word, and an additional
  scheduler channel payload in Event.
- The pattern route misses Word, the Map route misses Event/Sensor and cannot
  use a named HashMap/String rule, and the channel route is a required runtime
  boundary. Changing the String box itself would require a broad runtime value
  ABI or altered interning/retention semantics.
- Exact caller localization used the unchanged ten-CPU/three-allocation
  profile sets. All three public verifiers and strict interpreter-free graphs
  pass; binary hashes match the previous retained record and `cmd/ablec`
  passes.
- No compiler, stdlib, runtime, interpreter, VM, language, dependency, or WASM
  change was made.
- Next evaluate guarded canonical primitive `String.split` lowering to native
  Go Strings plus a fresh native `Array String`. This entails Unicode/error,
  empty-delimiter, ownership, dynamic fallback, dependency, profile, and
  repeated A/B gates; it matters because the split/`slice_bytes` semantic
  pipeline is now the next true owner shared by all three applications. See
  `docs/perf-baselines/2026-07-25-compiled-bridge-to-string-caller-closure.md`.

## 2026-07-25 — Compiled native UTF-8 validation retained

- Canonical primitive String validation now uses allocation-free Go
  `utf8.Valid` for valid native byte Arrays. Nil/invalid inputs retain the Able
  diagnostic path and exact `StringEncodingError` behavior.
- A caller-owned `utf8_decode` prototype was rejected because its result
  pointer still escaped through the union interface; it shifted allocation
  attribution without reducing totals.
- Twenty order-balanced baseline/candidate/Go cohorts improved Event Routing,
  Word Frequency, and Sensor Calibration by 2.37%-16.03%, with a 10.20%
  geometric mean. Exact allocation bytes fell 7.84%-16.70% and counts fell
  37.15%-49.19%.
- All timed, warmup, CPU-profile, and allocation-profile processes verified.
  Fresh strict graphs omit the interpreter; focused semantics and `cmd/ablec`
  tests pass. No stdlib, runtime, interpreter, VM, language, dependency, or
  WASM change was needed.
- Next trace exact `bridge.ToString` callers through generic nominal/interface
  boundaries in all three applications. This entails at most one general
  carrier correction plus semantic and repeated A/B gates; it matters because
  this is now the largest shared explicit runtime conversion. See
  `docs/perf-baselines/2026-07-25-compiled-string-utf8-validation-retained.md`.

## 2026-07-25 — Compiled String/static Array native boundary retained

- Statically proven primitive String/static `Array u8` kernel conversions now
  use direct Go `string`/`*__able_array_u8` helpers instead of boxing through
  runtime String and Array values. Erased and dynamic calls retain the
  compatibility path.
- Direct conversion preserves mutable Array ownership, nil rejection, UTF-8
  validation, evaluation order, and generated error control. New guards cover
  computed arguments, both erased directions, invalid byte arrays, generated
  source, and strict interpreter independence.
- Twenty order-balanced baseline/candidate/Go cohorts improved all three
  unlike applications by 45.68%-85.35%, with a 72.47% geometric mean. Exact
  main allocation bytes fell 64.31%-81.11% and counts fell 55.92%-64.84%.
- All 219 timed/profile processes passed public verification. Fresh strict
  binaries omit the interpreter dependency; focused compiler and `cmd/ablec`
  tests pass.
- No stdlib, runtime, interpreter, VM, language, dependency, or WASM change
  was needed.
- Next trace the shared `utf8_decode -> bridge.ToString/slice_bytes` pipeline,
  now the largest exact generated allocation owner common to Event Routing,
  Word Frequency, and Sensor Calibration.
- Record:
  `docs/perf-baselines/2026-07-25-compiled-string-array-native-boundary-retained.md`.

## 2026-07-25 — Compiled String split environment-entry closure

- Fresh strict Event Routing, Word Frequency, and Sensor Calibration binaries
  pass their verifiers and omit the interpreter dependency.
- The exact environment-entry hypothesis does not repeat. Event samples
  `SwapEnvIfNeeded -> currentGID -> runtime.Stack` beneath `String.split`;
  serial Word and Sensor contain only the cumulative entry parent and no
  identity-recovery CPU or allocation.
- All 120 alternating Able/Go timing processes verify. Able remains 62.291x,
  20.744x, and 51.069x Go, so the compiled target remains open.
- Retained no production change. Exact allocation profiles instead share
  primitive String/static `Array u8` conversion boundaries:
  `__able_string_from_builtin_impl` owns 18.16%-21.42% of allocation objects,
  with `__able_array_u8_to` and `__able_string_to_builtin_impl` beside it.
- Next add a general typed kernel-helper path that preserves native String and
  byte-Array carriers while retaining dynamic/error compatibility. It entails
  source/executable semantic guards and repeated A/B measurement; it is
  important because this is now the largest exact shared boxing boundary. See
  `docs/perf-baselines/2026-07-25-compiled-string-split-environment-entry-closure.md`.

## 2026-07-25 — Compiled channel payload fast path retained

- Generated channel send/receive helpers now try the nonblocking operation
  before scheduler-payload recovery and reuse one recovered payload for
  cancellation plus executor block/unblock accounting.
- Twenty verified order-balanced pairs improved Concurrent Document Pipeline
  81.43%, Concurrent Event Routing 43.16%, and Concurrent Policy Callbacks
  81.56%, for a 73.10% geometric mean. Candidate main allocation bytes and
  counts also fell in every application.
- The exact channel
  `__able_current_payload -> Runtime.Env -> currentGID -> runtime.Stack` path
  disappeared from all three candidate profiles. Event's remaining identity
  recovery is exclusively under the imported `String.split` package entry.
- Candidate Able still runs 3.562x, 64.489x, and 6.526x its equivalent Go
  applications. Focused concurrency, cancellation, scheduler, optional
  context, strict dependency/verifier, and CLI gates pass.
- Split the mutex-awaitable renderer out of the 1,000-line concurrency
  generator without changing generated behavior. No canonical stdlib, runtime
  package, interpreter, VM, language, dependency, or WASM change was needed.
- Next profile exact `String.split` environment entries across Event Routing,
  Word Frequency, and Sensor Calibration, then admit only a general
  environment-effect proof correction. This entails tracing every generated
  callee and repeated verifier-backed A/B measurement; it is important because
  repeated runtime-context recovery is now Event's largest measured native-Go
  lowering boundary. See
  `docs/perf-baselines/2026-07-25-compiled-channel-payload-fast-path-retained.md`.

## 2026-07-25 — Compiler correctness gate reconciled

- Static compiled applications can now evaluate built-in primitive binary
  operators from erased carriers without linking or requiring a concrete
  interpreter. Nominal and unsupported dotted dispatch remains dynamic.
- Generic identity now uses its canonical nominal base package, so imported
  aliases and their targets reuse one specialization body.
- Updated three stale generated-entry expectations after retained
  environment-independent direct-body localization. All six reported failures
  pass together in 26.774s.
- A 30-minute full-package run timed out only after reaching the large fixture
  matrix; the two active fixtures pass alone in 2.536s and 28.011s. The
  566.264s bounded non-matrix suite plus bridge, CLI, and representative
  interpreter-free fixture gates pass.
- No canonical stdlib, interpreter, VM, language, dependency, or WASM change
  was needed. Next profile the shared scheduler-payload recovery path across
  three unlike concurrent compiled applications. See
  `docs/perf-baselines/2026-07-25-compiler-correctness-gate-reconciled.md`.

## 2026-07-25 — Compiled forward callable binding retained

- Unannotated local lambdas now take a concrete native Go callable carrier
  when every usable future constraint is the same fully bound static callable
  parameter. Dynamic, conflicting, indirect, reassigned, and escaping uses
  retain erased carriers and runtime adapters.
- The scorer runtime round-trip and `__able_call_value` disappeared from
  Concurrent Document Pipeline, Concurrent Event Routing, and Policy Record
  Dispatch. All strict dependency graphs remain interpreter-free.
- Ten order-balanced verified pairs improved the applications 39.84%, 20.27%,
  and 6.67%, for a 23.50% geometric mean. Candidate Able remains 14.12x,
  77.29x, and 26.90x its source-equivalent Go programs.
- Focused contextual/dynamic callable, generic/interface, imported alias,
  spawn/channel/mutex/future, strict application, verifier, and CLI gates pass.
  `generator_assignments.go` remains below 1,000 lines.
- No canonical stdlib, runtime, interpreter, tree-walker, bytecode, language,
  dependency, or WASM change was needed.
- An extra full compiler sweep exposed six unrelated bounded failures plus a
  suite-level timeout; its named test passes alone. Next restore that
  correctness gate, then profile and narrowly localize compiled scheduler
  payload recovery without broad execution-context ABI work. See
  `docs/perf-baselines/2026-07-25-compiled-forward-callable-binding-retained.md`.

## 2026-07-24 — Compiled callable environment localization retained

- Generated environment effects now propagate only through raw body calls;
  a callee entry wrapper contains its own package requirement. Successfully
  lowered immutable module bindings are also environment-independent, while
  mutable, unresolved, and dynamic reads retain guards.
- The selected hot closure guard disappeared across Concurrent Document,
  Concurrent Event Routing, and Concurrent Policy Callbacks. Five verified
  order-balanced pairs improved them 31.82%, 20.37%, and 66.13%, a 43.13%
  geometric mean.
- Candidate Able remains 16.77x, 98.67x, and 21.70x its source-equivalent Go
  programs. Document/Event retain an erased-to-concrete callable round-trip;
  Policy's remaining runtime identity lookup is under channel scheduling.
- Focused environment/callable, imported dependency, concurrency, goroutine,
  flush, mutex, strict application, verifier, and CLI gates pass. No canonical
  stdlib, runtime, interpreter, tree-walker, bytecode, language, dependency, or
  WASM change was needed.
- Next find a third unlike strict application with the exact Document/Event
  callable round-trip, then preserve the compatible native Go callable through
  assignment and typed argument flow. See
  `docs/perf-baselines/2026-07-24-compiled-callable-environment-localization-retained.md`.

## 2026-07-24 — Compiled primitive String length call fast path retained

- Statically resolved canonical primitive `String.len_bytes()` calls now use
  the guarded native Go length operation before entering the package-aware
  compiled method. Computed receivers are evaluated once; invalid UTF-8 and
  oversized values retain the original fallback semantics.
- Five order-balanced verified pairs improved Validated Job Pipeline 63.98%,
  Concurrent Document Pipeline 28.81%, and Concurrent Event Routing 53.78%.
  The geometric-mean improvement is 50.88%.
- The selected entry owner disappeared from all three candidate profiles, and
  all strict dependency graphs still omit the interpreter.
- Focused primitive String, invalid-input, environment-effect, typed-callable,
  strict-root, concurrency, and CLI guards pass. No canonical stdlib, runtime,
  tree-walker, bytecode, language, dependency, or WASM change was needed.
- Next profile boxed typed-callable adapters across Concurrent Document,
  Concurrent Event Routing, and Concurrent Policy Callbacks. See
  `docs/perf-baselines/2026-07-24-compiled-string-len-bytes-call-fast-path-retained.md`.

## 2026-07-24 — Compiled qualified-struct registry fast path retained

- Generated package registrars now index every struct under the preserved
  loader identity and the normalized runtime identity when those differ.
  Exact qualified lookup now checks that registry before goroutine-local
  environment recovery.
- The shared `StructDefinition` owner disappeared from all three candidate
  profiles. Five verified A/B pairs improved Dependency Wave Validation
  53.80%, Validated Job Pipeline 11.11%, and Concurrent Document Pipeline
  15.49%.
- The initial lookup-order-only precursor missed because registration and
  conversion used different package identities; it was rejected before the
  general identity rule was implemented.
- Focused compiler/bridge, imported conversion, environment-effect, strict
  interpreter-root, concurrency, and CLI guards pass. No canonical stdlib,
  tree-walker, bytecode, language, dependency, or WASM change was needed.
- Next attribute residual static `SwapEnvIfNeeded` entry guards across two
  current applications and an unlike String/callback-heavy concurrent guard.
  See
  `docs/perf-baselines/2026-07-24-compiled-qualified-struct-registry-fast-path-retained.md`.

## 2026-07-24 — Compiled environment-independent callable entries retained

- Extended package-environment independence through typed callable
  invocations and generated lambdas. Proven-independent calls and adapters now
  use raw generated Go bodies; package readers and dynamic callers retain
  entry guards.
- Five order-balanced verified pairs improved Option/Result Config 14.29%,
  Dependency Wave Validation 43.39%, and Validated Job Pipeline 15.05%.
- Merged profiles reduced `bridge.currentGID` by 45.62% in Dependency Wave and
  41.76% in Validated, confirming the selected exact owner.
- Bounded environment, generic lowering, Iterator/Enumerable, strict
  interpreter-root, concurrency, and CLI guards pass. No canonical stdlib,
  runtime, interpreter, language, dependency, or WASM change was required.
- Next attribute the residual package-entry callers across three unlike
  applications and close only a shared, provably unnecessary boundary. See
  `docs/perf-baselines/2026-07-24-compiled-environment-independent-callable-entries-retained.md`.

## 2026-07-24 — Bytecode three-unlike semantic-owner refresh

- Froze current CLI and interpreter benchmark artifacts and profiled Policy
  Record Dispatch, Mandelbrot, and Concurrent Audio Voices on quiet CPU 6.
- Nine warmed CPU profiles, nine exact allocation processes, three sampled
  allocation profiles, and 45 matched Able/Python/Ruby timing processes
  completed; all timing processes passed the public verifier.
- The exact three-way CPU intersection contains only aggregate dispatch, two
  closed stack-carrier parents, and Go GC scanning. There is no three-way
  allocation owner.
- Fresh Able/Python/Ruby means are 6.880/0.012/0.040s,
  6.198/1.172/1.860s, and 1.246/0.112/0.104s.
- No candidate qualified, so no implementation A/B or production change was
  made. `TestBytecode` passes in 27.069s.
- Next reconcile every active VM v2 direction against the complete closure
  ledger before another implementation tranche. See
  `docs/perf-baselines/2026-07-24-bytecode-three-unlike-semantic-owner-refresh.md`.

## 2026-07-24 — Forward-looking PLAN cleanup

- Replaced the accumulated 20,924-line roadmap/history mixture with a concise
  forward-looking roadmap containing the current handoff, active obligations,
  deferred backlog, and permanent guardrails.
- Removed 71 checked checklist entries and their completed narrative from the
  active plan. Their outcomes were already preserved in the root/v12 logs,
  dated performance records, and design decisions.
- Reconciled the stale unchecked benchmark-coverage entry because Tapelang
  Alphabet is already implemented, cataloged, and present in the current
  external scoreboard.
- Preserved the current bytecode semantic-owner gate, VM v2 candidate
  directions, scoreboard/threshold maintenance, correctness gates, and
  explicitly deferred backlog. No implementation behavior changed.

## 2026-07-24 — Interpreter-free compiled runtime profile refresh

- Refreshed strict interpreter-free CPU, sampled allocation, exact allocation,
  and matched Go timing evidence across K-Nucleotide, Sudoku Masks, and Binary
  Trees.
- All 21 accepted profile/allocation processes and all 26 timed Able/Go
  processes verified; the three generated dependency graphs and binaries omit
  the interpreter package.
- The exact three-way CPU intersection has 26 Go allocation/GC symbols and
  zero generated or Able runtime symbols. Concrete owners split among
  text/map conversion, recursive search Arrays/arithmetic, and native tree
  nodes.
- Fresh Able/Go means are 2.8360/0.0600s, 2.2140/0.6900s, and
  8.1700/8.2867s. Binary Trees is 1.41% faster than Go.
- No general candidate cleared the unlike-program bar, so no production,
  stdlib, language, dependency, or WASM change was retained.
- Next return selection to a current three-unlike-application bytecode
  semantic-owner gate. See
  `docs/perf-baselines/2026-07-24-interpreter-free-compiled-runtime-profile-refresh.md`.

## 2026-07-24 — Compiler allocation flat-cache retained

- Replaced copied read-only import slices, allocated import string keys, and
  separate nested alias/normalization caches with owned immutable slices,
  comparable keys, and one flat expression/package fact cache.
- Sampled allocation fell 21.38%-39.39% across canonical Result/Matcher,
  Array/Iterator, and an executable strict Vector application.
- Repeated peak RSS fell 53.07%-64.89%. Array/Iterator and Vector wall time
  improved 29.89% and 32.76%; Result/Matcher remained neutral.
- Cache lifecycle, generic-interface, imported-alias, strict executable, and
  `ablec` guards pass. No stdlib, runtime, interpreter, language, dependency,
  or WASM change was needed.
- Next return to interpreter-free compiled application runtime profiles. See
  `docs/perf-baselines/2026-07-24-compiler-allocation-flat-cache-retained.md`.

## 2026-07-24 — Compiler type-normalization fixed point

- Added general, invalidation-safe caches for selector/re-export resolution,
  imported-alias presence, immutable source-expression normalization, and
  normalized type strings.
- The canonical Result/Matcher compile moved from repeated 55-60+ second
  timeouts to a 36.454-second three-run mean. Unrelated Array, Iterator,
  generic-interface, imported-alias, and `ablec` gates pass.
- Removed a separate direct-interface compatibility cache after it showed no
  material governing improvement.
- The remaining compiler wall is memory/finalization: an approximately 8.6 MB
  generated file still drives roughly 3.7-4.0 GB peak RSS. Profile that wall
  across three unlike compiles next.
- No stdlib, runtime, interpreter, language, dependency, or WASM change was
  needed. See
  `docs/perf-baselines/2026-07-24-compiler-type-normalization-fixed-point.md`.

## 2026-07-24 — Static interpreter-package cut retained

- Static fallback-free applications now omit the interpreter package; dynamic
  and bootstrap programs retain the original operator routes.
- Preserved native unary operands before outer union/interface coercion and
  added reference-compatible native equality for error-bearing unions.
- All 29 strict application rows verify with no interpreter dependency. The
  six performance guards improve 33.24% geometrically, binary size falls
  32.49%-37.23%, and normal-GC Binary Trees averages 7.4867s versus Go's
  7.6500s.
- Bounded compiler/CLI gates pass. The remaining broken gate is a
  canonical-stdlib native-interface compile test that exceeds 60 seconds
  during repeated adapter refresh; fix that fixed point next.
- No canonical-stdlib, language, VM, tree-walker, dependency, or WASM change
  was needed. See
  `docs/perf-baselines/2026-07-24-static-interpreter-package-cut-retained.md`.

## 2026-07-24 — Native generic-callable lowering

- Routed statically known generic named-union calls through the existing
  nominal specialization machinery before their structural Go carrier reaches
  dynamic dispatch.
- Result/Option map callbacks now keep concrete primitive parameter/result
  carriers. Validated Job Pipeline, Dependency Wave Validation, and
  Option/Result Config improved by 32.61%, 34.63%, and 57.95% across five
  verifier-backed processes each.
- Canonical stdlib and all runtime/interpreter/language surfaces remain
  unchanged. Existing Array, Enumerable, Iterator map/filter-map, dynamic
  fallback, and executable generic-union guards pass.
- Validated still runs about 38.3x slower than its Go reference. Next reapply
  the two boxed-operator interpreter linker-root cut and widen the semantic
  sweep before any performance decision.
- See
  `docs/perf-baselines/2026-07-24-native-generic-callable-lowering.md`.

## 2026-07-24 — Execution-context full-scorecard reconciliation

- Compared the default and experimental generated execution-context ABIs over
  all 61 portable compiled applications using one fresh Go reference and five
  verifier-backed processes per row; reverse-order repeats covered ten
  suspected regressions and both default rows with an initial timeout.
- Rejected a global rollout despite a 7.39% geometric-mean improvement:
  repeated pooled regressions range from 11.3% to 63.5% across wide/fixed
  integers, rationals, Unicode, and two concurrency workloads.
- Retained the native-interface mechanism behind its existing experimental
  flag. The default path, language, bytecode VM, tree-walker, canonical stdlib,
  dependencies, and WASM remain unchanged.
- Preserved the 12-closure pre-reconciliation invalidation report, then
  atomically rebased the reviewed compiler-production scope. All 21 performance
  closures are current. Next profile context-carrier escape/allocation cost in
  the confirmed numeric/text regressions before admitting another ABI
  candidate. See
  `docs/perf-baselines/2026-07-24-execution-context-full-scorecard-reconciliation.md`.

## 2026-07-22 — Concurrent Stencil Reduction application and scorecard gate

- Added one portable file-driven numeric application with four long-lived
  workers, 2,048 stencil tasks, signed Array processing, exact reduction
  output, and equivalent Go/Python/Ruby lanes.
- Retained two five-process cohorts in every timed lane because the workstation
  references were volatile. All 50 processes verified; pooled compiled is
  47.718x Go and bytecode is 15.469x Python / 15.999x Ruby.
- Compiled reproduced the already-rejected currentGID/fixed-context family;
  bytecode reproduced already-closed Array/raw-integer owners. No engine,
  stdlib, language, dependency, or WASM change was admitted.
- Promoted both Able rows and reconciled the scorecard/frontier and architecture
  chain: 95 selected rows, 87 misses, `162.31273684210527s` of target excess,
  and all 21 closures current. Added the application to bounded refresh
  partitioning and corrected strict per-mode reference validation for
  mixed-mode evidence.
- The complete handoff runner exposed deterministic boundary-marker, Sudoku
  static-kernel, Future cancellation-parity, and dynamic-package-object
  compiler failures, plus one fixture audit over one minute and a 30-minute
  compiler-package timeout. Reconcile those before the next application. See
  `docs/perf-baselines/2026-07-22-concurrent-stencil-reduction-application-gate.md`.

## 2026-07-22 — Shared-runtime closed-region cutover decision

- Checked five unlike material hot functions against an adapter-free ownership
  contract. All five clear the 25% target-excess model, but only primitive-only
  Distance Field has a bounded cut below the interpreter instance.
- Fixed Width, Word Frequency, Array Slice Window, and Reverse Complement cross
  legacy nominal, String, or Array identities; preserving identity expands
  their minimum cut through the Go-owned heap, environments, frames,
  collections, dispatch, errors, and host roots.
- Closed production shared-runtime migration because one bounded family cannot
  meet the required three-family breadth gate. No runtime, compiler, stdlib,
  benchmark, dependency, or WASM change landed. Next return to the portable
  application frontier with an unlike concurrent numeric workload. See
  `design/shared-runtime-closed-region-cutover.md`.

## 2026-07-22 — Shared value/heap production pilot

- Proved immediate shared cells, exact identity fallback, wide-Integer fallback,
  and unchanged errors at the common interpreter call/return boundary.
- Rejected and reverted the live path: two five-process baseline and candidate
  cohorts in three unlike applications and both modes produced 120 verified
  selection runs, with four regressions, one neutral row, and only one isolated
  volatile improvement.
- Added a checked self-contained evidence record and revert guard. Next model a
  closed function-entry ownership cut rather than another conversion veneer.
  No production runtime, compiler, stdlib, language, dependency, or WASM change
  remains. See
  `docs/perf-baselines/2026-07-22-shared-value-heap-production-pilot.md`.

## 2026-07-22 — Runtime-contract performance-evidence reconciliation

- Reconciled all 21 closures after the shared-runtime contract change; all are
  current, none is invalidated, and no earlier performance disposition changed.
- Across six iterator/control applications, compiled and bytecode each retained
  two five-process cohorts. All 120 runs verified, with six volatile rows kept
  visible rather than interpreted as gains or regressions.
- Added the checked reconciliation report and independent preparation timeout,
  then rebuilt the complete architecture/semantic-ABI fingerprint chain. Next
  begin one generic, reversible shared-value call/return pilot. See
  `docs/perf-baselines/2026-07-22-runtime-contract-performance-evidence-reconciliation.md`.

## 2026-07-22 — Shared value/heap contract reconciliation

- Added generation-checked owned storage for wide immediate scalars and exact
  Hasher state reconstruction without exposing runtime storage.
- Added an explicit Iterator host-driver/root boundary; resume, finalization,
  alias identity, and captured-root collection pass in the checked report.
- All 27 runtime-owned kinds are exact and all four host kinds intentionally
  opaque. The runtime change invalidated all 21 performance closures, so their
  repeated evidence must be refreshed before a bounded call/return pilot. No
  production execution, canonical stdlib, benchmark, or WASM path changed. See
  `design/shared-value-heap-contract-reconciliation.md`.

## 2026-07-22 — Shared value/heap Go binding conformance

- Retained a test-only two-phase adapter that preserves cycles, aliases,
  closures, nominal wrappers, Errors, and Future-held identities across current
  Go runtime graphs. Both interpreter modes pass ordinary graph vectors.
- Corrected generated Package/DynPackage metadata fields discovered by the
  audit. Production execution and canonical stdlib remain unchanged.
- Production migration stays closed on wide Integer ownership, private Hasher
  state, and private Iterator driver/finalizer closures. Next reconcile those
  contracts. See `design/shared-value-heap-go-binding-conformance.md`.

## 2026-07-22 — Shared value/heap conformance contract

- Generated exhaustive layouts for 20 shared runtime kinds, six internal
  environment/storage/context objects, and all four host-registry kinds.
- Added an isolated deterministic model with descriptor-gated mutation,
  generation reuse, root frames, host-held roots, cycle-safe tracing, and
  transactional stale-edge failure. Eight conformance vectors pass twice.
- No production runtime, interpreter, compiler, stdlib, benchmark, or WASM path
  changed. Next build a test-only generic Go runtime binding. See
  `design/shared-value-heap-conformance-contract.md`.

## 2026-07-22 — Execution-complete semantic ABI shadow lowering

- Evolved the isolated image to version 2 with typed registers, stable
  local/imported/member calls, 15 generic flow operations, real branches and
  backedges, typed match decisions, raise/match-fail terminators, and exact host
  continuations.
- Three unlike functions validate as 9-, 14-, and 16-block graphs with all 306
  AST nodes covered and deterministic encode/round-trip bytes. CFG reachability,
  terminators, definite assignment, types, and call arity are corruption-tested.
- Production execution and canonical stdlib remain unchanged. Next define one
  shared heap/host lifetime contract before any live value migration. See
  `design/semantic-abi-shadow-image-lowering.md`.

## 2026-07-22 — Semantic ABI codec/layout spike

- Added an isolated pure-Go semantic ABI package with a pointer-free 16-byte
  cell, stable IDs, deterministic eight-section codec, typed validator, and
  generated exhaustive kind/operation manifests.
- Shadow-encoded three unlike real functions into 3,596-, 4,641-, and
  6,905-byte images. All 306 visited nodes lower without AST fallback; source
  and callsite identity round-trip, and valid/corrupt determinism gates pass.
- No production execution or stdlib change landed. Next replace structural
  preorder records with execution-complete non-executing data/control flow. See
  `design/semantic-abi-codec-layout-spike.md`.

## 2026-07-22 — Shared runtime semantic-ABI feasibility

- Defined a conditional shared semantic runtime with a 16-byte pointer-free
  value cell, stable heap/host IDs, eight-section validated program image,
  one canonical heap, and structured host-effect resume records.
- Exhaustively mapped all 31 current runtime kinds. The existing whole-function
  model clears the 25% excess gate in all five serial/effectful applications.
- Admitted only a pure-Go codec/layout spike; retained no runtime, backend,
  dependency, execution, stdlib, or WASM change. Next shadow-encode three unlike
  functions without executing them. See
  `design/shared-runtime-semantic-abi-feasibility.md`.

## 2026-07-22 — Portable VM backend ABI/dependency ADR

- Compared C-ABI, portable-JIT, and direct-codegen engines with Go-owned and
  foreign-owned values. No variant satisfies ownership, semantic-authority,
  fallback, deployment, and prior-rejection gates together.
- Go-owned values force dense semantic exits across measured four-to-seven-
  instruction primitive spans; foreign-owned values require a duplicate heap,
  scheduler, dispatch/error model, and graph conversion. No dependency or
  prototype was admitted.
- Closed foreign backends under the current runtime contract. Next determine
  whether a single backend-neutral program/value/effect ABI is feasible. See
  `design/portable-vm-backend-abi.md`.

## 2026-07-22 — Cross-engine structural-strategy reconciliation

- Compared typed bytecode specialization, a portable lower-level VM backend,
  and further compiled nominal-ABI simplification against the current 93-row
  frontier and six unlike applications. All 21 evidence closures remain current.
- Freeing every observed proven scalar instruction clears the 25% excess gate
  in only three of six rows. Compiled nominal ABI evidence has no exact owner
  material in three unlike families. Both routes remain closed.
- A compiled-equivalent whole-engine proxy clears the bar in all five phase-one
  serial/effectful families, but no backend/ABI/ownership/distribution mechanism
  is selected. Kept no production or dependency change and admitted no
  prototype. Next complete the portable backend ABI/dependency ADR. See
  `docs/perf-baselines/2026-07-22-cross-engine-structural-strategy-reconciliation.md`.

## 2026-07-22 — Compiled typed semantic-boundary census

- Retained an opt-in, main-only twelve-category typed/runtime boundary census;
  normal compiled output contains no diagnostic branch or atomic.
- Fifteen telemetry processes and ten normal CPU profiles all verify. Value,
  nominal, and callable crossings are material only in Event Routing and
  Policy; Fixed Width and Distance record zero.
- The only three-program count, control/error conversion, is just 0.262% of
  pooled K-Nucleotide CPU and absent from Policy's profile. Kept no production
  candidate; next broaden the census to all 49 compiled applications. See
  `docs/perf-baselines/2026-07-22-compiled-typed-semantic-boundary-census.md`.

## 2026-07-22 — Bytecode six-application allocation-owner matrix

- Six sampled profiles and five exact counter samples per application pass.
  Package init was excluded and async executor roots were retained.
- No open three-family semantic allocation remains. Boxing and Array ownership
  are closed; positional structs have different definitions and async,
  loop-carried, or return-to-pattern lifetimes.
- Kept no runtime candidate. Next census typed/dynamic compiler-boundary reach.
  See
  `docs/perf-baselines/2026-07-22-bytecode-six-application-allocation-owner-matrix.md`.

## 2026-07-22 — Bytecode six-application clean CPU owner refresh

- Six bounded runtime-only profile processes pass across the current
  architecture-budget representatives with one fixed interpreter build.
- Only the dispatcher and three already-closed carrier helpers clear 1% flat
  CPU in three unlike applications; exact caller/source reconciliation exposes
  no new shared removable work.
- Kept no runtime candidate. Next run the corresponding allocation-owner
  matrix. See
  `docs/perf-baselines/2026-07-22-bytecode-six-application-clean-cpu-owner-refresh.md`.

## 2026-07-22 — Bytecode Array-slot value-role closure

- Extended the opt-in proven-integer load census with exact kernel operation
  and operand role; all 2,241,366 shared events reconcile to values, with
  2,241,334 feeding `push` and 32 feeding `write_slot`.
- Three independent traces prove the common push traffic already uses
  `array_push_u8_mono_fast`; no index extraction or generic integer
  materialization remains in the shared shape.
- Clean profiles make push CPU material only in Reverse Complement (6.99%),
  versus no samples in Event Routing and 0.81% in Word Frequency. Retained no
  execution-path candidate. See
  `docs/perf-baselines/2026-07-22-bytecode-array-slot-value-role-census.md`.

## 2026-07-22 — Bytecode integer-load carrier/consumer census

- Added opt-in exact carrier and source-enclosing consumer-opcode attribution
  for verifier-proven integer `LoadSlot` traffic; normal VM execution retains
  no census metadata or additional branch.
- Six unlike verifier-backed applications reconcile every count. Raw-i32 to
  `JumpIfBinaryCompareFalse` spans four applications; small boxed integers to
  `CallMemberArraySlot` is the next three-application exact shape.
- Tested and removed a generic direct-bool comparison result candidate. Six
  processes per side showed +8.66% Array Slice Window and +1.65% Reverse
  Complement regressions, while the zero-reach float control moved -5.51%.
  Retained diagnostics and evidence only. See
  `docs/perf-baselines/2026-07-22-bytecode-integer-load-carrier-consumer-census.md`.

## 2026-07-22 — Bytecode scalar-proof-gap census

- Added bounded, opt-in attribution for generic slot transport, exact nominal
  field loads, casts, and primitive comparisons using existing lowering and
  checked-inference facts. Normal bytecode execution builds no proof metadata.
- Six unlike verifier-backed applications pass with no dropped programs.
  Proven integer `LoadSlot` traffic is 5.08%-11.66% in five families; Distance
  Field instead has 28.21% proven float loads.
- Tested and reverted a guarded known-integer load hint after three-run averages
  regressed all guards by 0.95%-17.06%. Retained diagnostics only; next census
  carrier and consumer shapes before designing a distinct opcode. See
  `docs/perf-baselines/2026-07-22-bytecode-native-hot-tier-design-budget.md`.

## 2026-07-22 — Bytecode hot-function reach census

- Added bounded, opt-in per-program bytecode reach diagnostics with source
  identity, entry/instruction counts, conservative primitive/effect
  attribution, backedges, primitive spans, and boxed/environment metadata.
- All six unlike catalog applications verified with no dropped programs. Hot
  source functions own 38.50%-63.49% of instructions, but their pointer-free
  spans stop after only four to seven static instructions; the common class is
  effect-interleaved and cannot satisfy the leaf/no-Go-callback contract.
- Updated the checked native-tier budget and retained no backend, runtime fast
  path, compiler, stdlib, benchmark, or language change. Next census missing
  scalar proof in generic slot/field transport, casts, and compares. See
  `docs/perf-baselines/2026-07-22-bytecode-native-hot-tier-design-budget.md`.

## 2026-07-22 — Bytecode native hot-tier design and budget

- Defined the safe native boundary as pointer-free leaf execution with
  Go-owned roots, exact VM side exits, cooperative polling, portable fallback,
  and a bounded process-local W^X cache.
- Current compiled Able is a transformative but incomplete proxy: it removes
  88.68% of bytecode target excess, while 31/42 rows still miss. Existing typed
  regions qualify in only one of four current applications.
- Added checked source-fingerprinted design/budget tooling and kept no runtime,
  dependency, benchmark, or stdlib change. No backend/prototype is admitted;
  next census coarse hot-function reach. See
  `docs/perf-baselines/2026-07-22-bytecode-native-hot-tier-design-budget.md`.

## 2026-07-22 — Bytecode semantic-region tier feasibility closure

- Proved the proposed Go-level semantic-region lane was already completed:
  typed-region execution, whole-function registers, and Go PGO all failed
  their broad gates, while the current semantic audit admits no exact family.
- A uniform-instruction-cost free-region model closes only one of four current
  rows; the other three retain modeled 2.25x-21.58x gaps. It is explicitly a
  sizing model, while the repeated prototype regressions supply the gate.
- Added checked source-fingerprinted reconciliation tooling and kept no runtime
  or stdlib change. Next design and budget a non-WASM native hot-code tier. See
  `docs/perf-baselines/2026-07-22-bytecode-semantic-region-tier-feasibility.md`.

## 2026-07-22 — Current cross-engine architecture target-budget reconciliation

- Reconciled the complete fresh product frontier with optimistic bytecode and
  compiled architecture bounds. Bytecode owns 132.965684 of the 156.333684
  target-excess seconds.
- Freeing all modeled VM transport still leaves at least 7.79x required;
  freeing the largest local compiled owner still leaves at least 3.60x. No
  current local mechanism passes the three-unlike-family gate.
- Added a checked generator and fixed the opcode model to include retained
  `CallGenericUnionMember`. Kept no production/stdlib change. Next measure
  shared semantic hot regions before considering a primary VM tier. See
  `docs/perf-baselines/2026-07-22-current-cross-engine-architecture-target-budget-reconciliation.md`.

## 2026-07-22 — Current bytecode-versus-interpreter scorecard reconciliation

- Refreshed five independent verified bytecode, Python, and Ruby processes for
  all 42 selected rows; all 630 pass. Seven excluded rows received 21 bounded
  status probes with no failures.
- Await Channel Mux, JSON, and PiDigits meet both snapshot targets; 39 rows
  miss. The summed-row diagnostic is 8.584x the faster interpreter and
  132.965684 seconds above target budgets.
- Promoted the cohort, reconciled stability, and regenerated the fully current
  cross-mode frontier with zero actionable groups. Kept no code. Next build a
  current cross-engine architecture target-budget audit. See
  `docs/perf-baselines/2026-07-22-current-bytecode-interpreter-scorecard-reconciliation.md`.

## 2026-07-22 — Current compiled-versus-Go scorecard reconciliation

- Refreshed five independent verified Able and Go processes for every one of
  the 49 portable compiled applications. All 490 executions pass under matching
  catalog CPU/executor contracts.
- Five rows meet the snapshot target and 44 miss. The summed-row diagnostic is
  1.760x Go; Binary Trees, QuickSort, and JSON remain established guards while
  Base64 and Monte Carlo Pi are volatile crossings.
- Promoted the compiled cohort, reconciled stability, and regenerated the
  complete selected frontier with zero actionable groups. Kept no code. Next
  refresh all selected bytecode rows against fresh Python/Ruby references. See
  `docs/perf-baselines/2026-07-22-current-compiled-go-scorecard-reconciliation.md`.

## 2026-07-22 — Compiled post-ABI current profile refresh

- Excluded a pre-typed-key artifact pass after its profiles exposed the old
  allocating struct cache key, then profiled the exact retained-current
  binaries.
- All 139 governing verifier-backed processes pass across eight unlike
  applications. Repeated CPU and exact allocation evidence finds no new open
  three-application leaf after broadening beyond the union cohort.
- Kept no compiler/runtime/stdlib change. Next refresh and reconcile the
  compiled-versus-Go scorecard so the next candidate comes from current target
  gaps rather than stale frontier entries. See
  `docs/perf-baselines/2026-07-22-compiled-post-abi-current-profile-refresh.md`.

## 2026-07-22 — Bytecode cross-feature allocation-owner matrix

- Profiled allocation objects and space across five unlike bytecode feature
  families with a higher-resolution measured-main pass that excludes package
  initialization.
- Exact counter shapes are stable, but no removable allocation has qualifying
  breadth. The broad stack helper splits into raw-`i64` and float-copy lines;
  boxing, type-AST, Error-name, environment, and nominal mechanisms are closed
  or semantically required.
- Kept no code; full bytecode tests pass. Next refresh compiled post-ABI owner
  profiles. See
  `docs/perf-baselines/2026-07-22-bytecode-cross-feature-allocation-owner-matrix.md`.

## 2026-07-22 — Bytecode cross-feature exact-leaf matrix

- Profiled five unlike bytecode feature families with one fixed binary and
  stabilized 6.39-13.88-second CPU samples.
- No exact non-dispatch interpreter leaf met the 1%-flat/three-application
  admission rule. Raw-integer callers diverge, and the repeated fixed map is
  the already-rejected primitive metadata representation.
- Kept no code; `TestBytecode` passes. Next compare sampled allocation owners
  across the same representatives. See
  `docs/perf-baselines/2026-07-22-bytecode-cross-feature-exact-leaf-matrix.md`.

## 2026-07-22 — Bytecode return-frame composition census

- Counted exact semantic inline-return shapes across four owner applications
  and three unrelated controls under the bounded one-process guardrails.
- No immutable shape qualified: full-frame mechanics dominate controls too,
  while raw/coercion/slotless/self-fast shapes lack owner breadth or materially
  select a control.
- Removed all temporary instrumentation and retained only the evidence record.
  Focused restored-state tests pass. See
  `docs/perf-baselines/2026-07-22-bytecode-return-frame-composition-census.md`.

## 2026-07-22 — Bytecode post-provenance profile and Error-name gate

- Refreshed four bounded owner CPU/allocation profiles; exact return-frame
  leaves now form the strongest shared interpreter-owned CPU family.
- Rejected immutable Error-interface name arrays despite exact object
  reductions in every run. Option and Manifest improved and Policy was neutral,
  but the expanded eight-process Binary raw mean regressed 2.66%.
- Restored-state tests pass. Next collect stats-only return-frame composition
  across owners and controls before changing return logic. See
  `docs/perf-baselines/2026-07-22-bytecode-post-provenance-profile-error-name-gate.md`.

## 2026-07-22 — Bytecode typechecked generic-union call gate

- Retained exact typechecker method-selection provenance plus a dedicated
  bytecode instruction and guarded two-way cache only for proven generic
  named-union method-set calls.
- Three unrelated owners improve 3.94%-10.47%, Binary is neutral, and all
  three unrelated controls are neutral or faster. Iterator executes no
  specialized instructions, avoiding the preceding runtime-shape regression.
- Focused semantic/invalidation/lowering tests and the full typechecker
  package pass. Next refresh post-change owner profiles and admit only a flat
  leaf recurring in at least three unlike owners. See
  `docs/perf-baselines/2026-07-22-bytecode-typechecked-generic-union-call-gate.md`.

## 2026-07-22 — Bytecode instruction-local generic-union cache gate

- Completed the admitted call-site stability census and rejected every
  instruction-local cache implementation after a repeatable 2.66% iterator
  collect regression, despite 8.25%-35.46% gains in four owner applications.
- Removed all production and diagnostic code; retained only the performance
  report and next provenance-led lowering direction.

## 2026-07-22 — Bytecode generic-union type-resolution gate

- Found one exact generic-union method-match leaf at 2.56%-5.93% cumulative
  CPU across Binary Event Log, Option/Result Config, and Manifest
  Normalization, spanning both `Result` and `Option` definitions.
- Rejected a bounded global match cache: it removed the matcher and allocations
  but regressed the order-bracketed Option cohort 6.75%.
- Rejected copy-on-write immutable type-expression reuse despite 1.86%-4.20%
  owner allocation reductions and Binary/Option wins because the unrelated
  order-balanced iterator guard regressed 2.48%. All experimental runtime and
  test changes were reverted.
- All 70 verifier-backed direct processes passed. One oversized Binary profile
  calibration timed out; its bounded replacement and focused restored-state
  tests pass. No stdlib, compiler, benchmark, language, or WASM change was
  made. See
  `docs/perf-baselines/2026-07-22-bytecode-generic-union-type-resolution-gate.md`.

## 2026-07-22 — Bytecode three-shape post-compiler refresh

- Retained five warmed independent processes and one separate clean CPU
  profile each for split/join, iterator collect, and numeric Array map under
  the 55-second/1 GiB one-process guardrails; all 18 processes passed.
- Reconciled raw-integer extraction, string-map access, inline return, and
  typed-pattern/type-match descendants. Their callers remain heterogeneous or
  use already-rejected representations, so no performance candidate qualified.
- Kept no VM, compiler, runtime, stdlib, benchmark, fixture, language, or WASM
  change. See
  `docs/perf-baselines/2026-07-22-bytecode-three-shape-post-compiler-refresh.md`.

## 2026-07-22 — Compiled static match-type expression gate

- Audited generated type-expression lifetime and proved compiler-rendered
  match expressions are read-only, then tested broad and native-union/direct-
  `Error` package-level immutable AST tables.
- Both scopes removed the predicted allocations. The broad scope regressed
  Binary Trees 3.64%; the narrow scope improved Binary Event Log 2.60% and
  Option/Result 16.79% but repeatedly regressed Manifest 1.44%, Policy 1.64%,
  and N-Body 2.07%.
- Reverted the complete generator/runtime/test candidate. All 788 benchmark
  processes verified with no failure or timeout. No stdlib, VM, language,
  benchmark, or WASM change was made. See
  `docs/perf-baselines/2026-07-22-compiled-static-match-type-expression-gate.md`.

## 2026-07-22 — Compiled generic-union direct-known-method gate

- Retained a general generated-runtime path that invokes statically resolved
  native methods on generic named unions without boxing a bound method. It
  preserves receiver/context/control/nil/fallback semantics and adds no
  nominal-type or stdlib-container special case.
- All four unlike owner applications improve 5.61%-18.89%. Five repeated
  exact allocation deltas reduce objects 0.95%-16.58% and bytes 0.85%-19.85%;
  four exact profiles contain no old box.
- Retained all samples from 252 verified processes. Binary Trees and Mutex
  Ledger improve; three small positive serial-guard point estimates remain
  inside measured workstation noise after supplemental reversed cohorts. The
  admitted frontier item is closed pending fresh post-change profiles. See
  `docs/perf-baselines/2026-07-22-compiled-generic-union-direct-known-method-gate.md`.

## 2026-07-22 — Compiled nominal/Result allocation-shape census

- Preserved four unlike compiled applications and retained 50 verified CPU
  profiles, four exact allocation profiles, and twenty repeated main-phase
  allocation-stat runs. All 74 launches passed their public verifier.
- Static generic-union dispatch is 11.83%-58.97% cumulative CPU across all
  four. Three unlike applications materially allocate the same escaping
  64-byte NativeBoundMethodValue box: 225,280, 147,456, and 20,480 objects.
- Admitted one general direct-known-method invocation candidate and changed no
  production, stdlib, benchmark, VM, language, or WASM source. The 91-row
  frontier now has one actionable compiled group. See
  `docs/perf-baselines/2026-07-22-compiled-nominal-result-allocation-shape-census.md`.

## 2026-07-22 — Binary Event Log application gate

- Added a portable binary-file application and source-equivalent Go, Python,
  and Ruby references around one deterministic fixture and verifier. It covers
  variable record decoding, validation, nominal records, `Result`, interfaces,
  captured callables, arrays, maps, imports, and real program entry.
- Retained two five-process cohorts per lane with every sample averaged. Pooled
  target gaps are 57.750x Go for compiled and 34.122x Python / 25.019x Ruby for
  bytecode. The catalog now has 49 portable applications and 91 selected rows.
- Three profiles per mode expose no admissible candidate: compiled general
  union dispatch has breadth two, while the bytecode raw-integer leaf belongs
  to an already rejected broad family. Both affected closures were advanced;
  all 21 are current and the frontier remains zero actionable. No production,
  stdlib, language, or WASM change was retained. See
  `docs/perf-baselines/2026-07-22-binary-event-log-application-gate.md`.

## 2026-07-22 — Compiled execution-context recommendation reconciliation

- Reconciled the proposed spawn-scoped context against the completed
  2026-07-14 spawn-selected gate. That exact generic design improved two async
  applications but regressed Mutex Ledger 10.0%; program-wide and
  allocation-free package-linkage descendants separately failed N-Body and
  K-Nucleotide guards.
- Added a test-only source contract proving normal `spawn` compilation stays
  on the compatibility ABI unless the existing experimental option is
  explicitly enabled. The opt-in generated surface remains covered.
- Race-built nested spawn, await/cancellation fixtures, nested native context,
  and goroutine-local environment restoration pass. No production source or
  execution contract changed, so no unchanged timing cohort was rerun and no
  evidence closure was advanced. See
  `docs/perf-baselines/2026-07-22-compiled-execution-context-recommendation-reconciliation.md`.

## 2026-07-21 — Validated Job Pipeline file-entry application gate

- Evolved the existing pipeline into a real file/entry application: 32 input
  labels materially drive 2,048 tasks across four workers while retaining
  closures, `Result`, `Error` interface dispatch, channels, and futures. Able,
  Go, Python, and Ruby implementations share one verifier and fingerprinted
  `jobs.txt` contract.
- Closed the four remaining depth-two portable triples. Minimum three-family
  depth is now three and 158 of 165 triples improve over the reconstructed
  baseline. The catalog remains 48 applications / 127 programs.
- Retained two five-process exact-contract cohorts per lane with every sample
  in the means. The checked promotion remains a miss at 235.417x Go compiled,
  11.250x Python bytecode, and 6.656x Ruby bytecode.
- Three compiled profiles repeat `bridge.currentGID` at 94.16% cumulative;
  three bytecode profiles expose no new material shared VM leaf. No production,
  stdlib, language, or WASM change was retained. Both concurrency evidence
  closures were advanced and all 21 closures are current. See
  `docs/perf-baselines/2026-07-21-validated-job-file-entry-application-gate.md`.

## 2026-07-21 — Concurrent Document Pipeline Option reconciliation

- Recognized the existing pipeline as substantial portable Option coverage:
  `Channel.receive() -> ?DocumentTask` executes 1,024 task matches plus four
  closed-channel `nil` matches per normal run.
- Added the coverage membership and reconstructed-baseline contract, raising
  improved triples from 152 to 154 and reducing depth-two triples from six to
  four. The catalog remains 48 applications / 127 programs.
- No executable or dependency changed, so existing timing evidence remains
  current. Selection stays at 89 rows, all 21 closures are current, and the
  performance frontier remains zero actionable. No production, stdlib,
  language, or WASM change was made. See
  `docs/perf-baselines/2026-07-21-concurrent-document-pipeline-option-reconciliation.md`.

## 2026-07-21 — Truthiness/cast regex-concurrency closure refresh

- Refreshed compiled regex and bytecode concurrency with 450 verifier-backed
  timing processes. Every row retains matched second cohorts and ten samples
  per Able/Go/Python/Ruby lane.
- Only compiled Policy reaches generated truthiness, 2,048 times, which fails
  the three-application breadth rule. Two exact bytecode censuses per row find
  zero changed Error fallbacks and zero explicit casts; no profile or candidate
  was admitted.
- Removed all temporary instrumentation. Only these two closures were
  advanced; 16 are current and five remain selected. No production, stdlib,
  workload, verifier, reference, language, or WASM change was retained. See
  `docs/perf-baselines/2026-07-21-truthiness-cast-regex-concurrency-closure-refresh.md`.

## 2026-07-21 — Truthiness/cast byte-regex closure refresh

- Refreshed compiled byte/output and bytecode regex with 160 verifier-backed
  timing processes. Base64, FASTA, and Pi Digits retain matched second
  compiled/Go cohorts with every sample in the arithmetic means.
- All compiled rows have zero changed-path reach. Two exact bytecode censuses
  per row reproduce up to 1,565,212 primitive truthiness checks but zero
  changed Error fallbacks and zero explicit casts; no profile or candidate was
  admitted.
- Removed all temporary instrumentation. Only these two closures were
  advanced; 14 are current and seven remain selected. No production, stdlib,
  workload, verifier, reference, language, or WASM change was retained. See
  `docs/perf-baselines/2026-07-21-truthiness-cast-byte-regex-closure-refresh.md`.

## 2026-07-21 — Truthiness/cast text-byte next closure refresh

- Refreshed compiled text/map and bytecode byte/output with 115
  verifier-backed timing processes. Volatile I Before E and Word Frequency
  retain matched second compiled/Go cohorts with every sample in the means.
- All compiled rows have zero changed-path reach. Two exact bytecode censuses
  per row find only four primitive truthiness checks in Reverse Complement and
  zero changed Error fallbacks or explicit casts across all three rows; no
  profile or candidate was admitted.
- Removed all temporary instrumentation. Only these two closures were
  advanced; 12 are current and nine remain selected. No production, stdlib,
  workload, verifier, reference, language, or WASM change was retained. See
  `docs/perf-baselines/2026-07-21-truthiness-cast-text-byte-next-closure-refresh.md`.

## 2026-07-21 — Truthiness/cast wide-text closure refresh

- Refreshed compiled wide/numeric and bytecode text/map with 115
  verifier-backed timing processes, retaining second Python cohorts for two
  volatile workstation lanes.
- All compiled rows have zero changed-path reach. Two exact bytecode censuses
  per row find zero changed Error fallbacks and zero cast failures; only
  K-Nucleotide reaches the cast wrapper, 28 successful times per main, so no
  shared material leaf or candidate was admitted.
- Removed all temporary instrumentation. Only these two closures were
  advanced; ten are current and 11 remain selected. No production, stdlib,
  workload, verifier, reference, language, or WASM change was retained. See
  `docs/perf-baselines/2026-07-21-truthiness-cast-wide-text-closure-refresh.md`.

## 2026-07-21 — Truthiness/cast numeric-next closure refresh

- Refreshed compiled float/numeric and bytecode wide/numeric with 120
  verifier-backed timing processes, retaining matched additional cohorts for
  volatile workstation lanes.
- All compiled rows have zero changed-path reach. Bytecode Rational and Wide
  reach 1,000,002 and 768,003 successful casts per main, but four current
  profiles put zero flat CPU in the catchable wrapper; the remaining two-row
  descendants are already closed by broad wall-time evidence.
- Removed all temporary instrumentation. Only these two closures were
  advanced; eight are current and 13 remain selected. No production, stdlib,
  workload, verifier, reference, or WASM change was retained. See
  `docs/perf-baselines/2026-07-21-truthiness-cast-numeric-next-closure-refresh.md`.

## 2026-07-21 — Truthiness/cast near-path closure refresh

- Refreshed compiled iterator/control and bytecode float/numeric with 155
  verifier-backed timing processes. Volatile rows retain additional cohorts
  and every successful sample remains in the arithmetic means.
- Exact reach found zero changed Error-truthiness fallbacks in all nine rows
  and zero explicit casts in the four bytecode rows. Option/Result reaches the
  compiled cast bridge 24,384 times, but two current main profiles sample none
  of that path; no three-application shared leaf or candidate was admitted.
- Removed all temporary instrumentation. Only the two measured closures were
  advanced; six are current and 15 remain selected. No production, stdlib,
  workload, verifier, or reference change was retained. See
  `docs/perf-baselines/2026-07-21-truthiness-cast-near-path-closure-refresh.md`.

## 2026-07-21 — Truthiness/cast control-closure refresh

- Refreshed compiled current-control and bytecode iterator/control from the
  frozen post-correctness source state with 115 verified timing processes.
  Matrix is 1.119x Go, TapeLang is 1.987x Go, and the five bytecode rows are
  17.596x-48.643x Python.
- Fresh compiled source/profile evidence and two exact bytecode censuses per
  row find zero reach into the changed non-primitive Error-truthiness fallback
  and zero explicit runtime casts. No new CPU/allocation profiles or candidate
  are justified by a zero-reach path.
- Removed all temporary counters. Only the two measured closures were advanced;
  four closures are current and 17 remain selected. No production, stdlib,
  workload, verifier, or reference change was retained. See
  `docs/perf-baselines/2026-07-21-truthiness-cast-control-closure-refresh.md`.

## 2026-07-21 — Truthiness/cast target-guard refresh

- Refreshed compiled and bytecode target guards from one frozen current source
  state with fresh matched Go/Python/Ruby references. All 80 retained timing
  processes verified; ordinary rows use five samples and volatile Fib uses two
  order-balanced five-process cohorts with no outlier removal.
- Binary Trees, compiled JSON, and QuickSort meet at 0.672x, 0.425x, and 0.605x
  Go. Fib remains a 1.080x miss across 10+10 samples. Bytecode PiDigits and JSON
  meet at 0.479x Python and 0.405x Ruby.
- Added guarded partial ledger advancement. Only the two remeasured target
  closures are current; 19 unrefreshed closures remain selected. No runtime,
  compiler, VM, stdlib, workload, verifier, or reference implementation was
  changed. See
  `docs/perf-baselines/2026-07-21-truthiness-cast-target-guard-refresh.md`.

## 2026-07-21 — Runtime truthiness/cast alignment and evidence invalidation

- Fixed canonical/inherited `Error` truthiness so stdlib error values are
  falsy across tree-walker, bytecode, logical operators, and compiled dynamic
  boundaries.
- Made failed explicit runtime casts raise catchable Able errors across generic
  and specialized interpreter paths. Four failing fixtures now pass in both
  interpreters and compiled execution; two complete-corpus runs average 19.780
  seconds tree-walker and 18.108 seconds bytecode.
- Added shared interpreter semantics to the checked performance dependency
  scope and made checked invalidated reports first-class. All 21 closures are
  deliberately selected for revalidation; no baseline hash was advanced and
  no benchmark evidence was silently accepted. See
  `design/truthiness-cast-runtime-alignment.md` and
  `docs/perf-baselines/2026-07-21-performance-evidence-invalidation-ledger.md`.

## 2026-07-21 — Performance evidence invalidation ledger

- Added a checked selector over 18 frontier groups and three architecture
  closures, with exact evidence/benchmark identities and mode-aware spec,
  compiler, VM, runtime, and canonical-stdlib production scopes.
- All 21 closures are current and zero are selected. Tests prove closure-local
  evidence drift, compiled-only production selection, and test-only
  false-invalidation exclusion.
- Wired the exact non-timing check into the scoreboard workflow. No performance
  candidate is currently authorized. See
  `docs/perf-baselines/2026-07-21-performance-evidence-invalidation-ledger.md`.

## 2026-07-21 — Compiled architecture target budget

- Added a checked five-family target-budget model joining retained repeated
  frontier timing, logical-work allocation, exact CPU owners, source hashes,
  and the three-family admission rule.
- Required speedups are 7.54x-640.06x; making every row's largest exact owner
  free still leaves 4.28x-55.94x. No shared mechanism is CPU-material in three
  unlike families, so no compiler or stdlib candidate was built.
- Added explicit evidence-invalidation triggers to the active performance
  policy. Next build a checked closure-invalidation ledger and selector. See
  `docs/perf-baselines/2026-07-21-compiled-architecture-target-budget.md`.

## 2026-07-21 — Bytecode architecture target-budget reconciliation

- Confirmed that the proposed typed-register audit and its downstream
  executable gates were already complete; the checked current generator retains
  the `no-go-register-representation-only` decision.
- All 143 live opcodes, the six-application budget, eight semantic-closure
  areas, and three rejected empirical gates remain exact. Refreshed only the
  VM source/test line-count provenance by eight lines each.
- No implementation or stdlib candidate was built. Next construct the compiled
  architecture/semantic-amplification target budget. See
  `docs/perf-baselines/2026-07-21-bytecode-architecture-target-budget-reconciliation.md`.

## 2026-07-21 — Compiled primitive range/effect closure census

- Extended the report-only generated-Go analyzer with universal and
  main-reachable direct-call interval/effect closures, conservative checked
  primitive semantics, loop bounds, and loop-carried-state invalidation.
- All seven applications have zero call-site-specializable direct functions.
  K-Nucleotide adds two cold unreachable sign helpers; Mandelbrot adds a
  universally safe pixel shift whose control check is unsampled.
- Fib remains the sole material control-ABI row, so no ABI candidate was
  built. Next audit the bytecode typed-register architecture and target budget.
  See
  `docs/perf-baselines/2026-07-21-compiled-primitive-range-effect-closure-census.md`.

## 2026-07-21 — Compiled control-effect reach census

- Added a report-only generated-Go analyzer that conservatively resolves
  control-free functions, including recursive SCCs and constructed-control
  reassignment guards.
- Fresh modules reproduced 10/53 through 90/416 control-free direct functions,
  but profile reconciliation found material reach only in Fib; Tape's getter
  is already inlined and other free wrappers are cold or above host kernels.
- Sudoku, Tape mutation, and Mandelbrot remain fallible through checked
  primitive work. No ABI candidate cleared the three-family gate. Next audit
  interprocedural primitive range/effect closure. See
  `docs/perf-baselines/2026-07-21-compiled-control-effect-reach-census.md`.

## 2026-07-21 — Compiled exact-leaf seven-application selection

- Two main-only CPU and two exact-allocation processes each covered Fib,
  Sudoku Masks, Base64, Pidigits, K-Nucleotide, TapeLang, and Mandelbrot; all
  28 outputs passed their public verifiers.
- Exact owners split among direct recursion, search/checked integer work, Go
  Base64/MD5, `math/big`, HashMap/boxing, Array/state dispatch, and float math.
  Repeated GC leaves sit over incompatible allocation shapes.
- Fib's nil-control checks own 18.13%, but the matching checks are 0.14% in
  TapeLang, 0.25% in Sudoku, and unsampled in Mandelbrot. No candidate cleared
  the three-family gate. Next run a report-only control-free effect/reach
  census. See
  `docs/perf-baselines/2026-07-21-compiled-exact-leaf-seven-application-selection.md`.

## 2026-07-21 — Bytecode direct-Array semantic-boundary gate

- Array Slice, Reverse Complement, and Matrix Multiply completed repeated
  warmed-main timings/allocations, main-only profiles, and exact Array semantic
  counts; Word Frequency and Fixed Width served as controls.
- `Array.push` is broad, but its concrete descendants split among tracked
  raw-I32 storage, U8 append, and F64 handle size/write/synchronization. The
  shared cache is at least 99.9990% hot with zero fast misses; stronger Array
  Slice sampling confirms version proof is not a material shared leaf.
- Retained only general opt-in per-kind Array statistics plus their reset/count
  guard; no performance candidate cleared the shared-owner gate. Next return
  to the compiled generated-code residual frontier. See
  `docs/perf-baselines/2026-07-21-bytecode-array-semantic-boundary.md`.

## 2026-07-21 — Bytecode text/UTF-8 semantic gate

- Two clean CPU, two exact-allocation, and two semantic-count processes each
  covered K-Nucleotide, Word Frequency, Policy Record Dispatch, and the
  non-text Distance Field control from one frozen artifact.
- Inbound String conversion is material only in K (1.73%) and Word (1.04%);
  Policy's String/UTF-8 leaves are at or below 0.068%, and Distance records no
  String work. The three-family gate fails, so no performance candidate was
  built.
- Retained only the inert-when-unset per-interpreter
  `ABLE_BYTECODE_STRING_STATS` observer. Full bytecode tests pass. Next run the
  direct Array semantic gate with Matrix Multiply as the independent third
  family. See
  `docs/perf-baselines/2026-07-21-bytecode-text-utf8-semantic-gate.md`.

## 2026-07-21 — Weighted feature-interaction triple audit

- Added a report-only 165-triple frontier ranked by coverage depth, reviewed
  semantic weight, and adjacent current target excess; no triple is empty.
- Recognized Concurrent Text Index's 41,314 hot typed/nil/nullable selections
  and substantive control flow, reducing depth-one triples from eight to two
  and strengthening 85 triples.
- The two residual callable/concurrency triples have a deep Event Routing
  guard, so no duplicate app or performance code was added. Next build a
  cross-mode residual cost model across unlike high-excess families. See
  `docs/perf-baselines/2026-07-21-weighted-feature-interaction-triple-audit.md`.

## 2026-07-21 — Concurrency × lexical-pattern depth reconciliation

- Recognized Dependency Wave Validation's hot typed Channel and payload-union
  patterns; its default run guarantees at least 16,388 relevant selections.
- Exact membership-baseline reporting shows all 55 portable pairs now have
  depth at least two, with zero empty or depth-one interactions.
- Existing source-exact scorecard/profile evidence remains valid; no new app,
  implementation, stdlib, scorecard, or frontier change was needed. Next audit
  weighted portable three-family interactions. See
  `docs/perf-baselines/2026-07-21-concurrency-lexical-depth-reconciliation.md`.

## 2026-07-21 — Feature-interaction coverage-depth gate

- Added and promoted Policy Record Dispatch with source-equivalent Able, Go,
  Python, and Ruby programs plus a checked input and independent verifier.
- Depth-one portable interactions fall from nine to one; all eight targeted
  lexical/pattern pairs now have independent serial and concurrent coverage.
- Exact profiles reproduce closed compiled Regex/NFA and bytecode Array-slot,
  call/return, and raw-integer owners. No implementation or stdlib performance
  change was admitted. Next audit the remaining concurrency × lexical-pattern
  pair. See
  `docs/perf-baselines/2026-07-21-feature-interaction-coverage-depth-gate.md`.

## 2026-07-21 — Bytecode iterator/control seven-application refresh

- Five iterator/control rows plus Mandelbrot/Word Frequency completed two
  high-sample CPU and two exact one-main allocation processes from one frozen
  artifact; every selected profile contains 13.55-17.53 seconds of samples.
- Only completed member-cache, active-lookup, return/frame, and raw-integer
  leaves reach three selected rows; map/hash callers split among unrelated
  primitive, Array, environment, text, and union/type stores.
- All seven outputs and full bytecode tests pass; no candidate was admitted.
  Next strengthen the weakest feature-interaction coverage depth. See
  `docs/perf-baselines/2026-07-21-bytecode-iterator-control-seven-application-refresh.md`.

## 2026-07-21 — Bytecode concurrency fourteen-application refresh

- Eleven concurrency rows plus Mandelbrot/Word Frequency/JSON completed two
  current CPU and two exact measured-main allocation processes from one frozen
  artifact; all fourteen public outputs verified.
- `sync/atomic.(*Int32).Add` is broad only because all concurrent environment
  stores use `RWMutex`; traced callers split across lookup/revision/runtime-
  data/parent/definition/cache stores, not a removable scheduler counter.
- Full bytecode tests pass; no runtime or stdlib candidate was admitted. Next
  refresh the mixed-age bytecode iterator/control group. See
  `docs/perf-baselines/2026-07-21-bytecode-concurrency-fourteen-application-refresh.md`.

## 2026-07-21 — Compiled concurrency fourteen-application refresh

- Current frozen binaries across eleven concurrency rows plus three serial
  controls reproduce `bridge.currentGID` only in concurrency, at 72.73%-94.46%
  cumulative CPU.
- Five alternating pairs reject the existing opt-in context ABI: it helps two
  rows but regresses Event Routing, Mutex Ledger, NBody, Fib, and JSON, while
  still paying `currentGID` at task-entry/compatibility boundaries.
- No production or stdlib change was retained. Focused tests pass; next refresh
  bytecode concurrency. See
  `docs/perf-baselines/2026-07-21-compiled-concurrency-fourteen-application-refresh.md`.

## 2026-07-21 — Bytecode regex seven-application refresh

- Two current full-scale CPU and exact-allocation processes each across five
  regex rows plus text/map and numeric controls reproduce only closed NFA
  Array-slot, small named-field, stack, raw-integer, and dispatcher families.
- The retained Array cache already performs allocation-free direct hits with
  required invalidation proof, and today's broad small-field map candidate
  regressed every row. No production or stdlib candidate was built.
- Full bytecode tests pass. Next refresh compiled concurrency ownership. See
  `docs/perf-baselines/2026-07-21-bytecode-regex-seven-application-refresh.md`.

## 2026-07-21 — Bytecode float-numeric six-application refresh

- Two current CPU and exact-allocation processes each across Distance, RMS,
  Mandelbrot, Monte Carlo, Matrix, and Reverse reproduce only closed float
  carrier/typed-region and generic VM/Go runtime families.
- Matrix, Monte Carlo, and Reverse provide unlike dot-loop, integer-recurrence,
  and byte/Array discrimination. No new concrete owner qualified, so no
  production or stdlib candidate was built.
- Full bytecode tests pass. Next refresh bytecode regex ownership. See
  `docs/perf-baselines/2026-07-21-bytecode-float-numeric-six-application-refresh.md`.

## 2026-07-21 — Bytecode wide-numeric five-application refresh

- Current CPU/allocation profiles across three wide programs plus Distance and
  Reverse found only closed raw-integer/VM parents and unrelated map/GC work.
- A general reserved-primitive cast-target shortcut passed semantics but
  reversed sign between complete and CPU-0 broad wall cohorts; it regressed
  Reverse in the complete mean and all three targets in the CPU-0 mean, so it
  was fully removed.
- Restored tests pass. Next refresh bytecode float-numeric ownership. See
  `docs/perf-baselines/2026-07-21-bytecode-wide-numeric-five-application-refresh.md`.

## 2026-07-21 — Bytecode text/map five-application refresh

- Two current main-only CPU profiles and two exact allocation processes each
  for K-Nucleotide, Word Frequency, Inventory, Reverse Complement, and JSON
  expose no new concrete VM leaf shared by three unlike programs.
- Shared map/hash machinery has different semantic owners; the closed VM
  dispatcher/stack/raw-integer/call-frame families remain closed. No runtime
  or stdlib candidate was built.
- Retained exact timed-main allocation counters in the opt-in retention probe.
  Next refresh bytecode wide-numeric ownership. See
  `docs/perf-baselines/2026-07-21-bytecode-text-map-five-application-refresh.md`.

## 2026-07-21 — Dynamic `i64` lazy-slot frontier reconciliation

- Refreshed all 45 compiled rows with five verifier-backed processes: 225/225
  succeeded against the unchanged stdlib and same-day five-run Go references.
- Five compiled rows meet in the snapshot. Fib now misses; Matrix narrowly
  meets, but a second five-Able/five-Go cohort gives Matrix a 1.155x pooled
  miss, so it is not an established guard.
- Promoted the 83-row frontier with 8 meets, 75 misses, 5 established guards,
  134.951 excess seconds, and zero actionable groups. All strict checks pass.
- Next refresh bytecode text/map ownership, which contributes 51.681 target-
  excess seconds. See
  `docs/perf-baselines/2026-07-21-dynamic-i64-lazy-slot-frontier-reconciliation.md`.

## 2026-07-21 — Dynamic `i64` per-value lazy cache

- Current exact iterator/control profiles joined Document Audit and Lexical
  Rollup to the three prior dynamic-`i64` boundary applications.
- Retained independently lazy, concurrency-safe common-value slots. Document
  allocation falls 42%/68% bytes/objects and Lexical 11%/14%.
- All 156 paired wall processes verified; reached and broad guard means are
  neutral-to-favorable. Race, focused compiler/profile, and full bytecode tests
  pass. Next refresh the complete compiled scorecard with the same-day Go refs.
- See
  `docs/perf-baselines/2026-07-21-compiled-iterator-control-dynamic-i64-lazy-slot-gate.md`.

## 2026-07-21 — Post-i64 compiled frontier reconciliation

- Refreshed all 45 selected compiled applications and Go references with five
  verifier-backed processes each, then repeated four threshold rows in an
  independent five-run cohort.
- Five compiled rows meet in the snapshot. Fib is established at 1.029x
  pooled; Base64 is a volatile pooled miss and Matrix misses both cohorts.
- The promoted 83-row frontier has 75 misses, 6 established guards, 135.228
  excess seconds, and zero actionable groups after Fib/Base64 evidence
  membership reconciliation. No production code changed.
- Next refresh the pre-current compiled iterator/control group. See
  `docs/perf-baselines/2026-07-21-post-i64-compiled-frontier-reconciliation.md`.

## 2026-07-21 — Compiled unsigned dynamic-boundary census

- Eight unlike unsigned-heavy compiled applications completed generated-site,
  escape, and verified allocation classification.
- K-Nucleotide alone materially executes `bridge.ToUint`: 7.49 million
  sampled `u64` key boxes and 343 MiB, all from raw map get/set over the
  reused range `0..15`. Seven exact main profiles contain no unsigned bridge
  allocation.
- The three-application breadth gate fails, so no candidate or named-container
  rule was built. Focused profilehook, bridge, and compiler controls pass.
- Next refresh the compiled performance frontier after the retained `i64`
  improvement. See
  `docs/perf-baselines/2026-07-21-compiled-unsigned-dynamic-boundary-census.md`.

## 2026-07-21 — Compiled dynamic `i64` boundary cache

- Escape analysis identified one primitive native-`i64` to dynamic-value
  boundary across K-Nucleotide, Inventory, and Validated Job; a separate
  inlining-only trial was allocation-neutral and rejected.
- Retained `ToDynamicI64` only from generated `runtimeValueExpr` conversions.
  Allocation falls 16%-75%, while verified means improve 15.6%, 33.2%, and
  1.9% in the three primary applications.
- Extended TapeLang, short-program, ten-pair Binary Trees, and sixty-launch
  startup guards clear. Race, focused compiler, and full bytecode tests pass.
- Next breadth-gate the remaining unsigned dynamic boundary. See
  `docs/perf-baselines/2026-07-21-compiled-dynamic-i64-boundary-cache.md`.

## 2026-07-21 — Compiled `i64` bridge cache gate

- Allocation/escape evidence selected the same primitive `bridge.ToInt` owner
  in K-Nucleotide, Inventory Reconciliation, and Validated Job Pipeline.
- A lazy suffix-specific cache improved five-pair K-Nucleotide and Inventory
  means 15.3% and 26.4%, but ten verified TapeLang pairs regressed 4.17% with
  exactly unchanged main allocation. A later clean profile showed no hot
  `ToInt` call, correcting the earlier causal attribution; the global
  candidate was still correctly removed.
- Restored bridge, compiler, and full `TestBytecode` controls pass. Next
  classify escaping dynamic-boundary boxes separately from non-escaping
  conversions. See
  `docs/perf-baselines/2026-07-21-compiled-i64-bridge-cache-gate.md`.

## 2026-07-21 — Compiled primitive-runtime boundary revalidation

- The recovered generic `runtimeops` boundary passed current semantic,
  dependency, static-launcher, and dynamic-fallback proofs and cut generated
  binary size by 31.5%-39.9%.
- Verified source-matched Binary Trees means regressed 9.11% under the one-CPU
  stress guard and 6.24% under the current four-CPU/goroutine policy because
  GC cycles rose 35%-45%. The complete candidate was removed.
- Restored compiler/bytecode controls pass. Next build a cross-application
  generated-allocation/escape-shape matrix. See
  `docs/perf-baselines/2026-07-21-compiled-primitive-runtime-boundary-revalidation.md`.

## 2026-07-21 — Secondary bytecode architecture selection

- A fresh six-application profile sweep selected the general small named-field
  index path across Wide Integer Records, Unicode Scalar Pipeline, and Log
  Routing Redaction. Identity counts covered many unrelated definitions.
- Building definition-level field-index maps for small structs regressed every
  five-run primary/guard mean by 0.83%-5.19%. The candidate and diagnostics
  were fully removed; restored tests and verifier smokes pass.
- Next re-evaluate the compiled primitive-runtime boundary against today's much
  faster Binary Trees guard. See
  `docs/perf-baselines/2026-07-21-bytecode-secondary-architecture-selection.md`.

## 2026-07-21 — Dynamic boxed-integer cache gate

- Dynamic boxed-integer synchronization is material in four unlike measured
  applications, but reuse ranges from zero in Fixed Width to 95.77% in
  K-Nucleotide.
- The sole generic prototype reduced each operation to one ordinary mutex and
  one lookup. Fixed/Reverse five-pair means were neutral; ten Rational pairs
  pooled to a 3.19% regression after two cohorts reversed direction. The
  candidate was removed.
- Retained only exact one-main timing/profile support in the test harness;
  restored bytecode tests pass in 24.072s. Next select an architecture-level
  shared operation rather than another lock/map variant. See
  `docs/perf-baselines/2026-07-21-dynamic-boxed-integer-cache-gate.md`.

## 2026-07-21 — Feature-interaction scorecard promotion

- Promoted four feature-interaction applications in compiled and bytecode
  modes. The reviewed scorecard now has 83 selected and 90 full-status rows,
  with exact five-process Able/reference evidence for every selected row.
- The rebuilt frontier has eight snapshot meets, 75 misses, 134.540 seconds of
  target excess, and zero actionable groups. New profiles only widen the
  already-closed compiled concurrency and bytecode mixed-owner groups; no
  runtime optimization was retained. See
  `docs/perf-baselines/2026-07-21-feature-interaction-scorecard-reconciliation.md`.

## 2026-07-20 — Feature-interaction applications and pairwise matrix

- Added Concurrent Text Index and Validated Job Pipeline with byte-identical
  canonical/sibling Able sources, equivalent Go/Python/Ruby references, Docker
  contracts, and external verifiers. A reproducible 55-pair matrix shows 14
  formerly empty feature intersections closed and 32 pairs improved.
- Five verifier-backed means per implementation remain far outside the product
  targets. Exact profiles reproduce rejected compiled goroutine-identity and
  completed bytecode member/return/type/allocation owners, so no performance
  candidate was retained. See
  `docs/perf-baselines/2026-07-20-feature-interaction-application-gate.md`.

## 2026-07-17 — Mode-aware two-cohort scorecard reconciliation

- Cohorts A and B retain all 68 status rows and exact five-run evidence for
  all 61 selected contracts; strict variance, execution-contract, selection,
  source, and canonical-stdlib identity checks pass. Six rows meet the target
  in both cohorts, 51 miss in both, and four compiled rows flip with host load.
- A new aggregate evidence check runs before promotion. Bytecode Fib remains
  visible but excluded because its Python reference cannot finish five runs
  under the one-minute rule. No compiler, VM, runtime, stdlib, or workload
  optimization was retained. See
  `docs/perf-baselines/2026-07-17-mode-aware-two-cohort-scorecard.md`.

## 2026-07-17 — Mode-aware benchmark execution contracts

- Fresh reference, Able comparison, and scorecard rows now preserve catalog
  logical CPU budgets, resolved CPU affinity, and serial/goroutine executor
  policy; fresh mismatches are rejected before ratio creation and again during
  aggregation.
- Compiled parallel applications receive four CPUs while interpreter rows
  remain single-CPU. The grouped refresh derives per-row `GOMAXPROCS` rather
  than imposing one global value. Focused/dry-run/selection tests, reference
  parser smokes, scoreboard replay, and one end-to-end verifier-backed smoke
  pass. No runtime or performance candidate was retained. See
  `docs/perf-baselines/2026-07-17-mode-aware-execution-contracts.md`.

## 2026-07-15 — Canonical stdlib scorecard provenance

- Full scorecard refresh records the exact external `able-stdlib/src/**/*.able`
  tree before timing: deterministic content hash, file count, Git head when
  available, and dirty state. Its dated aggregate and `--cohort` replay retain
  that state; strict five-run cohort evidence now requires it.
- Legacy current-scorecard replay remains byte-identical. The dry-run lifecycle
  creates no artifacts, and no canonical stdlib, compiler, bytecode VM,
  workload, target, or timeout changed. See
  `docs/perf-baselines/2026-07-15-complete-scorecard-cohort-variance.md`.

## 2026-07-15 — Five-run promotion guard

- A full-scorecard refresh may now promote only exactly five timed processes
  per row. Lower-run smoke commands must pass `--no-promote`, preventing them
  from replacing the selected baseline.
- Normal five-run promotion and tagged candidate collection are unchanged. No
  benchmark, compiler, bytecode VM, stdlib, workload, target, timeout, or
  baseline changed. See
  `docs/perf-baselines/2026-07-15-complete-scorecard-cohort-variance.md`.

## 2026-07-15 — Independent scorecard cohort lifecycle

- `bench-scorecard-refresh --tag NAME --no-promote` now preserves a dated,
  independent candidate cohort without changing `external-scoreboard-current`.
  `bench-scoreboard --cohort <aggregate>` can later rebuild or promote its
  exact recorded source manifest without rerunning benchmarks.
- Distinct dry-run tags yielded 31 disjoint JSON paths each at the five-run
  default; default promotion remains unchanged and current-cohort replay is
  byte-identical. No benchmark, compiler, bytecode VM, stdlib, target, timeout,
  or baseline changed. See
  `docs/perf-baselines/2026-07-15-complete-scorecard-cohort-variance.md`.

## 2026-07-15 — Strict five-run cohort eligibility

- `bench_variance_report --require-runs 5` now requires five successful,
  numbered and verifier-hashed Able samples plus five fresh measured-reference
  samples for each requested language in every expanded comparison row.
- A retained five-run comparison and temporary disjoint cohort pass; the
  historical source cohort correctly fails because it lacks the new source
  state and has older aggregated data. No benchmark, compiler, bytecode VM,
  canonical stdlib, target, or timeout changed. See
  `docs/perf-baselines/2026-07-15-complete-scorecard-cohort-variance.md`.

## 2026-07-15 — Complete scorecard cohort variance

- The report-only variance reader can now expand two or more complete external
  scorecards, retaining per-run evidence and source-level ratios for every
  row. It rejects row-coverage differences and any reused comparison source,
  so a copied or partial scorecard cannot claim independent ratio evidence.
- Temporary disjoint-cohort verification passed, while the current and
  reconciled scorecards correctly fail the source-reuse guard. No compiler,
  bytecode VM, canonical stdlib, workload, target, timeout, or baseline
  changed. See `docs/perf-baselines/2026-07-15-complete-scorecard-cohort-variance.md`.

## 2026-07-15 — Per-run scorecard sample retention

- Focused Able and fresh Go/Python/Ruby reports now retain every run's status,
  timing, and verified output hash; comparison rows carry both sample sets.
  The variance report accepts one modern repeated report for timing spread but
  leaves ratio variation report-level because independently sampled runs are
  not paired observations.
- Separate five-run Option/Result compiled and bytecode checks prove retained
  samples reproduce the reported means. This is harness validation only; no
  runtime, compiler, stdlib, workload, target, or timeout changed. See
  `docs/perf-baselines/2026-07-15-per-run-sample-retention.md`.

## 2026-07-15 — Five-run default measurement policy

- All active focused, suite, comparison, reference-refresh, and full-scorecard
  performance commands now default to five independent verifier-backed
  processes. Quiet-core preflight remains optional; explicit lower counts are
  smoke-only and per-run data remains available for median/spread analysis.
- This changes measurement infrastructure only, not the compiler, bytecode VM,
  canonical stdlib, benchmark workloads, targets, or timeouts. See
  `docs/perf-baselines/2026-07-15-five-run-default-measurement-policy.md`.

## 2026-07-15 — Cross-runtime residual census

- Reconciled the promoted 33-application scorecard and the imported
  typed-pattern correctness repair against all retained compiler and bytecode
  profile gates. The coverage lowering audit passes with 148 application-entry
  functions and 7,827 instructions, but static opcode reachability is not CPU
  evidence and does not authorize a slot or typed-pattern fast path.
- No performance experiment or profile was admitted: shared parents still
  divide into distinct concrete leaves, while return/raw-value/map candidates
  and the compiler goroutine-identity route are already rejected. See
  `docs/perf-baselines/2026-07-15-cross-runtime-residual-census.md`.

## 2026-07-15 — Option/Result scorecard reconciliation and typed-pattern repair

- Repaired a shared correctness gap in optimized typed-pattern matching. Its
  fast entries now canonicalize a type through the active lexical environment,
  just as the ordinary pattern path does. An imported `Error` pattern therefore
  matches `able.core.interfaces.Error` rather than falling through as an
  untyped generic-union value. This applies to tree-walker clause reuse,
  bytecode match lowering, and regular fast matching; it is not an
  Option/Result-specific rule.
- `option_result_config` now runs twice in both tree-walker and bytecode modes
  under a permanent focused regression. A fresh five-process verifier-backed
  comparison recorded compiled `0.1960s` and bytecode `3.3880s`; its Go,
  Python, and Ruby ratios remain misses, so no performance optimization is
  retained.
- Promoted the repaired fresh report into the current scoreboard: 33 portable
  applications, 66 compiled/bytecode rows, and 25 explicit source reports.
  The reconciliation record is
  `docs/perf-baselines/2026-07-15-option-result-scorecard-reconciliation.md`.

## 2026-07-15 — Generic interface-default coverage audit

- Audited all 33 portable application lanes. Only Document Audit and Lexical
  Rollup use the repaired `Iterator<T>` default-method pipeline; Option/Result
  Configuration is generic-union dispatch and Channel Rollup uses manual
  loops. Every v12 feature family with a fair cross-language contract already
  has portable coverage.
- No synthetic third iterator application was added. The three-application
  rule admits a performance candidate; it is not a benchmark-coverage quota.
  See `docs/perf-baselines/2026-07-15-generic-interface-default-coverage-audit.md`.

## 2026-07-15 — Generic interface-default external guard

- Five independent verifier-backed compiled and bytecode processes completed
  for Document Audit and Lexical Rollup, the only two portable applications
  that use the repaired public lazy `filter`/`map`/`collect` default-method
  route. Every output verified; the five-run means are in
  `docs/perf-baselines/2026-07-15-interface-default-external-guard-decision.md`.
- The current source-aligned scorecard and earlier paired profiles provide no
  third unlike consumer or new concrete shared leaf. No VM, compiler,
  generated-runtime, canonical-stdlib, or benchmark optimization is retained.

## 2026-07-15 — Bytecode three-shape repeat refresh

- Five independent warmed bytecode processes passed for split/join, iterator
  collect, and numeric Array map under the 1 GiB / GC / one-P guardrails. The
  averages and attribution-only profiles are recorded in
  `docs/perf-baselines/2026-07-15-bytecode-three-shape-repeat-refresh.md`.
- Kept no VM/compiler/stdlib performance optimization: common parents split
  into text call/type-match, iterator generator/member, and numeric Array/raw
  work; raw integer, return, and fixed-map candidates are already rejected.
- Direct iterator validation exposed a general default-method generic metadata
  bug. Dynamic interface defaults now retain enclosing interface parameters,
  with both a focused metadata regression and an iterator-collect warmup test.

## 2026-07-15 — Qualified interface identity

- Added `13_09_qualified_interface_identity`: a package-local `Display`
  intentionally shares the kernel protocol's short name, while a private
  `Hidden` implementation returns, casts, passes as a parameter, and
  dispatches through the imported interface. Tree-walker, bytecode, parity,
  and compiled execution all emit `Ada` across repeated focused runs.
- Runtime interface definitions retain qualified nominal identities; impl and
  dictionary lookup canonicalize through them while preserving a
  non-overwriting short-name compatibility alias. Compiler static-import
  interface resolution and generated impl-thunk registration now preserve the
  same identity. This is semantic coverage for §§10.3.4, 13.4, and 13.5 only.

## 2026-07-15 — Static interface dictionary capture

- Added `13_08_static_interface_dictionary_capture`: a factory retains a
  private concrete `Hidden` type and `private impl Renderable`, returns the
  interface, and a static-importing consumer dispatches `render()` without
  importing the implementation. Tree-walker, bytecode, parity, and compiled
  execution all emit `Ada` across repeated focused runs. This is semantic
  coverage for §§10.3.4, 13.4, and 13.5 only.
- `13_09_qualified_interface_identity` now covers and resolves the former
  package-local `Display` versus kernel `Display` collision.

## 2026-07-15 — Explicit interface cast typechecking

- The checker now implements §6.3.5 C1/N3 exactly: explicit casts to an
  interface are accepted without static implementation proof and the runtime
  validates them on execution. Non-interface cast rejection and ordinary
  assignment compatibility are unchanged.
- Focused checker, tree-walker, bytecode, and compiler regressions cover the
  behavior. The shared `06_03_cast_unproven_interface` fixture rescues the
  resulting runtime error across tree-walker, bytecode, and compiled binary
  execution. Generated output retains the existing runtime cast boundary; no
  performance or stdlib change was made.

## 2026-07-15 — Repeated concurrency product screen
- Fresh repeated external references and verified Able averages confirm the
  concurrent gaps but not a new leaf; all three reduce to the rejected
  goroutine-identity ABI. See
  `docs/perf-baselines/2026-07-15-workstation-concurrency-repeat-decision.md`.

## 2026-07-15 — Workstation sampling policy
- A quiet-core preflight is preferred but no longer blocks profiler evidence
  on this shared workstation. Repeated canonical-verifier-backed runs must
  retain mean, median, and spread, while merged CPU profiles remain
  attribution-only. This changes no runtime, compiler, workload, or threshold.

## 2026-07-15 — Compiled call-path profile gate
- Fresh normal-binary CPU phase captures passed canonical Ruby verification
  for Option/Result Configuration, await-channel-mux, and mutex-await-journal.
  The generic-union/allocator work is isolated to Option/Result; both async
  controls reach the previously rejected goroutine-identity bridge below the
  same fast helper.
- Future Await Race completed with 120 repeated verifier-backed workstation
  launches after host preflights became too volatile. Its measured spread is
  recorded and its merged profile repeats the same rejected identity route.
  No optimization is selected. See
  `docs/perf-baselines/2026-07-15-compiled-call-path-profile-gate.md`.

## 2026-07-15 — Compiled residual call-path census
- Added explicit `--call-path-telemetry` compiler output and the root `just`
  `bench-compiled-call-path-audit` recipe. This is debug-only generated code:
  normal binaries do not contain the environment check, counters, or atomic
  operations. The 33-application verifier-backed audit completed 32 verified
  rows; Sudoku retained its existing 45-second status-only timeout.
- Generic-union dispatch was isolated to Option/Result Configuration and its
  fallback bridge was not reached. The general fast method-value helper
  reached seven applications (165,484 events), materially in Option/Result,
  await/channel, Future, and Mutex shapes. It therefore earns only a
  quiet-host normal-binary CPU-profile follow-up across those four applications.
  See `docs/perf-baselines/2026-07-15-compiled-call-path-reachability-decision.md`.

## 2026-07-15 — Option/Result compiled profile gate
- Completed the compiled selection gate across Option/Result Configuration,
  Dependency Plan, and Document Audit. Each binary was built once, then
  repeatedly launched through the generated CPU-only phase hook with
  `GOMEMLIMIT=1GiB`, `GOGC=50`, `GOMAXPROCS=1`, a 45-second cap, and its own
  passing quiet-host preflight. All 230 outputs passed their canonical Ruby
  verifier with stable per-application hashes.
- Option/Result Configuration alone has a material generic-union
  method-dispatch/allocation path. Dependency Plan reaches the previously
  rejected environment-swap ABI through Queue/Deque wrappers, while Document
  Audit is below useful profile resolution beyond profiler-start/GC and its
  file/iterator remainder. No concrete leaf repeats across all three unlike
  applications.
- Kept no compiler, VM, canonical-stdlib, or benchmark optimization. The
  record is
  `docs/perf-baselines/2026-07-15-option-result-config-compiled-profile-gate.md`.

## 2026-07-15 — Option/Result configuration profile gate
- Completed the quiet-host warmed bytecode gate for Option/Result
  Configuration, Dependency Plan, and Document Audit. Each one-process
  capture used `GOMEMLIMIT=1GiB`, `GOGC=50`, `GOMAXPROCS=1`, a 45-second cap,
  and an immediate single-core preflight. Normal compiled/bytecode launches
  for the same three applications were already Ruby-verifier backed.
- The warmed harness now retains one-time typechecker facts so ordinary
  generic-union member overloads retain their checked receiver identity.
  Setup and the explicit warmup remain outside the timed `main()` loop; the
  separate trusted `--skip-typecheck` CLI lane is unchanged. A new canonical
  stdlib regression calls the application `main` twice and checks both output
  values.
- No VM/compiler/stdlib/workload optimization is selected. The new application
  is dominated by lambda/function bytecode lowering and GC; it does not recur
  in the Array/member/index or iterator/member-cache controls. The only
  concrete three-way leaf remains the previously rejected
  `finishInlineReturn(...)` family. See
  `docs/perf-baselines/2026-07-15-option-result-config-profile-gate.md`.

## 2026-07-15 — Scorecard verifier/input fingerprints
- Fresh external comparison reports now capture a SHA-256 contract for their
  verifier script, declared program arguments, and declared regular-file
  inputs before starting the timed Able process. The existing cohort is
  reconstructed through the shared catalog as current-contract provenance;
  verifier/input/argument drift invalidates the checked-in report and blocks a
  bare rewrite. This intentionally covers declared inputs only, not implicit
  program file access. It is provenance only, not a benchmark, VM, compiler,
  runtime, stdlib, or performance change. See
  `docs/perf-baselines/2026-07-15-scorecard-verifier-input-fingerprints.md`.

## 2026-07-15 — Scorecard reference-source fingerprints
- Fresh Go/Python/Ruby reference refreshes now retain the selected source
  SHA-256. Comparison generation and scoreboard promotion validate that source
  before retaining a ratio; the existing fresh-reference cohort is explicitly
  current-source fingerprinted and rejects drift. Stored external result rows
  remain without a source identity only where no local reference source is
  available. This is provenance only, not a benchmark, VM, compiler, runtime,
  stdlib, or performance change. See
  `docs/perf-baselines/2026-07-15-scorecard-reference-source-fingerprints.md`.

## 2026-07-15 — Scorecard source-fingerprint contract
- Fresh `bench_compare_external` source reports now record each canonical Able
  target's SHA-256, while the scorecard validates that measured identity before
  promotion. The preexisting July cohort is clearly marked with current-source
  legacy hashes; source drift makes its check stale and prevents a bare
  scoreboard rewrite from relabeling old timings. This is provenance only, not
  a benchmark, VM, compiler, runtime, stdlib, or performance change. See
  `docs/perf-baselines/2026-07-15-scorecard-source-fingerprints.md`.

## 2026-07-15 — Canonical benchmark-source contract
- `bench_catalog_check` now proves every portable catalog entry targets a
  distinct source under `v12/examples/benchmarks` and that the full coverage
  catalog exactly inventories the 32 canonical sources. It ignores generated
  benchmark-local `target/` output; sibling harness `run.able` and
  `able-v12-*` packaging are not selected for measurement. This is static
  provenance protection, not a benchmark, VM, compiler, runtime, stdlib, or
  performance change. See
  `docs/perf-baselines/2026-07-15-canonical-benchmark-source-contract.md`.

## 2026-07-15 — Scoreboard unranked-reason transparency
- The generated external scorecard now records a stable `unranked_reason` for
  every row it cannot rank and renders the same explanation in Markdown. It
  distinguishes an incomplete/timed-out/failed Able launch from a missing
  required Go/Python/Ruby ratio, without inferring why a foreign reference has
  no valid ratio. This is report-only measurement clarity; no benchmark,
  timeout-policy, VM, compiler, runtime, canonical-stdlib, fixture, or
  performance behavior changed. See
  `docs/perf-baselines/2026-07-15-scoreboard-unranked-reason-transparency.md`.

## 2026-07-15 — Unranked-scorecard reconciliation
- Classified every promoted unranked row as a bounded-performance status, not
  a missing benchmark/reference lane. Compiled Sudoku and seven bytecode
  applications cap; K-Nucleotide is correctly incomplete after mixed bounded
  Able results; Fib's existing Python reference itself exceeds the guard.
  Retained no benchmark source, reference, timeout-policy, VM, compiler, or
  stdlib change. See
  `docs/perf-baselines/2026-07-15-unranked-scorecard-reconciliation.md`.

## 2026-07-15 — Bytecode main-stats profile gate
- Tested the only newly posed generic hypothesis from the main-only stats
  inventory: high `LoadSlot` counts do not become a shared CPU leaf. Fresh
  warmed one-process profiles split into Word Frequency hash-map/call work,
  Future Pipeline raw-integer/scheduler/binary work, and Base64 host
  base64/MD5 work. Retained no VM, compiler, runtime, canonical-stdlib,
  fixture, or benchmark change; generated profiles are cleanup-eligible. See
  `docs/perf-baselines/2026-07-15-bytecode-main-stats-profile-gate.md`.

## 2026-07-15 — Development-wrapper working-directory repair
- `abletw` and `ablebc` no longer execute their child CLI through `go run -C`,
  which changed relative program-input resolution to the Go module directory.
  They now rebuild cached mode-specific binaries in `.gocache` and execute
  them from the caller's directory. Relative-input Word Frequency verifies in
  both modes; source-root-only Future Pipeline verifies in bytecode mode. The
  v12 README documents the cache and working-directory contract. This is a
  shared wrapper correctness repair, not a benchmark optimization.

## 2026-07-15 — Bytecode main-only stats inventory
- Validated the existing opt-in bytecode stats output on verifier-backed Word
  Frequency, Future Pipeline, and Base64 launches. Main-only instruction counts
  repeat only VM envelope operations; their concrete call/type-pattern,
  integer-loop/member, and canonical-Array routes diverge. The snapshots are
  count attribution rather than timing evidence, so they retain no VM,
  compiler, runtime, canonical-stdlib, fixture, or benchmark change. See
  `docs/perf-baselines/2026-07-15-bytecode-main-stats-inventory.md`.

## 2026-07-15 — VM-v2 direction reconciliation
- Confirmed that `design/bytecode-vm-v2.md` is the active in-place VM contract,
  not a second interpreter or a pending typed-frame rewrite. The spec/TODO and
  active plan contain no unfinished non-WASM language, compiler-AOT, parser,
  regex, or external-stdlib feature that could admit a new performance slice.
  The roadmap now makes clear that typed end-to-end values, canonical
  Array/String bytecodes, and guarded quickening remain possible routes only
  after fresh three-application evidence identifies a concrete shared wall.
  No VM, compiler, runtime, canonical-stdlib, fixture, or benchmark behavior
  changed.

## 2026-07-15 — Bytecode candidate-admission inventory
- Reconciled the retained bounded bytecode reports after the execution-lane
  study instead of rerunning unchanged workloads. The apparent common frames
  are dispatch/envelope parents or the already-rejected raw-integer,
  inline-return, member-cache, and call/frame families; their material
  descendants split across unlike applications. No VM, compiler, runtime,
  canonical-stdlib, fixture, or benchmark change is admitted. See
  `docs/perf-baselines/2026-07-15-bytecode-candidate-admission-inventory.md`.

## 2026-07-15 — Bytecode execution-lane reconciliation
- Reconciled the full-process bytecode scorecard with the warmed runtime
  benchmark: normal CLI rows include loading, lowering, typechecking,
  bootstrap, and `main()`, whereas the runtime benchmark has already loaded
  and checked the source graph. The full scorecard remains unchanged.
- Added explicit `able run --skip-typecheck` for trusted, separately validated
  source only. Ordinary `run` still typechecks; `check` rejects the flag. The
  focused CLI test gate passes. `bench_perf` / `bench_compare_external` now
  provide a separately named `bytecode-prechecked` lane, with its mandatory
  check outside timed processes; the scoreboard refuses that mode as input.
  All CPUs initially failed the immediate quiet-host gate (CPU 15 was closest).
  A later CPU-8-pinned paired study completed three processes for Sudoku,
  Word Frequency, and Future Pipeline. It rejects the boundary as a material
  common wall: Sudoku timed out in both lanes, Word Frequency was 1.33 s
  versus 1.32 s, and Future Pipeline was 0.41 s in both lanes. Verifier hashes
  matched in the completing pairs. Retained no VM, compiler, runtime, stdlib,
  fixture, or benchmark change; the prechecked lane remains non-scorecard. See
  `docs/perf-baselines/2026-07-15-bytecode-execution-lane-reconciliation.md`.

## 2026-07-15 — Full-scorecard historical reconciliation
- Corrected the initially proposed Fixed Width 128/Rational Series/K-Nucleotide
  follow-up: existing paired and generated-main profiles already diverge into
  UInt128 checked-member, generic call/frame, and map/conversion/GC work.
  The full scorecard expanded coverage but did not change source attribution,
  so rerunning that named triad would not meet the candidate-admission rule.
- Retained no VM, compiler, generated-runtime, canonical-stdlib, fixture, or
  benchmark change. The full scorecard remains the broad guard; performance
  selection resumes only after a material language-wide compiler/runtime or
  semantic-portability change yields one concrete non-nominal leaf across
  three unlike verifier-backed applications. See
  `docs/perf-baselines/2026-07-15-full-coverage-scorecard-historical-reconciliation.md`.

## 2026-07-15 — Full portable performance scorecard
- Completed the first fresh, CPU-14-pinned, verifier-backed scorecard for all
  32 portable applications: 24 source reports, 64 compiled/bytecode rows, and
  fresh Go/Python/Ruby references under the standard three-process,
  45-second, 1-GiB one-process guard.
- The promoted current scoreboard records 4/31 rankable compiled rows at the
  95%-of-Go target and 3/23 rankable bytecode rows at both interpreter
  targets. One partially successful bytecode K-Nucleotide row is now reported
  as `incomplete`/`unranked`; the report tool no longer rejects valid mixed
  success/timeout evidence.
- This is a broader selection baseline, not a source optimization. No VM,
  compiler, generated runtime, canonical stdlib, fixture, or benchmark code
  changed. Its initially proposed profile follow-up was reconciled against
  existing divergent evidence; see
  `docs/perf-baselines/2026-07-15-full-coverage-scorecard-historical-reconciliation.md`.

## 2026-07-15 — Feature and scorecard coverage reconciliation
- Confirmed that the 32 portable applications already cover every fair,
  spec-defined cross-language v12 feature family. Dynamic packages,
  user-authored externs, cancellation policy, and browser-host behavior remain
  deliberately local semantic guards; no synthetic benchmark was added.
- Repaired the measurement boundary instead: the scorecard refresh now includes
  all ten coverage-only applications in fresh reference and bounded Able
  groups, and promotes its exact source cohort to the checked-in current
  scoreboard. The scoreboard checker now reads that recorded cohort instead of
  silently rebuilding July 14 inputs.
- Synchronized `external-scoreboard-current.*` to the completed July 15
  22-application selection cohort. The first expanded run will establish the
  32-application baseline; no VM, compiler, canonical-stdlib, fixture, or
  benchmark-source behavior changed. See
  `docs/perf-baselines/2026-07-15-feature-scorecard-coverage-reconciliation.md`.

## 2026-07-15 — Current bytecode three-shape profile gate
- Refreshed the warmed one-process bytecode profiles for split/join text,
  linked-list iterator collection, and numeric Array mapping under the 1 GiB,
  single-core guardrails. Each profile passed an immediate quiet-host check;
  the direct bytecode output checks produced `191484`, `382455000`, and
  `1097192358`.
- The common dispatcher parent splits below that point into named-call
  return/coercion and type matching, generator/member dispatch, and mono-Array
  slot transport. `finishInlineReturn` and raw-integer extraction repeat only
  as already-rejected helper families, with different semantic callers.
- No VM, compiler, canonical-stdlib, fixture, or benchmark-source change is
  retained. The profile paths, commands, and decision are recorded in
  `docs/perf-baselines/2026-07-15-current-bytecode-three-shape-profile-gate.md`.

## 2026-07-14 — Dependency-plan cross-application profile gate
- Completed the quiet-host, three-application refresh with fresh
  verifier-backed Go/Python/Ruby references and three verified Able processes
  per mode. The complete-process comparisons include startup and show a real
  target miss, but cannot alone select an inner VM or compiler change.
- Warmed bytecode profiles supplied 9.07 s for Document Audit, 7.23 s for Word
  Frequency, and 6.89 s for Dependency Plan. Their material work diverges into
  iterator/member-cache, string-key map/raw-integer, and Array/Queue/index
  paths. The only three-way leaf is the already-rejected
  `finishInlineReturn` family, so no candidate is authorized.
- The complete-program benchmark now honors the existing
  `ABLE_SOURCE_ROOT_ONLY` contract, preventing an input-only working directory
  from becoming a duplicate package root. The focused regression passes. See
  `docs/perf-baselines/2026-07-14-dependency-plan-profile-gate.md`.
- The focused test, catalog check, and diff hygiene pass. The existing full
  `TestExecFixtureParity` aggregate did not finish under either the 60- or
  90-second host guard, so this is not recorded as a full-package pass.

## 2026-07-14 — Host-gated profile pause and container audit
- A three-sample shared CPU ranking found no eligible core; CPU 14 was least
  noisy but still peaked at 33.00% busy and 29.00% I/O wait. The profile gate
  remains intentionally unrun.
- Nine legacy applications have 20 missing Able Docker image lanes, but their
  canonical targets and direct foreign verifier lanes exist and power the
  active local harness. Do not blindly duplicate their 823 lines of Able
  source into Docker folders without a source-sync design; that would create
  stale benchmark copies rather than performance evidence.

## 2026-07-14 — Quiet-host CPU ranking
- Added `bench_host_cpu_check --rank-cpus`, which samples all logical CPUs
  together and ranks peak busy/I/O-wait values. It is discovery only; the
  immediate per-core preflight remains mandatory before timing.
- CPU 10 also failed the formal gate (8.24–15.00% busy; up to 67.01% I/O
  wait), so no performance artifact or runtime candidate was created.

## 2026-07-14 — Portable-suite coverage containment
- `bench_catalog_check` now proves every named portable suite is contained in
  the authoritative `coverage` population, so a focused timing lane cannot
  silently bypass broad-corpus selection safeguards.
- CPU 12 also failed the fresh formal host check (9.18–11.22% busy), so no
  measurement was collected. No runtime, compiler, stdlib, or workload
  behavior changed.

## 2026-07-14 — Quiet-host profile gate remains closed
- No logical core was ready for timing. CPU 0, the least-busy candidate,
  failed all formal samples: 17.00–26.26% busy and 9.00–47.00% I/O wait versus
  5% limits.
- No profile, scorecard, or candidate was recorded. Resume only after
  `just bench-host-check --cpu CPU` passes, using a verifier-backed,
  cross-family three-application gate.

## 2026-07-14 — Per-suite portable catalog validation
- `bench_catalog_check --suite NAME` now accepts every named portable suite,
  instead of only `coverage` and `corpus-full`. The latter still includes all
  local fixtures; a named suite verifies just its declared portable lanes.
- This is no-timing benchmark-harness integrity work prompted by the new
  one-row Dependency Plan suite; no runtime, compiler, stdlib, or workload
  behavior changed.

## 2026-07-14 — Dependency-plan portable application coverage
- Added `dependency-plan` as the 32nd portable `coverage` application with
  matching Able, Go, Python, Ruby, Docker, and output-verifier lanes. It
  resolves deterministic 1,024-service deployment plans through general
  Array/Queue topological planning; all implementations produce
  `1024:12595200`.
- This is coverage only, not a graph-, queue-, or workload-specific
  optimization candidate, and it remains outside the stable `generality`
  timing screen. Any later candidate needs a concrete leaf shared with two
  unlike verifier-backed applications.
- Verification: all reference lanes, both Able interpreters, and an
  Able-compiled binary agree; catalog/scoreboard checks and the 109-program
  lowering audit pass (410 functions, 20,205 instructions). No timings were
  collected.

## 2026-07-14 — Performance guide reconciliation
- Updated the live benchmark, bytecode-audit, and performance-handoff guides
  with the actual 31 portable / 77 local / 108 corpus catalog, the 406-function
  / 19,906-instruction lowering audit, catalog validation, and the single-CPU
  quiet-host guard.
- The guides now distinguish static breadth from timing evidence and reject an
  unchanged scorecard as a selection trigger; historical measurements remain
  context, not operating instructions.
- Verification: catalog and report-only scoreboard integrity checks pass. No
  VM, compiler, runtime, external stdlib, fixture, benchmark-workload, or
  performance behavior changed.

## 2026-07-14 — Benchmark catalog integrity reconciliation
- Added `just bench-catalog-check`, a static no-timing check for every
  portable coverage row and local fixture. It verifies Able targets, sibling
  suite/run directories, verifiers, default Go/Python/Ruby sources, and local
  fixture entries.
- Corrected active coverage totals after Array Slice Window: 31 portable
  applications plus 77 local fixtures, or 108 corpus programs. The full
  bytecode lowering audit passes with 406 functions and 19,906 instructions.
  This is count reconciliation, not an Array or benchmark-specific candidate.
- Verification: full/coverage catalog checks, invalid-suite rejection, and
  `bench_bytecode_audit --suite corpus-full` passed. No timing run, VM,
  compiler, runtime, external stdlib, fixture, or benchmark-workload behavior
  changed.

## 2026-07-14 — Benchmark host preflight
- Added `bench_host_cpu_check` plus `just bench-host-check`, a generic Linux
  `/proc/stat` readiness check for one CPU-pinned timing lane. The scorecard
  refresh can invoke it with `--require-quiet-cpu --cpu-affinity CPU` before
  it creates reports or temporary workspaces; the opt-in guard needs one CPU.
- CPU 15 failed the three default one-second samples (16.83–25.51% busy; up
  to 19.80% I/O wait), so no noisy profile or scorecard was recorded. Shell
  syntax, permissive-control, and guarded dry-run checks pass; a passing
  preflight does not reopen performance selection. No VM,
  compiler, runtime, external stdlib, fixture, benchmark-workload, or
  performance behavior changed. See
  `docs/perf-baselines/2026-07-14-benchmark-host-preflight.md`.
- Extended the same opt-in guard to direct timing commands: `bench_perf`,
  `bench_compare_external`, `bench_refresh_go_refs`, and
  `bench_refresh_interpreter_refs`. Each requires one pinned CPU and checks
  before creating timing artifacts; the grouped scorecard keeps its one outer
  check to avoid redundant child samples. No benchmark was launched.

## 2026-07-14 — Go concurrency guide reconciliation
- Replaced the stale concurrency note with an active contributor guide. Public
  behavior routes to spec §12 and executor detail to
  `design/go-concurrency-scheduler.md`; serial is the Go default, while
  goroutine execution is an explicit selection. The TypeScript continuation
  proposal is historical only.
- Recorded helper arity, blocked-flush, synchronization, parity, and
  performance-selection boundaries without changing runtime behavior.
- Verification: focused interpreter concurrency coverage passed in 0.440s;
  the compiled goroutine blocked-flush control passed in 1.149s. No spec, AST,
  runtime, compiler, external stdlib, fixture, benchmark, or performance
  behavior changed.

## 2026-07-14 — Parser workspace README reconciliation
- Replaced the obsolete scaffold/status narrative with the active Go-first
  parser contract: checked-in Tree-sitter artifacts, Go AST mapping, source
  fixtures, source re-exports, and the retired `proc`/unspecced safe-navigation
  boundary.
- Documented the actual corpus, fixture-export, default Go parser, and forced
  cgo-relink commands. No parser, AST, runtime, compiler, external stdlib,
  fixture behavior, or benchmark behavior changed.

## 2026-07-14 — Parser documentation reconciliation
- Reclassified the 2025 parser roadmap and tree-sitter node inventory as
  historical bring-up records; their future-tense phases and missing-production
  claims are not an active syntax backlog.
- Made `design/parser-ast-coverage.md` the active maintenance matrix, added
  the completed source-re-export row, and recorded the required corpus,
  fixture, mapper, interpreter, and compiler checks for future syntax. The
  design index routes readers to these active documents.
- Verification: source-re-export tree-sitter corpus and default Go parser
  package pass (`0.180s`). No grammar, AST, runtime, compiler, external stdlib,
  fixture behavior, or benchmark behavior changed.

## 2026-07-14 — Non-WASM feature-selection audit
- Completed the proposed feature-led selection audit. `spec/TODO_v12.md` has
  no live compiler-AOT or external-stdlib gap; regex is complete; and the only
  remaining implementation markers are the deferred JS/WASM extern boundary
  and the unknown-bytecode-opcode safety error.
- The established feature/benchmark reconciliation remains authoritative: 31
  portable applications and 77 local fixtures cover the active non-WASM
  contract. The native-carrier TODO text is historical/guardrail material, not
  authority for a container- or benchmark-specific fast path. No synthetic
  application, source feature, VM/compiler change, or external-stdlib change
  is selected.

## 2026-07-14 — Performance selection revalidation and cleanup check
- Revalidated the current performance gate after source re-exports. That work
  leaves bytecode/compiler hot paths unchanged, so the retained 14 July
  three-shape CPU profiles and full verifier-backed scorecard remain current.
  Their shared dispatcher parents still diverge into text, iterator, and
  numeric material work; no repeated concrete leaf is eligible.
- No fresh benchmark was recorded because the designated CPU had sustained
  I/O wait during the host check. No VM, compiler, runtime, external stdlib,
  fixture, or benchmark source changed. Reopen selection only for a repeated
  non-nominal descendant in at least three unlike verified programs.
- Confirmed the deferred project cleanup command: `just cleanup` previews,
  `just cleanup-apply` removes generated ignored artifacts after a tracked-path
  refusal, and `just cleanup-archives` additionally removes saved profiles.
  It does not select source, fixtures, external `able-stdlib`, or v10/v11.

## 2026-07-14 — Explicit source re-exports
- Completed the grammar-first source re-export contract from spec §13.4:
  `export Name;` and `export * from package;` now parse into the shared
  `ast.ExportStatement` and are retained in `Module.Exports`. The tree-sitter
  corpus and `imports/source_reexport_syntax` AST fixture establish the
  syntax/JSON contract.
- The loader includes wildcard export sources in dependency closure. The
  program checker publishes named/wildcard re-exports through package
  summaries, rejects private selected-source exports, and preserves imported
  implementation/method propagation. Tree-walker and bytecode evaluation
  publish the original runtime values.
- Compiler static resolution and no-bootstrap import seeding follow original
  bindings through re-export chains, including nominal struct carriers, without
  an interpreter fallback or named-type special case. No external stdlib,
  benchmark, or performance change.
- Verification: source-reexport tree-sitter corpus; focused parser,
  typechecker, interpreter (tree-walker and bytecode), compiler groups, and
  the shared AST fixture harness.

## 2026-07-14 — Named-implementation import collision audit
- Completed static named-implementation import collision controls across the
  program checker and tree-walker. Direct/wildcard conflicts report a
  selector-alias recovery; renamed imports remain valid; private namespaces
  remain package-private; and a local declaration cannot replace an imported
  named implementation. Compiler namespace emission preserves privacy.
- Parser: added declaration precedence for a named implementation with an
  empty body, so `Fancy = impl Marker for Widget {}` reaches the canonical AST
  instead of becoming an assignment expression.
- Generic source re-exports remain parser/AST work, not a hidden package or
  compiler rule. See `design/reexport-named-implementation-import-audit.md`
  and `spec/TODO_v12.md`. No canonical-stdlib or performance work changed.
- Verification: the focused grammar corpus, full `pkg/parser` (0.167s), full
  `pkg/typechecker` (0.023s), focused runtime/compiler controls, and the named
  implementation fixture in both interpreter modes passed. The standard
  wrapper passed coverage and scoreboard preflights, then its unrelated full
  compiler package test exceeded the one-minute test expectation and was
  stopped; this is not a full-suite pass.

## 2026-07-14 — Typechecker roadmap reconciliation
- Documentation: replaced the stale staged roadmap with the live Go checker
  contract. It now identifies the module and program-wide APIs, dependency
  ordering, privacy-aware summaries, inference side tables, CLI/interpreter/
  compiler integration, and the evidence required before selecting new checker
  work. The companion historical record retains the 2025 bootstrap and retired
  TypeScript/Bun parity chronology without presenting either as active work.
- Architecture: refreshed the detailed checker note and both Go-facing READMEs
  so module-only checking is not mistaken for package validation, and so
  fixture diagnostic policy is not confused with runtime semantics.
- Verification: focused `pkg/typechecker`, `cmd/able`, and interpreter
  typecheck-integration groups pass in 0.004, 0.068, and 0.066 seconds under
  the one-process memory guard. No checker, AST, parser, VM, compiler, runtime,
  canonical-stdlib, fixture, benchmark, or performance behavior changed.

## 2026-07-14 — User testing and regex-guide reconciliation
- Documentation: updated the manual, tooling page, and matcher guide to state
  that `able test`, `able.test`, and external canonical `able.spec` are live.
  The guides now distinguish user tests from implementation fixtures, describe
  the test profile and command surface, and point to spec §17.
- Regex accuracy: replaced the obsolete literal-only/unfinished-engine claim.
  `match_regex` delegates supported RE2-style patterns to the shared regex
  engine; invalid or intentionally unsupported patterns retain the matcher's
  existing literal-equality compatibility fallback. No API or matcher behavior
  changed.
- Verification: focused test-CLI and regex exec controls pass under the
  one-process memory guard. No parser, AST, VM, compiler, runtime,
  canonical-stdlib, fixture, benchmark, or performance behavior changed.

## 2026-07-14 — Unified testing-plan reconciliation
- Documentation: replaced the future rollout with the current Go-first split:
  Go package/AST/exec/parity/compiler verification is separate from user and
  canonical-stdlib test modules. The active authorities are spec §17, the Go
  fixture harnesses, the `able test` protocol record, and external
  `able-stdlib/src/test`/`src/spec.able` plus its suites.
- Historical decision: moved the completed quarantine promotion, TypeScript
  skeleton, Go CLI wiring, and spec rollout chronology to a companion record.
  They do not select a runner, framework, or performance feature.
- Verification: focused fixture typecheck/root and exec-coverage checks pass
  under the one-process memory guard. No parser, AST, VM, compiler, runtime,
  canonical-stdlib, fixture, benchmark, or performance behavior changed.

## 2026-07-14 — Test CLI UX-draft reconciliation
- Documentation: reclassified the duplicate `testing-cli-design.md` as a
  historical UX record. Its low-ceremony, explicit-framework, structured
  output, and fixture-separation principles remain; its TypeScript skeleton,
  quarantine layout, reporter/CLI implementation phases, and unsupported flag
  proposals are explicitly superseded by the active protocol.
- Index: corrected the unified testing-plan references so the active protocol
  is the CLI/framework authority and this note is historical rationale only.
- Verification: focused `able test` command parsing, empty-list behavior, and
  compiled-integration-lane classification pass under the one-process memory
  guard. No parser, AST, VM, compiler, runtime, canonical-stdlib, fixture,
  benchmark, or performance behavior changed.

## 2026-07-14 — Test CLI/protocol reconciliation
- Documentation: replaced the draft with the implemented `able test` contract:
  recursive `.test.able`/`.spec.able` discovery, interpreted bytecode or
  tree-walker modes, generated-Go `--compiled` execution, filters, list/dry
  runs, doc/progress/TAP/JSON reporters, and actual exit status behavior.
- Ownership: the Go CLI owns discovery/loading/dispatch/rendering; canonical
  external `able-stdlib` owns `able.test` protocol, registry, harness,
  reporters, and `able.spec`. The old remote-runner, manifest/schema, and
  extra-flag proposals are historical, not an implementation assignment.
- Verification: focused normal command/discovery tests pass in 1.47 seconds
  under the one-process memory guard. The generated-Go reporter suite is an
  opt-in release lane; an in-place probe was stopped after it failed to finish
  within the 55-second guard, so this record claims no compiled-reporter pass.
  No parser, AST, VM, compiler, runtime, canonical-stdlib, fixture, benchmark,
  or performance behavior changed.

## 2026-07-14 — ArrayStore lifetime-record reconciliation
- Documentation: condensed `design/array-handle-lifetime.md` to the live
  generic last-owner contract: tracked handle-bearing owners, explicit host
  adoption, atomic carrier moves, final backing/kind/revision removal, and
  weak borrowed caches. The completed stages and dated retention readings now
  live in a historical companion.
- Boundary: the diagnostic-only VM frame-local observer is now explicitly
  separate. Generic ArrayStore reclamation does not authorize its rejected
  early-release sidecar, nor any Base64/source/container-specific exception.
- Verification: focused runtime lease/final-release and interpreter/compiler
  Array-alias groups pass under the one-process memory guard. No parser, AST,
  VM, compiler, runtime, canonical-stdlib, fixture, benchmark, or performance
  behavior changed.

## 2026-07-14 — Frame-local Array ownership reconciliation
- Documentation: replaced the apparent production sidecar assignment with the
  active diagnostic boundary. The package-private bytecode observer records
  canonical creation, return transfer, conservative escapes, and frame-local
  or error-unwound candidates only in opt-in tests/profiles; it never releases
  an ArrayStore lease and ordinary VMs allocate neither observer nor context.
- Historical decision: moved the staged sidecar/cleanup design and exact
  profile evidence into a companion record. The explicit-release follow-up was
  removed after a one-run 1.9% Base64 gain paired with an 8.9% I-Before-E
  regression, which fails the required three-unlike-application shared-leaf
  gate. Generic ArrayStore last-owner reclamation remains a separate live
  lifetime contract.
- Verification: focused ownership-metadata, creation/transfer, escape,
  explicit-return, and error-unwind tests pass under the one-process memory
  guard. No parser, AST, VM, compiler, runtime, canonical-stdlib, fixture,
  benchmark, or performance behavior changed.

## 2026-07-14 — Performance-selection policy reconciliation
- Documentation: reduced `design/performance-competitiveness-vision.md` to
  the active targets, current verifier-backed scorecard, measurement rules,
  and cross-application candidate gate. The target is compiled Able at least
  95% of Go and bytecode Able at least 95% of both Python and Ruby where both
  references exist; the current one-run screen has 4/21 and 2/14 rankable rows
  meeting those targets, respectively.
- Decision: the refresh exposes material misses but no same concrete,
  non-nominal leaf across three unlike applications. The known compiled async
  identity-bridge remedy previously failed broad default-enable guards, so no
  VM, compiler, runtime, stdlib, bridge, or benchmark source change is
  selected. The former dated benchmark slices and experiment lessons are now
  in `design/performance-competitiveness-historical.md`.
- Verification: `just bench-scoreboard-check` passes. No parser, AST, VM,
  compiler, runtime, canonical-stdlib, fixture, benchmark, or performance
  behavior changed.

## 2026-07-14 — Bytecode VM-v2 record split and reconciliation
- Documentation: reduced `design/bytecode-vm-v2.md` from 3,195 lines to a
  99-line active Go bytecode VM contract. It now states the actual
  stack/slot/frame/raw-lane model, semantic and dynamic boundaries, cache
  requirements, and the verifier-backed performance-selection gate.
- History: moved the superseded 2026-04 architecture/staging proposal and the
  later register/array/text/host/nominal/float experiment lessons into bounded
  historical records, then updated the design index. Neither archive selects
  a typed-frame, source-shape, named-container, or benchmark candidate.
- Verification: focused register-frame, inline-call/return, value-sidecar, and
  program-switch lifecycle tests pass in 0.15 seconds under the one-process
  memory guard. No parser, AST, VM, compiler, canonical-stdlib, fixture,
  benchmark, or performance behavior changed.

## 2026-07-14 — Bytecode i32-frame gate reconciliation
- Documentation: replaced the obsolete future-frame roadmap with the current
  VM contract. Pooled i32 register lanes and value-slot sidecars are live,
  including inline-frame detach/restore and program-switch lifecycle; only the
  typechecker-derived proof metadata is inert.
- Historical decision: retained the completed proof-plumbing stage and the
  reverted frame-object experiment, including its 10.7% MatrixMultiply
  regression. Neither a frame-owned primitive lane nor a general typed
  call/return ABI is an active candidate without fresh shared profile evidence.
- Verification: focused proof attachment/fallback, raw register storage,
  inline call preservation, value-sidecar restoration, and program-switch
  lifecycle tests pass in 0.07 seconds under the one-process memory guard. No
  parser, AST, VM, compiler, canonical-stdlib, fixture, benchmark, or
  performance behavior changed.

## 2026-07-14 — Compiler-union ABI record reconciliation
- Documentation: replaced the obsolete union bring-up plan with the current
  shared-carrier contract and a compact historical staging record. The compiler
  normalizes package-aware union/result shapes, synthesizes generated carriers
  only when every member is statically representable, and keeps matching and
  direct interface branches native.
- Specification TODO: removed the completed “first union pass” queue and the
  inaccurate claim that a partially native carrier may retain a broad runtime
  member. `runtime.Value` conversion remains an explicit dynamic/ABI edge.
- Verification: focused closed/named/multi-member, result, generic alias,
  interface-member, and successful/rejected dynamic callback union tests pass
  in 15.15 seconds under the one-process memory guard. No parser, AST,
  compiler, runtime, canonical-stdlib, fixture, benchmark, or performance
  behavior changed.

## 2026-07-14 — Interface-dispatch record reconciliation
- Documentation: replaced the stale dictionary-design proposal with the
  source-backed interpreter and dynamic-boundary record. It now identifies the
  shared dictionary cache by concrete type/interface/type arguments, the
  per-value overlay and bound-method memo, and invalidation when visible
  method/impl/type state changes.
- Compiler boundary: recorded that proved static interface paths use generated
  `__able_iface_*` Go carriers and direct calls; runtime-value conversion
  helpers are only explicit dynamic/ABI edges. The interpreter dictionary is
  not an alternate compiled static lowering strategy.
- Verification: focused interpreter cache/memo and compiler native-interface /
  dynamic-helper tests pass in 0.28 seconds under the one-process memory
  guard. No parser, AST, compiler, runtime, canonical-stdlib, fixture,
  benchmark, or performance behavior changed.

## 2026-07-14 — Mono-array ABI record reconciliation
- Documentation: reclassified `design/monomorphized-container-abi.md` as the
  historical staging record it is. Its runtime-value static-storage claim,
  staged ABI shapes, open questions, and checklists no longer describe a
  current ABI or work queue.
- Specification TODO: removed the stale pending mono-array/native-lowering
  workstreams. Current ownership is the direct compiler, the active
  native/generic guardrails, and strict no-fallback/generated-source tests;
  runtime arrays are boundary conversions and no container candidate is
  selected from the historical record.
- Verification: focused mono-array default-option, compiler-owned
  static-wrapper, and direct boundary-conversion tests pass in 0.26 seconds
  under the one-process memory guard. No parser, AST, compiler, runtime,
  canonical-stdlib, fixture, benchmark, or performance behavior changed.

## 2026-07-14 — Monomorphization-record reconciliation
- Documentation: changed `design/compiler-monomorphization.md` into a
  historical specialization/benchmark record with active generic-carrier
  guardrails. The completed mono-array and container checklist no longer reads
  as an implementation queue or a license for named-container work.
- Current ownership: the compatibility-named `ExperimentalMonoArrays` option
  defaults to enabled; eligible static arrays use compiler-owned typed wrappers
  and runtime arrays are boundary conversions. `Array` is a language
  syntax/kernel case, while `Box`, `Heap`, and all other nominal types remain
  shared-specialization proof cases.
- Verification: focused mono-array default-flag, compiler-owned static-wrapper,
  direct boundary conversion, and generic nominal-specialization audits pass in
  0.56 seconds under the one-process memory guard. No parser, AST, compiler,
  runtime, canonical-stdlib, fixture, benchmark, or performance behavior
  changed.

## 2026-07-14 — No-panic control-flow reconciliation
- Documentation: reconciled `design/compiler-no-panic-flow-control.md` with
  the generated control helper. It now records `__ableControlRaise` with
  runtime-value/error payloads for raised/error propagation, instead of the
  obsolete return/break/continue enum proposal. Lexical returns and loop
  control remain structural Go flow.
- Scope: the no-panic/no-IIFE rule now applies to normal static Able control
  paths. It does not mistake semantically necessary closures, host faults,
  compiler invariants, or callback containment for language-control
  regressions. The note selects no control-flow rewrite or performance work.
- Verification: focused control-bridge, raised-native-error, static-native,
  explicit-dynamic-helper, retired-panic-wrapper, and pattern/control source
  audits pass in 0.54 seconds under the one-process memory guard. No parser,
  AST, compiler, runtime, canonical-stdlib, fixture, benchmark, or performance
  behavior changed.

## 2026-07-14 — Native-lowering guardrail split
- Documentation: split the overloaded `design/compiler-native-lowering.md`
  into a concise active
  `design/compiler-native-lowering-guardrails.md` (73 lines) and a 977-line
  historical completion record. The active document retains shared nominal
  lowering, native static control, explicit dynamic boundaries, and proof
  requirements; the dated array, union, interface, control, benchmark, and
  release progression remains available for regression archaeology.
- References: updated the active compiler documents, design index, subordinate
  control/monomorphization/interface notes, spec TODO, and roadmap to point to
  the guardrails. References to the old file now explicitly identify it as
  historical.
- Verification: focused fallback-policy, dynamic-feature classification,
  telemetry, static-native helper reachability, explicit dynamic-helper,
  static-native touchpoint, and static/dynamic bootstrap tests pass in 0.85
  seconds under the one-process memory guard. No parser, AST, compiler,
  runtime, canonical-stdlib, fixture, benchmark, or performance behavior
  changed.

## 2026-07-14 — Go-lowering guardrail reconciliation
- Documentation: changed `design/compiler-go-lowering-spec.md` from a
  misleading implementation specification into active lowering guardrails and
  target shapes. It no longer claims that the direct compiler consumes a
  standalone typed semantic graph or requires public `Synthesize*` APIs, and
  it no longer cites the historical AOT proposal as authority.
- Current ownership: the direct compiler starts from the shared AST,
  ProgramChecker inference, generator collection, and dynamic-feature
  classification. The retained rules require shared nominal lowering, explicit
  dynamic boundaries, and generated-source plus behavioral proof; they do not
  authorize a rewrite or select a performance candidate.
- Verification: focused fallback-policy, dynamic-feature classification,
  telemetry, static-native helper reachability, explicit dynamic-helper,
  static-native touchpoint, and static/dynamic bootstrap tests pass in 0.84
  seconds under the one-process memory guard. No parser, AST, compiler,
  runtime, canonical-stdlib, fixture, benchmark, or performance behavior
  changed.

## 2026-07-14 — Go-lowering completion-record reconciliation
- Documentation: changed `design/compiler-go-lowering-plan.md` from an
  active-looking completion plan into a historical completion record with
  active guardrails. The dated native-carrier, control, dispatch, runtime-core,
  boundary, performance, and validation milestones remain available for
  regression context, but no longer form an implementation queue.
- Current ownership: the compiler typechecks, collects the program,
  classifies dynamic features, enforces fallback policy, and renders Go
  directly from the AST. The only active rules retained in the record forbid
  named non-primitive lowering branches, interpreter-based static execution,
  emitter-local lowering inventions, and performance candidates without a
  shared recurring leaf. The experimental execution-context ABI remains
  opt-in.
- Verification: focused fallback-policy, dynamic-feature classification,
  telemetry, static-native helper reachability, explicit dynamic-helper,
  static-native touchpoint, and static/dynamic bootstrap tests pass in 0.80
  seconds under the one-process memory guard. No parser, AST, compiler,
  runtime, canonical-stdlib, fixture, benchmark, or performance behavior
  changed.

## 2026-07-14 — Compiler/interpreter vision reconciliation
- Documentation: reclassified the typed-core-IR, shared-runtime ABI,
  stack-to-register migration, and immediate-next-step portions of
  `design/compiler-interpreter-vision.md` as historical direction. It no
  longer names the historical AOT proposal as an authority or presents an
  unimplemented architectural migration as current work.
- Current ownership: the spec remains semantic authority; the Go bytecode VM
  source and focused tests define the executable VM contract; the compiler is
  the direct AST-to-Go generator with its current lowering notes and strict
  boundary tests. The current VM record now distinguishes its operand stack
  from slot-indexed locals, proved `i32` register frames, and inline VM call
  frames.
- Verification: the focused bytecode call-name, inline-frame, return, and
  caller-active-lookup-cache regression group passes in 0.81 seconds under the
  one-process memory guard. No parser, AST, VM, compiler, canonical-stdlib,
  fixture, benchmark, or performance behavior changed.

## 2026-07-14 — AOT compiler proposal reconciliation
- Documentation: reclassified `design/compiler-aot.md` as the historical AOT
  proposal it is. Its authoritative claim, standalone typed-IR architecture,
  transition list, exhaustive ten-phase roadmap, and definition of done no
  longer present as current compiler work.
- Current ownership: the spec defines AOT-boundary semantics; `compiler.go`
  and the direct AST-to-Go generator define implementation; `spec/TODO_v12.md`,
  strict fixture/boundary tests, and the full-matrix guide define current
  completion and verification evidence. The experimental execution-context ABI
  remains opt-in under the existing broad performance guard.
- Verification: focused compiler tests pass for no-fallback policy, reachable
  dynamic-feature classification, opt-in boundary telemetry, static native
  lowering, static dynamic-helper avoidance, explicit dynamic helper use, and
  static versus dynamic bootstrap behavior. No parser, AST, compiler, runtime,
  canonical-stdlib, fixture, benchmark, or performance behavior changed.

## 2026-07-14 — Regex design-record reconciliation
- Documentation: changed `design/regex-plan.md` from an active-looking,
  pre-implementation rollout into an implementation record with historical
  design notes. Its old claim that regex lacked runtime/parser support, its
  interim option restrictions, TypeScript parity language, retired in-tree
  source paths, phases, and follow-up tasks no longer present as current work.
- Current ownership: spec §14.2 defines the API and semantics; the external
  `able-stdlib/src/text` regex and automata modules are canonical source; the
  `exec/14_02` through `exec/14_25` fixtures are behavioral evidence. The
  existing performance history selects no new regex-specific optimization.
- Verification: representative core Unicode matching, named captures,
  `RegexSet`, incremental scanning, and builder/program fixtures pass in the
  tree-walker and bytecode VM using the external canonical stdlib. A compiled
  builder/program fixture could not complete its generated `go build` within
  the 55-second test guard; the timed-out child build was stopped and cleaned.
  This is a test-timeout constraint, not a regex assertion failure.
  No parser, AST, runtime, compiler, canonical-stdlib, fixture, benchmark, or
  performance behavior changed.

## 2026-07-14 — Standard-library blueprint reconciliation
- Documentation: reclassified the 2025 `design/stdlib-v12.md` plan as a
  historical blueprint. Its external canonical source, obsolete in-tree
  `stdlib/` layout, TypeScript/Bun references, package inventory, transitions,
  phases, and task lists no longer present as live v12 work.
- Current ownership: `spec/full_spec_v12.md` defines behavior; the external
  `able-stdlib/src` tree is the implementation source; Go executable fixtures
  are the behavioral evidence. The protocol-contract and filesystem/I/O/text
  reconciliation records are linked directly from the historical blueprint.
- Verification: with an isolated `ABLE_HOME` selecting the external canonical
  source, `06_12_01_stdlib_string_helpers`,
  `06_12_10_stdlib_collections_list_vector`,
  `06_12_19_stdlib_concurrency_channel_mutex_queue`, and
  `06_12_27_stdlib_iterator_close` pass in tree-walker, bytecode, and compiled
  execution (each compiled fixture completed in 2.39--13.83 seconds).
  No parser, AST, runtime, compiler, canonical-stdlib, fixture, benchmark, or
  performance behavior changed.

## 2026-07-14 — Channel/Mutex and generator proposal reconciliation
- Documentation: archived the remaining TypeScript-era Channel/Mutex rollout
  and generator-continuation proposals. Their retired in-tree stdlib paths,
  `FutureYieldSignal` machinery, and pending-work statements no longer imply
  active v12 implementation work.
- Current ownership: spec §§6.7, 12.5, 12.7, and 14 define the behavior; the
  canonical external stdlib owns `Channel`/`Mutex` and their structured errors;
  the Go tree-walker, bytecode VM, and compiler provide the runtime boundary.
- Verification: `06_07_iterator_pipeline` and
  `12_07_channel_mutex_error_types` pass in tree-walker, bytecode, and
  compiled execution. No generator, scheduler, runtime, compiler, stdlib,
  fixture, or benchmark behavior changed.

## 2026-07-14 — Go executor contract reconciliation
- Specification: reconciled the stale §12 `future_flush(limit?: i32)` prose
  with the existing required builtin, typechecker, interpreters, and compiler:
  the public helper is `future_flush()` with zero arguments. It drains the
  configured executor and may return when every remaining task is blocked.
- Design: replaced the stale Go scheduler record with the actual serial and
  goroutine executor behavior, and marked the retired TypeScript
  `schedulerMaxSteps` / continuation proposal historical. The active workspace
  contains only the Go reference runtime.
- Manual: corrected the Markdown and rendered concurrency guides to use
  `future_flush()` and describe drain behavior without the retired
  TypeScript-only limit form.
- Regression: added `TestFutureFlushRejectsArguments` so the obsolete optional
  limit cannot re-enter the public type surface.
- Verification: focused typechecker, serial/goroutine executor, and compiled
  flush/blocked-task tests pass under the bounded one-process guard. No
  scheduler, VM, compiler, stdlib, fixture, or benchmark behavior changed.

## 2026-07-14 — Future unification status reconciliation
- Documentation: marked the completed `proc`-to-`Future` design as implemented
  in the Go tree-walker, bytecode VM, and AOT compiler. The active semantic
  source is spec §12; `ProcHandle` remains a distinct host-process interop
  type, not the removed Able `proc` feature.
- Parser docs: removed the obsolete `proc` keyword claim from the parser
  workspace README and tree-sitter reference notes. The live grammar reserves
  `spawn` and has no `ProcExpression` path.
- Verification: `12_04_future_handle_value_view` passes in tree-walker,
  bytecode, and compiled execution. It covers resolved values, repeated
  memoized failure access, cancellation observation, and terminal status. No
  scheduler, runtime, compiler, stdlib, fixture, or benchmark behavior changed.

## 2026-07-14 — String/text proposal reconciliation
- Documentation: reclassified the 2025 `string-plan.md` as historical. It
  referenced the retired in-tree stdlib and treated already-implemented
  `String` APIs—including UTF-8 lengths, substring, search/split/replace, and
  prefix/suffix helpers—as missing work.
- Decision: current API and coverage ownership remains the external canonical
  stdlib and `docs/perf-baselines/2026-07-14-fs-io-text-roadmap-reconciliation.md`.
  The remaining proposal ideas are not a v12 backlog or a performance
  candidate without a new specified behavior and normal cross-mode evidence.
- Verification: the canonical string-helper fixture matrix passes in
  tree-walker, bytecode, and compiled execution. No runtime, compiler,
  stdlib, fixture, or benchmark behavior changed.

## 2026-07-14 — Compiler Array carrier comment reconciliation
- Compiler documentation: corrected the stale `TypeMapper.mapArrayType`
  comment, which still described pre-specialization `*Array` lowering and an
  obsolete future slice-intrinsics TODO. Static `Array T` now first selects its
  compiler-owned specialized native slice carrier; the generic native Array
  carrier is the residual non-specialized/static boundary path.
- Verification: default mono-array type-mapping tests pass. This is a comment
  correction only—no compiler lowering, runtime, stdlib, fixture, or benchmark
  behavior changed.

## 2026-07-14 — Core test-skip audit
- Audit: reviewed every `t.Skip` across the 431 non-WASM interpreter/compiler
  test files after restoring the two stale parser gates. No further stale
  feature exclusion remains. The surviving skips are deliberate `-short`
  limits, absent toolchain/stdlib guards, explicit fixture-subset selection,
  or manifest target/typecheck policy.
- Verification: the full interpreter package and a canonical-stdlib compiler
  static-carrier guard completed under the bounded one-process limit. No test,
  runtime, compiler, stdlib, grammar, or benchmark behavior changed.

## 2026-07-14 — Go parser fixture-corpus gate restored
- Parser coverage: removed the obsolete `GO_PARSER_FIXTURES=1` opt-in gate.
  All 175 shared AST fixtures already carry `source.able` plus canonical
  `module.json`, and the parser harness compares the parser's serialized AST
  directly with that canonical fixture representation.
- Verification: the default full Go parser package completes the corpus in
  `0.171s` under the 1 GiB one-process guard; `-short` still skips the corpus
  deliberately. No fixture schema/decoder, grammar, AST, runtime, compiler,
  stdlib, or benchmark behavior changed.

## 2026-07-14 — Struct-literal comment parser regression coverage
- Parser coverage: re-enabled `TestParseStructLiteralIgnoresComments`. The
  tree-sitter grammar already treats `##` comments as extras and the Go mapper
  already ignores extras; the stale skip incorrectly claimed that comments
  inside struct literals were rejected.
- Verification: the focused struct literal/pattern tests and the full Go parser
  package pass under the bounded one-process guard. No grammar, AST, runtime,
  compiler, stdlib, or benchmark behavior changed.

## 2026-07-14 — Array slice performance-selection precondition
- Selection: the portable-source inventory finds only the canonical
  `array_slice_window` application using `Array.slice`; the external
  `run.able` is its byte-identical Docker snapshot, not a second application.
  The strict fixture is semantic coverage, not a performance carrier.
- Decision: retain no VM, compiler, runtime, or stdlib optimization. Do not
  manufacture slice-shaped applications simply to meet the three-unlike-app
  requirement. See
  `docs/perf-baselines/2026-07-14-array-slice-selection-precondition.md`.

## 2026-07-14 — Array slice portable coverage application
- Coverage: added `array-slice-window`, a verifier-backed rolling-batch
  application for the newly canonical copying `Array.slice` semantics. Able,
  Python, Ruby, and Go each produce an independent shallow range copy; the Go
  reference deliberately uses `make` plus `copy` instead of an aliasing
  subslice.
- Scope: this row is in the external `coverage` catalog only. It establishes
  feature and lowering coverage but is not evidence for an Array-specific
  compiler, VM, runtime, or stdlib optimization.
- Verification: the checksum passes in Go, Python, Ruby, tree-walker,
  bytecode, and compiled executions. The one-run reference rows are recorded
  as coverage only in `docs/perf-baselines/2026-07-14-array-slice-window-application-coverage.md`.

## 2026-07-14 — Canonical `Array.slice`
- Stdlib/spec: added `Array.slice(start: u64, end: u64) -> !Array T` to the
  canonical external stdlib and made its end-exclusive, independently-backed
  shallow-copy contract normative. Valid bounds are
  `0 <= start <= end <= size`; failures return `IndexError`; result capacity
  exactly equals result length.
- Scope: implemented solely through existing portable Array primitives. There
  is no VM opcode, kernel representation/view, ArrayStore lease change,
  compiler lowering branch, named-container exception, or benchmark change.
- Coverage: added `exec/06_12_29_stdlib_array_slice` to the default strict
  compiler fixture set and the canonical stdlib Array smoke test. The focused
  fixture passes tree-walker, bytecode, and compiled execution.
- Evidence: `design/array-slice-semantics-audit.md`.

## 2026-07-14 — Standard-library protocol contract reconciliation
- Specification: renamed §14 from the obsolete “Conceptual / TBD” label to
  “Standard Library Protocols,” established the external canonical stdlib as
  its implementation source, and aligned both iterator presentations with the
  public default-idempotent `close()` contract and cleanup propagation.
- Compiler gate: promoted `exec/06_12_27_stdlib_iterator_close` into the
  default strict no-fallback compiler fixture list.
- Evidence: `design/stdlib-protocol-contract-audit.md`; the focused
  `exec/06_12_27_stdlib_iterator_close` matrix remains the behavioral guard.
- Scope: documentation only; no parser, AST, interpreter, compiler, stdlib,
  benchmark, or runtime behavior changed.

## 2026-07-14 — Performance handoff reconciliation
- Documentation: reconciled the performance-competitiveness vision with the
  current scorecard and July profile gates. The former typed-frame/quicksort
  assignment order is now explicitly obsolete; the handoff requires a new
  shared non-nominal leaf across three unlike verified applications before any
  performance implementation work resumes.
- Evidence: `docs/perf-baselines/2026-07-14-performance-handoff-reconciliation.md`.

## 2026-07-14 — Grouped full-scorecard refresh command
- Tooling: added `just bench-scorecard-refresh`, which rebuilds fresh external
  references, runs the 16 generality and six async application rows in bounded
  compiled/bytecode groups, retains dated source reports, and aggregates them
  without overwriting the checked-in current scoreboard.
- Guardrails: it defaults to three runs, 45-second caps, and the 1 GiB
  one-process guard; it refuses report overwrites and supports a no-write
  `--dry-run`.
- Verification: shell syntax check plus an end-to-end dry-run confirmed all
  16 source-scorecard commands and the aggregate command, with no files or
  temporary directories created.

## 2026-07-14 — Full verifier-backed application scorecard refresh
- Measurement: refreshed all 16 generality programs and all six standalone
  asynchronous applications, with fresh CPU-15-pinned Go/Python/Ruby references,
  one-process 1 GiB guards, 45-second caps, and canonical output verifiers.
- Decision: kept no source change. The resulting 4/21 compiled and 2/14
  bytecode target counts are a one-run selection screen, not a regression
  claim. Concurrency misses recur across all six applications, but the
  existing cross-application profile gates already separate bytecode
  executor/dispatcher parents from their material children and identify the
  compiled identity bridge as an ABI candidate previously rejected for default
  enablement by broad guards.
- Evidence: `docs/perf-baselines/2026-07-14-full-scorecard-refresh.md` and
  `docs/perf-baselines/2026-07-14-full-scorecard-refresh-selection.md`.

## 2026-07-14 — Three-shape bytecode profile refresh
- Performance selection: refreshed pinned, warmed one-process CPU profiles
  for split/join text, linked-list iterator collect, and numeric Array map,
  plus a bounded mutex/`ensure` control and dispatch traces.
- Decision: retained no VM, compiler, runtime, stdlib, or benchmark change.
  The workloads share dispatcher/envelope parents only; their material routes
  are UTF-8/tracked-Array text work, iterator `next`/`yield`/inline work, and
  mono-`i32` Array transport respectively. `ensure` already executes a cached
  nested bytecode program, so it is not a repeated-lowering target.
- Evidence: recorded commands, samples, traces, and the next scorecard gate
  in `docs/perf-baselines/2026-07-14-three-shape-bytecode-profile-refresh.md`.

## 2026-07-14 — Lazy `able.fs.lines` resource iterator
- Stdlib: added `fs.lines(path) -> FileLines` in the canonical `able-stdlib`
  checkout. `FileLines` owns its `IoHandle` and `BufferedReader`, normalizes
  LF/CRLF, preserves a final unterminated line, closes at EOF or explicitly,
  and preserves a read error while best-effort cleanup marks the iterator
  closed.
- Bytecode: fixed general named-struct literal lowering for parsed shorthand
  fields and closure-held nominal definitions, so local struct fields stay in
  VM slots instead of falling back to tree evaluation.
- Fixtures: added `exec/06_12_28_stdlib_fs_lines` for EOF, explicit close,
  loop-break, missing path, and induced closed-handle read behavior. It is in
  the default compiler fixture set under the no-fallback gate.
- Docs: made the `FileLines` lifecycle contract canonical in the v12 spec,
  iterator-close audit, and plan.
- Tests: focused bytecode literal tests; fixture in tree-walker and bytecode;
  compiled fixture with `RequireNoFallbacks`; exec coverage index check.

## 2026-02-26 — Compiler struct/type resolution + dynamic warning reachability
- Compiler: switched struct metadata collection to package-qualified keys so same-named structs across packages no longer collide during AOT collection.
- Compiler: made `TypeMapper` resolve struct types with package/import context, including static selector/wildcard imports, instead of global-name lookup.
- Compiler: dynamic-feature reporting now tracks entry reachability and only treats reachable modules as dynamic for warnings and static-fallback policy checks.
- Compiler: entry struct seeding now skips ambiguous same-name structs and no longer depends on map keys being unqualified names.
- Tests: added `compiler_warning_scope_test.go` coverage for cross-package duplicate-name structs, unreachable dynamic warning suppression, and strict static fallback behavior with unreachable dynamic modules.
- Tests: `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache go test ./pkg/compiler -run 'TestCompilerCrossPackageStructNamesDoNotWarnAsDuplicates|TestCompilerDynamicWarningsIgnoreUnreachableModules|TestCompilerStaticFallbackGuardIgnoresUnreachableDynamicModules|TestDetectDynamicFeaturesUsesDynamicIgnoresUnreachableModules|TestDetectDynamicFeatures'`; `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache go test ./pkg/compiler`; `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache go test ./cmd/able`.

## 2026-02-09 — Compiled mutex runtime + await helpers
- Runtime: added a compiled mutex handle store (`MutexStoreNew/State`) with sync.Cond-backed state.
- Compiler: implemented compiled `__able_mutex_new`, `__able_mutex_lock`, `__able_mutex_unlock`, and `__able_mutex_await_lock` with awaiter tracking.
- Compiler: mapped mutex extern calls to compiled wrappers (no interpreter fallback).
- Tests: `cd v12/interpreters/go && go test ./pkg/compiler -run TestCompilerEmitsStructsAndWrappers -count=1`.

## 2026-02-09 — Compiled channel await helpers
- Compiler: added compiled `Awaitable` plumbing for channel await helpers (`__able_channel_await_try_recv`, `__able_channel_await_try_send`) with waiters/awaiters tracking and waker registration.
- Compiler: wired channel send/receive operations to notify awaiters and respect close signals without interpreter fallback.
- Runtime: changed channel store to signal closure via `CloseCh` (no close on value channel) to avoid send panics.
- Tests: `cd v12/interpreters/go && go test ./pkg/compiler -run TestCompilerEmitsStructsAndWrappers -count=1`.

## 2026-02-09 — Compiled channel send/receive helpers
- Compiler: implemented compiled runtime helpers for `__able_channel_send`, `__able_channel_receive`, `__able_channel_try_send`, and `__able_channel_try_receive` with channel-close error mapping.
- Compiler: mapped channel send/receive extern calls to compiled wrappers (no interpreter fallback).
- Tests: `cd v12/interpreters/go && go test ./pkg/compiler -run TestCompilerEmitsStructsAndWrappers -count=1`.

## 2026-02-09 — Compiled channel lifecycle helpers
- Compiler: added compiled runtime helpers for `__able_channel_new`, `__able_channel_close`, and `__able_channel_is_closed`, including concurrency error value construction.
- Runtime: added a shared channel handle store for compiled code (`ChannelStoreNew/Close/IsClosed`).
- Compiler: mapped channel lifecycle extern calls to compiled wrappers (no interpreter fallback).
- Tests: `cd v12/interpreters/go && go test ./pkg/compiler -run TestCompilerEmitsStructsAndWrappers -count=1`.

## 2026-02-09 — Compiled string/char + numeric extern bridges
- Compiler: added compiled runtime implementations for `__able_String_from_builtin`, `__able_String_to_builtin`, `__able_char_from_codepoint`, and `__able_char_to_codepoint` using the shared array store (no interpreter bridge).
- Compiler: added compiled runtime implementations for `__able_ratio_from_float`, `__able_f32_bits`, `__able_f64_bits`, and `__able_u64_mul`, raising standard error values for overflow/div-by-zero.
- Compiler: mapped string/char/numeric extern calls to compiled wrappers and added required Go imports in generated output.
- Docs: noted the extern transition in `design/compiler-aot.md`.
- Tests: `cd v12/interpreters/go && go test ./pkg/compiler -run TestCompilerEmitsStructsAndWrappers -count=1`.

## 2026-02-09 — Compiler AOT plan alignment for BigInt
- Docs: clarified that `BigInt` is a stdlib type compiled with stdlib packages, not a dedicated runtime primitive, and adjusted the compiled-runtime checklist accordingly.
- Plan: moved BigInt work under stdlib compilation in `PLAN.md`.
- Tests not run (plan/doc update only).

## 2026-02-09 — Compiled Ratio arithmetic fast-path
- Compiler: added compiled runtime Ratio arithmetic/comparison handling, plus Ratio coercion helpers, to avoid interpreter fallback for Ratio operations.
- Compiler: upgraded `__able_panic_on_error` to raise runtime error values emitted by compiled helpers.
- Docs: noted the Ratio fast-path in `design/compiler-aot.md`.
- Tests: `cd v12/interpreters/go && go test ./pkg/compiler -run TestCompilerEmitsStructsAndWrappers -count=1`.

## 2026-02-09 — AOT channel/mutex/future task decomposition
- Plan: split the Channel/Mutex/Future runtime work into smaller, verifiable slices in `PLAN.md`.
- Tests not run (plan update only).

## 2026-01-25 — Exec-mode flag + fixture mode runs
- CLI: added `--exec-mode=treewalker|bytecode` global flag and wired treewalker/bytecode wrappers to pass it.
- Tests: added an exec-mode flag for interpreter fixture tests and updated `v12/run_all_tests.sh` to run fixtures in bytecode mode.
- Docs: parity reporting notes now reference the exec-mode flag.
- Tests: `./run_all_tests.sh`; `./run_stdlib_tests.sh`.

## 2026-01-25 — Go fixture exporter + interface runtime fixes
- Tooling: added a Go fixture exporter (`v12/interpreters/go/cmd/fixture-exporter`) and wired `v12/export_fixtures.sh`; exposed parser-side fixture normalization and tightened integer literal JSON output.
- Runtime: fixed interface-method receivers and generic `Self` checks for interface dictionaries; wildcard generic bindings now accept concrete interface args so `Iterator` values satisfy `Iterable` at runtime.
- Docs: removed the stale “Go exporter TODO” note from the manual.
- Tests: `./run_all_tests.sh`; `./run_stdlib_tests.sh`.

## 2026-01-25 — v12 fork + Go-only toolchain
- Forked v11 → v12 and added `spec/full_spec_v12.md` plus `spec/TODO_v12.md`.
- Removed the TypeScript interpreter from v12; introduced Go-only CLI wrappers `abletw` (tree-walker) and `ablebc` (bytecode).
- Updated v12 docs and harnesses to reference v12 paths and Go-only workflows; parity docs now target tree-walker/bytecode.
- Tests not run (workspace + docs refactor).

## 2026-01-25 — Interface dictionary fixture coverage
- Fixtures: added exec coverage for default generic interface methods and interface-value storage (`exec/10_15_interface_default_generic_method`, `exec/10_16_interface_value_storage`) and updated the exec coverage index.
- Plan: removed the interface dictionary fixture-expansion item from `PLAN.md`.
- Tests: `./run_all_tests.sh`; `./run_stdlib_tests.sh`.

## 2026-01-24 — Iterator interface returns + constraint-arity fixture cleanup
- Go interpreter: treat Iterator interface return values as iterators during for/each by accepting `IteratorValue` in `adaptIteratorValue`.
- Fixtures: removed duplicate constraint-interface-arity diagnostics from exported manifests via the TS fixture definitions; re-exported fixtures.
- Tests: `./v12/export_fixtures.sh`; `cd v12/interpreters/go && ABLE_TYPECHECK_FIXTURES=strict go test ./pkg/interpreter -run 'TestFixtureParityStringLiteral/errors/constraint_interface_arity' -count=1`; `./run_all_tests.sh --version=v12`.

## 2026-01-24 — Interface dictionary arg dispatch + fixture expansion
- Go interpreter: coerce interface-typed generic values into interface dictionaries so interface arguments are preserved for bindings, params, and return coercions.
- Fixtures: added interface dictionary exec coverage for default chains, overrides, named impl + inherent method calls, interface inheritance, interface-arg dispatch (bindings/params/returns), and union-target dispatch; added AST error fixtures for ambiguous impl constraints + missing interface methods; updated exec coverage index and typecheck baseline.
- Tests: `cd v12/interpreters/go && go test ./pkg/interpreter -run TestExecFixtures/10_11_interface_generic_args_dispatch -count=1`; `cd v12/interpreters/go && go test ./pkg/interpreter -run TestExecFixtures/10_12_interface_union_target_dispatch -count=1`; `cd v12/interpreters/go && go test ./pkg/interpreter -run TestExecFixtures/10_13_interface_param_generic_args -count=1`; `cd v12/interpreters/go && go test ./pkg/interpreter -run TestExecFixtures/10_14_interface_return_generic_args -count=1`; `cd v12/interpreters/ts && ABLE_FIXTURE_FILTER=10_13_interface_param_generic_args bun run scripts/run-fixtures.ts`; `cd v12/interpreters/ts && ABLE_FIXTURE_FILTER=10_14_interface_return_generic_args bun run scripts/run-fixtures.ts`.

## 2026-01-24 — Named impl method resolution fix
- Interpreters (TS + Go): attach named-impl context to impl methods so default methods (and peers) can resolve sibling methods via `self.method()` in the same impl.
- Tests: `cd v12/interpreters/ts && ABLE_FIXTURE_FILTER=10_05_interface_named_impl_defaults bun run scripts/run-fixtures.ts`; `cd v12/interpreters/ts && ABLE_FIXTURE_FILTER=10_06_interface_generic_param_dispatch bun run scripts/run-fixtures.ts`; `cd v12/interpreters/ts && ABLE_FIXTURE_FILTER=13_05_dynimport_interface_dispatch bun run scripts/run-fixtures.ts`; `cd v12/interpreters/go && go test ./pkg/interpreter -run TestExecFixtures/10_05_interface_named_impl_defaults`; `cd v12/interpreters/go && go test ./pkg/interpreter -run TestExecFixtures/10_06_interface_generic_param_dispatch`; `cd v12/interpreters/go && go test ./pkg/interpreter -run TestExecFixtures/13_05_dynimport_interface_dispatch`.

## 2026-01-15 — Hash/Eq fixture and test coverage
- Fixtures: added AST fixtures for primitive hashing, kernel hasher availability, custom Hash/Eq, and collision handling; added exec fixtures for primitive hashing plus custom Hash/Eq + collisions; updated exec coverage index.
- Tests: added TS + Go unit coverage for hash helper builtins and kernel HashMap dispatch (custom + collision keys).
- Tests not run (edited code + fixtures only).

## 2026-01-15 — Remove host hasher bridges
- Kernel: dropped the `__able_hasher_*` extern declarations and the unused `HasherHandle` alias so hashing flows through `KernelHasher` only.
- Interpreters: removed host hasher state/builtins from Go + TypeScript, along with runtime stub and typechecker builtin entries.
- Docs/spec: scrubbed hasher bridge references from the kernel contract and extern execution/design notes.
- Tests not run (edited code + docs only).

## 2026-01-15 — Kernel Hash/Eq runtime alignment
- Kernel: added primitive `Eq`/`Ord`/`Hash` impls (ints/bool/char/String) plus float-only `PartialEq`/`PartialOrd`, and wired the Able-level FNV-1a hasher with raw byte helpers.
- Stdlib: trimmed duplicate interface impls and routed map hashing through the sink-style `Hash.hash` API.
- Interpreters: hash map kernels now dispatch `Hash.hash`/`Eq.eq`; numeric equality follows IEEE semantics; Go/TS typecheckers exclude floats from `Eq`/`Hash`.
- Fixtures: added float equality + hash-map key rejection exec coverage.
- Tests not run (edited code + docs only).

## 2026-01-15 — Kernel interfaces + Hash/Eq plan
- Added the kernel interface/primitive Hash/Eq design plan plus stdlib linkage notes (`v12/design/kernel-interfaces-hash-eq.md`, `v12/design/stdlib-v12.md`).
- Updated `spec/TODO_v12.md` and expanded the detailed work breakdown in `PLAN.md`.
- Tests not run (planning/doc updates only).

## 2026-01-15 — Manual syntax alignment
- Updated the v12 manual docs to match spec lexing and pipe semantics (line comments use `##`, string literals can use double quotes or backticks with interpolation, and pipe docs no longer mention a `%` topic token) in `v12/docs/manual/manual.md` and `v12/docs/manual/variables.html`.
- Tests not run (docs-only changes).

## 2026-01-15 — Primitive Hash/Eq constraints
- Updated the TS typechecker to treat primitive numeric types as satisfying `Hash`/`Eq` constraints (matching Go) and adjusted the example to iterate directly over `String` so `for` sees an `Iterable` (`v12/interpreters/ts/src/typechecker/checker/implementation-constraints.ts`, `.examples/foo.able`).
- Tests: `./v12/abletw .examples/foo.able`; `./v12/ablebc .examples/foo.able`.

## 2026-01-14 — Go module splits
- Split the Go constraint solver, literals, and member-access helpers into focused files to keep modules under 900 lines (`v12/interpreters/go/pkg/typechecker/constraint_solver_impls.go`, `v12/interpreters/go/pkg/typechecker/constraint_solver_methods.go`, `v12/interpreters/go/pkg/typechecker/statement_checker.go`, `v12/interpreters/go/pkg/typechecker/member_access_methods.go`, `v12/interpreters/go/pkg/typechecker/member_access_matching.go`) and trimmed `v12/interpreters/go/pkg/typechecker/constraint_solver.go`, `v12/interpreters/go/pkg/typechecker/literals.go`, and `v12/interpreters/go/pkg/typechecker/member_access_helpers.go`.
- Split the Go extern host into focused files (`v12/interpreters/go/pkg/interpreter/extern_host_cache.go`, `v12/interpreters/go/pkg/interpreter/extern_host_builder.go`, `v12/interpreters/go/pkg/interpreter/extern_host_coercion.go`, `v12/interpreters/go/pkg/interpreter/extern_host_module.go`) and trimmed `v12/interpreters/go/pkg/interpreter/extern_host.go`.
- Split the Go interpreter type helpers into focused files (`v12/interpreters/go/pkg/interpreter/interpreter_type_info.go`, `v12/interpreters/go/pkg/interpreter/interpreter_interface_lookup.go`, `v12/interpreters/go/pkg/interpreter/interpreter_type_matching.go`, `v12/interpreters/go/pkg/interpreter/interpreter_type_coercion.go`) and removed the oversized `v12/interpreters/go/pkg/interpreter/interpreter_types.go`.
- Split the Go interpreter operations helpers into focused files (`v12/interpreters/go/pkg/interpreter/interpreter_operations_dispatch.go`, `v12/interpreters/go/pkg/interpreter/interpreter_operations_ratio.go`, `v12/interpreters/go/pkg/interpreter/interpreter_operations_arithmetic.go`, `v12/interpreters/go/pkg/interpreter/interpreter_operations_compare.go`) and removed the oversized `v12/interpreters/go/pkg/interpreter/interpreter_operations.go`.
- Tests not run.

## 2026-01-12 — Stdlib fs/io/os coverage
- Added stdlib spec tests for `able.fs`, `able.io`, and `able.os` covering open/read/write, directory ops, metadata, buffered IO, and environment/cwd helpers (`v12/stdlib/tests/fs.test.able`, `v12/stdlib/tests/io.test.able`, `v12/stdlib/tests/os.test.able`).
- Tests not run.

## 2025-12-30 — Drop snapshots from testing framework
- Removed snapshot matchers/stores and the `--update-snapshots` flag across stdlib tests, docs, and CLI design notes; deleted the snapshot store design doc and scrubbed spec references.
- Regenerated `v12/stdlib/src/spec.able` after removing snapshot exports from the spec DSL.
- Tests not run.

## 2025-12-30 — Testing module suffix policy
- Added the testing framework section to `spec/full_spec_v12.md`, codifying `.test.able` and `.spec.able` as test modules plus the `able test` contract.
- Cleared the suffix-policy open decision in `v12/design/testing-plan.md` and removed the corresponding TODO from `PLAN.md`.
- Tests not run.

## 2025-11-11 — Stdlib Module Search Paths
- **Pipe semantics parity**: Added the `pipes/multi_stage_chain` AST fixture so multi-stage pipelines that mix `%` topic steps, placeholder-built callables, and bound methods stay covered; `bun run scripts/run-fixtures.ts` (TypeScript) and `GOCACHE=$(pwd)/.gocache go test ./pkg/interpreter` (Go) stay green with no parity divergences observed.
- **Typechecker strict fixtures**: TypeScript’s checker now hoists struct identifiers for static calls, predeclares `:=` bindings (so future handles can reference themselves), binds iterator driver aliases plus struct/array pattern destructures, and hides private package members behind the standard “has no symbol” diagnostic. The full fixture suite now passes under `ABLE_TYPECHECK_FIXTURES=strict bun run scripts/run-fixtures.ts`, and manifests/baselines were updated where diagnostics are expected.
- **Dynamic interface collections & iterables**: added the shared fixture `interfaces/dynamic_interface_collections` (plus exporter wiring) so both interpreters prove that range-driven loops and map-like containers storing interface values still choose the most specific impl even when union targets overlap. `bun run scripts/run-fixtures.ts` is green; Go parity remains blocked by existing fixture failures (`functions/hkt_interface_impl_ok`, `imports/static_alias_private_error`).
- **Privacy & import spec gaps**: enforced package privacy across both interpreters/typecheckers (selectors, aliases, wildcard bindings), tightened dynimport placeholder handling (TypeScript + Go parity plus fixture coverage), and updated `spec/full_spec_v12.md` with the canonical `Future` handle definitions so runtimes and tooling share the same ABI (`FutureStatus`, `FutureError`, `status/value/cancel` contracts).
- **Interface & impl completeness**: higher-kinded/visibility edge cases now have end-to-end coverage. Overlapping impl ambiguity diagnostics land in both interpreters/typecheckers, TypeScript enforces method-set backed constraints with parity coverage, both interpreters honour interface self-type patterns (including higher-kinded targets) and reject bare constructors unless `for …` is declared, and parser/exporter propagate the `for …` patterns so fixtures surface the diagnostics automatically. Shared AST fixtures (`errors/interface_self_pattern_mismatch`, `errors/interface_hkt_constructor_mismatch`, plus the positive `functions/hkt_interface_impl_ok`) keep both runtimes + typecheckers aligned.

- Future cancellation coverage is complete: both interpreters expose `Future.cancel()`, cancellation transitions produce the `Cancelled` status, and the new `concurrency/future_cancel_nested` fixture exercises a task awaiting a spawned future that is cancelled mid-flight. The exporter and strict harness generate the fixture automatically, keeping the TypeScript + Go runtimes/typecheckers in lockstep for nested cancellation chains across both executors.
- Channel error helpers now emit the stdlib error structs in both runtimes. TypeScript + Go runtimes route every channel error through `ChannelClosed`, `ChannelNil`, or `ChannelSendOnClosed`, and new Bun/Go tests cover the behaviour (`v12/interpreters/ts/test/concurrency/channel_mutex.test.ts`, `interpreter-go/pkg/interpreter/interpreter_channels_mutex_test.go`).
- Lightweight executor diagnostics are now wired into both runtimes: the new `future_pending_tasks()` helper surfaces cooperative queue length via `CooperativeExecutor.pendingTasks`, Go’s serial and goroutine executors expose the same data, and coverage comes from a dedicated AST fixture (`concurrency/future_executor_diagnostics`) plus Bun/Go unit tests. The spec, manuals, and `design/concurrency-executor-contract.md` now describe the helper so fixtures can assert drain behaviour deterministically.
- String host bridge externs (`__able_string_from_builtin`, `__able_string_to_builtin`, `__able_char_from_codepoint`) are wired into both interpreters and typecheckers with dedicated tests (`v12/interpreters/ts/test/string_host.test.ts`, `interpreter-go/pkg/interpreter/interpreter_string_host_test.go`).
- Hasher externs (`__able_hasher_create/write/finish`) now back the stdlib hash maps across TypeScript and Go, complete with parity tests and stub support for tooling.
- Added the `concurrency/channel_error_rescue` AST fixture and exposed `Error` member access (message/value) in both interpreters so Able code can assert the struct payloads produced by the channel helpers.
- Go parity now runs the new `concurrency/channel_error_rescue` fixture under the goroutine executor, and a dedicated Go test (`TestChannelErrorRescueExposesStructValue`) verifies that rescuing channel errors exposes the struct payload via `err.value`.
- Added the `errors/result_error_accessors` AST fixture so both interpreters exercise `err.message()/cause()/value` inside `!T else { |err| ... }` flows; fixture exporter + TS harness updated accordingly.
- Go typechecker now recognises `Error.message()`, `.cause()`, and `.value`, and the spec documents the runtime-provided `Error.value` payload hook; the typechecker baseline entry for `channel_error_rescue` was removed once diagnostics cleared.
- Future runtime errors now record their cause payloads in both interpreters, the new `concurrency/future_error_cause` fixture exercises `err.cause()` end-to-end, and matching Bun/Go tests keep the regression harness green.
- Generator laziness parity closed: iterator continuations now cover if/while/for/match across both runtimes, stdlib helpers (`stdlib/src/concurrency/channel.able`, `stdlib/src/collections/range.able`) use generator literals, and new fixtures (`fixtures/ast/control/iterator_*`, `fixtures/ast/stdlib/channel_iterator`, `fixtures/ast/stdlib/range_iterator`) keep the shared harness authoritative.
- Automatic time slicing verified for long-running futures: the new `concurrency/future_time_slicing` fixture proves that handles without explicit `future_yield()` still progress under repeated `future_flush()` calls, capturing both the intermediate `Pending` status and the eventual resolved value across runtimes.
### AST → Parser → Typechecker Completion Plan _(reopen when new AST work appears)_
- Full sweep completed 2025-11-06 (strict fixture run, Go interpreter suite, Go parser harness, and `bun test` all green). Archive details in `LOG.md`; bring this plan back only if new AST/syntax changes introduce regressions.

## 2025-11-09 — Executor Diagnostics Helper
- Added the `future_pending_tasks()` runtime helper so Able programs/tests can observe cooperative executor queue depth. TypeScript wires it through `CooperativeExecutor.pendingTasks()` while the Go runtime surfaces counts from both the serial and goroutine executors (best-effort on the latter via atomic counters). The helper is registered with both typecheckers so Warn/Strict fixture runs understand the signature.
- New coverage keeps the helper honest: Bun unit tests exercise the helper directly (`v12/interpreters/ts/test/concurrency/proc_spawn_scheduling.test.ts`), Go gains matching tests (`TestProcPendingTasksSerialExecutor`, `TestProcPendingTasksGoroutineExecutor`), and a serial-only AST fixture (`fixtures/ast/concurrency/future_executor_diagnostics/`) ensures both interpreters prove that `future_flush` drains the cooperative queue.
- Spec + docs now describe the helper alongside `future_yield`/`future_flush` (see `spec/full_spec_v12.md`, `docs/manual/*.md/html`, `design/concurrency-executor-contract.md`, and `AGENTS.md`), and the PLAN TODO for “Concurrency ergonomics” is officially closed out.

## 2025-11-08 — Fixture Diagnostics Parity Enforcement
- `v12/interpreters/ts/scripts/run-parity.ts` now diffs typechecker diagnostics for every AST fixture so warn/strict parity runs catch unexpected checker output even when manifests do not declare expectations. The parity JSON report captures the mismatched diagnostics to speed up triage.
- Added `test/scripts/parity/fixtures_compare.test.ts` to cover the helper logic that determines when diagnostics mismatches should fail parity.
- Go’s typechecker now treats unannotated `self` parameters inside `methods {}` / `impl` blocks as `Self`/concrete receiver types and seeds iterator literals with the implicit `gen` binding, eliminating the extra diagnostics that used to appear only in Go’s warn/strict runs.
- Added Go regression tests (`pkg/typechecker/checker_impls_test.go`, `pkg/typechecker/checker_iterators_test.go`) so implicit `self` bindings and iterator generator helpers stay covered.
- Bun’s typechecker mirrors the same behaviour: iterator literals now predefine the implicit `gen` binding, implicit `self` parameters default to `Self`, and new tests (`test/typechecker/method_sets.test.ts`, `test/typechecker/iterators.test.ts`) keep the coverage locked in.
- The Go fixture runner and CLI now continue evaluating programs in warn/strict modes even when diagnostics are reported (matching the Bun harness), so parity checks capture both the expected runtime results and the shared diagnostics payloads.

## 2025-11-07 — AST → Parser → Typechecker Cycle Revalidated
- Added proc handle memoization fixtures (success + cancellation) and ensured both interpreters plus the Go parser harness run them under strict typechecking (`bun run scripts/run-fixtures.ts`, `cd interpreter-go && GOCACHE=$(pwd)/.gocache GO_PARSER_FIXTURES=1 go test ./pkg/parser`).
- Verified the full suite remains green (`./run_all_tests.sh --typecheck-fixtures=strict`, `bun test`, `cd interpreter-go && GOCACHE=$(pwd)/.gocache go test ./pkg/interpreter`), keeping the Priority 0 gate satisfied.
- Updated PLAN.md to mark the current AST → Parser → Typechecker cycle complete and advance the focus to Phase α (Channel & Mutex runtime bring-up).
- Added stdlib specs for channel/mutex behaviour (`stdlib/tests/concurrency/channel_mutex.test.able`) so the Phase α bullet “add unit tests covering core operations” is now satisfied.

## 2025-11-07 — Serial Executor Future Reentrancy
- Go’s SerialExecutor now exposes a `Drive` helper that runs pending tasks inline, so nested `future.value()` calls no longer deadlock and match the TypeScript scheduler semantics. The helper steals the targeted handle from the deterministic queue, executes it re-entrantly (including repeated slices when `future_yield` fires), and restores the outer task context once the awaited handle resolves.
- Fixtures `concurrency/future_value_reentrancy` and `concurrency/future_flush_fairness` now pass under the Go interpreter’s serial executor, keeping the newly added fairness/re-entrancy corpus green for both runtimes (`ABLE_TYPECHECK_FIXTURES=strict bun run scripts/run-fixtures.ts`, `cd interpreter-go && GOCACHE=$(pwd)/.gocache go test ./pkg/interpreter`).
- PLAN immediate actions trimmed: the “Concurrency stress coverage & docs” placeholder has been cleared now that the blocking fixtures run cleanly; follow-up concurrency work can graduate to design docs instead of the top-level plan.
- Added a Go-side regression test (`TestSerialExecutorFutureValueReentrancy`) that mirrors the new fixture to ensure nested `future.value()` calls stay green even if future contributors touch the executor; the design note `design/go-concurrency-scheduler.md` now documents the inline-driving behaviour and follow-up doc work.
- Added a companion fixture (`concurrency/future_value_reentrancy`) plus TypeScript exporter wiring + Go regression test (`TestSerialExecutorProcValueReentrancy`) so both interpreters exercise nested `future.value()` waits under the serial executor; parser coverage tables were updated accordingly.
- Documented the goroutine-executor fairness contract (what `future_yield`/`future_flush` mean under `GoroutineExecutor`, how we rely on Go’s scheduler, and when tests must fall back to the serial executor) to close out the remaining PLAN follow-up (`design/go-concurrency-scheduler.md`).
- Updated the v12 spec to codify re-entrant `future.value()` semantics so both interpreters (and future targets) must guarantee deadlock-free nested waits (§12.2.5 “Re-entrant waits”).
- Added direct Go unit tests (`TestProcHandleValueMemoizesResult`, `TestProcHandleValueCancellationMemoized`) to ensure repeated `value()` calls return memoized results/errors even after cancellation, satisfying the remaining “exercise repeated value() paths” item from the concurrency plan.
- Introduced the `concurrency/future_value_memoization` fixture (plus exporter wiring) so both interpreters prove future handles memoize values, and updated the Go parity harness to run it with the goroutine executor.
- Added `concurrency/future_value_cancel_memoization` to assert that cancelled future handles return the same error for repeated `value()` calls without re-running their bodies; the fixture exporter, AST corpus, and fixture run all cover this scenario now.

## 2025-11-06 — Tree-Sitter Mapper Modularization Complete
- Go declarations/patterns/imports all run through the shared `parseContext`, removing the last raw-source helpers so both runtimes share one parser contract (`interpreter-go/pkg/parser/{declarations,patterns,statements,expressions}_parser*.go`).
- TypeScript parser README now calls out the shared context, and `PLAN.md` logged the Step 6 regression sweep so future contributors know the refactor is locked in (`v12/interpreters/ts/src/parser/README.md`, `PLAN.md`).
- Wrapper exports like `parseExpression(node, source)` / `parseBlock` / `parsePattern` were removed; all Go parser consumers now flow through the context pipeline.
- Tests: `./run_all_tests.sh --typecheck-fixtures=warn` and `cd interpreter-go && GOCACHE=$(pwd)/.gocache GO_PARSER_FIXTURES=1 go test ./pkg/parser`.
- Follow-up: confirm any remaining helpers that still accept raw source (e.g., host-target parsing) genuinely require it before migrating them to `parseContext`.

## 2025-11-03 — AST → Parser → Typechecker Completion Sweep
Status: ✅ Completed. Parser coverage table reads fully `Done`, TypeScript + Go checkers share the bool/comparison semantics, and `ABLE_TYPECHECK_FIXTURES=strict bun run scripts/run-fixtures.ts` executes cleanly after wiring the remaining builtin signatures, iterator annotations, and error fixture diagnostics into the manifests.

Open items (2025-11-02 audit):
- [x] Iterator literals dropped both the optional binding identifier and optional element type annotation. Update both ASTs + parsers/interpreters so the metadata survives round-trips and execution.
- [x] Teach both typecheckers to honor the iterator element annotation so every `yield` matches the declared element type (TS `src/typechecker/checker.ts`, Go `pkg/typechecker/literals.go` + iterator tests).
- [x] Carry iterator element metadata through `for` loops so typed loop patterns validate across array/range/iterator inputs (TS `src/typechecker/checker.ts`, Go `pkg/typechecker/{control_flow,literals,type_utils}.go` + new cross-loop tests).
- [x] Give the TS checker parity with Go for block/proc/spawn typing and array/range literal inference so all three stages (AST → parser → typechecker) agree on the element/result metadata (§6.8, §6.10, §12.2).
- [x] Enforce async-only builtins (`future_yield`) and add concurrency smoke tests so TS emits the same diagnostics as Go when authors call scheduler helpers outside `proc`/`spawn`.
- [x] Implement `if`/`while` diagnostics + inference in the TS checker so control-flow expressions match the Go implementation (§8.1/§8.2).
- [x] Mirror Go's match/rescue guard enforcement in the TS checker (§8.1.2 / §11.3).
- [x] Enforce package privacy + import diagnostics in the TS checker so private packages/definitions behave identically to Go (updated `imports.test.ts` + package summaries carry `visibility` metadata).

## Historical Status Notes

### 2025-10-30
- Comments are now ignored during parser → AST mapping for both interpreters.
  - ✅ Go: `ModuleParser` / helper utilities skip `comment`, `line_comment`, `block_comment` nodes and `TestParseModuleIgnoresComments` asserts the behaviour.
  - ✅ TypeScript: `tree-sitter-mapper` filters the same node types; `fixtures_mapper.test.ts` covers the mapping path and `fixtures_parser.test.ts` ensures the raw grammar parses comment-heavy sources.
- TODO: audit remaining parser/mapping gaps per `design/parser-ast-coverage.md` (pipes/topic combos, functional updates, etc.) and backfill fixtures/tests.
- DONE: comment skipping now wired through struct literals, struct patterns, and related mapper helpers across both runtimes.
- TODO: Build end-to-end coverage across **all three facets** (parsing, tree → AST mapping, AST evaluation) for both interpreters. Use the coverage table to drive fixture additions, parser assertions, and runtime tests until every spec feature is green.
- TODO: Extend the **typechecker** suites (Go + TS) so they verify type rules and inference across modules. Assemble an exhaustive inference corpus exercising expression typing, generics, interfaces/impls, and cross-module reconciliation; ensure these scenarios are evaluated alongside runtime fixtures.

### 2025-10-31
- Regenerated the tree-sitter-able artifacts with the freshly rebuilt grammar (interface-composition fix now baked into `parser.c`/`.wasm`) using the local Emscripten toolchain; no diff surfaced, confirming the repo already carried the correct bits.
- Cleared local Go build caches (`.gocache`, `interpreter-go/.gocache`) and re-ran `GOCACHE=$(pwd)/.gocache GO_PARSER_FIXTURES=1 go test ./pkg/parser` to mimic CI picking up the refreshed grammar without stale entries.
- ACTION: propagate the cache-trim guidance to CI docs if flakes recur; otherwise move on to the remaining parser fixture gaps (`design/parser-ast-coverage.md`).
- Mirrored the TypeScript placeholder auto-lift guardrails inside the Go interpreter so pipe placeholders evaluate eagerly, keeping the shared `pipes/topic_placeholder` fixture green.
- Parser sweep: both the TypeScript mapper and Go parser now skip inline comments when traversing struct literals, call/type argument lists, and struct definitions, with fresh TS/Go tests guarding the behaviour.
- TypeScript checker scaffold landed: basic environment/type utilities exist under `v12/interpreters/ts/src/typechecker`, exported via the public index, and fixtures respect `ABLE_TYPECHECK_FIXTURES` ahead of the full checker port.
- TypeScript checker now emits initial diagnostics for logical operands, range bounds, struct pattern validation, and generic interface constraints so the existing error fixtures pass under `ABLE_TYPECHECK_FIXTURES=warn`.

### 2025-11-01
- TypeScript checker grew a declaration-collection sweep that registers interfaces, structs, methods, and impl blocks before expression analysis, mirroring the Go checker’s phase ordering.
- Implementation validation now checks that every `impl` supplies the interface’s required methods, that method generics/parameters/returns mirror the interface signature, and flags stray definitions; only successful impls count toward constraint satisfaction.
- Canonicalised type formatting to the v12 spec (`Array i32`, `Result string`) and keyed implementation lookups by the fully-instantiated type so generic targets participate in constraint checks.
- Extended the TypeScript checker’s type info to capture `nullable`, `result`, `union`, and function signatures, and taught constraint resolution to recognise specialised impls like `Show` for `Result string`, with fresh tests covering the new cases.
- Added focused Bun tests under `test/typechecker/` plus the `ABLE_TYPECHECK_FIXTURES` harness run to lock in the new behaviour and guard future regressions.
- Mirrored the Go checker and test suite to use the same spec-facing type formatter and wrapper-aware constraint logic so diagnostics now reference `Array i32`, `string?`, etc., and added parity tests (`interpreter-go/pkg/typechecker/*`).

### 2025-11-02
- The Bun CLI suite (`v12/interpreters/ts/test/cli/run_module_cli.test.ts`) now covers multi-file packages, custom loader search paths via the new `ABLE_MODULE_PATHS` env, and strict vs warn typecheck enforcement so the ModuleLoader refactor stays covered without pulling `stdlib/` into every run.
- Introduced the `able test` skeleton inside `scripts/run-module.ts`: it parses the planned flags/filters, materialises run options + reporter selection, and prints a deterministic plan summary before exiting with code `2` while the stdlib testing packages remain unparsable. (See `design/testing-cli-design.md` / `design/testing-cli-protocol.md`.)
- Extracted the shared package-scanning helpers (`discoverRoot`, `indexSourceFiles`, etc.) into `scripts/module-utils.ts` so other tooling (fixtures runner, future harnesses) can reuse the multi-module discovery logic without duplicating it.
- **Deferral noted:** full stdlib/testing integration is still on pause until the parser accepts `stdlib/src/test/*`; once that unblocks, wire the CLI skeleton into the `able.test` harness per the design notes.

### 2025-11-05
- Step 6 regression sweep ran end-to-end: `./run_all_tests.sh --typecheck-fixtures=warn` stayed green post-refactor, and `GOCACHE=$(pwd)/.gocache GO_PARSER_FIXTURES=1 go test ./pkg/parser` uncovered/validated the Go-side AST gaps.
- Go AST parity improvements: `FunctionCall.arguments` now serialises as an empty array when no args exist, `NilLiteral` carries `value: null`, and break/continue statements omit label/value when not supplied (matching the TS mapper contract).
- Parser helper normalisation no longer nukes empty slices before fixture comparison, and parameter lists default to `[]`, which brought the channel/mutex fixtures back in sync.
- Fixture `structs/functional_update` gained explicit `\"isShorthand\": false` flags so both interpreters agree on struct literal metadata going forward.

### 2025-11-06
- Go CLI now exposes `able check` alongside `able run`, sharing the manifest/target resolution pipeline. Both commands surface ProgramChecker diagnostics + package summaries and fail fast when typechecking reports issues, keeping the TypeScript + Go CLIs aligned.
- Added dedicated CLI tests to cover `able check` success/failure cases so future refactors keep the new mode wired through manifest resolution, typechecker reporting, and exit codes.

### 2025-11-07 — Phase α (Channel/Mutex) Completion
- Audited channel/mutex stdlib wiring across both runtimes: helper registration, typechecker signatures, fixtures, and prelude exports now match; no AST or scheduler drift detected.
- Added Bun smoke tests for nil-channel cancellation and mutex re-entry errors (`v12/interpreters/ts/test/concurrency/channel_mutex.test.ts`) to mirror the Go parity suite.
- Documented the audit and captured the remaining TODO (map native errors to `ChannelClosed`/`ChannelNil`/`ChannelSendOnClosed`) in `design/channels-mutexes.md` and `spec/TODO.md`.
- Cleared `Phase α` from the active roadmap; next milestone is Phase 4 (cross-interpreter parity/tooling).

### 2025-11-07 — Fixture Parity Harness (Phase 4 Kick-off)
- Added a Go CLI entry point (`cmd/fixture`) that evaluates a single AST fixture and emits normalized JSON (result kind/value, stdout, diagnostics) with respect to `ABLE_TYPECHECK_FIXTURES`. The helper reuses the interpreter infrastructure and supports serial/goroutine executors while sandboxing the Go build cache.
- Refactored the TypeScript fixture loader into `scripts/fixture-utils.ts` so both the CLI harness and Bun tests can hydrate modules, install runtime stubs, and intercept `print` output consistently.
- Rebuilt `run-fixtures.ts` on top of the shared utilities (no behavior change) to keep fixture execution logic single-sourced.
- Introduced a Bun parity test (`test/parity/fixtures_parity.test.ts`) that exercises a representative slice of the shared fixture corpus (currently 20 fixtures across basics + concurrency) against both interpreters and asserts matching results/stdout via the new Go CLI.

### 2025-11-08 — Parity CLI Reporting
- Added `v12/interpreters/ts/scripts/run-parity.ts`, reusable parity helpers, and Bun parity suites that now share the same execution/diffing logic across AST fixtures and curated examples.
- `run_all_tests.sh` now invokes the parity CLI so local + CI runs execute the same cross-interpreter verification and drop a JSON report at `tmp/parity-report.json` for machine-readable diff tracking; `tmp/` landed in `.gitignore` to keep artifacts out of commits.
- The helper script also honors `ABLE_PARITY_REPORT_DEST` or `CI_ARTIFACTS_DIR` so pipelines can copy the parity JSON into their artifact buckets without bespoke wrapper scripts.
- Updated `v12/interpreters/ts/README.md` and `interpreter-go/README.md` with parity CLI instructions, env knobs (`ABLE_PARITY_MAX_FIXTURES`, `ABLE_PARITY_REPORT_DEST`), and guidance on keeping the cross-interpreter harness green.
- Added Go package docs (`pkg/interpreter/doc.go`, `pkg/typechecker/doc.go`) plus README guidance on regenerating `go doc`/pkg.go.dev pages so the documentation workstream is unblocked.
- Landed `dynimport_parity` and `dynimport_multiroot` in `v12/interpreters/ts/testdata/examples/` to cover dynamic package aliasing, selector imports, and multi-root dynimport scenarios end-to-end; the parity README + plan now list them alongside the other curated programs, and the Go CLI + Bun harness honor `ABLE_MODULE_PATHS` when resolving shared deps.
- Authored `docs/parity-reporting.md` and linked it from the workspace README so CI pipelines know how to persist `tmp/parity-report.json` via `ABLE_PARITY_REPORT_DEST`/`CI_ARTIFACTS_DIR`.
- The Go CLI (`cmd/able`) now honors `ABLE_MODULE_PATHS` in addition to `ABLE_PATH`, with new tests ensuring the search-path env works; stdlib docs reference the alias so multi-root dynimport scenarios can rely on a single env knob across interpreters.
- Fixed the `..`/`...` range mapping bug in both parsers so inclusive ranges now follow the spec (TS + Go parser updates, new parser unit tests, interpreter for-loop regression tests, and fizzbuzz-style parity coverage).

### Phase 5 Foundations — Parser Alignment
- Canonical AST mapping now mirrors the fixture corpus across both runtimes. The TypeScript mapper’s fixture parity suite (`bun test test/parser/fixtures_mapper.test.ts`) and the Go parser harness (`go test ./pkg/parser`) stay green, so every tree-sitter node shape maps to the shared AST contract with span/origin metadata.
- `tree-sitter-able` grammar coverage is complete for the v12 surface (see `design/parser-ast-coverage.md`); new syntax is added directly with fixture+cXPath tests so the grammar remains authoritative.
- Translators and loaders are live in both interpreters: TypeScript’s `ModuleLoader` and Go’s `driver.Loader` now ingest `.able` source via tree-sitter, hydrate canonical AST modules, and feed them to their respective typechecker/interpreter pipelines.
- End-to-end parse → typecheck → interpret tests exercise both runtimes: `ModuleLoader pipeline with typechecker` (Bun) covers the TS path, and `pkg/interpreter/program_pipeline_test.go` drives the Go loader/interpreter via `GOCACHE=$(pwd)/.gocache go test ./pkg/interpreter`.
- Diagnostic coverage now rides on the same pipelines: the new Bun test asserts missing import selectors surface typechecker errors before evaluation, and the Go suite verifies that `EvaluateProgram` halts (or proceeds when `AllowDiagnostics` is set) when return-type violations are reported.

### 2026-01-14
- Go typechecker implementation validation now skips prelude impls, aligns impl target labels with TS by alias-expanding and wildcard-normalizing target type expressions, and tracks local type declarations to avoid rewriting in-module types.
- Where-clause mismatch diagnostics for impl methods now point at the impl definition node to match fixture baselines.
- Tests: `./run_all_tests.sh --version=v12`.
- Ran `./run_stdlib_tests.sh` (TS + Go stdlib suites) to confirm current stdlib coverage remains green.
- Added a focused Go typechecker test to lock in alias-expanded impl label canonicalization.
- Split the Go test CLI harness into smaller files (`test_cli*.go`) to keep modules under 900 lines.
- Tests: `cd v12/interpreters/go && go test ./pkg/typechecker -run ImplementationLabelCanonicalizes && go test ./cmd/able`.
- Split TS implementation collection helpers into `implementation-collection-helpers.ts` so the main module stays under 900 lines.
- Split Go program checker helpers into `program_checker_types.go` and `program_checker_summaries.go` for smaller modules.
- Extracted Go loader helper utilities into `loader_helpers.go`.
- Moved Able CLI dependency fetchers (registry/git) into `deps_fetchers.go` to shrink `deps_resolver.go`.
- Tests: `cd v12/interpreters/go && go test ./pkg/typechecker -run ImplementationLabelCanonicalizes && go test ./cmd/able`.
- Fixed `fs.copy_dir` overwrite behavior by clearing destination contents after a removal attempt when needed.
- Tests: `./run_stdlib_tests.sh`; `./run_all_tests.sh --version=v12`.
- Dropped redundant where-clauses from `HashMap` impls so stdlib typechecking is clean in strict mode.
- Updated strict fixture manifests for expected typechecker diagnostics and refreshed the baseline for `expressions/map_literal_spread`.
- Removed the `PathType` alias from fs helpers; paths now use `Path | String` directly and fs helpers extend `Path`.
- Tests: `./run_all_tests.sh --version=v12 --typecheck-fixtures-strict --fixture`; `./run_stdlib_tests.sh`.
- Fixed impl validation to compare interface method where clauses against method-level constraints instead of impl-level where clauses (TS + Go typecheckers).
- Tests: `cd v12/interpreters/ts && bun test test/typechecker/implementation_validation.test.ts`; `cd v12/interpreters/go && go test ./pkg/typechecker`.
- Added exec fixture `exec/10_02_impl_where_clause` to cover impl-level where clauses without method-level duplication and updated the coverage index.
- Tests: `cd v12/interpreters/ts && ABLE_FIXTURE_FILTER=10_02_impl_where_clause bun run scripts/run-fixtures.ts`.
- Added exec fixture `exec/04_05_04_struct_literal_generic_inference`, updated the exec coverage index, and enforced exec-fixture typechecking when manifests specify diagnostics (TS + Go).
- Fixed struct literal generic type-argument handling in the TS and Go typecheckers (placeholder args in TS; inferred args in Go).
- Tests: `./run_all_tests.sh --version=v12 --typecheck-fixtures-strict`.
- Clarified spec call-site inference to include return-context expected types and documented return-context inference design notes; updated PLAN work queue.

### 2026-01-15
- TS typechecker now uses expected return types to drive generic call inference (explicit + implicit return paths), with new return-context unit tests.
- Go typechecker now propagates expected return types through implicit return blocks, plus a focused unit test for implicit-return inference.
- Added exec fixture `exec/07_08_return_context_generic_call_inference` and updated the exec coverage index.
- Tests: `cd v12/interpreters/ts && bun test test/typechecker/return_context_inference.test.ts`; `cd v12/interpreters/go && go test ./pkg/typechecker -run TestGenericCallInfersFromImplicitReturnExpectedType`.
- TS typechecker now treats method-shorthand exports as taking implicit self for overload resolution, and uses receiver substitutions when enforcing method-set where clauses on exported function calls.
- TS runtime now treats unresolved generic type arguments on struct instances as wildcard matches when comparing against concrete generic types.
- Tests: `./run_all_tests.sh --version=v12 --typecheck-fixtures-strict`.
- Documented kernel Hash/Eq decisions (sink-style hashing, IEEE float equality, floats not Eq/Hash), updated spec wording, and expanded the PLAN work breakdown for interpreter alignment.
- Extended the kernel Hash/Eq plan to move the default `Hasher` implementation into Able with host bitcast helpers; updated spec TODOs and PLAN tasks accordingly.
- Added a kernel-level FNV-1a Hasher (Able code) with big-endian byte emission, introduced `__able_f32_bits`/`__able_f64_bits`/`__able_u64_mul` helpers in TS/Go, and updated stdlib hashing call sites + tests to use the new sink-style Hash API.

### 2026-01-23
- Renamed async scheduler helpers to `future_*` across v12 runtimes, docs, fixtures, and tests (`future_yield`, `future_cancelled`, `future_flush`, `future_pending_tasks`) while keeping fixture IDs intact.
- Updated typechecker diagnostics for `future_yield` to reference async tasks rather than `proc` bodies.
- Tests: `./run_all_tests.sh --version=v12 --fixture`.

### 2026-02-09
- Routed compiled `__able_array_*` externs through the interpreter bridge so array handles stay consistent with runtime `ArrayValue` literals in compiled runs.
- Dropped the unconditional `math/big` import from compiled output generation to avoid unused-import build failures.
- Tests: `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=06_08_array_ops_mutability,06_12_02_stdlib_array_helpers go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`.
- Lowered compiled array literals to kernel `Array` handles (struct instances) and extended array pattern extraction to read kernel-backed arrays.
- Bridge struct lookups now fall back to interpreter package registries so kernel types resolve without explicit imports in compiled code.
- Tests: `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=06_08_array_ops_mutability,06_12_02_stdlib_array_helpers,06_01_compiler_match_patterns,05_02_array_nested_patterns,06_01_compiler_for_loop_pattern,06_01_literals_array_map_inference go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`.
- Added a shared runtime array store so compiled and interpreted array handles share storage; compiled `__able_array_*` externs now use the runtime store directly (no interpreter bridge), and compiled array value extraction reads from the runtime store.
- Compiled output only imports `sync` when iterator helpers are emitted.
- Tests: `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=06_08_array_ops_mutability,06_12_02_stdlib_array_helpers,06_01_compiler_match_patterns,05_02_array_nested_patterns,06_01_compiler_for_loop_pattern,06_01_literals_array_map_inference go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`; `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache go test ./pkg/interpreter -run TestBytecodeVM -count=1`.
- Compiled index get/set now handle kernel-backed Array values directly via the runtime store (no interpreter fallback), returning `IndexError` values for out-of-bounds assignment semantics.
- Tests: `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=06_08_array_ops_mutability,06_12_02_stdlib_array_helpers,06_01_compiler_match_patterns,05_02_array_nested_patterns,06_01_compiler_for_loop_pattern,06_01_literals_array_map_inference go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`.
- Compiled member get/set now handle Array metadata directly (storage handle/length/capacity) without interpreter fallback, syncing kernel Array metadata to the runtime store.
- Tests: `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=06_08_array_ops_mutability,06_12_02_stdlib_array_helpers,06_01_compiler_match_patterns,05_02_array_nested_patterns,06_01_compiler_for_loop_pattern,06_01_literals_array_map_inference go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`.
- Added a shared runtime hash map store so interpreter hash map handles use the runtime store instead of interpreter-local state.
- Tests: `cd v12/interpreters/go && go test ./pkg/interpreter -run TestHashMapBuiltins -count=1`.
- Exported interpreter HashMap hashing/equality helpers for compiler bridge use and added compiled hash map extern helpers backed by the runtime store.
- Compiler map literal + IR map literal lowering now call compiled hash map helpers instead of interpreter bridge calls.
- Tests: `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=06_01_compiler_map_literal,06_01_compiler_map_literal_spread go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`; `cd v12/interpreters/go && go test ./pkg/interpreter -run TestHashMapBuiltins -count=1`.
- Added interpreter-level fast paths for numeric/string binary ops and numeric unary ops, and wired compiled runtime helpers to use them before falling back to interpreter dispatch.
- Tests: `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=04_02_primitives_truthiness_numeric go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`; `cd v12/interpreters/go && go test ./pkg/interpreter -run TestEvaluateBinaryAddition -count=1`.
- IR array literal lowering now calls compiled array helpers directly instead of `rt.Call`.
- Tests: `cd v12/interpreters/go && go test ./pkg/compiler -run TestIREmitFunctionLiterals -count=1`.
- Compiled index get/set now handle kernel HashMap values directly using the runtime hash map store (returning IndexError for missing keys) before falling back to interpreter dispatch.
- Tests: `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=06_01_compiler_map_literal,08_01_bytecode_if_indexing go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`.
- Compiled member access now returns the HashMap `handle` field directly without interpreter fallback.
- Tests: `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=06_01_compiler_map_literal go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`.
- Compiled member assignment now handles HashMap `handle` updates directly (ensuring a runtime store entry) without interpreter fallback.
- Tests: `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=06_01_compiler_map_literal go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`.
- Added compiled Future scheduler helpers (spawn, future_yield/cancelled/flush/pending_tasks), Future member access bindings, and serial executor to keep compiled futures deterministic without interpreter fallback.
- Refactored compiler runtime helper emission to keep files under 1000 lines and added `__able_member` alias for await wakers.
- Fixed compiled helper generation to avoid non-pointer StructInstanceValue cases and deduplicated `__able_int64_from_value`; serial executor now blocks tasks until explicitly flushed to preserve expected spawn ordering.
- Implemented compiled await execution (arm selection, registration, waker wakeups, cancellation, and fairness) so compiled await no longer relies on interpreter dispatch.
- Tests: `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=06_01_compiler_spawn_await,06_01_compiler_await_future,12_06_await_fairness_cancellation go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`.
- Tests: `cd v12/interpreters/go && go test ./pkg/compiler -run TestCompilerEmitsStructsAndWrappers -count=1`; `cd v12/interpreters/go && go test ./pkg/compiler -run TestIREmitFunctionSpawn -count=1`; `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=12_02_async_spawn_combo go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`.
- Added compiled helpers for `__able_await_default` and `__able_await_sleep_ms` (duration parsing + timer awaitable) and routed named calls to those helpers so compiled awaitables no longer fall back to the interpreter.
- Tests: `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=12_01_bytecode_await_default go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`.
- Fixed compiler type mapping to treat struct references as supported regardless of definition order (avoids false unsupported param/return types) while keeping unknown types marked unsupported; verified regex fixture and fallback audit now pass.
- Tests: `cd v12/interpreters/go && ABLE_COMPILER_FALLBACK_AUDIT=1 GOCACHE=$(pwd)/.gocache go test ./pkg/compiler -run TestCompilerExecFixtureFallbacks -count=1`; `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=14_02_regex_core_match_streaming go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`.
- Added a compiled call registry so `__able_call_named` dispatches directly to compiled wrappers per package environment (with correct partial/optional-arg handling) before falling back to the interpreter.
- Tests: `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=07_05_partial_application go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`.
- Added fast-path native function/method invocation in `__able_call_value` (with partial/arity checks and context-aware errors) to avoid interpreter dispatch for native/compiled callables.
- Tests: `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=07_05_partial_application,06_01_compiler_bound_method_value go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`.
- Added a compiled method registry and `__able_member_get_method` fast-paths for inherent method lookup (instance/static) so member access can return compiled bound methods without interpreter dispatch when unambiguous.
- Tests: `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=09_02_methods_instance_vs_static go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`.
- Compiled `main` now prefers the generated wrapper (compiled execution path) instead of dispatching through `interp.CallFunction` when available, preserving exit-code handling.
- Tests: `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache go test ./pkg/compiler -run TestCompilerExecHarness -count=1`.
- Registered compiled call entries for overloaded functions using the overload dispatcher (and added partial handling for arity -1) so `__able_call_named` can stay in compiled space.
- Tests: `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=07_07_overload_resolution_runtime go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`.
- Added compiled method overload dispatchers (type-based selection + partial handling) and registered overload wrappers in the compiled method registry for instance/static methods when all overloads are compileable.
- Tests: `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=09_02_methods_instance_vs_static go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`.
- Compiled impl methods now emit Go wrappers/thunks and register them with the interpreter for interface dispatch; `__able_call_value` fast-paths compiled thunks on function/bound-method values to avoid interpreter execution when available.
- Tests: `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=10_03_interface_type_dynamic_dispatch go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`.
- Compiled interface default methods per impl (using interface package environments) and ensured impl wrappers treat interface/impl generics as generic for runtime type checks.
- Tests: `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=10_04_interface_dispatch_defaults_generics go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`.
- Added a compiled interface dispatch table for concrete impls (no impl generics/constraints/union targets), with bound-method caching that mirrors interpreter behavior and avoids partial application on interface member calls.
- Tests: `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=10_03_interface_type_dynamic_dispatch go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`; `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=10_04_interface_dispatch_defaults_generics go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`.
- Extended compiled interface dispatch to generic impl targets and interface-arg template matching (still skipping where-clause constraints and union targets).
- Tests: `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=10_03_interface_type_dynamic_dispatch go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`; `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=10_04_interface_dispatch_defaults_generics go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`; `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=10_06_interface_generic_param_dispatch go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`.

### 2026-02-10
- Made compiled impl thunk registration constraint-aware so generic impls with identical targets no longer overwrite each other (restores correct constraint-based impl selection in compiled runs).
- Tests: `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=10_02_impl_specificity_named_overrides go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`.
- Compiled interface dispatch now handles impl method overloads by emitting per-group overload dispatchers; added exec fixture `10_17_interface_overload_dispatch`.
- Tests: `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=10_17_interface_overload_dispatch go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`.

### 2026-07-21

- The register-native `MemberAccess` slice materially reached six unlike
  applications and passed all semantic/public-verifier guards, but true
  five-pair preserved-binary means were neutral or slower everywhere, with
  four 5.27%-14.98% regressions.
- Fully removed the candidate and restored the ordinary VM; full bytecode tests
  pass. Next attribute per-entry register-frame allocation/setup across three
  unlike reached workloads before changing executor infrastructure.
- Record:
  `docs/perf-baselines/2026-07-21-bytecode-register-member-access-gate.md`.
- Completed a 14-application operand-register closure census covering all 143
  current opcodes, complete unsupported sets, and exact transfer/CFG failures.
- `MemberAccess` is the selected minimum closure: 253,448 dynamic entries
  across six unlike applications, all passing the second-stage state/CFG gate.
  `StructLiteralNamedFast` and `CallMember` also clear breadth but are more
  concentrated or substantially more complex.
- Removed all temporary analyzer/executor code after byte-for-byte output
  checks. Next: implement and reach-gate the cold-fallback register-native
  `MemberAccess` slice before any repeated A/B timing.
- Record:
  `docs/perf-baselines/2026-07-21-bytecode-register-admission-closure-census.md`.
- Specified and exercised an operand-register call/continuation ABI that
  delegates named dispatch, inline frames, return coercion/restoration,
  errors, and scheduler suspension to the existing bytecode VM.
- Dynamic admission across fourteen verified applications materially reached
  only Distance Field and Option/Result Config, failing the required
  three-unlike-program breadth gate before timing.
- Removed the complete prototype and retained the architecture/census record
  in `docs/perf-baselines/2026-07-21-bytecode-register-call-continuation-abi-gate.md`.
- Next: refresh complete unsupported-set and CFG transfer/merge failures before
  selecting another register-engine semantic closure.
- Verified compiled interface dispatch handles where-clause constraints and union target variants in exec fixtures.
- Tests: `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=10_02_impl_where_clause,10_12_interface_union_target_dispatch go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`.
- Added a strict interface dispatch flag in compiled output (enabled only when all unnamed impl methods are compileable) to reduce interpreter fallback once coverage is complete.
- Tests: `cd v12/interpreters/go && GOCACHE=$(pwd)/.gocache ABLE_COMPILER_EXEC_FIXTURES=10_17_interface_overload_dispatch go test ./pkg/compiler -run TestCompilerExecFixtures -count=1`.
- Completed the six-application bytecode exact-leaf selection gate. Shared Go
  map/hash leaves reconcile to different Able-owned maps, while every other
  three-application intersection is a dispatcher, Go runtime machinery, or a
  documented closed VM family.
- Retained no runtime code; full `TestBytecode` tests pass in 25.378s. Next run
  the analogous current generated-main compiler sweep across six unlike
  non-concurrency misses.
- Record:
  `docs/perf-baselines/2026-07-21-bytecode-cross-corpus-exact-leaf-selection.md`.
- Completed the six-application compiled exact-leaf selection gate with 60
  verified preserved Able timings, 30 verified Go timings, 138 verified
  main-only CPU profiles, and six verified exact allocation profiles.
- Matrix Multiply meets the current target at 1.0409x Go; every cross-program
  CPU leaf is Go GC/allocation machinery whose concrete Able owners divide by
  application, so no code candidate was admitted.
- Next feasibility-gate packed eager raw-i32 cache storage while preserving
  eager semantics and guarding Binary Trees GC pacing plus ordinary bytecode.
- Record:
  `docs/perf-baselines/2026-07-21-compiled-cross-corpus-exact-leaf-selection.md`.
- Reconciled and closed packed eager integer-cache feasibility without a keep.
  The new extended boxed-`i32` trial removed 245,758 init allocations and 1.97
  MB, but ten-pair TapeLang regressed 3.48% with 25% more collections and
  three-pair Binary Trees performed 2.13% more collections despite neutral
  wall time. Ordinary bytecode guards were neutral-to-positive.
- Fully removed the candidate; restored `TestBytecode` passes in 24.336s.
  Next expand dynamic boxed-integer reuse/ownership evidence across Fixed
  Width, Reverse Complement, K-Nucleotide, and Rational Series using the
  existing opt-in diagnostics rather than retrying fixed eager storage.
- Record:
  `docs/perf-baselines/2026-07-21-packed-eager-boxed-i32-cache-feasibility.md`.

## 2026-07-21 - Truthiness/cast architecture closure refresh

- Collected 400 verified timing processes for compiled concurrency and the
  six-family bytecode register-architecture selection, retaining matched
  second cohorts for every row.
- Compiled truthiness/cast bridges reached seven/six applications but received
  zero CPU samples in six representative profiles; the known goroutine-ID wall
  remained dominant. Bytecode rows reached no corrected Error fallback or cast
  leaf.
- Removed diagnostic telemetry and kept no production or stdlib candidate.
- Record:
  `docs/perf-baselines/2026-07-21-truthiness-cast-architecture-closure-refresh.md`.

## 2026-07-21 - Broad architecture closure reconciliation

- Rebuilt the checked compiled architecture budget from current post-fix
  family timing artifacts and retained every successful sample.
- Required target speedups remain 7.27x-498.07x; perfect removal of the largest
  per-application exact owners still leaves 3.55x-43.53x.
- Refreshed the cross-family census over 85 frontier rows and nine boundaries.
  The corrected truthiness/cast boundary has reach but no three-family sampled
  CPU leaf, so no compiler, VM, runtime, or stdlib candidate was retained.
- Advanced only compiled target-budget and cross-family ownership. The ledger
  is 20/21 current, with compiled Sudoku quotient remaining.
- Records:
  - `docs/perf-baselines/2026-07-21-compiled-architecture-target-budget.md`
  - `docs/perf-baselines/2026-07-21-truthiness-cast-cross-family-architecture-ownership-audit.md`

## 2026-07-21 - Final Sudoku quotient closure refresh

- Pooled ten verified compiled Sudoku processes at 2.073s and ten Go processes
  at 0.637319s, retaining every sample.
- All nine generated application bodies avoid corrected truthiness/cast
  helpers. Two current profiles place signed Euclidean division at 12.53%
  cumulative, but only in Sudoku and far below the complete target gap.
- Retained no code candidate. All 21 closures are current and the frontier has
  zero actionable groups.
- Repaired stale recursion and static-import operation-depth links exposed by
  the catalog check; all 21 operation entries now reconcile to claimed rows.
- Record:
  `docs/perf-baselines/2026-07-21-truthiness-cast-sudoku-quotient-closure-refresh.md`.

## 2026-07-21 - Concurrent document-pipeline application gate

- Added the source-equivalent Concurrent Document Pipeline application across
  Able, Go, Python, and Ruby with a shared input and verifier.
- The catalog now has 47 portable applications and the 87-row selected
  frontier has no depth-one triple gaps. All 60 retained timing processes
  verified.
- Pooled compiled remains 65.34x Go; pooled bytecode remains 14.30x Python and
  7.29x Ruby. Current compiled profiles reproduce the already-closed
  goroutine-identity wall; bytecode adds no admissible exact shared leaf.
- Retained no compiler, runtime, VM, stdlib, language, or WASM change. All 21
  evidence closures are current and the performance frontier is zero
  actionable.
- Record:
  `docs/perf-baselines/2026-07-21-concurrent-document-pipeline-application-gate.md`.

## 2026-07-21 - Manifest normalization application gate

- Added the portable Manifest Normalization application across Able, Go,
  Python, and Ruby, strengthening files/text × closures × Option/Result to
  three unlike applications.
- All 60 normal timing processes verified. Pooled compiled remains 41.96x Go;
  bytecode remains 71.22x Python and 32.15x Ruby.
- A newly admissible generic String byte-conversion candidate had mixed signs
  across 35 retained candidate/control processes and was reverted. Bytecode
  exposed only closed VM owners.
- The 89-row performance frontier remains zero actionable. Retained no
  compiler, runtime, VM, stdlib, language, or WASM change.
- Record:
  `docs/perf-baselines/2026-07-21-manifest-normalization-application-gate.md`.

## 2026-07-23 - Compiler full-suite contract reconciliation

- Restored general compiler and generated-runtime correctness for primitive
  writes, cancellation, dynamic imports, discarded control flow, type/result
  inference, interface identity, and exact raised-value diagnostics.
- Direct discarded conditional/block lowering and conservative implicit-void
  inference keep recursive static functions on native carriers without
  special-casing Quicksort or any nominal container.
- The final 24-batch fixture audit and all repeated focused groups pass. All
  100 ranked performance processes verify; Quicksort pools at 1.011x Go.
- The default compiler handoff now uses bounded 25-test short-mode batches.
  No canonical-stdlib, VM, benchmark-specific, or WASM change was needed.
- The final default handoff passes all noncompiler packages, all 31 compiler
  batches, and the complete bytecode fixture pass.
- Next strengthen concurrency × arrays/files × user-interface coverage with
  one complete portable application before selecting another candidate.
- Record:
  `docs/perf-baselines/2026-07-23-compiler-full-suite-contract-reconciliation.md`.

## 2026-07-23 - Concurrent Signal Dispatch application gate

- Added a deterministic file-driven four-worker benchmark across Able, Go,
  Python, and Ruby. Its hot worker path chooses between two concrete kernels
  through the user-defined `SignalKernel` interface over shared signed Array
  data.
- All 50 timed processes verified. Pooled compiled remains 53.905x Go;
  bytecode remains 26.403x Python and 19.385x Ruby, with every cohort-mean
  spread below 15%.
- Compiled profiling reproduces `bridge.currentGID`/`runtime.Stack` at 93.75%
  cumulative. Bytecode profiling exposes no new exact generic child beyond
  closed Array, raw-integer, binary, call/member, and type-match families.
- Reconciled 52 applications, 97 selected rows, 164.126842 seconds of target
  excess, and all 21 closures. No engine, canonical-stdlib, dependency, or
  WASM implementation change was retained.
- The default handoff passes all noncompiler packages, all 31 bounded compiler
  batches, and the complete bytecode fixture pass.
- Next add the portable callable/data concurrency application before selecting
  another candidate.
- Records:
  - `docs/perf-baselines/2026-07-23-concurrent-signal-dispatch-application-gate.md`
  - `docs/perf-baselines/2026-07-23-concurrent-signal-dispatch-scorecard-reconciliation.md`

## 2026-07-24 - Compiled static-native carrier closure

- Made native static Array lowering permanent: legacy compiler options,
  `ablec`/`able build` flags, and the environment setting remain accepted but
  cannot select runtime-store carriers.
- Fixed general wildcard discard lowering so `_ = expression` evaluates and
  discards primitive, String, Array, nominal, union/interface, and callable
  native carriers without boxing through `runtime.Value`.
- Added all-primitive native signature/body guards, strengthened numeric Array
  body guards, and added runtime-to/from-Array reach counters to opt-in
  typed-boundary telemetry.
- Six unlike portable applications executed and verified. Array-to-runtime
  reach was zero; all 34 runtime-to-Array events were explicit filesystem or
  string kernel return boundaries.
- Focused compiler/CLI guards pass. Broad short-mode package batches hit the
  cumulative 60-second harness deadline without assertion failures; the two
  interrupted tests pass separately in 11.928s and 44.047s.
- No benchmark-speed claim or stdlib change was needed.
- Next census conservative generated static-closure reach/size across serial
  and concurrent applications before considering a pruning candidate.
- Record:
  `docs/perf-baselines/2026-07-24-compiled-static-native-closure.md`.

## 2026-07-24 - Compiled context-effect regression gate

- Extended the existing conservative environment-effect proof to inherent
  methods without introducing primitive, container, nominal, or benchmark
  selection. Pure imported methods use raw generated bodies; dependent methods
  retain package entry wrappers.
- Experimental package-context entries now use caller-owned locals, eliminating
  the measured helper allocation, and pure native-interface adapters bypass
  their package entries under the same proof.
- The admitted default-ABI method candidate passes all 160 verified processes
  and improves the six-guard geometric mean by 3.61%. The experimental ABI is
  still barred because Mutex Work Queue remains 13.7% slower.
- CPU profiles place the remaining experimental wall under
  `bridge.currentGID`/`runtime.Stack` through dynamic callable and await paths.
  A package-purity-based context omission made that guard worse and was
  reverted.
- No stdlib, language, VM, dependency, or WASM change was needed.
- Next model and measure scheduler-context effects at generated
  callable/await boundaries.
- Record:
  `docs/perf-baselines/2026-07-24-compiled-context-effect-regression-gate.md`.

## 2026-07-24 - Static interpreter-dependency cut gate

- Added strict `ablec --no-fallbacks` support and made the compiled boundary
  audit enforce it while recording final interpreter-package reachability.
- The strict core suite compiles and verifies without AST fallback. A candidate
  removing the two boxed operator linker roots cut six binary sizes by 32-37%
  and improved their geometric mean by 34.08%.
- The candidate was fully reverted because exact-source Binary Trees was
  10.60% slower and collected 205.33 versus 144.00 times. The interpreter's
  unrelated initialization currently acts as GC ballast for excessive
  generated nominal allocation.
- Retained the strict compiler/audit tooling only. No stdlib, language, VM,
  dependency, or WASM change was needed.
- Next fix general nominal construction and escape behavior before repeating
  the interpreter package cut.
- Record:
  `docs/perf-baselines/2026-07-24-static-interpreter-dependency-cut-gate.md`.

## 2026-07-24 - Static package-cut/native-parity reconciliation

- Proved generated Binary Trees allocation is already Go-equivalent:
  interpreter-free Able averaged 7.8167s versus Go 7.8400s under normal GC.
  The linked 7.4833s mean benefits from accidental interpreter heap ballast.
- Confirmed Sudoku's position values use native Go slices and nominal records
  use generated Go structs; no general nominal-allocation candidate repeats
  across the required unlike guards.
- Reapplied and widened the two-root package cut. The strict core suite again
  verified with no interpreter dependency, but Validated Job Pipeline exposed
  a semantic failure and forced a full revert.
- The failure is a real remaining lowering boundary:
  `Result i64.map<i64>` uses an erased `runtime.Value -> runtime.Value`
  callback, so its statically typed arithmetic still needs the shared
  primitive operator fast path. Restoring only those exact roots restored
  verified completion in 3/3 processes.
- No compiler/runtime production change or stdlib, language, VM, dependency,
  or WASM change was retained.
- Next lower instantiated primitive generic callables to native Go callable
  carriers across Result, Option, Array, and iterator mapping before trying the
  package cut again.
- Record:
  `docs/perf-baselines/2026-07-24-static-package-cut-native-parity-reconciliation.md`.

## 2026-07-26 - Compiled corpus owner census and lazy common-i32 cache

- Rebuilt and verified all 61 strict compiled applications; every final graph
  omits `pkg/interpreter`. The current corpus supplied 1,639 main CPU-profile
  and 183 lightweight allocation processes.
- Exact allocation profiles admitted the general primitive `bridge.ToInt`
  cache owner across six unlike applications. Independently lazy slots replace
  the fixed 4,225-object first-use charge with 1-35 reached values.
- Repeated allocation improvements range from 2.13% to 98.95%. K-Nucleotide
  remains neutral when it reaches nearly the whole range, and Fib is exactly
  neutral when it never reaches the cache.
- All 2,232 balanced A/B/Go timing processes verified. Three affected short
  applications improve 2.87-5.87%; the remaining candidate and control rows
  are neutral within workstation-noisy intervals.
- Generated source is identical, binary-size movement is at most 0.03%, all
  candidate graphs remain interpreter-free, and focused bridge/compiler/CLI
  guards pass.
- The existing `TestCompilerHashMapNativeCarrierExecutes` failure reproduces
  under both cache states. Repair that general handle path next, then begin
  typed bytecode `i32` slots. No named-container rule, stdlib change, or WASM
  work is authorized.
- Record:
  `docs/perf-baselines/2026-07-26-compiled-corpus-owner-census-lazy-i32-cache-retained.md`.

## 2026-07-26 - Compiled positional named-field consumer reconciliation

- Completed the retained positional named-struct encoding contract on its
  consumer side. Generated Array/HashMap index helpers and the compiler host
  bridge now accept map-backed or definition-ordered positional named fields.
- The exact HashMap generated A/B contains only four changed handle reads.
  The general bridge correction also repairs positional `Box.value` conversion
  and covers String bytes and Array handles without adding a named nominal
  lowering path.
- Direct and generated representation guards, exact HashMap and Go-extern
  execution, the full extern/bridge groups, nominal/interface/dynamic
  boundaries, three strict applications, `cmd/ablec`, all 32 compiler batches,
  and the complete bytecode fixture pass are green. All strict graphs remain
  interpreter-free.
- No canonical-stdlib, runtime package, interpreter, bytecode, language,
  dependency, or WASM change was required.
- Next refresh three unlike current bytecode profiles and admit only one exact
  shared semantic/allocation leaf. Existing `i32` lanes and sidecars are
  retained completed infrastructure, not a new tranche.
- Record:
  `docs/perf-baselines/2026-07-26-compiled-positional-named-field-consumer-reconciliation.md`.

## 2026-07-26 - Bytecode three-unlike exact-owner closure

- Frozen-artifact profiling covered Word Frequency, RMS Norm, and Concurrent
  Signal Dispatch with five verified Able/Python/Ruby comparisons per lane,
  three CPU profiles, three exact allocation-counter processes, and one
  sampled allocation profile per application.
- Current Able means are 1.492s, 4.424s, and 1.494s respectively; every row
  remains far outside both interpreter targets.
- The only three-way CPU symbols at 1% are the aggregate dispatcher, the
  already-closed slot-to-stack carrier parent, and Go GC scanning. Allocation
  owners divide into text/map/nominal/Array, float, and concurrent
  i64/environment families, with no three-way exact leaf.
- Retained no VM, runtime, compiler, stdlib, benchmark, language, dependency,
  or WASM change. `TestBytecode` passes in 36.148s; the complete handoff passes
  every non-compiler package, all 32 compiler batches, and the full bytecode
  fixture corpus.
- Next build a corpus-wide current-artifact bytecode exact-owner frequency
  census. It will rank non-closed concrete leaves by unlike-program reach and
  total target excess, then either admit one general candidate or establish
  the stopping condition for local profile-driven VM work.
- Record:
  `docs/perf-baselines/2026-07-26-bytecode-three-unlike-exact-owner-closure.md`.

## 2026-07-26 - Bytecode corpus-wide owner census closure

- Completed the current-artifact frequency census over all 54 rankable
  bytecode rows and 22 semantic families. CPU, sampled allocation, and
  independent counter phases passed for every row.
- Ranked 407 material exact CPU symbols and 133 material exact allocation
  lines by unlike reach and governing-target excess, then removed aggregates,
  Go GC/map symptoms, completed typed lanes/stack transport, required semantic
  values, and previously rejected cache/frame/ABI routes.
- `bindMemberMethodTemplate` was the sole admissible leaf: a cache fill made a
  temporary bound wrapper that the cache immediately converted back to an
  unbound template. A general predicate eliminated the wrapper across four
  unlike families.
- Five-run measured-main A/B confirmed allocation reductions up to 14.23%,
  but five interleaved public-verifier comparisons slowed Binary Event Log
  2.64% and Rational Series 3.52%. The broad gate therefore rejected it.
- Fully restored the baseline. Fresh interpreter and CLI builds exactly match
  hashes `5069b6df...` and `5f1108bc...`; no candidate production or stdlib
  change remains.
- The complete handoff passed every coverage/scorecard check, non-compiler
  package, all 32 compiler batches, and the final bytecode fixture corpus.
- Local profile-driven VM optimization is exhausted until new evidence or an
  architectural/semantic invalidation. Next refresh all strict compiled rows,
  interpreter-free dependency graphs, and equivalent-Go comparisons, then
  select only a shared generated/native owner or unjustified
  `runtime.Value` boundary. This directly tests native-carrier Go parity.
- Record:
  `docs/perf-baselines/2026-07-26-bytecode-corpus-owner-census-closure.md`.

## 2026-07-26 - Current strict compiled scorecard and owner closure

- Rebuilt and verifier-checked all 61 strict coverage applications and five
  fresh equivalent-Go processes per row: 305/305 passed in each lane.
- Audited every final graph; all 61 omit `pkg/interpreter`, preserving the
  fallback-free package cut and native compiled/interpreted boundary.
- The current strict result is 8 target passes and 53 misses. Binary Trees,
  Matrix Multiply, Pidigits, Quicksort, Base64, JSON, Monte Carlo Pi, and
  I Before E meet the 1.052632x Go target.
- Three fresh current-binary CPU profiles and three exact main allocation
  counters per selected row reproduce disjoint owners across the five largest
  absolute misses and two native controls. No concrete CPU or allocation leaf
  reaches the required three unlike applications.
- Retained no production, stdlib, benchmark, fixture, spec, dependency, or
  WASM change. This closes current-profile local compiled owner selection for
  the refreshed corpus.
- The complete `./run_all_tests.sh` handoff passed every contract,
  non-compiler package, all 32 compiler batches, and the 83.905-second final
  bytecode fixture corpus.
- Next compare generated and reference Go semantic work across Tapelang, Fib,
  and Sudoku, with Matrix and Pidigits controls. Source, assembly, loop/call
  counts, checks, allocations, and adapters must expose one repeated general
  proof/elision gap before another implementation experiment.
- Record:
  `docs/perf-baselines/2026-07-26-current-strict-compiled-scorecard-owner-closure.md`.

## 2026-07-26 - Compiled semantic-work equivalence closure

- Compared fresh generated Go, emitted assembly, dynamic work, exact
  allocation evidence, and five balanced timing pairs for three native misses
  and two controls. Every strict graph remains interpreter-free and every
  timing process passed its public verifier.
- Fib's equal algorithm exposes 2,269,806,338 unreachable fallible-control
  tests, but it is the only material total recursive function in the current
  and prior effect censuses.
- Tapelang's flat jump-table source performs 1,268,239,314 dispatches against
  the recursive Go source's 690,455,687 instruction visits. That 83.68% extra
  work is application representation, not generated compiler work.
- Sudoku executes the same search counts, but its dynamic position Array
  allocates a carrier and backing slice on each of 3,893,780 best updates,
  exactly matching the prior 7,787,560-object profile. Go returns scalars.
- Matrix is work-equivalent at the noisy target boundary; Pidigits performs
  fewer expensive big-integer operations in Able and beats Go in this cohort.
- No shared proof/elision candidate reaches three unlike programs. Retained no
  production, stdlib, benchmark, fixture, spec, dependency, or WASM change.
- The complete `./run_all_tests.sh` handoff passed every contract,
  non-compiler package, all 32 compiler batches, and the 85.166-second final
  bytecode fixture corpus.
- Next normalize the Tapelang and Sudoku benchmark/reference algorithms and
  data shapes, preserve Fib as an effect sentinel, and refresh five-pair
  ratios. Equivalent work is necessary before another compiled miss can
  authorize compiler optimization.
- Record:
  `docs/perf-baselines/2026-07-26-compiled-semantic-work-equivalence-closure.md`.

## 2026-07-26 - Benchmark semantic-work equivalence normalized

- Replaced the recursive Go Tapelang reference with the same flat
  structure-of-arrays jump-table model used by Able. This also fixes the old
  reference parser's zero-opcode bug, which turned 82 ignored comment and
  whitespace bytes into increments.
- Instrumented retained Tapelang work is identical by construction:
  577,783,636 increments, 57,780,404 moves, 288,891,814 loop starts,
  288,891,813 loop ends, and 1,213,347,694 total dispatches.
- Replaced Sudoku's per-best-update dynamic three-element Array with one
  reusable three-field `Position` per recursive solve call. Synchronized the
  external Able source and changed the Go reference to comparable slice-backed
  boards/masks plus the same output-parameter shape.
- The Sudoku search still performs 1,918,450 solve/find calls, 155,394,450
  scanned cells, 3,893,780 best updates, and 1,912,510 backtracks. Generated
  escape analysis proves the hot `Position` does not escape, eliminating the
  prior 7,787,560 source-chosen heap allocations.
- Both `--no-fallbacks` graphs have 96 packages and no `pkg/interpreter`;
  extracted hot functions have no runtime-value, bridge, wrapper, or thunk
  calls. Public verifiers and coverage/catalog contracts pass.
- The authoritative five-process lanes measured Tapelang at 3.7060s Able
  versus 2.9649s Go (1.2500x) and Sudoku at 1.5740s versus 0.7027s (2.2399x).
  All 20 processes verified and both Able rows averaged zero GC cycles. A
  second balanced cohort independently confirmed the classification.
- Retained benchmark/reference changes only. The ratios remain misses and are
  not compiler speedups.
- Reconciled the changed source fingerprints into the full compiled cohort and
  current scoreboard. Sudoku's unselected bytecode status remains a timeout
  under its repeated 55-second contract. `./run_all_tests.sh` passes all
  contracts, non-compiler packages, 32 compiler batches, and the final
  80.691-second bytecode fixture corpus.
- Next profile these normalized rows with Fib and admit only a general exact
  generated-code owner that is material across all three unlike applications.
  This is the direct evidence gate for further native-Go lowering work.
- Record:
  `docs/perf-baselines/2026-07-26-benchmark-semantic-work-equivalence-normalized.md`.

## 2026-07-26 - Normalized compiled generated-owner closure

- Rebuilt normalized Tapelang Alphabet, Sudoku Masks, and Fib in strict mode;
  all three final 96-package graphs remain free of `pkg/interpreter`.
- Merged three measured-main CPU profiles per Able application and three
  full-workload profiles per exact Go reference. Allocation evidence includes
  three identical Able main counters per row and three independent Go
  counters.
- The material CPU leaves are disjoint. Fib is 99.63% its recursive generated
  function; Tapelang is `execute`/checked `Tape.inc`/`Tape.move`; Sudoku is
  search plus signed division, checked multiply, bit count, and shifts. No
  bridge, runtime-value, wrapper, thunk, interpreter, or GC leaf repeats.
- Disassembly resolves the source-level control false positive: Go inlines
  total `Tape.get` and `Tape.ensure`, deletes their control results/checks, and
  leaves only fallible `inc`/`move` calls. Sudoku's remaining controls can also
  raise for unrestricted function arguments.
- No general removable owner reaches three unlike applications, so no
  production candidate or five-pair A/B gate was justified. Retained no
  compiler, generated-runtime, runtime, interpreter, VM, stdlib, benchmark,
  reference, fixture, spec, dependency, or WASM change.
- `./run_all_tests.sh` passed every contract, non-compiler package, all 32
  compiler batches, and the final bytecode fixture corpus in 85.495 seconds.
- Next extend the existing control census only as a report-only relational
  proof observer, then rank safe check elimination across the strict corpus.
  Production work requires material breadth in three unlike applications.
- Record:
  `docs/perf-baselines/2026-07-26-normalized-compiled-generated-owner-closure.md`.

## 2026-07-26 - Compiled relational safety-proof census closure

- Extended `cmd/compiled-control-census` as a report-only relational observer
  for static-Array shapes, integer and bitmask intervals, struct fields,
  aliases, direct-call aggregate facts, successful returns, parameter
  mutation, and exact counted-loop growth. Unknown calls, incompatible call
  sites, and recursive fixed points remain conservative.
- Built and publicly verified all 61 coverage applications with
  `-no-fallbacks`. Every final graph omits `pkg/interpreter`; 60 contain 96
  packages and Base64 contains 119 because of its additional Go imports.
- The observer found 1,006 reachable generated Array conditions and proved 89
  safe across 12 applications. Only NBody's 22,500,243 dynamic checks are
  material. Sudoku's four proven sites execute 9,040 times, and the remaining
  53 sites are one-shot literal-index reads.
- No relational rule is material in three unlike applications, so no
  production prototype or A/B/Go cohort was warranted. Retained no compiler,
  generated-runtime, runtime, interpreter, VM, stdlib, benchmark, language,
  dependency, or WASM change.
- The focused observer tests, all 61 strict verifier runs, dependency audits,
  and the complete `./run_all_tests.sh` handoff pass.
- Next build and evaluate typed bytecode `i32` slot storage across three
  unlike material applications. This entails native integer slot carriers,
  conservative materialization at dynamic boundaries, semantic guards, and
  five-or-more balanced baseline/candidate/Python/Ruby cohorts. It is
  important because bytecode still has a broad boxing/transport opportunity,
  whereas the compiled proof routes now identify only a single material
  application.
- Record:
  `docs/perf-baselines/2026-07-26-compiled-relational-safety-proof-census-closure.md`.

## 2026-07-26 - Bytecode typed-i32 slot storage closure

- Fresh interpreter and CLI binaries exactly match the artifacts used by the
  current three-application and 54-row bytecode owner censuses, proving that
  their profile closure remains current.
- Audited the retained native architecture: typechecker-proved `i32` register
  frames, value-slot `i32` sidecars, raw slot/stack carriers, direct typed and
  untyped stores, call-frame ownership, and explicit dynamic-boundary
  materialization are already implemented.
- Fresh verifier-backed carrier counts covered Array Slice Window, Word
  Frequency, and Fixed Width 128, with Distance Field as the zero-integer
  control. The applications recorded 1,020,268, 944,384, and 5,000,006 proven
  integer loads respectively.
- Three ordinary measured-main CPU/allocation processes per selected
  application found no concrete slot or materialization owner shared by all
  three. Generic raw-integer inspection is a broad parent over disjoint
  callers; slot-to-stack transport is material only in Word, while boxed and
  raw-result allocation each reach only two applications.
- No production candidate cleared the breadth gate, so no A/B cohort was
  warranted. Retained no VM, runtime, compiler, tree-walker, stdlib,
  benchmark, fixture, language, dependency, or WASM change.
- Focused native-lane, invalidation, overflow, call-boundary, materialization,
  carrier-observer, and frame-proof tests pass.
- The complete `./run_all_tests.sh` handoff passes every contract,
  non-compiler package, all 32 compiler batches, and the 107.961-second final
  bytecode fixture corpus.
- Next run a report-only primitive-materialization boundary census across all
  54 rankable bytecode applications. It will classify every raw-to-boxed
  transition by carrier, source/opcode, and semantic reason, then admit only an
  avoidable reason material in three unlike applications. This is important
  because it tests whether different helper sites hide one shared unnecessary
  boundary without replacing the whole slot/frame ABI.
- Record:
  `docs/perf-baselines/2026-07-26-bytecode-typed-i32-slot-storage-closure.md`.

## 2026-07-26 - Bytecode primitive-materialization boundary census closure

- Retained an opt-in, reason-aware raw-to-boxed primitive observer and focused
  lifecycle coverage.
- All 54 rankable bytecode applications passed public verification. The census
  recorded 18,696,646 transitions with no drops: 13,306,959 candidate-static
  and 5,389,687 required-dynamic.
- Collection materialization dominates at 11,088,674 transitions across 37
  applications. Named-struct `i32` fields account for 5,597,919 transitions;
  Array member-slot `i32` values account for 2,731,820 across nine.
- A general immutable raw-field prototype reduced some memory traffic but
  failed the balanced runtime gate: Sensor regressed 6.93% and Rational
  regressed 4.28%. The prototype was fully reverted, with byte-identical
  restored CLI and interpreter-test artifacts.
- No runtime semantics, compiler, tree-walker, stdlib, benchmark, language,
  dependency, or WASM change was retained.
- The complete `./run_all_tests.sh` handoff passed every contract, all 32
  compiler batches, and the 108.207-second final bytecode fixture corpus.
- Next reconcile primitive Array element materialization with existing raw
  caches and direct stores. It requires three unlike diagnostics-off profiles,
  a control, focused guards, and five verifier-backed A/B runs for any admitted
  general rule. This matters because static Arrays should stay native without
  compromising the shared nominal representation.
- Record:
  `docs/perf-baselines/2026-07-26-bytecode-primitive-materialization-boundary-census-closure.md`.

## 2026-07-26 - Bytecode primitive Array element boundary closure

- Traced raw-`i32` `CallMemberArraySlot` values through tracked Array writes,
  push, alias synchronization, cached native reads, and public
  materialization.
- Policy, Regex Set, Log Routing, and Array Slice passed public verification
  and received three exact one-main allocation runs plus diagnostics-off
  CPU/allocation profiles.
- Counts were 697,712, 566,764, 346,302, and 288,259. The first three are
  overwhelmingly the same four `regex_nfa_upsert_thread` push/write sites, not
  independent Array mechanisms.
- Array-slot dispatch is broadly hot, but boxing is CPU-material only in Regex
  Set and allocation-material only in Policy. It is small or unsampled in the
  other applications.
- No candidate cleared the three-unlike CPU, allocation, or mechanism gate.
  No production, stdlib, benchmark, language, dependency, or WASM change was
  retained.
- The complete `./run_all_tests.sh` handoff passed every contract, all 32
  compiler batches, and the 90.940-second final bytecode fixture corpus.
- Next reconcile candidate-static primitive returns by immediate caller
  consumer and ownership. It will focus on recurring ordinary `Return`
  `i32`/`i64` shapes and admit only a local raw handoff compatible with current
  scratch ownership. This matters because internal static returns are the next
  direct native-carrier boundary without reopening the broad frame ABI.
- Record:
  `docs/perf-baselines/2026-07-26-bytecode-primitive-array-element-boundary-closure.md`.

## 2026-07-26 - Bytecode static primitive-return consumer closure

- Added a dedicated low-overhead primitive-materialization observer mode and
  static-return attribution for saved frame kind, caller program, return IP,
  next opcode, and caller source.
- All 54 rankable applications passed public verification. A temporary
  semantic stack-lifetime probe reconciled all 1,678,255 static-return
  transitions across 39 applications with no drops, then was removed.
- The broadest ordinary owned-slot leaf was `Return`
  `integer_result<i64>` to `StoreSlotNew`: 210,974 transitions across Audio
  Voices, Packet Codecs, Scene Tiles, and Mutex Work Queue.
- Three interleaved samples per variant rejected a local raw return-to-slot
  rule: allocation counts rose 0.56%-2.29% in all three profiled applications;
  Packet slowed 5.87% and Scene slowed 4.84%. The experiment was reverted.
- Retained diagnostics only. No runtime semantics, compiler, tree-walker,
  stdlib, benchmark, language, dependency, or WASM change landed.
- The complete handoff passed all contracts, all 32 compiler batches, and the
  88.052-second fixture pass. Unchanged compiler batches 19, 28, and 29 took
  185.073, 75.304, and 93.208 seconds and need a short repeated audit.
- Next refresh compiled AOT generated native-carrier owners across at least
  three unlike current strict target misses. This matters because
  interpreter-free applications should approach Go when primitive carriers
  remain native through their complete generated call graphs.
- Record:
  `docs/perf-baselines/2026-07-26-bytecode-static-return-consumer-closure.md`.

## 2026-07-26 - Compiled AOT native-carrier owner closure

- Repeated compiler batches 19, 28, and 29. Their three-sample means are
  185.264, 76.471, and 90.236 seconds; every individual test stayed below one
  minute.
- Strictly rebuilt and publicly verified N-Body, Inventory Reconciliation, and
  Unicode Scalar Pipeline with the current compiler artifact. Each
  96-package dependency graph remains free of `pkg/interpreter`.
- Twenty diagnostics-off main CPU profiles per Able and equivalent Go
  application separate the owners: native `float64` Array computation,
  runtime-backed generic Map semantics, and UTF-8/iterator nominal results.
- Three allocation-counter and three exact-profile processes per
  language/application confirm disjoint allocation shapes. The report-only
  typed observer likewise finds no material category in all three.
- No production candidate or balanced A/B cohort was justified. No compiler,
  runtime, interpreter, VM, stdlib, benchmark, reference, language,
  dependency, or WASM change was retained.
- Retained a test-only extern-plugin lifecycle correction after the full
  handoff exposed a Linux `SIGBUS`: the invalid-artifact guard now builds the
  initially valid plugin in a short-lived helper before corrupting its file.
  Production plugin behavior is unchanged; three focused repetitions and the
  related extern group pass.
- The complete `./run_all_tests.sh` handoff passes every contract,
  non-compiler package, all 32 compiler batches, and the final 86.579-second
  bytecode fixture pass.
- Next run a full strict-corpus typed-boundary callsite census. It will
  attribute conversion carrier, consumer, source, and semantic reason across
  all 61 verified applications, then profile only shapes material in three
  unlike target misses. This is important because it gives the native-carrier
  hypothesis a corpus-wide evidence gate.
- Record:
  `docs/perf-baselines/2026-07-26-compiled-aot-native-carrier-owner-closure.md`.

## 2026-07-26 - Full strict typed-boundary callsite census closure

- Retained a low-overhead extension to the opt-in compiled typed-boundary
  observer. Nonzero shapes now include generated helper, Able source,
  carrier, immediate consumer, semantic reason, and exact count.
- All 61 strict coverage applications built with `--no-fallbacks`, passed
  public verification, and produced final graphs without `pkg/interpreter`.
- The census observed 107 concrete shapes; fourteen reached three
  applications. None is both new and material across three unlike target
  misses.
- A 17-application refinement split `runtime.Value` to `int64` by semantic
  consumer: 238,838 channel-handle recoveries, 30 channel-capacity
  validations, and 43,016 mutex-handle recoveries.
- Channel/mutex conversion and Awaitable/Future adapters remain explicit,
  related concurrency-service boundaries. The encompassing integer helper is
  already below 1% in retained normal-binary profiles; startup I/O/String and
  dynamic conversions occur once or a few times per application.
- Retained no production lowering, runtime, interpreter, VM, stdlib,
  benchmark, language, dependency, or WASM change.
- The full handoff passes every contract, all non-compiler packages, all 32
  compiler batches, and the 86.857-second final fixture pass. Audited
  aggregate batches 19, 28, and 29 took 186.551, 75.252, and 86.757 seconds.
- Next compare generated Go with equivalent Go for Tapelang Alphabet, Fib, and
  Sudoku Masks, using Matrix Multiply and Pidigits as controls. Seek only a
  compiler-added operation repeated in three unlike applications.
- Record:
  `docs/perf-baselines/2026-07-26-full-strict-typed-boundary-callsite-census-closure.md`.
## 2026-07-26 - Performance frontier reconciliation

- Removed a stale handoff loop from the root roadmap: semantic-work
  equivalence, benchmark normalization, generated-owner profiling, relational
  proofs, typed primitive slots, materialization consumers, Array/return
  boundaries, full VM exact owners, and compiled typed callsites are already
  closed.
- Verified exact coverage of the sibling suite: 61 portable benchmark
  directories equal the 61 catalog applications after hyphen/underscore
  normalization, with no source update after the latest closure.
- `PLAN.md` now records an evidence gate instead of another speculative
  implementation task. Performance mutation requires a new broad workload,
  retained execution change, correctness invalidation, or exact non-closed
  owner material in three unlike applications.
- Retained no v12 source, runtime, VM, compiler, stdlib, benchmark, fixture,
  language, dependency, or WASM change.
- The complete handoff passes every contract, non-compiler package, all 32
  compiler batches, and the 86.401-second bytecode fixture corpus. Canonical
  stdlib tests pass in both tree-walker and bytecode modes.
- Next select correctness/release work from a concrete failing gate and refresh
  performance evidence only if that work changes execution ownership.
- Record:
  `docs/perf-baselines/2026-07-26-performance-frontier-reconciliation.md`.

## 2026-07-26 - Compiled numeric release-readiness closure

- The release audit first removed a permanent generated-Go debug directory and
  unreachable source tails that blocked repository-wide vet/build checks.
- Canonical compiled numeric gates then exposed invalid Go emission for an
  intentionally out-of-range `u128` literal and constant-folded overflowing
  float multiplication. Both now use general typed-boundary/runtime-evaluation
  rules.
- Wide primitives now encode through `IntegerValue()` when a generic interface
  boundary requires `runtime.Value`.
- Interface recovery now probes known concrete adapters before sibling
  semantic interfaces, fixing `Matcher<Result<f64>>` recovery without a
  matcher-, Result-, or stdlib-specific branch.
- `generator_binary.go` was split to 931 lines; the extracted operation helpers
  occupy 123 lines. Compiled numeric release cases were split by canonical
  source file for clearer timing and failures.
- Focused tests, interpreter-free Int128 execution, direct canonical numeric
  suites, `go vet ./...`, `go build ./...`, the full compiled-CLI release lane,
  and canonical tree-walker/bytecode stdlib suites pass.
- No stdlib, runtime, interpreter, VM, language, dependency, benchmark,
  reference, or WASM change landed.
- Next run a report-only strict-corpus census of concrete-to-lifted-interface
  coercions. Implement a direct native coercion only for a general shape
  material in three unlike applications; otherwise close the observation with
  no additional code.
- Record:
  `docs/perf-baselines/2026-07-26-compiled-numeric-release-readiness.md`.

## 2026-07-26 - Concrete-to-lifted-interface census closure

- Added a report-only `interface_lift_via_runtime` attribution at the final
  native-interface fallback and restricted candidate registration to fully
  bound source carriers.
- All 61 strict coverage applications built with `--no-fallbacks`, passed
  public verification, produced one telemetry snapshot, and retained final
  Go graphs without `pkg/interpreter`.
- The exact route has zero emitted callsites and zero runtime events.
  Seventeen registrations across Inventory Reconciliation and four
  concurrency applications came from speculative coercion comparison; native
  adapters won before emission.
- Compiled representative collections, Rational, String, and foundational
  assertion/test packages show the same result. The foundational binary exits
  zero; its five assertion candidates emit no target marker call.
- No general shape exists in three unlike applications, so no profile,
  prototype, or repeated A/B/reference cohort was justified. No production or
  canonical-stdlib change landed, and WASM remains deferred.
- Focused guards, repository-wide vet/build, the full 33-batch handoff, the
  87.571-second fixture corpus, and canonical tree-walker/bytecode stdlib
  suites pass.
- Next reproduce the 138.88-second observer-enabled foundational integration
  duration without diagnostics. Attribute compile/build/execution phases and
  change only the test lane if three warm-cache ordinary runs still violate
  the one-minute rule.
- Record:
  `docs/perf-baselines/2026-07-26-concrete-to-lifted-interface-census-closure.md`.

## 2026-07-27 - Compiled foundational stdlib integration sharding retained

- Three ordinary warm-cache reproductions put the foundational compiled case
  at a 139.206-second mean and 138.924-second median.
- Temporary file-based timing attributed 117.998 seconds to Able compilation,
  19.873 seconds to generated-Go build, and 0.070 seconds to execution; the
  observer was removed after the measurements.
- The compiled CLI lane now runs Simple, Assertions, and Enumerable
  Array/Iterator as independent strict cases. The unrelated eager HashSet map
  assertion moved to the canonical HashSet suite, and its existing compiled
  case now asserts that coverage.
- Three diagnostics-off repetitions per shard passed with 39.32, 52.62, and
  51.05-second means. The worst sample was 53.62 seconds and aggregate work
  increased 2.72%.
- Focused tree-walker and bytecode canonical suites pass. The existing compiled
  HashMap/HashSet case passes in 96.50 seconds and is the next known
  individual-test violation.
- The complete handoff passes all contracts, non-compiler packages, all 33
  compiler batches, and the final 85.483-second bytecode fixture corpus.
  Complete canonical stdlib tree-walker and bytecode suites pass in 21.07 and
  17.42 seconds.
- No production compiler/runtime, interpreter, VM, canonical stdlib
  implementation, benchmark, language, dependency, or WASM change landed.
- Next census the rest of the compiled stdlib lane for over-one-minute cases
  and pursue general build reuse or coverage-preserving package shards.
- Record:
  `docs/perf-baselines/2026-07-27-compiled-foundational-stdlib-integration-sharding-retained.md`.

## 2026-07-27 - Compiled stdlib integration lookup caches retained

- The full compiled stdlib census exposed six aggregate rows over one minute.
  Feature-package sharding preserved all assertions, but Vector, LazySeq,
  PersistentQueue, and HashSet remained intrinsically at or above the limit.
- Temporary phase timing showed Able compilation dominated all four while
  execution stayed near 0.06 seconds.
- Cross-graph profiles selected two general compiler owners: rebuilding
  immutable package export-name sets and linearly rescanning structs for
  unique unqualified names.
- Both lookups now use lifecycle-safe caches. Focused tests cover zero-
  allocation reuse, re-export invalidation, struct-table growth, and ambiguity.
- Three final runs per governing graph produced 55.047, 39.487, 41.903, and
  44.563-second means, with a 55.33-second worst sample.
- All 32 final compiled cases passed in 1,038.379 seconds; the worst full-lane
  row was 55.70 seconds. Aggregate time fell 12.20% versus the original
  26-case census despite six additional processes.
- Full repository and canonical stdlib handoffs pass. No generated execution,
  runtime, interpreter, VM, canonical stdlib implementation, language,
  dependency, benchmark, or WASM change landed.
- Next evaluate content-addressed generated-output/build reuse across unlike
  cases so this bounded lane can become practical routine coverage without
  hiding cold work or weakening assertions.
- Record:
  `docs/perf-baselines/2026-07-27-compiled-stdlib-integration-lookup-caches-retained.md`.

## 2026-07-27 - Compiled stdlib content-addressed cache retained

- Retained an opt-in content-addressed cache for `able test --compiled`;
  callers choose the disk-backed root through
  `ABLE_TEST_COMPILED_CACHE_DIR`.
- Keys include all loaded Able source paths/bytes and package edges, generated
  runner/harness configuration, compiler options, Go module build sources,
  and exact Go/toolchain identity.
- Cache manifests carry executable SHA-256 checksums. Publication is atomic,
  repeat writers reuse a valid winner, and corrupt entries are rejected and
  repaired.
- The synthetic test runner now has a stable package/origin. Cache-enabled Go
  builds use a private trimmed module; none of the 32 final binaries contains
  a deleted random workspace path.
- The final cold 32-case lane passed in 1,045.16 seconds with 32 misses.
  Three warm repetitions averaged 22.403 seconds with 96 hits, a 97.86% wall
  reduction and 46.65-times speedup; mean peak RSS fell 90.33%.
- Final salted Vector, HashSet, and LazySeq probes proved one miss then one hit
  per unlike family. Focused invalidation/corruption guards and the complete
  696.74-second repository handoff pass.
- No production compiler/generated runtime, runtime, interpreter, VM,
  canonical stdlib, language, dependency, benchmark, or WASM change landed.
- Next bound cache growth with explicit inspect/prune controls and safe
  concurrent-use guards before enabling it in the canonical compiled script.
- Record:
  `docs/perf-baselines/2026-07-27-compiled-stdlib-content-addressed-cache-retained.md`.

## 2026-07-27 - Compiled-test cache bounded lifecycle retained

- Retained explicit compiled-test cache inspection and pruning with JSON and
  human output, byte and age bounds, dry runs, and valid/corrupt/staging/
  obsolete-schema/unknown classification.
- A shared OS lock now covers cache lookup/publication through executable
  completion. Exclusive nonblocking pruning reports busy and removes nothing
  while a compiled test is active; cross-process coverage proves the contract.
- The canonical disk-backed cold command passed in 950.71 seconds with 42
  misses. Three warm runs averaged 183.00 seconds and repeated 32 stable hits
  plus ten expected path-specific misses, a 5.20-times total speedup.
- Dry-run and actual 1536 MiB pruning agreed exactly: 104 entries and
  3,756,646,712 bytes became 42 entries and 1,580,708,064 bytes. The pruned
  canonical rerun passed with 32 hits/10 misses, and final cleanup left the
  same bound with no invalid residue.
- No automatic deletion, default cache path, production execution change,
  canonical stdlib change, dependency change, benchmark change, or WASM work
  landed.
- The complete `./run_all_tests.sh` handoff passed in 692.07 seconds; all 33
  compiler batches and the final 86.265-second bytecode fixture pass are
  green.
- Next keep the cache explicit and reopen native-lowering work only when a new
  exact non-closed owner is material in three unlike applications.
- Record:
  `docs/perf-baselines/2026-07-27-compiled-test-cache-bounded-lifecycle-retained.md`.

## 2026-07-27 - Compiled generated-artifact drift closure

- The current compiler source identity differs from the frozen normalized
  compiled-profile compiler, so Tapelang Alphabet, Sudoku Masks, and Fib were
  rebuilt under `--no-fallbacks` as a narrow report-only invalidation check.
- Fib plus Tapelang and Sudoku's seven recorded hot helpers reproduce all
  frozen machine-code sizes, instruction counts, call counts, and branch
  counts exactly.
- All nine current smoke processes passed public verification. Each strict
  graph has 96 packages and omits the interpreter from dependencies and
  generated source.
- Identical hot execution provides no new owner to profile or mutate. No
  production, stdlib, benchmark, language, dependency, or WASM change landed.
- The complete repository handoff passed in 632.07 seconds, including all 33
  compiler batches and the final 86.660-second bytecode fixture corpus.
- Next keep compiled mutation paused until a real correctness/release failure
  or new three-unlike-application production owner invalidates the frontier.
- Record:
  `docs/perf-baselines/2026-07-27-compiled-generated-artifact-drift-closure.md`.

## 2026-07-27 - Compiler/AOT release-readiness no-code closure

- Repository-wide vet/build, the explicit cached compiled-CLI lane, the
  complete handoff, and canonical tree-walker/bytecode stdlib suites pass.
- The compiled lane completed in 181.72 seconds with 32 hits and ten expected
  path-sensitive misses. Pruning removed those ten entries and restored the
  retained 42-entry/1,580,708,024-byte cache below 1536 MiB.
- The full handoff completed in 637.05 seconds with all 33 compiler batches and
  the 88.039-second bytecode fixture pass green.
- Per-test timing proved the 132.800 and 68.617-second aggregate compiler
  batches have individual-test maxima of only 33.35 and 10.58 seconds.
- Canonical stdlib tests completed in 31.26 seconds; both execution modes
  reported 15 seconds.
- No production, stdlib, language, dependency, benchmark, fixture, reference,
  or WASM change landed. All disposable audit output was removed.
- Next inventory the dirty v12 worktree against retained records without
  deleting, resetting, committing, or rewriting history, then propose safe
  review boundaries for maintainer authorization.
- Record:
  `docs/perf-baselines/2026-07-27-compiler-aot-release-readiness-no-code-closure.md`.

## 2026-07-27 - V12 worktree release-consolidation inventory

- The pre-record dirty-tree baseline contains 5,142 visible paths: 559 tracked
  modifications and 4,583 untracked files, with no deletion or rename.
- All paths map to dependency-ordered subsystem, evidence, deferred WASM, or
  generated-local boundaries; none is unmatched.
- Shared files span many chronological records, so safe consolidation uses
  final subsystem states rather than pretending to recreate historical
  per-tranche commits.
- Twenty-six WASM paths remain isolated. Ninety-five visible generated/local
  paths must not be staged, and a non-mutating cleanup dry run found 79.52 GiB
  of reproducible project-local state.
- Five handwritten files exceed 1,000 lines; four are Go files and one is the
  tree-sitter grammar. One additional Go file needs `gofmt`.
- No source, fixture, artifact, cache, index, commit, or history state changed.
- Next perform behavior-preserving splits of the four oversized Go files and
  format the typechecker bounds table, leaving grammar modularization for a
  parser-specific follow-up.
- Record:
  `docs/perf-baselines/2026-07-27-v12-worktree-release-consolidation-inventory.md`.

## 2026-07-27 - Go source-hygiene modularization retained

- Moved intact compiler loop-control, Array-boundary tests, interpreter Array
  members, slot-constant overflow tests, and specialization type-matching
  helpers into five cohesive sibling files.
- Original/extracted line counts are 830/313, 848/212, 923/168, 985/108, and
  907/712. Every active Go file is now below 1,000 lines.
- `type_utils.go` received only `gofmt` alignment in the integer-bounds table.
- Focused compiler, interpreter/VM, typechecker, specialization, and vet gates
  pass. The full handoff passed in 701.68 seconds; all 33 compiler batches and
  the 89.857-second bytecode fixture corpus are green.
- Canonical tree-walker and bytecode stdlib suites pass, reporting 17 and 15
  seconds.
- The reviewed default cleanup removed 79.57 GiB of reproducible local build
  state. Profile evidence and disk-backed reusable caches remain.
- No execution, language, stdlib, dependency, benchmark, fixture expectation,
  reference, or WASM behavior changed.
- Next modularize the remaining 1,650-line handwritten tree-sitter grammar,
  regenerate parser assets, and run the parser/AST/full handoff gates.
- Record:
  `docs/perf-baselines/2026-07-27-go-source-hygiene-modularization-retained.md`.

## 2026-07-27 - Tree-sitter grammar modularization retained

- Replaced the 1,650-line grammar monolith with an 86-line ordered entry point
  and four rule/helper modules, all at or below 650 lines.
- Package publication includes `grammar/**`; a dry-run package inventory
  confirms every module is present.
- Generated parser C, grammar JSON, and node-types JSON remain byte-identical
  across the split.
- Refreshed one stale inline-lambda corpus tree to the current generated CST
  shape. All 28 tree-sitter cases and the forced Go parser relink pass.
- The full handoff passed in 683.69 seconds with all 33 compiler batches and
  the 85.523-second bytecode fixture pass green. Canonical tree-walker and
  bytecode stdlib modes reported 17 and 15 seconds.
- The active handwritten Go/JavaScript source boundary now has no known
  1,000-line violation.
- Cleanup removed 214 MiB of reproducible local residue; reusable disk-backed
  caches and profile evidence remain.
- No execution, language, stdlib, dependency, benchmark, reference, or WASM
  behavior changed.
- Next review the language/parser/typechecker release boundary without
  staging or committing, and emit a deterministic review manifest.
- Record:
  `docs/perf-baselines/2026-07-27-tree-sitter-grammar-modularization-retained.md`.

## 2026-07-27 - Language/parser/typechecker release-boundary review

- Captured an authoritative 82-path manifest for the current language, AST,
  parser, and typechecker delta: 54 tracked modifications, 28 untracked files,
  and no unclassified path.
- Reviewed 77 source/test/fixture/documentation paths, marked three generated
  parser files regenerate-only, and held the two shared spec files for their
  downstream runtime/compiler boundaries.
- Final-state ownership is backed by the v12 spec, the source re-export and
  named-implementation audit, typechecker design/log entries, and retained
  parser/origin performance records.
- Focused AST/parser/typechecker vet and tests, semantic guards, all 28 native
  tree-sitter cases, fixture export, forced Go parser relink, generated hashes,
  formatting, and whitespace checks pass.
- The full handoff passed in 11:34.57 at 4,633,264 KB peak RSS; all 33 compiler
  batches and the 85.244-second bytecode corpus are green.
- The review added evidence and handoff documentation only. It made no
  execution, language-contract, stdlib, dependency, benchmark, reference, or
  WASM change and performed no Git mutation.
- Removed 140 KiB of project-local generated cache and 72 MiB of disposable
  review-specific `/var/tmp` state; retained reusable disk-backed caches.
- Next audit runtime carriers, tree-walker, bytecode VM, execution fixtures,
  runtime support, and wrappers as the second dependency-ordered boundary.
- Record:
  `docs/perf-baselines/2026-07-27-language-parser-typechecker-release-boundary-review.md`.

## 2026-07-27 - Runtime/tree-walker/bytecode release-boundary review

- Captured an authoritative 666-path manifest for runtime carriers,
  tree-walker, bytecode VM, engine guards, execution fixtures, support, and
  wrappers: 277 tracked modifications, 389 untracked files, and no
  unclassified path.
- Reviewed 658 paths and held eight deferred WASM shims.
- Fixed a general serial-scheduler race in which Flush could return after a
  worker dequeued a task but before it published that task as active.
- The final accounting covers only that locked handoff. A rejected
  intermediate scope exposed by the mutex fixture proved why blocked tasks
  must not remain marked as runnable worker work.
- Deterministic handoff, blocked-task, scheduler race, bytecode, runtime,
  wrapper, vet, formatting, and complete interpreter gates pass.
- The complete handoff passed in 10:20.17 at 4,742,496 KB peak RSS; all 33
  compiler batches and the 83.750-second bytecode corpus are green.
- Both canonical stdlib modes passed in 37.13 seconds at 870,304 KB peak RSS,
  reporting 16 seconds for tree-walker and 15 seconds for bytecode.
- No performance code, compiler lowering, stdlib, language, dependency,
  benchmark, reference, fixture expectation, or WASM path changed.
- Guarded cleanup removed 332 MiB of wrapper cache plus small Python and
  tranche scratch state; no generated project candidate remains.
- Next audit compiler/AOT lowering and semantic ABI consumers to prove static
  values keep native Go carriers and strict applications omit the interpreter.
- Records:
  `docs/perf-baselines/2026-07-27-runtime-treewalker-bytecode-release-boundary-review.md`
  and
  `docs/perf-baselines/2026-07-27-serial-executor-flush-handoff-retained.md`.

## 2026-07-27 - Compiler/AOT/semantic-ABI release-boundary review

- Classified and reviewed all 340 current compiler/AOT, generated-runtime,
  bridge, semantic-ABI, compiler-facing CLI, embedded-kernel, module, and
  analysis-tool paths: 174 tracked modifications and 166 untracked files.
- Confirmed static primitives and Arrays keep native Go carriers,
  non-primitive nominals use shared translation, and dynamic values cross
  only explicit language/host/runtime boundaries.
- Current strict Fib, Sudoku Masks, and Tapelang Alphabet builds contain no
  fallback or interpreter dependency; nine public-verifier executions pass,
  and repeated Fib generation is exact.
- Corrected the compiler-generated serial executor's dequeue-to-active Flush
  race using the same narrow locked handoff rule retained in the reference
  interpreter. A generated race test passed 20 repetitions.
- Focused lowering, package-cut, dynamic-boundary, concurrency, semantic-ABI,
  CLI, vet, module, formatting, line-limit, and whitespace gates pass.
- The explicit 42-case compiled lane passed in 19:31.59 at 3,399,964 KB peak
  RSS. The full handoff passed in 15:14.81 at 4,545,104 KB peak RSS, including
  all 33 compiler batches and the 97.109-second bytecode fixture pass.
- Both canonical stdlib modes passed in 50.25 seconds at 856,468 KB peak RSS,
  reporting 20 seconds for tree-walker and 19 seconds for bytecode.
- Pruned exactly 42 obsolete compiled-test cache entries and 1,580,708,024
  bytes; the current 42-entry working set remains bounded below 1536 MiB.
  Removed the exact 405 MiB audit workspace and 37 MiB stale RAM-backed
  extern cache after verification.
- No performance optimization, canonical stdlib, language, dependency,
  benchmark, fixture expectation, reference, interpreter, bytecode, or WASM
  source changed. No Git history operation was performed.
- Next classify remaining documentation, examples, benchmark/evidence paths,
  and shared held spec/design material as the fourth consolidation boundary.
- Records:
  `docs/perf-baselines/2026-07-27-compiler-aot-semantic-abi-release-boundary-review.md`
  and
  `docs/perf-baselines/2026-07-27-compiled-serial-executor-flush-handoff-retained.md`.

## 2026-07-27 - Documentation, benchmark, and evidence release-boundary review

- Audited the final consolidation boundary for canonical documentation,
  examples, benchmark contracts/tools, governing decisions, raw/profile
  evidence, shared contracts, and release tooling.
- All 1,798 JSON artifacts, two gzip archives, 285 log-linked governing
  records, 3,029 local Markdown links, coverage/selection contracts, and
  checked derived-performance reports pass.
- The refreshed evidence chain retains 115 selected rows, 107 target misses,
  zero actionable groups, and no implementation candidate. The reconciled
  closure ledger has 21 current entries and no invalidation.
- Retained only general integrity corrections: cleanup coverage for generated
  compiler/statistics artifacts, exact canonical/embedded kernel drift
  checking with the canonical `f64` square-root declarations restored, and a
  corrected parser-design link.
- `./run_all_tests.sh` passed in 13:58.84 at 4,679,628 KB peak RSS; all 33
  compiler batches and the 87.323-second bytecode fixture aggregate are
  green. Canonical stdlib passed in 57.22 seconds, reporting 19 seconds
  tree-walker and 16 seconds bytecode.
- Cleanup removed 214 MiB of reproducible project-local cache. No performance
  optimization, stdlib implementation, dependency, benchmark workload,
  reference, or WASM path advanced, and no Git mutation occurred.
- Next obtain maintainer review of all four manifests and explicit staging
  authorization so only reviewed final-state boundaries enter history.
- Record:
  `docs/perf-baselines/2026-07-27-documentation-benchmark-evidence-release-boundary-review.md`.

## 2026-07-27 - Release-consolidation index review

- Derived one exact stageable union from the four reviewed manifests. The
  original union contains 5,054 paths; the 34-path complement is precisely the
  deferred WASM set.
- All 5,046 manifest-recorded paths matched their line, byte, and SHA-256
  identities before staging; eight final self-metadata files completed that
  union.
- Cached whitespace review found 19 defects hidden in previously untracked
  files. Removed trailing spaces/extra final blank lines, reran the affected
  scorecard and shared-heap contract tests, and refreshed their manifest rows.
- The final index contains 5,056 paths including this review pair. Only the 34
  WASM paths are unstaged. No forbidden prefix, secret-like filename,
  generated-local artifact, v10/v11 path, or deprecated stdlib path entered
  the index.
- The two staged binaries are explicitly reviewed gzip profiles.
  `git diff --cached --check`, exact path-complement, and manifest checks pass.
  No commit or history rewrite occurred.
- Next create one atomic release-consolidation commit after explicit
  authorization. This preserves the only fully verified final state rather
  than manufacturing untested intermediate history.
- Record:
  `docs/perf-baselines/2026-07-27-release-consolidation-index-review.md`.

## 2026-07-27 - Post-Fasta derived-contract propagation retained

- Confirmed the 61-application corpus has sufficient operation depth, at least
  two applications for every feature pair, and coverage for all 165 feature
  triples; no new benchmark application is justified.
- Corrected two stale post-Fasta expectations: the triple frontier and
  residual cost model now use the current `182.007474`-second frontier total,
  and the residual selected-excess share is `41.482334%`.
- Made both fast contracts mandatory in their owning catalog and architecture
  aggregate gates.
- The affected contracts plus catalog, scorecard, frontier, evidence-ledger,
  and complete architecture gates pass. No benchmark, compiler, runtime, VM,
  language, stdlib, dependency, measurement, or WASM code changed.
- Next retain the evidence-gated pause until a genuine new application domain,
  semantic invalidation, correctness failure, or non-closed owner across three
  unlike applications appears. This keeps native lowering evidence-driven.
- Record:
  `docs/perf-baselines/2026-07-27-post-fasta-derived-contract-propagation-retained.md`.

## 2026-07-27 - Scorecard evidence recipe defaults retained

- Rechecked the complete portable catalog and current performance evidence:
  61 applications, 115 selected rows, zero actionable groups, and 21 current
  closures with zero invalidations.
- Fixed the general no-argument scorecard evidence recipe to pass the current
  scorecard, reviewed selection manifest, and required five-run threshold.
- The repaired recipe verifies all 115 selected rows and 122 full-status rows
  with five successful Able/reference samples per selected row.
- All catalog, coverage, selection, scoreboard, frontier, ledger,
  deterministic architecture, and whitespace checks pass. No compiler,
  runtime, VM, language, benchmark, stdlib, dependency, or WASM code changed.
- Cleanup removed 309 MiB of reproducible local Go cache, 60 KiB of Python
  bytecode, and an empty generated temporary directory while retaining the
  reusable disk-backed extern cache.
- Next preserve the performance pause until new evidence identifies one
  non-closed owner material across three unlike applications. This keeps
  native carriers and compiled/interpreted boundaries evidence-driven.
- Record:
  `docs/perf-baselines/2026-07-27-scorecard-evidence-recipe-defaults-retained.md`.

## 2026-07-27 - Post-consolidation Fasta source-identity invalidation closure

- Exact-source checking caught the retained removal of Fasta Generation's
  extra final blank line. The semantic program was unchanged, but both
  scorecard modes and the two byte-output closures correctly became stale.
- Fresh fixed-CPU measurements retained five verified Go/Python/Ruby samples
  and two independent five-sample Able cohorts. The selected compiled row is
  0.0420 seconds versus Go at 0.0166; bytecode is 1.9580 seconds versus Python
  at 0.2031 and Ruby at 0.3046. Both remain misses.
- Rolled forward only the two affected rows and propagated their exact source
  and frontier identities through every dependent checked report.
- Current status remains 115 selected rows, eight meets, 107 misses, zero
  actionable groups, and 21 current closures with zero invalidations.
- The complete deterministic architecture gate passes in 8.36 seconds at
  164,376 KB peak RSS. No production or WASM code advanced.
- Next wait for a new authoritative invalidation before profiling or mutating
  performance code; a candidate still needs one shared, non-closed owner in
  three unlike applications.
- Record:
  `docs/perf-baselines/2026-07-27-post-consolidation-fasta-source-identity-invalidation-closure.md`.

## 2026-07-27 - Atomic v12 release consolidation

- Explicit maintainer authorization advanced the exact reviewed boundary into
  the one atomic commit containing this entry.
- All 5,056 reviewed paths are included. Exactly 34 deferred WASM paths remain
  outside the commit, with no unrelated or generated-local path admitted.
- The pre-commit audit revalidated 5,046 eligible manifest identities, the
  conceptual combined final manifest, cached whitespace, forbidden paths,
  binary disposition, ignored paths, and exact index membership.
- Current `HEAD` and `HEAD^{tree}` are the authoritative commit and tree
  identities; the handoff reports their exact values.
- Next retain the evidence-gated pause. Reopen performance mutation only when
  an authoritative invalidation identifies a general, non-closed owner across
  at least three unlike applications.
- Record:
  `docs/perf-baselines/2026-07-27-release-consolidation-index-review.md`.

## 2026-07-27 - Backup Dedup application gate and generic-storage closure

- Added `backup_dedup` as the 62nd portable application with equivalent
  Go/Python/Ruby sources and exact six-lane output parity.
- Five verified measurements per lane put compiled Able at 0.0600 seconds
  versus Go at 0.0092, and bytecode at 1.9160 seconds versus Python at 0.2794
  and Ruby at 0.1168.
- The strict 96-package graph is interpreter-free. Compiled profiling keeps
  native byte and integer carriers until runtime-backed generic nominal
  storage, where `ToDynamicI64` accounts for 1,467 KiB and 31,296 allocation
  objects in the representative exact profile.
- Warmed VM profiles and trace evidence show mono-`u8` Array reads,
  inline/native map dispatch, and only already-closed arithmetic, integer,
  stack, and dispatcher owners.
- Retained benchmark and evidence changes only. No compiler, runtime, VM,
  canonical-stdlib, language, dependency, or WASM production code changed.
- The current frontier has 117 selected rows, eight meets, 109 misses,
  `183.850842` seconds of excess, zero actionable groups, and 21 current
  closures.
- Catalog, selection, scorecard evidence, frontier, ledger, architecture,
  JSON, and whitespace gates pass. Cleanup removed about 855 MiB of generated
  and disposable state.
- Next census typed generic-nominal storage reach across strict generated
  applications, including user-defined nominal controls. That determines
  whether one general storage representation can remove this boxing without a
  forbidden named-container rule.
- Record:
  `docs/perf-baselines/2026-07-27-backup-dedup-application-gate.md`.

## 2026-07-27 - Typed generic-nominal storage reach census

- Strictly emitted all 62 catalog applications and verified zero interpreter
  dependencies in their final graphs.
- Added `bench_typed_generic_storage_census` and its deterministic 62-row TSV.
  Direct generic nominal storage has 36 distinct native carrier variants and
  no `runtime.Value` fields.
- Runtime-backed storage reaches 25 applications through two different
  services: 14 HashMap and 14 Channel applications, with three overlaps.
- Three exact Dependency Wave main-phase profiles attribute mean cumulative
  allocation reach of 36,304 objects to map operations and 47,166.33 to
  channel operations. The application-defined `WaveTask` and `WaveResult`
  carriers remain native until those explicit runtime-service boundaries.
- Retained no production change. HashMap hashing/mutation/identity and Channel
  ordering/cancellation/payload lifetime do not form one lowering owner, and
  the corpus has no application-defined generic storage nominal.
- Focused native generic carrier, interface, alias, Map signature, and Channel
  localization guards pass in 3.113 seconds. The census rerun is
  byte-identical.
- Removed the 4.1 GiB disposable `/var/tmp` workspace.
- Next admit a genuine unlike application/domain whose natural design uses
  application-defined generic storage, then measure its source-equivalent
  lanes repeatedly. Its result determines whether the next work belongs in
  general nominal lowering or remains confined to explicit kernel/runtime
  services.
- Record:
  `docs/perf-baselines/2026-07-27-typed-generic-nominal-storage-reach-census.md`.

## 2026-07-27 - Discrete Event Simulation generic-storage application gate

- Added `discrete_event_simulation` as the 63rd portable application with
  equivalent Go/Python/Ruby sources and exact six-lane output parity.
- Its application-defined `EventQueue T` stores `Scheduled T` in an
  Array-backed heap and validates mutation through an alias. It does not use a
  runtime-service container or dynamic execution.
- Three compiled cohorts average 0.05467 seconds versus Go at 0.01395; three
  bytecode cohorts average 4.708 seconds versus Python at 0.1851 and Ruby at
  0.21435.
- Strict generated code uses
  `[]*Scheduled_SimulationEvent`, `*EventQueue_SimulationEvent`, native
  pointers, `int64`, and `int32`. The hot path calls specialized heap methods
  directly, and the final dependency graph contains no interpreter.
- Thirty main-only CPU profiles and three exact allocation profiles expose no
  boxing boundary. Exactly 108,192 of 108,224 main allocations are the
  source-level event objects also present in Go; remaining CPU belongs to the
  heap, Go GC/write barriers, and closed checked arithmetic.
- Warmed bytecode profiles add no storage-specific leaf and remain covered by
  the closed member/call, propagation/type, frame/stack, cache-map, and
  dispatcher families.
- Retained evidence and benchmark coverage only. No production or WASM change
  advanced. The current frontier has 119 selected rows, eight meets, 111
  misses, `188.451579` seconds of excess, zero actionable groups, and 21
  current closures.
- Next maintain the evidence-gated pause. After a genuine source, semantic,
  stdlib, or application invalidation, refresh only the selected closures and
  require one non-closed exact owner in three unlike applications before
  changing production code.
- Record:
  `docs/perf-baselines/2026-07-27-discrete-event-simulation-application-gate.md`.

## 2026-07-27 - Scorecard evidence hermeticity and full correctness gate

- The evidence selector reported zero invalidations across all 21 closures;
  no compiler AOT or stdlib externalization gap was tracked.
- The full gate exposed two Backup Dedup comparison reports whose fresh
  reference paths still named disposable `/var/tmp` files. Their exact
  retained Go and interpreter reports now provide the durable dependencies.
- `bench_scorecard_evidence_check.py` now requires every source and reference
  report of a retained cohort to live under `docs/perf-baselines`. The current
  cohort verifies 119 selected rows, 126 full-status rows, 20 source reports,
  and 33 reference reports. A focused test rejects an external reference even
  while it exists.
- `run_stdlib_tests.sh` now respects explicit `GOCACHE`; validation used
  disk-backed `/var/tmp` without creating `interpreters/go/.gocache`.
- `./run_all_tests.sh` passed all deterministic checks, non-compiler packages,
  33 compiler short-mode batches, and the bytecode fixture pass. The
  canonical stdlib passed in both tree-walker (20s) and bytecode (15s).
- Per-test timing of the three long compiler batches found maxima of 37.97,
  16.05, and 10.95 seconds, so no individual test exceeds one minute.
- Retained no production or WASM change. Removed 66 MiB of tranche artifacts
  from `/var/tmp` and a stale 4.1 MiB extern cache from RAM-backed `/tmp`.
- Next wait for a genuine source, application, semantic, or stdlib
  invalidation; then refresh only affected closures and require one
  non-closed shared owner across three unlike programs before production
  mutation.
- Record:
  `docs/perf-baselines/2026-07-27-scorecard-evidence-hermeticity-correctness-gate.md`.

## 2026-07-27 - Compiler release positional-nominal boundary repair

- The separate compiler release matrix found three compiled fixtures failing
  because generated Ratio and map-spread boundaries assumed named-map struct
  storage after nominal values had moved to shared positional storage.
- Ratio `num`/`den` reads and both direct/IR map-spread `handle` reads now use
  `__able_struct_named_field_value`. This is the existing general
  representation boundary and preserves native positional carriers.
- Structural regression tests forbid the former direct `Fields` reads. The
  focused guards pass in 1.060 seconds, the three repaired fixtures in 2.417
  seconds, and `cmd/ablec` in 5.751 seconds.
- All 24 shards of each full fallback, execution, strict-dispatch,
  interface-lookup, and boundary-marker audit pass. The former
  168.531-second fallback monolith is now a complete 24-shard release audit;
  every supported shard completed below one minute.
- `./run_all_tests.sh --compiled-cli` passed. Its complete `cmd/able`
  canonical-stdlib aggregate took 1260.300 seconds and remained CPU-active
  throughout.
- Retained no runtime, interpreter, bytecode VM, language, canonical-stdlib,
  dependency, performance candidate, or WASM change.
- The current scorecard evidence remains complete, while the checked ledger
  conservatively selects 12 compiled closures for compiler-scope drift. Nine
  bytecode-only closures remain current.
- Next strictly compile all 63 portable applications and classify actual calls
  to the corrected paths separately from emitted helper definitions, then
  refresh every reached compiled row with at least five verifier-backed
  Able/Go processes before advancing closure snapshots. This preserves the
  evidence contract without rerunning unrelated closed work.
- Record:
  `docs/perf-baselines/2026-07-27-compiler-release-positional-nominal-boundary-repair.md`.

## 2026-07-27 - Compiler positional-boundary zero-reach closure

- Strictly emitted all 63 portable applications with the catalog-selected
  source policy and canonical stdlib, then linked and inspected every final
  graph. All 63 omit `pkg/interpreter`.
- Added `bench_compiler_positional_boundary_reach_census`; its two 63-row runs
  were byte-identical with SHA-256
  `72ee4aa385e6d07d5087bd57f4783ebbd5a2b77231317f7523caa8b2341d66c7`.
- Forty-one linked binaries retain the Ratio helper through shared numeric
  paths. Go-overlay sentinel binaries ran with catalog setup, arguments,
  executor, working directory, and public verifier; 41/41 passed with zero
  corrected field-read hits.
- The original Ratio failure fixture hit the exact sentinel and panicked,
  proving the probe. Twenty-two applications do not retain the helper, and no
  application emits or links map-spread code.
- The changed-path application set is empty, so no new timing cohort was
  eligible. Existing five-run scorecard evidence measures unchanged execution
  and no performance claim was made.
- The evidence bootstrap changed only the compiler-production scope hash from
  `cc6df4aa...35a5` to `30e71f61...b13d`. No closure record, input, selection,
  timing, or disposition changed. All 21 closures are current again.
- Cleanup removed 5.6 GiB of generated/sentinel artifacts and a 4.9 GiB
  reproducible Go cache; no RAM-backed Able temporary path remains.
- Retained no further production or WASM change.
- Next inventory and classify all remaining generated direct
  `StructInstanceValue.Fields` reads. Migrate only unsafe semantic reads to the
  shared accessor, with positive controls and full compiler release matrices;
  retain no code if the census finds no unsafe read.
- Record:
  `docs/perf-baselines/2026-07-27-compiler-positional-boundary-zero-reach-closure.md`.

## 2026-07-27 - Compiler generated struct-field read audit retained

- Audited all 85 textual compiler `Fields[...]` occurrences and routed 14
  semantic reads plus both runtime/IR functional-update copies through the
  shared positional-aware named-field accessor.
- The remaining 71 occurrences close as 53 writes, 14 compile-time metadata
  reads, two shared-accessor implementations, and two same-map enumeration
  reads. There are no unclassified semantic field reads.
- Positive guards cover String bytes, Array handles, generic and callable
  member access, FutureError details, generated-main Array support, and
  runtime/IR functional updates. Map-backed synthetic Awaitable behavior and
  positional bridge controls pass.
- Compiler bridge, all 32 core batches, three outliers, and all 120 fallback,
  execution, strict-dispatch, interface-lookup, and boundary-marker release
  shards pass. `cmd/ablec` passed in 5.830 seconds.
- No boxing, interpreter fallback, named-container lowering branch, runtime,
  interpreter, bytecode VM, canonical stdlib, language, dependency,
  application, or WASM change was introduced.
- The compiler scope now invalidates 12 compiled-related performance closures;
  nine bytecode-only closures remain current. No timing claim or evidence
  rebase was made.
- Cleanup removed the exact 23 GiB `/var/tmp` release workspace and confirmed
  no `/tmp/able-*` directory remains.
- Next run a positive-controlled 63-application reach census for each migrated
  branch and collect five-or-more balanced baseline/candidate/Go runs for every
  reached application. This preserves the Go-performance goal while closing
  the correctness boundary generically.
- Record:
  `docs/perf-baselines/2026-07-27-compiler-generated-struct-field-read-audit-retained.md`.

## 2026-07-27 - Compiler struct-field reach closure

- Added strict overlay instrumentation for eleven generated/runtime
  semantic-read families and ran all 63 catalog applications with their real
  setup, arguments, executor policy, working directory, and public verifier.
- All 63 final graphs omit `pkg/interpreter` and verify. Only
  `callable_member_get` reached in four applications and `awaitable_shape`
  reached in three; the other nine families had zero application reach.
- Reconstructed the old direct-map behavior for the reached branches and ran
  21 rotating baseline/candidate/equivalent-Go cohorts per application. All
  252 processes verified, and every paired 95% interval included zero.
- A general fast-map/cold-positional helper split reduced Go's inliner score
  from 127 to 98 but did not clear the 80-unit budget. It was rejected and
  removed; the retained code is exactly the reviewed correctness repair.
- The ledger bootstrap changes only the compiler-production scope hash. Its
  check and seven-test guard pass with 21 current closures and zero
  invalidations.
- Removed 12 GiB and 704 MiB disk-backed workspaces and generated Python
  caches; no RAM-backed Able temporary directory remains.
- Next profile `await_channel_mux`, `mutex_await_journal`, and
  `mutex_work_queue` for CPU, allocations, `runtime.Value` materializations,
  dynamic calls, and bridge transitions. The work is important because these
  unlike static applications remain 51x-222x slower than Go; advance only one
  exact shared non-closed mechanism with repeated verifier-backed A/B evidence.
- Record:
  `docs/perf-baselines/2026-07-27-compiler-struct-field-reach-closure.md`.

## 2026-07-27 - Compiled concurrency boundary-owner closure

- Collected five ordinary compiled CPU profiles, three main-allocation stats,
  one sampled allocation profile, five exact `currentGID` counters, five typed
  counters, and five dynamic counters for each of `await_channel_mux`,
  `mutex_await_journal`, and `mutex_work_queue`.
- Every strict binary remains interpreter-free and every verifier passed.
  `bridge.currentGID`/`runtime.Stack` owns 85.47%, 91.08%, and 91.36% of
  merged CPU samples; exact lookup means are 23,555, 27,879.8, and 63,014.4.
- Equivalent Go allocation counts are lower by factors of 47.17, 4,875.21,
  and 12,375.39. Allocation leaves outside `currentGID` are not a shared
  material CPU owner across all three applications.
- Profiles under the existing opt-in fixed-context mode leave the owner at
  87.23%-92.94%. Its static/native-interface context does not cross generated
  runtime-callable or captured-lambda entries, so a real excision requires the
  broader callable context ABI excluded from this tranche.
- The final evidence covers 120 verifier-backed processes with zero failures
  and zero timeouts. Added debug-only `currentGID` overlay and Go-reference
  phase-profile generators; corrected typed-boundary report rendering for
  nested `telemetry.categories`.
- Removed the exact 1.9 GiB disk-backed workspace and generated Python cache;
  no RAM-backed `/tmp/able-*` directory remains.
- No compiler/runtime production, interpreter, bytecode VM, canonical stdlib,
  language, dependency, benchmark-source, or WASM change advanced.
- Next retain the pause unless the maintainer authorizes a general
  allocation-free native-callable context design with captured/cross-package,
  dynamic compatibility, the three current applications, earlier
  packet/audio/scene/graph controls, and unrelated serial guards including
  N-body. This matters because the owner is dominant, but the previous broad
  default-context experiment caused a verified 54.7% N-body regression.
- Record:
  `docs/perf-baselines/2026-07-27-compiled-concurrency-boundary-owner-closure.md`.

## 2026-07-27 - Compiled await callable execution context retained

- Carried the opt-in generated execution context through native callable
  types, captured lambdas, bound method values, runtime-callable adapters,
  compiled await helpers, and native Awaitable callbacks. Known-environment
  Await definitions now avoid goroutine-based environment rediscovery.
- A general entry-package `await` reach gate prevents emission of the new
  callable ABI in await-free applications. Default builds and dynamic
  compatibility paths preserve their prior contracts.
- Five verifier-backed runs per Able side and equivalent Go application
  measured 0.2880 -> 0.1240 seconds for Await Channel Mux, 0.3480 -> 0.0360
  for Mutex Await Journal, and 0.8560 -> 0.0420 for Mutex Work Queue. The
  reductions are 56.94%, 89.66%, and 95.09%.
- Exact `currentGID` means fell from 23,555.0, 27,879.8, and 63,014.4 to
  8,707.0, 97.7, and 206.0. Allocation bytes fell 3.71%, 31.49%, and 31.18%;
  allocation objects fell 2.36%, 29.67%, and 28.90%.
- All target graphs remain interpreter-free. Await-free Packet, Audio, Scene,
  Graph, and N-body controls omit the new path. Focused callable, captured,
  cross-package, bound-method, dynamic compatibility, fixture-parity, public
  Mutex, bridge, and `cmd/ablec` tests pass.
- The checked ledger selects 12 compiled-related closures for
  `compiler-production` drift and leaves nine bytecode-only closures current.
  The partial experimental cohort was not used to rebase unrelated evidence.
- Retained the general design behind `ExperimentalExecutionContext`; it
  remains unsuitable for default rollout because the target applications are
  still 9.00x-25.31x slower than Go. No stdlib, interpreter, bytecode,
  language, dependency, named-container/non-primitive nominal, or WASM change
  was made.
- Next complete a 63-application default/experimental reach census, resolve
  only the selected closures, then collect residual profiles across unlike
  await-bearing applications. This is important to restore current evidence
  before advancing another shared compiled owner.
- Record:
  `docs/perf-baselines/2026-07-27-compiled-await-callable-context-retained.md`.

## 2026-07-27 - Callable-context reach and native-call view retained

- Added `bench_compiler_callable_context_reach_census` and its diagnostic-only
  overlay. The two-mode 63-application pass produced 126 interpreter-free,
  verifier-correct rows with identical default/experimental output hashes.
- Only Future Await Race, Await Channel Mux, Mutex Await Journal, and Mutex
  Work Queue emit and execute the context-aware callable ABI. Final execution
  counts are 954/192, 9,216/1,024, 8,549/2,048, and 17,200/4,096
  callable/await calls.
- Future Await Race completed the missing five-run A/B: 0.0800 -> 0.0360
  seconds (-55.00%) versus fresh Go at 0.0046 seconds.
- Context-aware native calls now reuse a `runtime.NativeCallContext` embedded
  in the already-required execution context. The rule is behind the existing
  experimental-plus-entry-`await` gate.
- Five exact allocation runs per side reduced objects 5.40%-8.24% and bytes
  0.22%-1.90% across all four reached applications. Fifteen rotating A/B pairs
  per application found wall time neutral at 95% confidence.
- A final 63-application default pass exactly matched every prior generated
  source, binary, and stdout hash; all graphs and verifiers remained green.
  The ledger scope-only bootstrap restored 21 current closures and zero
  invalidations; all seven ledger tests pass.
- Focused callable/captured/cross-package/native-interface/bound-method,
  fixture parity, dynamic compatibility, public Mutex, bridge, and `cmd/ablec`
  gates pass.
- Residual profiles leave `currentGID` at 85.00%, 64.77%, 38.46%, and 14.71%
  cumulative CPU. No additional owner was both exact and material across at
  least three programs.
- No canonical stdlib, runtime interpreter, bytecode, language, dependency,
  named-container/non-primitive nominal, or WASM change was made.
- Next instrument exact branches inside context-aware method/callable dispatch
  and attribute residual environment lookups. This is important to find one
  broad native-lowering target rather than optimize the two remaining
  lookup-heavy applications alone.
- Record:
  `docs/perf-baselines/2026-07-27-compiled-callable-context-reach-and-native-view-retained.md`.

## 2026-07-28 - Compiled callable-dispatch branch closure

- Added a diagnostic-only generated-code/bridge overlay for exact callable
  branch counters, reached native-bound names/borrowing state, and complete
  residual `currentGID` caller attribution.
- Five public-verifier runs per reached application identify context
  native-bound method lookup/receiver injection as the only large branch
  shared by all four: means are 902.4, 8,704.0, 6,472.8, and 13,110.6 calls.
  Context compatibility/default, environment swap, lookup/error control, nil
  result, and node-aware branches are all zero.
- The only residual environment-lookup caller present in all four is the
  legacy Await waker, at 108.8, 512.0, 116.6, and 292.8 mean calls. It is not a
  material shared owner.
- Tested safe `BorrowArgs` receiver-prefix scratch with four slots and one
  slot. Both forms reduced objects in three applications, but every
  application allocated more bytes; the one-slot result was +0.26%-1.89%
  bytes and 0.00% to -6.82% objects. Escape diagnostics show that the indirect
  native call transfers borrowed-storage escape cost to its owner.
- Rejected and removed both production candidates before wall/Go-reference
  timing. Final generated source exactly matches the pre-experiment source for
  all four applications; strict graphs remain interpreter-free and every
  verifier passes.
- Retained only `bench_compiler_callable_dispatch_instrument.py` and the dated
  evidence. No compiler/runtime execution, interpreter, bytecode VM, stdlib,
  language, dependency, named-container/non-primitive nominal, or WASM change
  advanced.
- Removed the exact 2.6 GiB disk-backed workspace and generated Python cache;
  the unrelated pre-existing `/tmp/able-v12-extern-go` directory was preserved.
- Next prototype a diagnostic-only compiler-owned split-receiver bound value
  that preserves the existing general direct method entry. This matters
  because it can avoid receiver-slice construction rather than moving the
  escape, while keeping nominal method lowering generic.
- Record:
  `docs/perf-baselines/2026-07-28-compiled-callable-dispatch-branch-closure.md`.

## 2026-07-28 - Split-receiver compiled-entry route closed at zero reach

- Added exact diagnostic counters for successful native-bound lookups with
  and without an existing compiled direct entry.
- Five verifier-backed processes per reached application found no direct
  entry: 0.0 of 912.6 Future Await Race, 8,704.0 Await Channel Mux, 6,511.8
  Mutex Await Journal, and 12,977.4 Mutex Work Queue mean lookups.
- The reached methods are Future built-ins and generated closure-owned
  Awaitable, AwaitRegistration, and AwaitWaker kernel callbacks. Ordinary
  compiled method direct entries exist in the emitted registrars but are not
  on this hot branch.
- The proposed split-receiver direct-entry carrier was closed before
  prototype, allocation, and timing work because it has zero measured reach.
  No production execution, interpreter, bytecode VM, stdlib, language,
  dependency, nominal special case, or WASM change advanced.
- All 20 executions passed their public verifier and every strict graph
  remains interpreter-free.
- Next test receiver-free native-callable storage for the closure-owned kernel
  methods behind the existing experimental gate. This matters because it
  targets the actual common injection owner without a nominal-type rule.
- Record:
  `docs/perf-baselines/2026-07-28-compiled-split-receiver-direct-entry-zero-reach.md`.

## 2026-07-28 - Closure-owned kernel callables retained receiver-free

- Under the experimental callable gate, generated Awaitable,
  AwaitRegistration, and AwaitWaker closures are stored directly as
  `runtime.NativeFunctionValue`; their already-captured service receiver is no
  longer injected through a new `[]runtime.Value`.
- Five main-phase allocation A/B processes per side reduced bytes
  1.64%-5.32% and objects 1.96%-7.54% across all four reached applications.
- Fifteen balanced baseline/candidate/Go cohorts found lower raw means in all
  four but neutral paired intervals. The remaining Go time ratios are
  3.15x-18.38x.
- Production output exactly matches the candidate overlay. Default
  await-bearing and experimental await-free source identity is preserved;
  focused capture/interface/dynamic/error/concurrency tests and all public
  verifiers pass; every graph remains interpreter-free.
- No stdlib, interpreter, bytecode VM, language, dependency, nominal special
  case, or WASM change was made.
- Next refresh residual branch, CPU, allocation, and goroutine-ID attribution.
  This matters because the prior residual profile still charges the
  native-bound branch that this rule moved to native functions.
- Record:
  `docs/perf-baselines/2026-07-28-compiled-closure-owned-kernel-callables-retained.md`.

## 2026-07-28 - Native-interface runtime type metadata cache retained

- Post-representation profiling across four unlike concurrent applications
  closed remaining native-bound and goroutine-environment work as a shared
  optimization owner.
- Exact allocation attribution identified repeated generated
  `ast.Gen(ast.Ty(...))` construction in native-interface recovery. Boundary
  helpers now keep one immutable runtime type expression per interface
  specialization and reuse it for semantic `bridge.MatchType` checks.
- Five rotating allocation runs per side reduced main bytes/objects by
  15.05%/14.54% for Await Channel Mux, 14.61%/14.33% for Mutex Await Journal,
  and 14.26%/13.89% for Mutex Work Queue. Future Await Race changed only
  -0.15%/-0.23% as the low-reach control.
- Fifteen baseline/candidate/Go cohorts per application were wall-neutral at
  95% confidence. All retained executions verified and all dependency graphs
  omit `pkg/interpreter`.
- The new generated-source guard, compiled CLI, bridge, and focused
  interpreter concurrency suites pass. The full compiler package reached its
  ten-minute aggregate timeout; its in-progress fallback fixture passed alone
  in 167.402 seconds.
- No canonical stdlib, interpreter, bytecode VM, language, dependency,
  nominal special case, or WASM change was made.
- Next instrument Awaitable `toStruct`, waker construction, and
  native-interface/runtime conversion consumers through Array/await
  selection. It is important because those are the next shared allocation
  owners and may permit typed Go carriers to survive until an actual dynamic
  boundary.
- Record:
  `docs/perf-baselines/2026-07-28-compiled-native-interface-runtime-type-metadata-cache-retained.md`.

## 2026-07-28 - Native Awaitable carrier retained

- Compiled channel, mutex, timer, and default Awaitables now implement the
  general kernel `runtime.NativeAwaitableValue` carrier. The runtime struct is
  created only for a genuine dynamic or interpreter consumer.
- Five exact runs apiece moved 2,048/2,048/4,096 eager materializations to
  zero across Await Channel Mux, Mutex Await Journal, and Mutex Work Queue.
- Five allocation runs per side reduced bytes 22.38%-35.56% and objects
  21.98%-36.92%.
- Fifteen balanced baseline/candidate/Go cohorts improved wall time
  9.29%-18.14%; all paired 95% intervals exclude zero. Every public verifier
  passed and every strict graph remains interpreter-free.
- Explicit Awaitable method calls, both execution-context modes, repeated
  mutex contention, focused native-interface/bridge/runtime/interpreter
  concurrency guards, and `cmd/ablec` pass.
- The scope-only ledger bootstrap and seven-test guard leave 21 current
  closures and zero invalidations.
- No stdlib, interpreter semantics, bytecode VM, language, dependency,
  nominal special case, application, or WASM change was made.
- Next trace and gate the remaining native Awaitable Array-to-runtime and
  per-element interface conversions before compiled await selection.
- Record:
  `docs/perf-baselines/2026-07-28-compiled-native-awaitable-carrier-retained.md`.

## 2026-07-28 - Typed Awaitable Array input rejected

- A general compiled `await` prototype consumed static native
  `Array<Awaitable<T>>` values directly and left arbitrary/dynamic Iterable
  inputs on the established runtime path.
- Exact counters moved 1,024/2,048/4,096 runtime Array conversions to the same
  number of native Array collections in Await Channel Mux, Mutex Await
  Journal, and Mutex Work Queue. Element interface conversions and Awaitable
  protocol calls remained unchanged.
- Five allocation processes per side improved bytes by 2.12%-7.97% and objects
  by 1.82%-8.62%.
- Fifteen balanced baseline/candidate/Go cohorts changed raw wall time by
  -0.38%, +4.67%, and +1.37%; all paired intervals crossed zero. The
  wrapper-only candidate therefore failed the broad wall-time retention bar.
- Every selected measurement verified, candidate/baseline dependency graphs
  remained interpreter-free, and focused semantic guards passed.
- Removed all production prototype code and its temporary source guard.
  Retained the reusable diagnostic extension and decision record only. No
  stdlib, interpreter, bytecode VM, language, dependency, non-primitive
  nominal, application, or WASM change was retained.
- Removed the exact 2.1 GiB disk-backed workspace without touching unrelated
  temporary directories.
- Next attribute and test the remaining per-element Awaitable
  interface-to-runtime crossing through a general typed arm abstraction. This
  is important because keeping the protocol calls native, not merely removing
  their Array wrapper, is the next plausible full boundary removal.
- Record:
  `docs/perf-baselines/2026-07-28-compiled-native-awaitable-array-round-trip-rejected.md`.

## 2026-07-28 - Native Awaitable protocol arms rejected

- A general compiled `await` prototype kept static
  `Array<Awaitable<T>>` arms on native carriers and directly invoked the
  native Awaitable protocol while retaining dynamic Iterable/runtime
  fallbacks.
- Three exact counter runs per side moved 1,024/2,048/4,096 runtime Array
  conversions, 2,560/2,048/4,096 interface-to-runtime conversions, and every
  dynamic protocol call to zero across the three reached applications.
- Five main-allocation runs per side improved bytes by 2.33%-7.72% and
  objects by 5.01%-13.60%. Escape diagnostics showed one persisted value
  slice in place of escaping per-arm state pointers.
- Fifteen balanced baseline/candidate/Go cohorts changed raw wall time by
  -0.45%, -7.57%, and +1.28%; all paired intervals crossed zero. The complete
  protocol candidate therefore failed the broad retention bar.
- All selected executions verified and strict graphs remained
  interpreter-free. After removal, all three regenerated sources exactly
  match baseline and pass again.
- Retained only the layout-compatible diagnostic overlay and decision record.
  No stdlib, interpreter, bytecode VM, language, dependency, non-primitive
  nominal, application, or WASM change was retained.
- Next reprofile retained code and count shared Await waker/registration
  construction before prototyping. This is important because measured current
  ownership, not another boundary-removal hypothesis, must drive the next
  attempt.
- Record:
  `docs/perf-baselines/2026-07-28-compiled-native-awaitable-protocol-arms-rejected.md`.

## 2026-07-28 - Native Await service carriers retained

- Reprofiled Await Channel Mux, Mutex Await Journal, and Mutex Work Queue from
  fresh strict experimental binaries. Five exact counter runs found
  1,024/2,048/4,096 Await wakers and 1,024/50-82/198-214 registrations per
  application run.
- Allocation profiles attributed about five objects to each map-backed
  `AwaitWaker` and `AwaitRegistration` construction. Generated Await now keeps
  one lazy native carrier instead and materializes the semantic struct only for
  a dynamic user Awaitable or explicit protocol consumer.
- Five main-allocation A/B runs per side reduced bytes by
  16.05%/36.22%/34.86% and objects by 9.55%/18.28%/17.24%.
- Fifteen balanced baseline/candidate/Go cohorts improved raw wall means by
  1.39%/26.66%/23.80%. Journal and Queue have clear paired improvements;
  Channel is neutral within noise. Remaining Go ratios are
  25.63x/3.28x/5.35x.
- User-defined Awaitable wake materialization, explicit registration
  cancellation, representation gating, source identity, strict dependency
  graphs, public verifiers, focused concurrency/context/dynamic tests, full
  experimental fixture parity, bridge, and `cmd/ablec` all pass.
- Updated the performance ledger only from compiler-production tree hash
  `2c82a4f6...dce45` to `a79251ac...1c4074`; 21 closures are current and zero
  invalidated.
- No canonical stdlib, runtime package, interpreter, bytecode VM, language,
  dependency, nominal special case, benchmark source, or WASM change was
  required.
- Next split exact task-creation versus native-call reverse construction in
  `__able_context_from_environment` and `__able_context_from_native`. This is
  important because those allocations are now the largest generated owner
  shared by all three and may admit a narrow general reverse-view reuse.
- Record:
  `docs/perf-baselines/2026-07-28-compiled-native-await-service-carriers-retained.md`.

## 2026-07-28 - Native reverse-context reuse retained

- Five exact construction runs per application showed that compiled native
  callbacks rebuilt 3,073 contexts in Await Channel Mux, roughly 6,300 in
  Mutex Await Journal, and roughly 12,700 in Mutex Work Queue, versus
  1,536/4/4 semantically necessary task contexts.
- Each experimental callable task payload now owns its primary compiled
  execution context. Native reverse adaptation reuses it only when environment
  identity matches and otherwise preserves the established fallback.
- The retained path reuses every eligible context and leaves one main-entry
  fallback per run. Main-delta profiles independently remove the same
  3,072/6,000-6,500/12,000-13,000 object owners.
- Five allocation runs per side improve bytes by
  2.62%/17.34%/16.78% and objects by 3.01%/17.23%/16.48%.
- Fifteen balanced cohorts change raw time by +0.75%/-8.58%/-8.62%.
  Journal improves significantly; Channel and Queue are neutral at 95%
  confidence. Remaining Go ratios are 22.45x/2.93x/4.67x.
- Full execution-context fixture parity, focused package/capture/dynamic and
  nested-spawn guards, all three race-enabled applications, public verifiers,
  strict graphs, output-identity checks, bridge, runtime, and compiled CLI
  pass.
- The compiler-production scope hash advances from `a79251ac...1c4074` to
  `af726275...f6b1d`; all seven ledger tests pass with 21 current closures and
  zero invalidations.
- The field and reuse code emit only under the reached callable-context gate,
  so default builds and experimental await-free N-body remain byte-identical.
- No canonical stdlib, runtime package, interpreter, bytecode VM, language,
  dependency, nominal special case, benchmark source, or WASM change was made.
- Next count `ensureWaitCh`, wake signals, and Await rearm cycles, then test a
  task-owned reusable signal only if overlapping generated waits are absent.
- Record:
  `docs/perf-baselines/2026-07-28-compiled-native-reverse-context-reuse-retained.md`.

## 2026-07-28 - Task-owned compiled Await signal retained

- Fixed generated Future registration for the lazy native Await waker before
  taking the performance baseline; Future Await Race is correct again.
- Exact lifecycle counters prove one active generated Await state per task
  across four unlike applications and both executors. The experimental
  compiled payload now owns one reusable zero-sized wake channel and no longer
  allocates a redundant AST-expression state map.
- Five allocation runs per side improve bytes
  3.63%/3.53%/15.34%/14.41% and objects
  2.07%/1.85%/6.65%/6.29% for Channel Mux, Future Race, Journal, and Queue.
- Fifteen balanced cohorts make Channel Mux and Journal significantly faster;
  Future Race and Work Queue are neutral. Remaining candidate/Go ratios are
  15.24x/3.43x/1.91x/2.89x.
- Late-waker publication and cleanup are mutex-serialized and drain a losing
  token before reuse. Dynamic user Awaitables, serial/goroutine behavior,
  races, full fixture parity, focused boundaries, default byte identity,
  strict dependency graphs, public verification, bridge/runtime/CLI, and the
  21-closure ledger pass.
- A tagged pointer-channel design was rejected because its buffer allocated
  one additional object per task and failed the broad gate.
- No stdlib, runtime package, interpreter, VM, language, dependency, nominal
  special case, application, or WASM change was required.
- Next capture fresh post-retention CPU and exact allocation profiles and
  select only a remaining Await state/arm/registration owner shared by at
  least three unlike programs. This matters because the previous profiles are
  now structurally stale.
- Record:
  `docs/perf-baselines/2026-07-28-compiled-task-owned-await-signal-retained.md`.

## 2026-07-28 - Task-owned compiled Await state retained

- Fresh post-signal profiles selected generated Await-state construction as
  the largest open allocation owner shared by Await Channel Mux, Future Await
  Race, Mutex Await Journal, and Mutex Work Queue.
- The experimental task payload now retains one primary Await state and its
  wake channel. Sequential Await expressions reuse it; active nested Await
  falls back to a transient state; current-waker identity rejects stale wakes.
- Exact state objects fall 192→96, 2,048→4, and 4,096→4 in Future Race,
  Journal, and Queue. Channel Mux remains the neutral 1,024→1,024 control.
- Five main-allocation runs per side reduce Future bytes 1.02%, Journal
  bytes/objects 12.73%/6.99%, and Queue bytes/objects 11.76%/6.59%; Channel is
  neutral. Fifteen balanced timing cohorts show no significant regression.
- All four strict applications pass three serial and three goroutine verifier
  runs plus a goroutine race build, and omit `pkg/interpreter`. Full
  experimental fixture parity passes in 44.079 seconds; focused Await guards,
  default/measured source identity, bridge, runtime, CLI, and ledger checks
  pass.
- Removed an intermediate acquire/release locking form after it regressed the
  one-state-per-task control. Only shared wake/waker state remains locked.
- No canonical stdlib, runtime package, interpreter, bytecode VM, language,
  dependency, nominal special case, application, or WASM change was required.
- Next prototype task-owned arm-scratch reuse only after cleanup and reference
  clearing, with nested fallback and broad A/B gates. This matters because the
  remaining collector allocates 3,584/384/4,096/8,192 slice-and-arm objects,
  while the typed protocol-arm route is already rejected.
- Record:
  `docs/perf-baselines/2026-07-28-compiled-task-owned-await-state-retained.md`.

## 2026-07-28 - Task-owned compiled Await arm scratch retained

- The experimental primary Await state now retains arm-slice capacity and arm
  records, clears their references after cleanup, and reuses them only for a
  later sequential Await. Nested collection/commit Await uses transient
  storage.
- Exact arm-storage objects fall 384→192, 4,096→8, and 8,192→8 in Future
  Race, Journal, and Queue. Channel Mux remains the neutral 3,584→3,584
  control.
- Five allocation A/B runs significantly improve Future bytes and both
  Journal/Queue measures; Journal/Queue objects fall 15.06%/13.87%. Fifteen
  balanced Go timing cohorts are neutral in all four applications.
- A separate activity boolean was removed after increasing control bytes by
  0.28%. The final pending-waker sentinel uses no additional state field.
- Race-enabled nested and stale-waker tests, all four race applications,
  repeated dual-executor verifiers, full fixture parity, focused guards,
  source identity, interpreter-free graphs, bridge/runtime/CLI, vet, and the
  21-closure ledger pass.
- No canonical stdlib, runtime package, interpreter, bytecode VM, language,
  dependency, nominal special case, application, or WASM change was required.
- Next audit whether native-only wakers can split a task endpoint from an
  immutable per-Await generation token while exposed wakers remain distinct.
  This matters because one native waker allocation remains per Await in all
  four applications and naive reuse is semantically unsafe.
- Record:
  `docs/perf-baselines/2026-07-28-compiled-task-owned-await-arm-scratch-retained.md`.

## 2026-07-28 - Native Await service materialization cache retained

- Generated native Await wakers and registrations now publish their lazy
  runtime struct through one atomic pointer instead of a pointer plus dormant
  `sync.Once`; unique per-Await identity is unchanged.
- Exact waker storage falls 25% in Channel Mux, Future Race, Journal, and
  Queue. Five allocation A/B runs significantly reduce bytes in three;
  Future and all object-count results are neutral.
- Thirty balanced baseline/retained/equivalent-Go cohorts are statistically
  neutral in all four applications. Retained performance remains only
  4.12%-38.92% of Go.
- Direct endpoint reuse is closed because cancellation cannot prove that an
  old native or dynamic waker invocation has finished. An immutable generation
  token preserves safety but also preserves the per-Await allocation.
- Repeated dual-executor verifiers, four race applications, race-enabled
  nested/stale/materialization guards, full fixture parity, focused Await
  guards, default identity, interpreter-free graphs, bridge/runtime/CLI, vet,
  and the 21-closure ledger pass.
- No canonical stdlib, runtime package, interpreter, bytecode VM, language,
  dependency, nominal special case, application, or WASM change was required.
- Next refresh post-retention CPU and exact allocation ownership, excluding
  the required unique waker and already-closed typed Awaitable conversion
  routes. This is important because the next candidate must be a new shared
  compiled/runtime boundary, not a reopened unsafe or rejected route.
- Record:
  `docs/perf-baselines/2026-07-28-compiled-native-await-service-materialization-cache-retained.md`.

## 2026-07-28 - Compiled Await post-materialization owner closure

- Fresh strict builds of Await Channel Mux, Future Await Race, Mutex Await
  Journal, and Mutex Work Queue remain fallback-free and interpreter-free.
- Twenty-five measured-main CPU profiles and three exact allocation profiles
  per application were public-verifier backed.
- Retained no production change. Distinct waker identity is required; typed
  Awaitable Array/protocol conversion, ordinary `bridge.ToInt` boxing, and
  reverse-context `currentGID` are already closed; mutex/channel owners do not
  reach three unlike applications.
- Exact allocation means are 5,175,597 bytes/96,765 objects for Channel,
  681,728/10,979 for Future, 965,867/22,784 for Journal, and
  2,117,917/49,520 for Queue.
- The global `i64` cache remains rejected by its earlier TapeLang broad guard;
  the retained dynamic-boundary-only helper is unchanged.
- All 112 profile outputs verify, focused guards and dependency checks pass,
  and the performance ledger remains current at compiler-production hash
  `8d69533a...e1f7433`.
- No compiler, runtime, stdlib, interpreter, VM, language, application,
  dependency, nominal-special-case, or WASM production change was made.
- Next refresh the broad strict compiled scorecard and owner ranking outside
  Await. This matters because Await's remaining shared leaves are closed while
  the 95%-of-Go goal remains unmet.
- Record:
  `docs/perf-baselines/2026-07-28-compiled-await-post-materialization-owner-closure.md`.

## 2026-07-28 - Current strict compiled scorecard and owner closure

- Rebuilt all 63 current compiled applications with `--no-fallbacks` and the
  experimental execution context. All 315 Able and 315 fresh equivalent-Go
  executions passed their public verifiers; all 63 dependency graphs omit
  `pkg/interpreter`.
- The current compiled scorecard has 9 target passes and 54 misses. Its
  geometric-mean Able/Go ratio is 5.2579x, its median is 6.9231x, and positive
  target excess totals 5.4254 seconds.
- Captured 15 measured-main CPU profiles and three exact allocation snapshots
  for each of ten poorly explained misses. All 180 outputs match the
  scorecard's verifier-backed output hashes.
- Retained no production change. No open exact owner is material in three
  unlike applications. Repeated leaves are closed checked arithmetic or
  boxing, same-family UTF-8/regex work, required map/nominal/concurrency
  semantics, or already-native application and Go runtime work.
- Preserved a compact 63-row sample/source/verifier TSV, dependency and output
  audits, three-run allocation summaries, machine-readable classification,
  and 40 representative profile tops. The compiler-production ledger remains
  unchanged at `8d69533a...e1f7433`.
- Deleted the exact 5.8 GiB disk-backed build/profile workspace and its
  pointer after verifying the retained hashes.
- No compiler, generated-runtime, runtime, stdlib, interpreter, bytecode VM,
  benchmark, language, dependency, nominal-special-case, or WASM production
  source changed.
- Next audit semantic-work and generated-assembly equivalence for TapeLang,
  Sudoku, and N-Body with Matrix Multiply and Pidigits controls. This is
  important because the sustained unexplained gaps are now inside native
  generated bodies, so the next admissible rule must remove proof-backed
  redundant generated work rather than reopen a closed boundary.
- Record:
  `docs/perf-baselines/2026-07-28-current-strict-compiled-scorecard-owner-closure.md`.

## 2026-07-28 - Compiled semantic-work recommendation reconciled

- Found that the proposed TapeLang/Sudoku/N-Body semantic-work tranche and
  its normalization, generated-owner, relational-proof, and frontier
  follow-ups were already completed on 2026-07-26.
- Rebuilt TapeLang, Sudoku, N-Body, Matrix Multiply, and Pidigits with the
  current strict experimental compiler. All five public smoke verifiers pass
  and all five final dependency graphs omit `pkg/interpreter`.
- TapeLang and Sudoku Able/Go source hashes exactly match the normalized
  audit. Current assembly retains every prior hot call and jump count; the
  execution-context ABI adds only 0-7 straight-line instructions to the
  compared functions.
- The current relational observer reproduces 0/13 safe reachable checks for
  TapeLang, 4/32 for Sudoku, and 32/52 for N-Body. Matrix proves 0/8 and
  Pidigits 0/2. N-Body remains the only dynamically material fixed-length
  proof family.
- Retained no production change and entered no A/B gate. Preserved compact
  source/artifact, control, hot-function, and assembly TSV evidence.
- Focused control-census and `ablec` tests, the 119-row selection contract,
  and all 21 evidence-ledger closures pass.
- Deleted the exact 469 MiB disk-backed artifact workspace and its pointer
  after verifying the compact evidence.
- Corrected `PLAN.md` to remove the stale recommendation. Next run current
  correctness and release-readiness gates, reopening performance mutation
  only after a concrete admission invalidation. This is important because
  another local performance experiment would target one application,
  aggregate unrelated work, or weaken required semantics.
- Record:
  `docs/perf-baselines/2026-07-28-compiled-semantic-work-recommendation-reconciliation.md`.

## 2026-07-28 - V12 correctness and release-readiness closure

- Propagated the current closure-ledger and generated-runtime identities
  through the stale structural-strategy, portable-backend, semantic-ABI, and
  closed-region decision chain. All decisions remain unchanged.
- Vet/build, the complete ordinary handoff, the separate compiler release
  matrix, cold and warm generated-Go CLI lanes, and canonical stdlib parity
  pass.
- All 120 generated-program release shards pass. Exact event timing found a
  34.94-second compiler maximum and 31.09-second compiled-CLI maximum; no
  individual test exceeds one minute.
- Three-run means disproved two noisy apparent shard violations:
  interface-lookup shard 8 averaged 19.63 seconds and boundary shard 22
  averaged 17.89 seconds.
- Checksum-aware pruning removed 52 managed compiled-cache entries and
  1.879 GB, leaving 42 entries and 1.585 GB. Unknown content was preserved.
- The final deterministic surface has 119 selected rows, 21 current closures,
  zero invalidations, and zero actionable frontier groups.
- No production compiler, generated runtime, runtime, interpreter, VM,
  canonical stdlib, language, dependency, benchmark, fixture, reference, or
  WASM change landed.
- Next refresh the non-mutating release-consolidation inventory for retained
  July 27–28 work, because the green dirty state must become reviewable
  without inventing another performance candidate.
- Record:
  `docs/perf-baselines/2026-07-28-v12-correctness-release-readiness-closure.md`.

## 2026-07-28 - Post-consolidation release inventory

- Captured an immutable 386-path pre-record snapshot at
  `cbba634af6c5766e5b8aa162e8e66643654f620e`: 93 tracked modifications,
  293 untracked paths, and a clean index.
- Classified exactly 337 retained paths, 34 deferred WASM paths, and 15
  generated-local exclusions. No path is unmatched.
- Preserved a 386-row per-path state, disposition, line/byte, and SHA-256
  manifest with identity
  `dd8c6c8ea7a5dc0ea6d7563dd20fc9410c9902d46646f16e18fc76cd582b71bc`.
- The accumulated July 27–28 state maps to 28 unique dated records in each
  log. All referenced records exist.
- JSON, gzip, whitespace, Go formatting, maintained-source size, scope, and
  common secret-signature checks pass. The largest dirty maintained source
  file is 999 lines.
- A cleanup dry run found 12.35 GiB of generated artifacts; none was deleted
  during this non-mutating tranche.
- Deleted an exact stale 10,684,494-byte external-Go workspace under `/tmp`
  after confirming that it had no open handles.
- No production, stdlib, dependency, WASM, index, or history mutation was
  made.
- Next obtain explicit maintainer authorization before exact staging or
  committing the 340-path post-record candidate. This matters because the
  release boundary is now auditable but history mutation remains a separate
  decision.
- Record:
  `docs/perf-baselines/2026-07-28-post-consolidation-release-inventory.md`.

## 2026-07-28 - Authorized post-consolidation exact-index commit

- The maintainer authorized one exact 340-path local commit: 337 retained
  pre-record paths plus three inventory metadata paths.
- The sorted newline-delimited candidate has SHA-256
  `d48d0a39dc528bc23bf7f954c15cd6295eb2bd4814b6f8783a78ad731c8cfaa0`.
  Refreshed line, byte, and SHA-256 identities cover all 337 non-self metadata
  paths.
- Used only the exact NUL-delimited Git pathspec. Cached path, scope,
  whitespace, binary, JSON/gzip, formatting, and common secret-signature
  checks pass.
- Created one local commit while preserving all 34 deferred WASM paths and
  excluding generated-local cache paths.
- No push, reset, rewrite, stdlib/dependency change, nominal special case,
  benchmark-specific optimization, or WASM work occurred.
- Next request explicit maintainer authorization before publishing the local
  branch, because remote mutation is separate from this local consolidation.
- Record:
  `docs/perf-baselines/2026-07-28-post-consolidation-release-inventory.md`.

## 2026-07-28 - Bytecode unranked-coverage admission search

- Confirmed publication of the five reviewed release commits. Local and
  remote-tracking `master` resolve to
  `5bbe9e4594bfb2b7e7f8c84b9fbed9314c0ee8db`; deferred WASM was not included.
- Classified the seven compiled-selected applications without a selected
  bytecode row: five current 90-second Able timeouts and two verifier-passing
  rows blocked by unavailable usable Python timings.
- Preserved 15-second CPU profile tops and separate five-second main-only
  instruction censuses for Binary Trees, N-Body, Quicksort, Sudoku Masks,
  and TapeLang Alphabet.
- The profiles have distinct workload owners. Common exact VM leaves are
  already-closed stack, inline-return, frame, raw-integer, or Array work.
  Inline misses are zero; Array specialization records 3,456,204 fast hits
  against 75 fallbacks.
- Fresh current Able bytecode runs passed public verification 5/5 per row.
  Fib averaged 0.1560 seconds and Matrix Multiply averaged 4.5900 seconds.
  Existing Ruby timings were not refreshed, so their descriptive ratios are
  not treated as paired A/B results.
- Focused selection, refresh, evidence, and closure-ledger contracts pass, as
  do JSON, whitespace, and maintained-source-size checks.
- Retained no production, stdlib, benchmark, language, dependency,
  nominal-special-case, or WASM source change.
- Removed the exact 315 MiB `/var/tmp` profiling workspace and pointer, a
  35 MiB ignored stale generated compiler tree, and test-created Python
  cache.
- Next establish smaller portable workload scales for all seven rows in
  temporary Able/Python/Ruby copies, preserving algorithms, features, inputs,
  and public verifiers, before collecting at least five balanced runs. This
  matters because the resulting comparable evidence can admit a real shared
  VM owner without reopening closed routes.
- Record:
  `docs/perf-baselines/2026-07-28-bytecode-unranked-coverage-admission-search.md`.

## 2026-07-28 - Bytecode portable workload-scale calibration

- Demonstrated smaller equivalent contracts for all seven unranked bytecode
  applications in disk-backed temporary Able/Python/Ruby copies.
- Selected depth 15 Binary Trees, 50,000-step N-Body, 500,000-value
  Quicksort, one ten-puzzle Sudoku pass, a five-level TapeLang delay,
  `fib(40)`, and 400×400 Matrix Multiply.
- Five rotated processes per application and language produced 105/105
  verified samples. The slowest process was below 24.5 seconds.
- Able exceeds both references for Fib and Matrix Multiply. The other five
  applications remain 11.2662×-42.3894× slower; the fourteen-ratio geometric
  mean is 4.232748×.
- Full calibrated profiles reproduce distinct allocation/struct,
  float/call/Array, parse/integer/Array, bitwise/recursive/Array, and
  member/struct/Array owner shapes. Only dispatch and the closed raw-integer
  carrier route occur in all five tops.
- Admitted no compiler, runtime, interpreter, VM, stdlib, dependency,
  language, nominal-special-case, benchmark-specific optimization, or WASM
  change.
- Retained the 105-run TSV, compact summary and source manifests, full-profile
  identities/tops, and exact calibrated TapeLang input. Canonical benchmark
  sources, external suite, selection, and scorecard remain unchanged.
- Focused selection, refresh, evidence, closure-ledger, JSON, sample-count,
  timeout, whitespace, and source-size checks pass.
- Removed the exact 267 MiB `/var/tmp` workspace and test cache.
- Next add general mode-aware benchmark arguments and per-mode verifier/input
  contracts, then refresh all 126 rows before moving bytecode selection from
  56/63 to 63/63. This matters because compiled rows must keep their broad
  canonical workloads while bytecode gains complete comparable evidence.
- Record:
  `docs/perf-baselines/2026-07-28-bytecode-portable-workload-scale-calibration.md`.

## 2026-07-28 - Mode-aware benchmark contract and 126-row promotion

- Added a general benchmark-mode argument/input contract. Compiled and Go
  retain canonical workloads; bytecode, Python, and Ruby use the seven
  verifier-equivalent portable scales established by calibration.
- Exposed ordinary workload arguments in the affected Able, Go, Python, and
  Ruby programs; added portable Quick Sort and TapeLang inputs and updated
  public verifiers and benchmark documentation.
- Added focused mode-contract tests and a disk-backed `--work-root` option to
  the complete scorecard refresh.
- Collected five verified processes for all 126 Able rows and all 189 required
  Go/Python/Ruby reference rows: 1,575/1,575 timed processes passed with zero
  timeouts, execution failures, or verifier failures.
- Promoted selection from 63 compiled/56 bytecode to complete 63/63 coverage.
  Current target passes are 6/63 compiled and 4/63 bytecode; geometric means
  are 5.575597× Able/Go and 12.780200× across bytecode reference ratios.
- Retained no compiler, generated-runtime, runtime, interpreter, VM,
  canonical-stdlib, language, dependency, named-container/non-primitive
  nominal, or WASM production change.
- Next map current CPU/allocation-profile coverage across the 59 bytecode
  misses, exclude already-closed routes, and profile the largest genuinely
  uncovered misses in at least three unlike families. This matters because
  complete coverage exposes 221.503684 seconds of bytecode target excess, but
  the five calibrated miss profiles already failed the shared-owner gate.
- Record:
  `docs/perf-baselines/2026-07-28-mode-aware-benchmark-contract-closure.md`.

## 2026-07-28 - Mode-aware target stability and evidence ledger reconciled

- Repeated every one of the ten current snapshot passes in an independent
  current-contract cohort with matched CPU identities and five verifier-backed
  Able/reference processes per comparison. All 120 retained timed processes
  passed without timeout, failure, or verifier error.
- All six compiled passes and all four bytecode passes repeated below the
  1.052632 target ratio. They are now established ten-sample guards rather
  than one-cohort snapshot crossings.
- Regenerated the complete 126-row cross-mode frontier. Added the five unlike
  calibrated bytecode misses as an explicit closed-no-shared-leaf group and
  reconciled benchmark-only source/definition drift; all 22 performance
  closures are current with zero invalidations.
- Excluded one otherwise verified 30-process compiled cohort because the Able
  and Go Binary Trees rows used different CPU pools; the comparison harness
  rejected that contract mismatch before evidence emission.
- Retained no compiler, generated-runtime, runtime, interpreter, VM, stdlib,
  language, dependency, nominal-special-case, or WASM production change.
- Next build a source-identity CPU/allocation profile coverage map across the
  59 bytecode misses and profile only the largest uncovered rows spanning at
  least three unlike families. This is important because it targets the
  221.503684-second bytecode excess without repeating closed VM routes.
- Record:
  `docs/perf-baselines/2026-07-28-mode-aware-benchmark-contract-closure.md`.

## 2026-07-28 - Bytecode current-source CPU/allocation coverage closed

- Added `bench-bytecode-profile-coverage.json` and its checked generator/test.
  The generated 59-row map validates selected source hashes, current CPU and
  allocation evidence, complete miss coverage, and closed frontier
  dispositions.
- Reprofiled Binary Trees, N-Body, QuickSort, Sudoku Masks, and TapeLang from
  the exact current scorecard sources: two main CPU profiles and two separate
  exact allocation processes per row. All 20 primary processes completed, and
  allocation counts were stable within 0-9 objects per application.
- The CPU intersection contains only dispatcher, Go GC/locking, and closed
  stack/raw-integer/call/return/store routes. Concrete allocation owners split
  among recursion environments/structs, float materialization, i32 boxing,
  Sudoku structs, and TapeLang's negligible 163,968-byte main.
- Retained no compiler, runtime, interpreter, VM, stdlib, benchmark, language,
  dependency, nominal-special-case, or WASM production change.
- Regenerated the frontier and advanced the portable-workload closure; all 22
  closure-ledger entries are current with zero invalidations. Focused
  coverage/frontier tests and generated checks pass.
- Removed the disk-backed binary, Go cache, raw pprof files, and temporary
  reports.
- Next instrument release-disabled primitive materialization/boxing boundaries
  by primitive kind and consumer, then census the largest unlike bytecode
  misses. This is important because full profile coverage now rules out a new
  visible shared leaf; boundary reach can identify where native carriers are
  actually lost without naming applications or non-primitive containers.
- Records:
  `docs/perf-baselines/2026-07-28-bytecode-current-source-profile-coverage-closure.md`
  and
  `docs/perf-baselines/2026-07-28-bytecode-current-profile-coverage.md`.

## 2026-07-28 - Bytecode semantic-boundary reach closure

- Audited and reused the retained release-disabled primitive-materialization
  observer; no new instrumentation or ordinary-execution work was needed.
- Eight current-source family representatives passed public verification and
  recorded 25,994,536 main-only transitions with no dropped sites. Repeated
  K-Nucleotide and Sudoku runs produced identical totals and outputs.
- The only material three-family shapes were primitive nominal construction
  and member retention. They are the same generic raw nominal-field rule whose
  five-run A/B previously regressed Sensor 6.93% and Rational 4.28%.
- No production candidate, prototype, or timing cohort was admitted. No
  compiler, runtime, interpreter, VM, stdlib, benchmark, language, dependency,
  nominal-special-case, or WASM code changed.
- Focused diagnostics/CLI tests and 30 evidence/coverage/frontier contracts
  pass; the 23-entry ledger is current with zero invalidations. Removed 336
  MiB of disk-backed census work and a dead 27 MiB `/tmp` extern cache.
- Next run current correctness and release-readiness gates; reopen performance
  mutation only when checked evidence is invalidated or a new exact open owner
  reaches three unlike families.
- Record:
  `docs/perf-baselines/2026-07-28-bytecode-semantic-boundary-reach-closure.md`.

## 2026-07-28 - V12 correctness and release-readiness refreshed

- Reconciled the derived cross-mode evidence chain with the complete 126-row
  frontier. Architecture and feasibility decisions remain unchanged; the
  23-entry ledger is current with zero invalidations and the frontier has zero
  actionable groups.
- Split the 117.87-second typed-Array compiler test into independently
  reported String/i32 cases, narrowed the canonical import, and retained all
  generated-Go and concrete-specialization assertions. The replacements take
  7.40 and 7.17 seconds.
- Bounded concurrency/diagnostics parity reporting and raised the five full
  compiler release matrices from 24 to 128 partitions. All 640 partitions
  pass. Serial JSON timing found no individual test over one minute; the
  compiler-core maximum was 53.09 seconds.
- The ordinary handoff, vet/build, both canonical-stdlib modes, cold
  compiled-CLI integration, architecture/scoreboard/frontier reproduction,
  JSON, shell, whitespace, and maintained-source-size checks pass.
- Deleted the exact 28 GiB release workspace, 29 MiB extern cache, and 284 KiB
  Python bytecode cache after preserving compact evidence.
- Retained no production compiler, generated-runtime, runtime, interpreter,
  VM, stdlib, language, dependency, benchmark, fixture, reference,
  nominal-special-case, or WASM behavior change.
- Next refresh the non-mutating post-frontier release-consolidation inventory,
  mapping new changed/untracked paths to dated records and review boundaries
  without staging, committing, resetting, deleting, or touching deferred WASM
  work. This matters because release correctness is green while current
  profile and boundary evidence admits no new general performance owner.
- Record:
  `docs/perf-baselines/2026-07-28-v12-correctness-release-readiness-closure.md`.

## 2026-07-28 - Post-frontier release inventory

- Captured an immutable 212-path pre-record snapshot at
  `5bbe9e4594bfb2b7e7f8c84b9fbed9314c0ee8db`: 74 tracked modifications,
  138 untracked files, no deletions/renames, and an empty index. `HEAD` equals
  `origin/master`.
- Classified 178 retained paths into seven dependency-ordered review
  boundaries and held the exact preceding 34-path WASM set outside the
  release boundary. No path is unmatched or generated-local.
- Preserved per-path state, review order, governing record, disposition,
  line/byte counts, and SHA-256 in the 212-row manifest with identity
  `13154fd551ada27f96b02538c0b321343672d13283bd1f585a63e9bda9f7ee66`.
- Identity, JSON, Go formatting, whitespace, source-size, scope, and common
  secret-signature checks pass. A cleanup dry run identified 4.01 GiB of
  ignored generated cache without deleting it.
- No production, stdlib, dependency, benchmark behavior, WASM, index, or
  history mutation occurred.
- Next obtain explicit authorization before refreshing identities, staging
  the exact 181-path post-record candidate, inspecting cached checks, and
  creating one local commit without pushing. This is important because the
  verified boundary is exact but Git mutation remains separately authorized.
- Record:
  `docs/perf-baselines/2026-07-28-post-frontier-release-inventory.md`.

## 2026-07-28 - Active Go dependency security refresh

- Retained Go 1.26.5, `go-git` v5.19.1, `x/crypto` v0.52.0,
  `x/sys` v0.45.0, and their tidied safe transitive dependency graph for the
  active v12 Go module.
- `govulncheck` v1.6.0 now reports zero symbol-reachable vulnerabilities and
  zero vulnerabilities in imported packages, down from 26 reachable findings.
- Module verification, vet/build, the 105-test package-fetch CLI suite, the
  complete 1,084-test compiler package, and the ordinary bounded handoff suite
  pass under Go 1.26.5.
- Matrix Multiply, QuickSort, and Binary Trees all compile strictly with
  `--no-fallbacks`, pass public verification, and omit `pkg/interpreter` from
  their final Go dependency graphs.
- Five-process compiled Able/Go 1.26.5 means are 1.1140s/0.9771s for Matrix
  Multiply, 2.1280s/2.7274s for QuickSort, and 8.0360s/11.6445s for Binary
  Trees. QuickSort and Binary Trees remain faster than Go; Matrix Multiply
  remains the same existing miss.
- No compiler, generated runtime, runtime semantics, interpreter, VM, stdlib,
  language, benchmark, fixture, archived workspace, nominal special case, or
  WASM code changed. Large work stayed in the exact disk-backed
  `/var/tmp/able-v12-security-upgrade-20260728` workspace.
- The maintainer authorized exact consolidation and publication of the
  six-path security tranche; deferred WASM remains outside that boundary.
- After publication, next refresh all 63 compiled Able/Go rows with five
  matched Go 1.26.5 processes per side. This is important because the
  three-application security smoke is green while the authoritative full
  frontier still records Go 1.26.4 references.
- Record:
  `docs/perf-baselines/2026-07-28-active-go-dependency-security-refresh.md`.

## 2026-07-28 - Authorized post-frontier exact-index commit

- The maintainer authorized one exact 181-path local commit: the 178 retained
  pre-record paths plus the three inventory metadata files.
- Refreshed the 178 non-self identities, confirmed the unchanged 34-path
  deferred WASM complement, and used only the exact NUL-delimited pathspec
  with SHA-256
  `fd41801a0261058210ab24e5c31660ea1ea59ed990b1208a8edde1056a8596f8`.
- Cached identity, JSON, formatting, whitespace, source-size, scope,
  common-secret, evidence, and exact-complement checks passed before the one
  local consolidation commit.
- No broad staging, deferred WASM staging, or push occurred.
- Next obtain separate authorization before publishing the local branch,
  confirming the remote and exact commit range first. This matters because
  the authorized local consolidation does not authorize external mutation.
- Record:
  `docs/perf-baselines/2026-07-28-post-frontier-release-inventory.md`.

## 2026-07-29 - Complete compiled Go 1.26.5 scorecard refresh

- Ran five verified strict Able processes and five verified Go 1.26.5
  processes for every one of the 63 selected compiled applications: 630
  successful timed processes with zero timeouts or failures.
- Discarded the first partial cohort after proving that root-level reference
  builds used Go 1.26.4 while module-local generated builds selected Go
  1.26.5, and that recurring unrelated host jobs contaminated its early rows.
  The retained cohort explicitly forced Go 1.26.5 on both sides.
- Compiled results are 7 target passes, 56 misses, a 4.637116x geometric-mean
  Able/Go ratio, and 5.353263 seconds of positive target excess.
- Independently repeated the new `i_before_e` pass. Its two five-process ratios
  are 1.049069x and 0.917108x, with a pooled 0.984465x ratio.
- Promoted the new scorecard, regenerated the fixed and dated frontiers,
  advanced only the changed `compiled-text-map` closure, and reconciled every
  downstream architecture artifact. The 126-row frontier has 11 established
  guards, 115 misses, zero actionable groups, and 23 current closures.
- All 30 benchmark contract test files and all affected checked generators
  pass. No production or WASM code was retained.
- Next add an explicit Go toolchain/version contract to the refresh driver and
  wrappers, propagate it to both build paths, reject mixed reports, and add
  fast tests. This matters because trustworthy native-performance decisions
  require both sides to use the same recorded Go patch release.
- Record:
  `docs/perf-baselines/2026-07-29-compiled-go1265-scorecard-refresh.md`.

## 2026-07-29 - Benchmark Go toolchain provenance contract

- Promoted full-scorecard refreshes now require an exact Go patch selector.
  The driver resolves it once and propagates the selector and exact version to
  all external Go-reference and Able CLI/generated-Go build processes.
- Reference, measurement, and comparison JSON records preserve the toolchain
  contract. Fresh compiled comparisons reject absent, mixed, or mislabeled
  selector/version evidence.
- Added focused contracts covering selector syntax, resolution, expected
  version, mismatch rejection, promotion refusal, and all commands in the
  63-application dry-run graph.
- All 31 benchmark contract files pass. A real strict Fibonacci smoke verified
  one process per side and recorded matching `go1.26.5` /
  `go version go1.26.5 linux/amd64` contracts throughout.
- No production or WASM behavior changed.
- Next refresh the exact non-mutating release inventory for the completed
  scorecard and toolchain tranches, preserving the 34 deferred WASM files as
  an explicit complement. This matters because the verified baseline should
  be releasable without absorbing unrelated dirty work.
- Record:
  `docs/perf-baselines/2026-07-29-benchmark-go-toolchain-contract.md`.

## 2026-07-29 - Post-toolchain release inventory

- Captured 121 fully expanded dirty files at
  `2243143aa449c9c764d7215496faf473b19fc73d` with `HEAD` equal to
  `origin/master`, zero staged paths, and no deletions or renames.
- Classified exactly 87 retained paths and the unchanged 34-file deferred WASM
  hold. Ordinary porcelain had reported 32 WASM entries only because the
  three files under `v12/wasm/samples/modules/` were collapsed to one
  directory entry.
- The retained dependency order is benchmark toolchain contract, compiled Go
  1.26.5 scorecard evidence, derived frontier reconciliation, then handoff and
  release records.
- All 121 identities and all JSON, formatting, syntax, whitespace,
  source-size, scope, secret, benchmark-contract, scorecard, frontier, and
  ledger checks pass.
- The three inventory metadata files produce an exact 90-path post-record
  candidate. Nothing was staged, committed, pushed, reset, reverted, or
  deleted.
- Next obtain explicit authorization before refreshing identities, staging
  only the exact candidate, and creating one local commit without pushing.
  This is important because the release boundary is verified while Git
  mutation remains separately authorized.
- Record:
  `docs/perf-baselines/2026-07-29-post-toolchain-release-inventory.md`.

## 2026-07-29 - Compiled frontier admission closure

- Published the exact 90-path Go 1.26.5 scorecard/toolchain boundary as
  `b28a896ab45a592ed3155908f07ae3324923fd25` on `origin/master`, preserving
  the exact 34-file deferred WASM hold outside history.
- The current scorecard evidence check passes for all 126 selected rows with
  five successful samples per side; the dated frontier reproduces with zero
  actionable groups.
- The checked performance ledger remains at 23 closures and zero
  invalidations, so the toolchain refresh did not reopen production ownership.
- Text/map work, concurrency environment recovery, and Sudoku quotient work
  account for 75.05% of compiled target excess, but respectively fail the
  shared-leaf, excluded broad-ABI, and three-unlike-program breadth gates.
- Retained no production or WASM change and did not start a prototype or A/B
  cohort.
- Next keep production mutation paused until checked evidence is invalidated
  or one new exact general owner becomes material in three unlike programs.
- Record:
  `docs/perf-baselines/2026-07-29-compiled-frontier-admission-closure.md`.

## 2026-07-29 - Active v12 correctness and security revalidation

- Go 1.26.5 module verification and `govulncheck` v1.6.0 pass with zero
  reachable and zero imported-package vulnerabilities. The same eight
  unreachable module-only findings remain documented and accepted.
- Vet, build, every bounded handoff preflight, all non-compiler packages, all
  34 compiler short-mode shards, and the bytecode fixture pass succeed.
- Serial JSON timing over the 75 tests in the three aggregate shards above one
  minute found zero individual test over 60 seconds; the maximum was 24.49
  seconds.
- Removed generated Python bytecode, the exact timing artifact, and an idle
  unheld 37 MiB `/tmp` extern cache. The worktree remains exactly the 34-file
  deferred WASM hold with an empty index.
- Retained no source or dependency change. Next keep the production
  performance pause until a checked closure is genuinely invalidated.
- Record:
  `docs/perf-baselines/2026-07-29-active-v12-correctness-security-revalidation.md`.

## 2026-07-29 - Authorized post-toolchain exact-index commit

- The maintainer authorized exact staging and one local commit for the
  inventory's 90-path retained candidate.
- Refreshed the 87 pre-record identities, preserved the exact 34-file deferred
  WASM complement, and used only the recorded NUL-delimited pathspec with
  SHA-256
  `ef537311e2af6a320989f5fbe665ab7cc5f92483aa404f5771f2ac587c27b562`.
- Cached content, scope, evidence, and complement checks passed before the one
  local commit. No broad staging, deferred WASM staging, or push occurred.
- Next obtain separate authorization before publishing the local branch,
  confirming the remote and exact commit range first. This matters because
  local consolidation authority does not authorize a push.
- Record:
  `docs/perf-baselines/2026-07-29-post-toolchain-release-inventory.md`.

## 2026-07-29 - Callable-context default-activation design

- Completed the explicitly authorized design/proof revisit of the compiled
  callable execution-context boundary.
- Found no missing ABI to invent: the retained experimental implementation
  already transports an allocation-free task context through static,
  interface, callable, captured-lambda, bound-method, runtime-adapter, Await,
  and reverse native-call paths while preserving compatibility entries.
- Rejected a global default again and specified the narrower production
  candidate: activate the existing ABI only for a loaded static program graph
  containing `await`, including imported modules, with byte-identical output
  required for all await-free programs.
- Defined task/package/lexical-environment invariants, failure modes, focused
  source and semantic guards, exact boundary/allocation proof, and the full
  63-application verifier-backed A/B/Go admission cohort.
- No compiler, generated-runtime, runtime, interpreter, bytecode VM, stdlib,
  language, dependency, benchmark, fixture, non-primitive nominal, or WASM
  production source changed.
- Next implement and measure the activation-only candidate. This is important
  because all four currently reached applications improve 55.00%-95.09% with
  the retained ABI, while safe default selection must keep its cost out of the
  other 59.
- Records:
  `design/compiler-callable-context-default-activation.md` and
  `docs/perf-baselines/2026-07-29-compiled-callable-context-default-activation-design.md`.

## 2026-07-29 - Callable-context default activation retained

- Changed compiler activation, not representation: a loaded program graph
  containing `await` now selects the retained callable execution-context ABI
  by default, while dynamic/host compatibility entries remain available.
- Option-independent detection covers imported modules. The experimental flag
  remains a force-on diagnostic and await-free default programs retain their
  previous generated source.
- A 63-application frozen A/B census passed all 126 strict builds, executions,
  public verifiers, and interpreter-dependency checks. Fifty-nine generated
  sources were byte-identical and only the four expected await applications
  changed.
- Seven rotating A/B/Go cohorts improved the four reached rows
  45.77%-97.09%; their geometric-mean time fell 87.15%.
- Five-process allocation evidence reduced main-phase bytes
  20.69%-78.50% and objects 10.62%-69.24%. Five-process bridge overlays
  reduced `currentGID` calls 48.38%-99.96%, while exact callable/await
  counters proved default-path execution.
- Bounded source, imported-module, nested-task, Awaitable, stale-waker,
  dynamic/host compatibility, fixture-parity, CLI, bridge, and closed
  benchmark guards pass. No individual focused command exceeded one minute.
- No runtime, interpreter, bytecode VM, canonical stdlib, language,
  dependency, benchmark, fixture, non-primitive nominal, or WASM change was
  required.
- Next refresh and promote the complete compiled scorecard and frontier with
  five matched Go 1.26.5 processes per side. This is important because the
  next shared owner must be selected from the post-activation residuals.
- Record:
  `docs/perf-baselines/2026-07-29-compiled-callable-context-default-activation-retained.md`.

## 2026-07-29 - Post-callable-context scorecard promoted

- Completed a paired five-process Go 1.26.5/Able refresh for all 63 strict
  compiled applications: all 630 launches passed and every Able result passed
  its public verifier.
- Rejected the full replacement cohort because unrelated host load inflated
  the 59 byte-identical controls. Promoted the seven-row async refresh and
  preserved same-toolchain evidence for the other 56 compiled rows and all 63
  bytecode rows.
- Compiled remains at 7/63 target passes; its geometric Able/Go ratio improves
  8.05% to 4.263718× and positive target excess falls 11.25% to 4.750737
  seconds.
- The four changed rows improve 14.67%-91.77% in the formal scorecard. The
  combined frontier now has 11 guards, 115 misses, 226.254421 seconds of
  positive target excess, and one actionable group.
- `compiled-concurrency` is open at 0.891158 seconds of excess. Its 19
  byte-identical unresolved rows retain the exact
  `bridge.currentGID`/`runtime.Stack` owner removed from the four await rows.
- Rebased the 23-closure ledger after source-identity, verifier, and generated
  source-shape reconciliation; it evaluates with zero invalidations.
- Static census finds `spawn` in those 19 concurrency rows and Binary Trees,
  yielding 20 newly reached applications and 39 spawn-free controls.
- Current scorecard, five-run evidence, frontier, ledger, and focused tooling
  tests pass. This refresh made no further production, stdlib, runtime,
  interpreter, VM, language, dependency, benchmark, nominal-special-case, or
  WASM change.
- Next implement and gate spawn-aware scheduler-context selection across
  loaded modules. This entails semantic/source guards and five-or-more
  balanced A/B/Go, allocation, and exact boundary samples for the 20 reached
  programs and 39 controls. It matters because it is the only actionable
  general compiled boundary, and Binary Trees guards against sacrificing
  already-native performance.
- Record:
  `docs/perf-baselines/2026-07-29-post-callable-context-scorecard-refresh.md`.

## 2026-07-29 - Spawn-gated callable context retained

- Scheduler-context detection now recognizes `spawn` as well as `await`
  across every loaded module, selecting the existing callable-context ABI
  without changing data carriers or dynamic/host compatibility.
- Corrected spawn-only generated helper closure: context-aware Awaitable
  surfaces emit native waker and registration helpers even when no explicit
  source `await` exists.
- A complete frozen 63-application baseline/candidate census passed all 126
  strict executions, verifiers, and interpreter-dependency checks. Twenty
  generated sources changed; 39 spawn-free and four already await-gated
  controls were byte-identical.
- Seven or fifteen rotating A/B/Go pairs per reached row improved 13 of 20
  applications and produced a 0.884258 cross-application paired geometric
  ratio. Without the large Mutex Ledger win, the other 19 improve 2.96%.
- Binary Trees retained the native-performance guard across fifteen pairs:
  16.624459-second candidate mean versus 16.246988-second Go mean and 97.67%
  of Go performance by paired geometric ratio.
- Five-run allocation evidence is broadly neutral; Mutex Ledger cuts bytes
  56.17% and objects 49.39%. Five-run diagnostic overlays reduce exact
  `currentGID` calls in 19 of 20 applications.
- Seven short rows remain 0.90%-6.66% slower. The general syntax gate was
  retained because a narrower application, family, nominal-type, or measured
  runtime-count predicate would be benchmark-specific.
- Bounded source-shape, imported spawn, nested task, captured callable,
  Channel, Future, Mutex, cancellation, Awaitable/waker, stale-waker,
  executor, CLI, bridge, formatting, and source-size checks pass.
- No runtime, interpreter, bytecode VM, canonical stdlib, language,
  dependency, benchmark, fixture, non-primitive nominal, or WASM change was
  made.
- Next selectively refresh the compiled scorecard and derived evidence for
  the 20 source-changed rows while preserving the 43 source-identical rows.
  This matters because the pre-spawn frontier no longer represents the
  residual costs from which the next general compiler/runtime owner must be
  selected.
- Records:
  `design/compiler-spawn-gated-callable-context-activation.md` and
  `docs/perf-baselines/2026-07-29-compiled-spawn-gated-callable-context-retained.md`.

## 2026-07-29 - Post-spawn scorecard and residual-owner closure

- Measured five matched Go 1.26.5 and Able processes for the 20 changed
  compiled rows and nine byte-identical controls. All 290 accepted processes
  verified; a 15-process mixed-toolchain attempt was discarded.
- The nine controls drifted by a 10.31% geometric mean under unrelated host
  load, so the promoted scorecard replaces only the 20 source-changed rows and
  preserves the other 43 compiled rows.
- Compiled remains at 7/63 target passes, 4.320152× Go geometric mean, and
  4.751789 seconds of positive target excess. Binary Trees remains a guard at
  0.930962× Go time.
- Fresh 25-profile CPU and three-profile exact allocation intersections
  across Await Channel Mux, Validated Job Pipeline, and Concurrent Stateful
  Pipeline found ordinary `bridge.ToInt` boxing in all three. The global
  integer cache remains rejected by its 4.17% TapeLang regression.
- `currentGID` is material only in the first two applications, while nominal
  struct construction is material only in the latter two. No new owner clears
  both breadth and admissibility.
- The 126-row frontier now has zero actionable groups. The scope-reviewed
  closure baseline contains 23 current entries and zero invalidations.
- Added and tested `bench_select_comparison_rows`. Checked scorecard,
  five-sample evidence, frontier, ledger, Python, whitespace, and
  `cmd/ablec` verification pass. No production or WASM change was made.
- Next run bounded v12 correctness and release verification until a concrete
  change invalidates a performance closure. This keeps the native-carrier and
  interpreter-free gains protected without reviving rejected candidates.
- Record:
  `docs/perf-baselines/2026-07-29-post-spawn-context-scorecard-and-owner-closure.md`.

## 2026-07-29 - Post-spawn correctness and release gate

- `./run_all_tests.sh` passed every deterministic contract, non-compiler
  package, all 34 compiler short-mode batches, and the complete bytecode
  fixture corpus under Go 1.26.5 and strict fixture typechecking.
- The full gate took 15:50.94 at 4,651,676 KB peak RSS with no swaps. The
  final bytecode fixture pass took 83.886 seconds.
- Three compiler aggregates exceeded one minute under unrelated host load.
  Per-test JSON replays found maxima of 22.910 seconds in batch 20, 15.150
  seconds in batch 29, and 10.740 seconds in batch 30; no individual test
  violates the one-minute limit.
- Canonical stdlib tests passed in tree-walker mode in 19 seconds and bytecode
  mode in 15 seconds. Its source tree and dirty state were unchanged.
- Authoritative scorecard, zero-actionable frontier, 23-current closure
  ledger, and whitespace checks remain green.
- Retained no production, stdlib, dependency, benchmark, fixture,
  nominal-special-case, or WASM change.
- Next refresh the exact retained/deferred worktree release inventory without
  staging, committing, or pushing. This is important because accumulated
  retained evidence must remain separate from the deferred WASM boundary and
  generated local artifacts.
- Record:
  `docs/perf-baselines/2026-07-29-post-spawn-correctness-release-gate.md`.

## 2026-07-29 - Post-spawn release inventory

- Captured an immutable 178-file fully expanded worktree snapshot with an
  empty index and `HEAD` equal to `origin/master`.
- Classified exactly 144 retained paths and the unchanged 34-file deferred
  WASM hold. No dirty path is unmatched or generated-local.
- Reproduced every line, byte, and content identity. JSON, formatting,
  language syntax, whitespace, source-size, scope, common-secret, scorecard,
  frontier, closure-ledger, and five-sample evidence validation pass.
- Defined an exact 147-path post-record retained candidate whose complement
  must remain the 34 deferred WASM files. No staging, commit, push, cleanup
  application, implementation change, or WASM work occurred.
- The cleanup audit found 7.909 GiB of ignored generated cache data.
  Disposable benchmark workspaces are gone and no `/tmp/able-*` path remains.
- Next obtain explicit authorization for exact local consolidation. This
  entails refreshing handoff identities and staging only the verified NUL
  pathspec before one local commit if authorized; it is important because the
  deferred WASM boundary must remain outside release history.
- Record:
  `docs/perf-baselines/2026-07-29-post-spawn-release-inventory.md`.

## 2026-07-29 - Exact post-spawn local consolidation

- The maintainer authorized exact staging and one local commit for the
  147-path retained candidate, but did not authorize a push.
- Refreshed the 144 non-self identities and revalidated the exact 147 retained
  paths against the unchanged 34-file deferred WASM complement.
- Used only the exact NUL-delimited retained pathspec. All deferred WASM files
  remained unstaged; no broad `git add` pathspec was used.
- Identity, JSON, formatting, syntax, whitespace, source-size, scope,
  common-secret, five-sample scorecard, frontier, and closure-ledger checks
  passed before the one local commit.
- Next obtain explicit publish authorization. This entails checking the final
  one-commit divergence and pushing only that exact range if authorized; it is
  important because deferred WASM and unintended history must remain local.
- Record:
  `docs/perf-baselines/2026-07-29-post-spawn-release-inventory.md`.

## 2026-07-29 - Exact post-spawn publication

- Pushed exactly
  `6efad0a53120129510fdfbab7fbcc84dcd081768` to `origin/master` without
  force and verified zero local/remote divergence.
- The published commit contains the exact authorized 147 paths. The index
  remained empty and all 34 deferred WASM paths remained outside the commit.
- GitHub's push banner reported 75 repository-wide dependency alerts, so the
  next tranche attributed active-v12 reachability before further performance
  mutation.

## 2026-07-29 - Dependency-alert attribution

- Active v12 production and test scans under Go 1.26.5 report zero reachable
  symbol and zero imported-package vulnerabilities. Parser Go bindings are
  also clean.
- Seven `x/net v0.54.0` advisories and the unfixable, unimported
  `x/crypto/openpgp` advisory remain module-only.
- A Go 1.26.4 control reaches a TLS advisory fixed by Go 1.26.5, confirming
  the checked-in toolchain pin is security-significant.
- Frozen v10 and v11 each retain 25 reachable historical findings; policy
  keeps both untouched. All npm lockfiles report zero vulnerabilities.
- All 197 dependency manifests and 34 deferred WASM paths remained
  byte-identical. No production or dependency change was retained.
- A temporary probe proves `x/net v0.56.0`, `x/crypto v0.53.0`, and
  `x/sys v0.46.0` remove all seven fixable active-v12 module advisories while
  retaining zero reachable findings.
- Next perform that bounded proactive refresh and rerun Go 1.26.5
  vulnerability and correctness gates. This prevents future imports from
  activating already-fixed `x/net` vulnerabilities.
- Record:
  `docs/perf-baselines/2026-07-29-dependency-alert-attribution.md`.

## 2026-07-29 - Bounded v12 x/net security refresh retained

- Advanced `x/net` to `v0.56.0` with resolver-required
  `x/crypto v0.53.0` and `x/sys v0.46.0`; tidy normalized only the coupled
  `x/term` and `x/text` sums.
- The three `go.mod` replacements and 20-line `go.sum` replacement are tidy
  and verified, with no new module family.
- Explicit Go 1.26.5 production and test scans remain at zero reachable and
  zero imported-package vulnerabilities. All seven fixable `x/net`
  advisories disappear.
- The only remaining module finding is the unfixable, unimported
  `x/crypto/openpgp` advisory.
- Every package compiles, focused dependency-resolver tests pass, and the
  authoritative scorecard, frontier, and closure ledger remain current.
- No source, runtime, compiler, interpreter, benchmark, stdlib, frozen
  workspace, or deferred WASM file changed.
- Next refresh the non-mutating nine-retained/34-deferred release inventory.
  This is important because the security correction needs an exact safe
  consolidation boundary.
- Record:
  `docs/perf-baselines/2026-07-29-v12-x-net-security-refresh.md`.

## 2026-07-29 - Post-security release inventory

- Captured an immutable 43-file fully expanded worktree snapshot with an
  empty index and synchronized local/remote commit.
- Classified exactly nine retained security/handoff paths and the same 34
  deferred WASM files. No path is unmatched or generated-local.
- Identity, JSON, formatting, syntax, whitespace, source-size, scope,
  common-secret, dependency, scorecard, frontier, and closure-ledger checks
  pass.
- Defined an exact 12-path post-record retained candidate whose complement
  must remain the 34 deferred WASM files. No staging, commit, push, behavior
  change, or WASM work occurred.
- Cleanup found no generated project artifact or temporary Able workspace.
- Next obtain explicit authorization for exact local consolidation. This
  entails refreshing handoff identities and staging only the verified
  12-path NUL pathspec before one local commit if authorized; it is important
  because deferred WASM must remain outside the security release history.
- Record:
  `docs/perf-baselines/2026-07-29-post-security-release-inventory.md`.

## 2026-07-29 - Exact post-security local consolidation

- The maintainer authorized exact staging and one local commit for the
  12-path retained candidate, with no push authorization.
- Refreshed all nine non-self identities and proved that the exact retained
  candidate still complements the unchanged 34 deferred WASM files.
- Staged only the exact NUL-delimited 12-path pathspec. The deferred WASM
  boundary remained unstaged and no broad `git add` was used.
- Identity, JSON, whitespace, dependency, common-secret, scorecard,
  five-sample evidence, frontier, and closure-ledger validation passed before
  the local commit.
- Next obtain explicit publish authorization. This requires checking the
  final one-commit divergence and remote destination, then pushing only that
  exact range if authorized; it is important because deferred WASM and
  unintended history must remain local.
- Record:
  `docs/perf-baselines/2026-07-29-post-security-release-inventory.md`.

## 2026-07-29 - Exact post-security publication

- Pushed exactly
  `5f84e42c43d3d514c231c7887e835895a9ba557a` to `origin/master` without
  force and verified zero local/remote divergence.
- The published commit contains the exact authorized 12 paths. The index
  remained empty and all 34 deferred WASM files remained outside history.
- GitHub still reports the same repository-wide 75-alert aggregate. The
  completed attribution and bounded refresh remain authoritative for active
  v12: zero reachable or vulnerable imported-package findings, with all seven
  fixable `x/net` advisories removed.
- Next run a whole-corpus compiled native-carrier and semantic-boundary census
  and compare every remaining generated boundary with the 23 current closure
  entries. Only an uncovered owner repeated by at least three unlike
  applications may proceed to repeated CPU/allocation and equivalent-Go
  measurement. This is important because the current frontier contains no
  actionable shared owner and closed routes must not be repeated without
  invalidation.
- Record:
  `docs/perf-baselines/2026-07-29-post-security-release-inventory.md`.

## 2026-07-29 - Compiled native-carrier census reconciled

- The requested whole-corpus census is already represented by the completed
  July 26 61-row exact-owner census and the post-spawn July 29 63-row
  scorecard/owner refresh.
- All 23 performance closures are current, none is invalidated, and the
  frontier has zero actionable groups. Relevant compiler, bridge, spec, and
  benchmark sources are unchanged after the closure refresh.
- All 63 compiled rows remain verified with five Able/reference samples each.
  The external suite has no unselected portable application: only historical
  diagnostic `sudoku` is outside coverage, and it is non-comparable and
  unbounded under the current contract.
- Retained no implementation change and did not repeat closed builds,
  profiles, prototypes, or A/B/Go measurements.
- Next refresh the exact retained/deferred release inventory for the three
  handoff files and two census records. This is important because the
  no-code stopping condition must remain separate from the unchanged 34
  deferred WASM files.
- Record:
  `docs/perf-baselines/2026-07-29-compiled-native-carrier-census-reconciliation.md`.

## 2026-07-29 - Post-census release inventory

- Captured an immutable 39-file fully expanded worktree snapshot with an empty
  index and synchronized local/remote commit.
- Classified exactly five retained census/handoff paths and the same 34
  deferred WASM files. No path is unmatched or generated-local.
- Identity, JSON, formatting, syntax, whitespace, source-size, scope,
  common-secret, scorecard, frontier, and closure-ledger checks pass.
- Defined an exact eight-path post-record retained candidate whose complement
  must remain the 34 deferred WASM files. No staging, commit, push, behavior
  change, or WASM work occurred.
- Next obtain explicit authorization for exact local consolidation. This
  entails refreshing the five non-self identities and staging only the
  verified eight-path NUL pathspec before one local commit if authorized; it
  is important because deferred WASM must remain outside the census history.
- Record:
  `docs/perf-baselines/2026-07-29-post-census-release-inventory.md`.

## 2026-07-29 - Exact post-census local consolidation

- The maintainer authorized exact staging and one local commit for the
  eight-path retained candidate, with no push authorization.
- Refreshed all five non-self identities and proved that the exact retained
  candidate still complements the unchanged 34 deferred WASM files.
- Staged only the exact NUL-delimited eight-path pathspec. The deferred WASM
  boundary remained unstaged and no broad `git add` was used.
- Identity, JSON, whitespace, scope, common-secret, scorecard, five-sample
  evidence, frontier, and closure-ledger validation passed before the local
  commit.
- Next obtain explicit publish authorization. This requires checking the
  final one-commit divergence and remote destination, then pushing only that
  exact range if authorized; it is important because deferred WASM and
  unintended history must remain local.
- Record:
  `docs/perf-baselines/2026-07-29-post-census-release-inventory.md`.

## 2026-07-29 - Exact post-census publication

- Pushed exactly
  `358368c24f84367ee8f39c5df6b07ec488b477ef` to `origin/master` without
  force and verified zero local/remote divergence.
- The published commit contains the exact authorized eight paths. The index
  remained empty and all 34 deferred WASM files remained outside history.
- GitHub's aggregate banner now reports 74 alerts—one fewer moderate alert
  than the preceding push. Existing active-v12 attribution remains
  authoritative.
- Next choose and authorize a genuine evidence invalidation trigger: new
  specified language/stdlib behavior or a new source-equivalent portable
  application with applicable references. Correctness and coverage land
  first; only ledger-selected closures may then be refreshed. This is
  important because the 23-entry ledger is current and the existing portable
  suite has no unselected application.
- Record:
  `docs/perf-baselines/2026-07-29-post-census-release-inventory.md`.

## 2026-07-29 - Transaction Ledger Audit application and owner gate

- Added the 64th portable application, Transaction Ledger Audit, with
  byte-identical canonical/external Able sources and equivalent Go 1.26,
  Python 3.14, and Ruby 4.0 implementations. Strict compiled, tree-walker,
  bytecode, and all references produce the same verifier-approved output.
- Five-run CPU-15 means are 0.0500 seconds compiled versus Go at 0.0073
  seconds, and 4.5900 seconds bytecode versus Python at 0.0344 seconds and
  Ruby at 0.0914 seconds. The strict binary is fallback-free and omits the
  interpreter dependency.
- Refreshed compiled CPU and exact main-allocation ownership across
  Transaction Ledger Audit, Inventory Reconciliation, and Sensor Calibration.
  Map storage is material only in the first two; parsing/integer conversion
  only in the first and third. No exact general Able owner reaches all three.
- Retained no compiler, runtime, interpreter, VM, stdlib, language,
  dependency, or WASM change. The 128-row frontier has 11 guards, 117 misses,
  zero actionable groups, complete bytecode profile coverage, and 23/23
  current closure entries.
- Focused evidence and architecture generators, `go test ./cmd/ablec`, and
  the full `./run_all_tests.sh` handoff suite pass. The latter completed every
  preflight, non-compiler package, all 34 compiler batches, and the bytecode
  fixture pass.
- Removed about 432 MiB of exact disposable build/profile workspaces plus
  generated Python and extern caches. No matching `/var/tmp` or
  `/tmp/able-*` artifact remains.
- Next add a portable user-defined generic nominal storage application. This
  entails equivalent Able/Go/Python/Ruby identity, aliasing, interface, and
  iteration behavior, five-run verification, and a closure-selected profile
  refresh. It matters because this can establish whether the two-program map
  boundary is a general nominal-encoding cost without permitting a
  named-container special case or unnecessary dynamic boxing.
- Record:
  `docs/perf-baselines/2026-07-29-transaction-ledger-audit-application-gate.md`.

## 2026-07-29 - Generic Slot Buffer native-nominal application gate

- Added Generic Slot Buffer as portable application 65, with equivalent Able,
  Go 1.26, Python 3.14, and Ruby 4.0 sources covering generic mutable nominal
  storage, identity, aliasing, interfaces, iteration, static Arrays, and
  nullable primitive reads.
- All engines produce verifier-approved output. Five-run CPU-15 means are
  0.0420 seconds compiled versus Go at 0.0051 seconds, and 2.2580 seconds
  bytecode versus Python at 0.1847 seconds and Ruby at 0.1037 seconds.
- Strict generated Go uses native Array, `Slot<i64>`, `VersionedBuffer<i64>`,
  integer, pointer-identity, and concrete interface carriers. Its final graph
  has no interpreter dependency and its hot path has no dynamic bridge.
- Thirty compiled CPU profiles and an exact main-allocation profile show
  131,329 direct `?i64` pointer boxes in `VersionedBuffer.get`, not nominal
  storage materialization. The two map controls allocate through different
  dynamic conversion/recovery leaves; no exact general owner clears the
  three-program gate.
- Three bytecode runtime profiles expose only previously closed
  call/member/frame/stack/raw-integer/cast/cache owners. Retained no production
  change. The 130-row frontier has zero actionable groups and all 23 closures
  are current.
- Focused catalog/evidence checks, `go test ./cmd/ablec`, and the complete
  `./run_all_tests.sh` handoff suite pass. Corrected its stale 64-application
  feature-coverage expectation to 65 before the green full rerun.
- Removed 190 MiB of exact Generic Slot Buffer build/profile workspaces and
  generated Python caches after retaining readable evidence.
- Next run a complete nullable-scalar carrier census and semantic design
  across static, interface, union, dynamic, and host boundaries. Prototype a
  general value-plus-valid carrier only if that gate stays sound. This is
  important because it targets a broad source of primitive boxing without
  disturbing native nominal lowering or adding forbidden special cases.
- Record:
  `docs/perf-baselines/2026-07-29-generic-slot-buffer-application-gate.md`.

## 2026-07-30 - Compiled primitive nullable value carrier retained

- Compiled primitive nullable scalars now use one generated
  `__able_nullable[T] { value, valid }` carrier instead of `*T`. Present zero
  remains distinct from absent. `?Error`, non-primitive nominal nullables, and
  explicit runtime/dynamic boundaries remain unchanged.
- Five verifier-backed frozen A/B runs per application improved Generic Slot
  Buffer from 0.0560 to 0.0340 seconds, Inventory Reconciliation from 0.1220
  to 0.1100 seconds, and Transaction Ledger Audit from 0.0480 to 0.0400
  seconds. Their strict graphs omit the interpreter.
- Three independent exact main-phase measurements cut allocation objects by
  99.60%, 48.88%, and 8.88%. All three intended nullable owners disappear.
  Remaining dynamic-map conversions are required explicit boundaries.
- Replaced a non-repeating 2.224-second Pidigits scorecard sample after frozen
  A/B showed the candidate neutral-to-faster. I Before E was conservatively
  demoted after a current repeated miss. The reconciled frontier has 10
  established guards, 120 misses, no unestablished snapshot meet, and zero
  actionable frontier groups.
- Focused semantic/boundary tests, `go test ./cmd/ablec`, and the full
  `./run_all_tests.sh` suite pass. No canonical stdlib, tree-walker, bytecode
  VM, runtime package, language, dependency, or WASM change was needed.
- Removed 442 MiB of exact disk-backed nullable build/profile workspaces and
  the generated v12 Python cache after retaining readable evidence.
- The changed compiler-production identity selects 12 compiled or
  cross-family closure records for fresh evidence. They remain invalidated
  rather than being falsely advanced by this three-application result.
- Next profile at least three unlike current compiled misses selected from the
  invalidated closures. Use strict interpreter-free builds, repeated
  main-only CPU and exact allocation measurements, and admit only one general
  exact owner that repeats across all three and passes balanced A/B/Go
  verification. This matters because remaining work must start from the
  post-carrier compiler state.
- Design:
  `design/compiler-primitive-nullable-value-carrier.md`.
- Record:
  `docs/perf-baselines/2026-07-30-compiled-primitive-nullable-value-carrier-retained.md`.

## 2026-07-30 - Post-nullable compiled architecture owner closure

- Retained no production change after refreshing six unlike compiled families:
  Concurrent Event Routing, Distance Field, Fixed Width 128, K-Nucleotide,
  Manifest Normalization, and Policy Record Dispatch.
- Collected 60 verified main-only CPU profiles and 18 verified exact
  allocation profiles from fresh `--no-fallbacks` builds. All six 96-package
  graphs omit the interpreter.
- Exact main allocation means are 4,186,712/70,680 bytes/objects for Event
  Routing, 512/11 for Distance Field, 35,536,416/2,220,986 for Fixed Width,
  598,182,443/12,232,484 for K-Nucleotide, 3,559,528/76,234.67 for Manifest,
  and 20,768,720/435,019.67 for Policy.
- No concrete owner is material in three unlike families. `ToInt`,
  `ToString`, and positional semantic structs each stop at two; checked
  arithmetic is sparse or already closed; allocator/GC parents have different
  Able descendants.
- No compiler, generated runtime, runtime package, interpreter, bytecode VM,
  canonical stdlib, benchmark, language, dependency, or WASM source changed.
  No A/B candidate was warranted.
- Added readable CPU/allocation reports and exact phase stats to the checked
  compiled architecture closure evidence. The ledger remains at 12
  invalidations because advancing one record cannot mask shared compiler
  scope drift.
- Retained 42 compact readable evidence files, then removed the exact 833 MiB
  disk-backed raw build/binary-profile workspace and generated Python caches.
- Next reconcile `compiled-current-control` causally across Fib, Matrix
  Multiply, and TapeLang. Scan strict generated code and current profiles for
  nullable-carrier reach; reprofile only if material reach exists. This is
  important because it restores current closure evidence without repeating
  native-control experiments selected only by a coarse directory hash.
- Record:
  `docs/perf-baselines/2026-07-30-post-nullable-compiled-architecture-owner-closure.md`.

## 2026-07-30 - Post-nullable compiled current-control reconciliation

- Retained no production change. Fresh strict Fib, Matrix Multiply, and
  TapeLang Alphabet builds each have 96-package dependency graphs without the
  interpreter, and all three public-verifier smoke processes pass.
- Fib and Matrix generated application bodies contain zero primitive-nullable
  carrier references. TapeLang reaches the carrier only in a one-time parser
  stack pop: 14 loop closures in the 275-byte public workload.
- The profiled material bodies remain carrier-free: direct `fib`, direct
  primitive `matmul`, and TapeLang `execute` plus `Tape.inc/get/move`.
  Existing exact ownership is therefore causally current without new
  CPU/allocation profiles.
- The post-carrier five-run ratios remain 1.2685x, 1.1581x, and 1.2459x Go.
  They are real misses but expose no common owner and no nullable candidate.
- Registered the reach evidence in `compiled-current-control`. The ledger
  retains all 12 invalidations because advancing only one record would mask
  compiler-scope drift shared by the other closures.
- Removed the exact 458 MiB raw build workspace, 125 MiB focused-test cache,
  and generated Python cache; no matching `/var/tmp` artifact remains.
- No compiler, runtime, interpreter, VM, canonical stdlib, benchmark,
  language, dependency, nominal special case, or WASM source changed.
- Next causally reconcile the six rows in `compiled-target-guards`, profiling
  only if the carrier reaches a material path. This is important because
  protecting compiled rows already at or above 95% of Go takes precedence over
  selecting another optimization.
- Record:
  `docs/perf-baselines/2026-07-30-post-nullable-compiled-current-control-reconciliation.md`.

## 2026-07-30 - Post-nullable compiled target guards preserved

- Retained no production change. Fresh strict Base64, Binary Trees, JSON,
  Monte Carlo Pi, PiDigits, and QuickSort builds all verify and omit the
  interpreter. Base64 has 119 native-codec graph packages; the others have 96.
- Primitive carrier reach is zero in every material protected owner. JSON's
  only nullable downstream reference is unchanged `?Error`; QuickSort reaches
  only an absent outer result while recursive sorting remains carrier-free.
- Current Able/Go ratios are 0.9344x, 1.0060x, 0.8235x, 1.0079x, 1.0100x,
  and 0.7138x. All six remain under the 1.052632x target threshold.
- No material changed path justified new profiles or timing. Added the causal
  record to `compiled-target-guards`; shared compiler drift keeps the ledger's
  12 invalidations selected until all affected closures are reviewed.
- Removed the exact 728 MiB raw build workspace, 125 MiB focused-test cache,
  and generated Python cache; no matching `/var/tmp` artifact remains.
- No compiler, runtime, interpreter, VM, canonical stdlib, benchmark,
  language, dependency, nominal special case, or WASM source changed.
- Next reconcile `compiled-iterator-control` using Generic Slot Buffer's
  retained nullable A/B evidence and scanning the other rows for material
  reach. This is important because that closure contains the allocation owner
  the carrier actually removed.
- Record:
  `docs/perf-baselines/2026-07-30-post-nullable-compiled-target-guards-reconciliation.md`.

## 2026-07-30 - Post-nullable compiled iterator/control reconciliation

- Reconciled all eight compiled iterator/control rows against the primitive
  nullable value carrier; retained no production change.
- All fresh strict binaries verify, contain 96 packages, and omit
  `pkg/interpreter`.
- Binary Event Log reaches `__able_nullable[int64]` through static
  `HashMap.raw_get`; Dependency Plan reaches `__able_nullable[int32]` through
  Queue/Deque dequeue; Generic Slot Buffer reaches `__able_nullable[int64]`
  through its direct generic nominal/interface path; and Option/Result Config
  reaches `__able_nullable[int32]` through specialized Option paths.
- Array Slice Window, Discrete Event Simulation, Document Audit, and Lexical
  Rollup have no material changed-carrier reach.
- The retained Generic Slot Buffer gate remains the causal proof: 0.0560 to
  0.0340 seconds and 264,215 to 1,046 exact main allocation objects.
- Post-carrier concrete residuals split among EventRecord conversion,
  Queue/graph work, direct native storage/arithmetic, and static
  generic-union/match work. No exact successor reaches three unlike
  applications, so no fresh profile or candidate was admitted.
- No compiler, runtime, interpreter, VM, stdlib, language, dependency, or
  WASM source changed.
- Removed the exact 921 MiB disk-backed build, binary, compiler, and Go-cache
  workspace after retaining the readable evidence.
- Next reconcile `compiled-text-map` from the retained Inventory and
  Transaction nullable A/B evidence, auditing all nine strict rows and
  profiling only a materially reached residual with three-application
  breadth. Dynamic map conversion remains an explicit semantic boundary and
  cannot justify a named-container compiler rule.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-nullable-compiled-iterator-control-reconciliation.md`.

## 2026-07-30 - Post-nullable compiled text/map reconciliation

- Reconciled all nine compiled text/map rows against the primitive nullable
  value carrier; retained no production change.
- All fresh strict binaries verify, contain 96 packages, and omit
  `pkg/interpreter`.
- Backup Dedup, Inventory Reconciliation, K-Nucleotide, Transaction Ledger
  Audit, and Word Frequency reach primitive carriers through map results.
  Manifest Normalization reaches `Option<String>`, while Sensor Calibration
  and Transaction reach `Result<i64>` match bindings. I Before E and Unicode
  Scalar Pipeline have no material reach.
- The retained Inventory gate remains 0.1220 to 0.1100 seconds and 553,060 to
  282,723 exact main allocation objects. Transaction remains 0.0480 to 0.0400
  seconds and 115,269 to 105,029 objects. Removed nullable recovery owners
  stay at zero.
- The only broad repeated residual is the named `HashMap` kernel's explicit
  dynamic boundary, which cannot authorize a named-container compiler rule.
  Parsing/`ToInt` stops at two unlike applications, and Manifest's owner is
  String/nominal work.
- No admissible successor reaches three unlike applications, so no fresh
  profile or candidate was admitted. No compiler, runtime, interpreter, VM,
  stdlib, benchmark, language, dependency, or WASM source changed.
- Regenerated the frontier and ledger, then removed the exact 1,164 MiB
  disk-backed raw workspace.
- Next reconcile the six `compiled-regex` rows, separating actual
  NFA/capture/state primitive-carrier reach from dormant support definitions
  and profiling only a still-open residual. This is important because regex
  has one shared owner where optional primitive positions could make the
  carrier change broadly causal.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-nullable-compiled-text-map-reconciliation.md`.

## 2026-07-30 - Post-nullable compiled regex reconciliation

- Reconciled all six compiled regex rows against the primitive nullable value
  carrier; retained no production change.
- All fresh strict binaries verify, contain 96 packages, and omit
  `pkg/interpreter`.
- Canonical NFA acceptance materially uses direct `value + valid` `int32`
  carriers in every row. Generated normal paths contain no runtime-value
  conversion and acceptance allocates zero objects.
- Fresh Config, Log, and Stream profiles merged 40 verified CPU processes
  each and added one exact verified allocation process each. Acceptance is
  1.19%-5.41% flat, entirely in the native state scan and `accepts_state`;
  nullable match boilerplate and bridge calls receive zero samples.
- The larger shared owners remain NFA closure, move, and thread upsert.
  Removing the scan requires the already-rejected state index, while arenas,
  character specialization, raw integers, Array members, and carrier/call
  alternatives remain closed.
- No compiler, runtime, interpreter, VM, stdlib, language, dependency,
  nominal special case, or WASM source changed. `go test ./cmd/ablec` passed.
- Removed the exact 1,092 MiB disk-backed raw workspace.
- Next reconcile the four `compiled-float-numeric` rows by strict rebuild and
  material nullable-`f64` reach, profiling only a shared exact residual. This
  is important because native float arithmetic is a direct test of Able's Go
  lowering goal and is more causal than repeating closed NFA work.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-nullable-compiled-regex-reconciliation.md`.

## 2026-07-30 - Post-nullable compiled float-numeric reconciliation

- Reconciled all four compiled float-numeric rows against the primitive
  nullable carrier; retained no production change.
- Fresh strict Distance Field, Mandelbrot, N-Body, and RMS Norm binaries all
  verify, contain 96 packages, and omit `pkg/interpreter`.
- Generated material bodies have zero nullable-`f64` sites. Scalar loops are
  native `float64`, and N-Body's primitive Arrays use direct `Elements`
  storage with no float conversion boundary.
- Only N-Body reaches the changed carrier: `advance` returns an
  allocation-free nullable `i32` loop result that its caller ignores. The
  other three rows have no material primitive-nullable reach.
- The retained Distance profile remains native `main`/`hypot`/`sqrt`, with
  512 bytes, 11 exact main allocations, and zero GC. Output bridges execute
  only once or twice per application.
- No changed residual reaches three rows, so no new profile, candidate, or
  production source change was admitted. `go test ./cmd/ablec` passed.
- Removed the exact 564 MiB disk-backed raw workspace and four standalone
  audit files.
- Next reconcile the three `compiled-wide-numeric` rows, separating primitive
  wide nullable carriers from Rational, Result, and record nominal encodings.
  This is important because the family has exact three-program breadth and
  substantial Go gaps, but must not acquire named-wide-type special cases.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-nullable-compiled-float-numeric-reconciliation.md`.

## 2026-07-30 - Post-nullable compiled wide-numeric reconciliation

- Reconciled all three compiled wide-numeric rows against the primitive
  nullable carrier; retained no production change.
- Fresh strict Fixed Width 128, Rational Series, and Wide Integer Records
  binaries all verify, contain 96 packages, and omit `pkg/interpreter`.
- Whole generated modules have zero nullable-wide construction sites and zero
  nullable-wide helper calls. Emitted nullable Int128/Uint128 support is
  dormant.
- Material internals use direct `runtime.Int128`/`runtime.Uint128` arithmetic,
  comparison, and `DivMod`. Pointer results are general UInt128, Int128,
  Rational, Result, and record nominal encodings and cannot be specialized by
  name.
- The retained repeated owner remains package publication through
  `SwapEnv`/`sync/atomic.StorePointer`; its broad alternatives already
  regressed unrelated programs.
- No changed residual exists to profile, so no candidate or production source
  change was admitted. `go test ./cmd/ablec` passed.
- Removed the exact 472 MiB disk-backed raw workspace.
- Next reconcile the two `compiled-byte-output` rows by strict rebuild and
  native `u8` Array/output reach. This is important because byte slices should
  lower directly, while two rows alone cannot satisfy the candidate breadth
  rule.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-nullable-compiled-wide-numeric-reconciliation.md`.

## 2026-07-30 - Post-nullable compiled byte-output reconciliation

- Reconciled FASTA Generation and Reverse Complement against the primitive
  nullable carrier and retained no production change.
- Fresh strict FASTA, Reverse Complement, and unlike Mandelbrot control
  binaries all verify, contain 96 packages, and omit `pkg/interpreter`.
- Generated application bodies have zero nullable-`u8` sites and zero runtime
  byte conversions. Byte Arrays use native `[]uint8` backing with direct
  indexed reads, assignment, and Go `append`.
- Reverse input wraps `os.ReadFile`'s byte slice directly. All three output
  paths borrow `bytes.Elements`; nullable-byte work is confined to text
  helpers and the conditional partial-write suffix.
- The only shared conversion is the required once-per-process host
  handle/result boundary. Retained material owners split among FASTA
  arithmetic, Reverse transform/backing/GC, and Mandelbrot pixel rendering.
- No shared changed residual exists, so no new profile, candidate, or
  production source change was admitted. `go test ./cmd/ablec` passed.
- Removed the exact 418 MiB disk-backed raw workspace.
- Next reconcile `compiled-sudoku-quotient` by strict rebuild and
  nullable-`i32`/Euclidean-division reach. This is important because a
  primitive operation must still repeat materially in three unlike programs.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-nullable-compiled-byte-output-reconciliation.md`.

## 2026-07-30 - Post-nullable compiled Sudoku quotient reconciliation

- Reconciled Sudoku Masks against the primitive nullable carrier and retained
  no production change.
- A fresh current-source strict binary passed its public verifier, contains 96
  packages, and omits `pkg/interpreter`.
- The nullable carrier reaches only `main`'s allocation-free absent result
  after all solve work. Material search functions contain zero nullable-`i32`,
  runtime-value, bridge, or compiler/interpreter boundary references.
- `square_index` retains exactly two signed-`i32` divmod calls. The shared
  positive-operand helper branch already uses native Go `/` and `%`.
- The retained two-profile merge puts the helper at 12.53% cumulative, but it
  remains material only in Sudoku. Rational uses distinct Int128 `DivMod`,
  Regex quotient work is cold, and K-Nucleotide quotient formatting is
  negligible.
- Perfect helper removal would leave the current 2.736637x ratio at 2.393736x,
  so no fresh profile, candidate, or A/B implementation was admitted.
- Focused division/carrier guards and `go test ./cmd/ablec` passed. Removed
  the exact 310 MiB disk-backed compiler, build, smoke, audit, and Go-cache
  workspace.
- Next reconcile all 23 `compiled-concurrency` rows for primitive-nullable
  reach at Future/channel/mutex boundaries. This matters because concurrency
  is the last unreviewed compiled family and remains 5.33x-19.65x Go.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-nullable-compiled-sudoku-quotient-reconciliation.md`.

## 2026-07-30 - Post-nullable compiled concurrency reconciliation

- Reconciled all 23 compiled concurrency rows against the primitive nullable
  carrier; retained no production change.
- All 23 fresh strict binaries pass their public verifiers, contain 96
  packages, and omit `pkg/interpreter`.
- The carrier materially reaches Await Channel Mux, Channel Rollup, Future
  Await Race, and Future Pipeline. Three consume native results directly; only
  Await crosses a nullable value through its explicit runtime-callable ABI.
- Await's residual nullable cast has one-application breadth. Mutex casts come
  from dynamic `Future.value()` results, not native nullable carriers, and do
  not establish a shared changed shape.
- Channel-handle reach remains 238,838 events in 14 rows and mutex-handle
  reach remains 43,016 events in three, exactly matching the pre-nullable
  census. Their retained profile disposition remains causal.
- The retained three-row `bridge.ToInt` leaf remains rejected because its
  global cache slowed TapeLang 4.17%; `currentGID` and nominal construction
  retain breadth two.
- No new profiles, timings, candidate, or production source change were
  admitted. Focused compiler guards, `go test ./cmd/ablec`, frontier tests,
  and evidence-ledger tests passed.
- Removed the exact 2,563 MiB disk-backed raw workspace.
- Next reconcile cross-family architecture ownership and, if it passes,
  advance all 12 compiler-scope closures together. This matters because it is
  the final causal review required to make the ledger current after the
  nullable compiler change.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-nullable-compiled-concurrency-reconciliation.md`.

## 2026-07-30 - Post-nullable cross-family ownership reconciliation

- Reconciled cross-family architecture ownership after all ten compiled
  family closures were causally reviewed; retained no production change.
- The family evidence covers all 65 compiled frontier rows exactly once, with
  current strict verifier success and zero interpreter dependencies.
- Primitive nullable carriers materially reach 22 applications, but their
  common operation is already a direct native validity check.
- `bridge.ToInt` is material across iterator/control, text/map, and
  concurrency. Its only general cache implementation remains rejected after a
  4.17% TapeLang regression, so real three-family reach does not reopen it.
- Other recurrent shapes are either explicit ABIs with at most two-family
  breadth, named-container boundaries, aggregate allocation parents, or
  previously rejected general mechanisms.
- No fresh profile, timing cohort, A/B implementation, compiler, runtime,
  interpreter, VM, stdlib, language, dependency, or WASM source change was
  admitted.
- The ledger's partial-advance guard correctly refused shared scope drift.
  The complete reviewed baseline was then rebased atomically: all 23 closures
  are current, invalidations are zero, and selection is empty.
- Frontier and ledger tests/direct checks and `go test ./cmd/ablec` passed.
  Removed the exact 122 MiB temporary Go cache; no tranche artifact remains.
- Next perform bounded v12 correctness/release verification rather than an
  unchanged performance rerun. This is important because future optimization
  work now requires a real checked invalidation.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-nullable-cross-family-architecture-ownership-reconciliation.md`.

## 2026-07-30 - Post-nullable correctness and release gate

- Completed the bounded v12 correctness/release tranche; retained no
  production change.
- `./run_all_tests.sh` passed every contract, all non-compiler packages, all
  34 compiler batches, and the complete bytecode fixture pass in 15:50.29
  with zero swaps.
- JSON replays of compiler batches 5, 20, 29, and 30 established slowest
  individual durations of 3.420s, 24.040s, 15.310s, and 10.720s.
- Interpreter JSON replays found no named test over one minute; fixture parity
  was slowest at 34.590s in short mode and 34.070s in bytecode mode.
- The unchanged canonical stdlib passed in both tree-walker (19s) and bytecode
  (14s) modes.
- Scorecard, five-sample evidence, frontier, and ledger checks passed: 130
  rows, zero actionable groups, 23 current closures, zero invalidations, and
  an empty selector.
- Removed the exact disk-backed release workspace after preserving the
  readable release record.
- Next refresh the retained/deferred worktree release inventory without
  staging, committing, or pushing. This matters because an exact inclusion
  boundary now carries more release value than another unchanged performance
  rerun.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-nullable-correctness-release-gate.md`.

## 2026-07-30 - Post-nullable release inventory

- Captured an immutable 324-row expanded worktree snapshot with an empty index
  and identical `HEAD`/`origin/master`.
- Classified exactly 290 retained paths and the unchanged 34 deferred WASM
  paths. No path is unmatched, multiply classified, out of v12/handoff scope,
  or newly WASM-like.
- The three inventory metadata files produce a 293-path retained candidate;
  its complete complement is exactly the deferred 34-path set.
- JSON, gzip, Go formatting, JavaScript/Python/shell syntax, whitespace,
  source-size, path, and secret checks passed. No dirty source reaches 1,000
  lines.
- Current scorecard, five-sample evidence, frontier, ledger, selector, and
  canonical stdlib source-state checks passed.
- Reclaimed 9.76 GiB from inactive project caches/temp directories and the
  unheld external-Go host cache. No generated project artifact remains.
- No repository or production mutation was performed or authorized.
- Next obtain explicit authorization before staging the exact retained
  pathspec and creating one local consolidation commit. A push remains
  separately gated.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-nullable-release-inventory.md`.

## 2026-07-30 - Post-publication security reconciliation

- Confirmed published commit `be9ecc505161085e1ec11f704571f589b3366c13`
  at local, tracking, and remote `master` with zero divergence.
- Active-v12 Go 1.26.5 production and test-inclusive vulnerability scans
  report zero reachable symbols and zero vulnerable imported packages. The
  sole module advisory remains unimported `x/crypto/openpgp`.
- Parser production and direct dependency OSV checks are clean; all v10/v11,
  v12 parser, and deferred-WASM npm lockfiles report zero npm findings.
- The 74-alert GitHub banner is one moderate lower than the prior attributed
  repository-wide count and is not an active-v12 reachability result.
- Module verification, tidy-diff, scorecard, frontier, ledger, and selector
  checks passed; retained no dependency or production change.
- Removed 441.74 MiB across three stale ignored benchmark target trees.
- The nested parser Go binding's own tests cannot load because its module path,
  dependency declaration, imports, and absent `go.sum` disagree.
- Next resolve that standalone binding contract, then rerun its sub-minute Go
  test/security scan and the official parser harness. Do not begin AST mapping
  or WASM work.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-publication-security-reconciliation.md`.

## 2026-07-30 - Post-nullable exact local consolidation

- The maintainer authorized exact staging of the verified 293 retained paths
  and one local commit; no push was authorized.
- Reproduced the exact candidate and 34-path deferred complement, refreshed
  the non-self retained identity, and staged only the NUL-delimited pathspec.
- Cached-path, whitespace, index-tree, scorecard, frontier, ledger, and
  complement checks passed before the single commit.
- The final worktree contains only the unchanged deferred WASM paths. The
  index is empty and local `master` is one commit ahead of `origin/master`.
- Next obtain explicit authorization before pushing the exact consolidation
  commit-to-branch refspec. This keeps remote publication independently
  reviewable.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-nullable-release-inventory.md`.

## 2026-07-30 - Parser Go binding contract retained

- Confirmed that v12 advertises an active Go binding and that the
  grammar-root module path, generated binding imports, and official
  Tree-sitter Go runtime agree.
- Removed the obsolete nested `bindings/go/go.mod`, which had shadowed the
  binding behind a contradictory module path and legacy runtime dependency.
- Added the root sum identity and tidy-required `go-pointer v0.0.1` indirect
  declaration without changing the retained `go-tree-sitter v0.24.0`
  dependency.
- The routine v12 runner now executes the explicit binding package under a
  one-minute test limit, preventing a future nested module from turning the
  check into a successful no-package warning.
- Standalone grammar loading, module tidy/verify, the official Able parser
  packages, runner syntax, and scoped whitespace checks pass. Every measured
  command stayed below one minute.
- Go 1.26.5 `govulncheck v1.6.0 -test` scanned the binding, external test
  package, test binary, three modules, and standard library with zero
  vulnerabilities.
- The complete default v12 runner passed every preflight, the new standalone
  binding gate, all non-compiler packages, all 34 compiler batches, and the
  bytecode fixture pass in 16:13.55 with zero swaps.
- No grammar, generated parser, binding implementation, AST, language,
  compiler, runtime, interpreter, VM, stdlib, benchmark, fixture, frozen
  workspace, performance evidence, or WASM source changed.
- Removed all disk-backed probe, Go, full-suite, scanner, toolchain, and
  raw-output state after preserving evidence, reclaiming 2.97 GiB.
- Next refresh the exact retained/deferred release inventory without staging,
  committing, or pushing. This protects the verified 34-path deferred WASM
  boundary while making the parser correction independently reviewable.
- Evidence:
  `docs/perf-baselines/2026-07-30-parser-go-binding-contract-retained.md`.

## 2026-07-30 - Post-parser release inventory

- Inventoried all 45 fully expanded pre-record dirty paths with an empty index
  at published `be9ecc505161085e1ec11f704571f589b3366c13`.
- The exact boundary is 11 retained security, parser-contract, runner, and
  handoff paths plus the unchanged 34 deferred WASM files. Adding the three
  inventory metadata paths yields a 14-path retained candidate.
- The 45-row TSV records Git state, dependency order, family, disposition,
  governing record, lines, bytes, SHA-256, and path. The deleted obsolete
  nested parser module is represented and verified as absent.
- All manifest identities reproduce. JSON, Go, JavaScript, shell, whitespace,
  source-size, scope, secret, parser-module, and cleanup checks pass.
- The 130-row five-sample scorecard remains current; the frontier has zero
  actionable groups; all 23 ledger closures are current with an empty
  selector; and the canonical 70-file external stdlib source hash is
  unchanged.
- The deferred path-list SHA-256 remains
  `7f29de27f03da83138df19696be182a5139aa6536ba0bdb363a1a23ffafd1f4e`;
  all 34 deferred content identities also reproduce.
- No code, dependency version, semantics, performance input, canonical stdlib,
  frozen workspace, or WASM source changed during the inventory. Nothing was
  staged, committed, or pushed.
- Next obtain explicit authorization before exact 14-path local
  consolidation. This protects the 34-path deferred complement and keeps any
  push independently gated.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-parser-release-inventory.md`.

## 2026-07-30 - Post-parser exact local consolidation

- The maintainer authorized exact staging of the verified 14 retained paths
  and one local commit; no push was authorized.
- Reproduced the exact candidate and 34-path deferred complement, refreshed
  the non-self retained identity, and staged only the NUL-delimited pathspec.
- Cached-path, whitespace, index-tree, parser-module, manifest, and complement
  checks passed before the single commit.
- The final worktree contains only the unchanged deferred WASM paths. The
  index is empty and local `master` is one commit ahead of `origin/master`.
- Next obtain explicit authorization before pushing the exact consolidation
  commit-to-branch refspec. This keeps remote publication independently
  reviewable.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-parser-release-inventory.md`.

## 2026-07-30 - Post-parser publication

- Explicitly published only
  `da8cb15bc394b6528dfd6a6b0eb1de30e12fef51` to `origin/master`.
- Pre-push checks proved the expected GitHub destination, exact remote parent,
  one-commit fast-forward, empty index, and unchanged 34-path deferred
  complement.
- Post-push `ls-remote`, local tracking identity, and ahead/behind checks all
  agree at `da8cb15b` with zero divergence.
- The repository-wide GitHub banner remains 74 alerts at the same severity
  distribution. No deferred WASM path entered history, and no other ref or
  commit was published.
- Next reconcile the exact clean published tree's main/parser modules,
  standalone binding test, and test-inclusive parser vulnerability result
  under disk-backed temporary state, then remove that state. This proves the
  shared artifact independently of the developer worktree.

## 2026-07-30 - Post-parser clean-tree publication reconciliation

- Reconciled exact published `da8cb15b` from a 10,953-file clean Git archive;
  the export excludes every untracked deferred WASM file.
- Main and parser module manifests, empty tidy diffs, and verification
  reproduce under Go 1.26.5.
- Standalone grammar loading passes in 15.80s. Test-inclusive
  `govulncheck v1.6.0` finds zero vulnerabilities in the same three reachable
  modules and reproduces the committed raw output hash exactly.
- Retained no code, dependency-version, semantic, benchmark, evidence, stdlib,
  frozen-workspace, or WASM change. Removed 1,223,340 KiB of disk-backed
  temporary state after recording results.
- Next inventory the exact five retained handoff/reconciliation paths and 34
  deferred WASM paths without repository mutation.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-parser-publication-reconciliation.md`.

## 2026-07-30 - Post-parser-publication release inventory

- Inventoried all 39 fully expanded pre-record dirty paths at published
  `da8cb15bc394b6528dfd6a6b0eb1de30e12fef51`, with an empty index and zero
  local, tracking, or remote divergence.
- The exact boundary is five retained reconciliation/handoff paths plus the
  unchanged 34 deferred WASM files. Adding the three inventory metadata files
  yields an exact eight-path retained candidate.
- The 39-row TSV records Git state, dependency order, family, disposition,
  governing record, lines, bytes, SHA-256, and path. Every identity
  reproduces, and the deferred content/path set exactly matches the prior
  authoritative inventory.
- JSON, Go, JavaScript, whitespace, source-size, scope, secret, scorecard,
  frontier, ledger, selector, canonical-stdlib, Git-index, remote, and cleanup
  checks pass.
- No production, dependency-version, compiler, runtime, interpreter,
  bytecode-VM, canonical-stdlib, language, benchmark, fixture, frozen v10/v11,
  or WASM change occurred. Nothing was staged, committed, or pushed.
- Next obtain explicit authorization before exact eight-path local
  consolidation. This protects the 34-path deferred complement and keeps any
  push independently gated.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-parser-publication-release-inventory.md`.

## 2026-07-30 - Post-parser-publication exact local consolidation

- The maintainer authorized exact staging of the verified eight retained paths
  and one local commit; no push was authorized.
- Reproduced the exact candidate and 34-path deferred complement, refreshed
  the non-self retained identity, and staged only the NUL-delimited pathspec.
- Cached-path, whitespace, index-tree, manifest, evidence-ledger, and
  complement checks passed before the single commit.
- The final worktree contains only the unchanged deferred WASM paths. The
  index is empty and local `master` is one commit ahead of `origin/master`.
- Next obtain explicit authorization before pushing the exact consolidation
  commit-to-branch refspec. This keeps remote publication independently
  reviewable.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-parser-publication-release-inventory.md`.

## 2026-07-30 - Post-parser-publication reconciliation publication

- Explicitly published only
  `e7a054032e189e9a606dad68c827be50af897417` to `origin/master`.
- Pre-push checks proved the expected GitHub destination, exact remote parent,
  one-commit fast-forward, empty index, exact eight-path commit boundary, and
  unchanged 34-path deferred complement.
- Post-push `ls-remote`, local tracking identity, and ahead/behind checks all
  agree at `e7a05403` with zero divergence.
- The repository-wide GitHub banner remains 74 alerts at the same severity
  distribution. No deferred WASM path entered history, and no other ref or
  commit was published.
- Next reconcile the exact clean published tree's reconciliation/inventory
  identities and unchanged module/performance inputs under disk-backed
  temporary state, then remove that state. This proves the shared artifact
  independently of the developer worktree.

## 2026-07-30 - Post-parser-publication shared-tree reconciliation

- Reconciled exact published `e7a05403` from a clean 10,958-file Git archive,
  excluding every untracked deferred WASM file.
- The eight-path commit set, record/manifest hashes, refreshed non-self
  identity, and main/parser module identities reproduce exactly. No tested
  code, dependency, or evidence input changed across the publication commit.
- The 130-row frontier remains current with zero actionable groups.
- Clean relocation reveals that all 31 selected reports retain absolute
  original-workspace paths: 161 occurrences across source and reference-report
  fields. The scoreboard and five-sample validators therefore cannot resolve
  those reports inside the archive.
- The native `bytecode-production` ledger baseline contains 206 files because
  it includes three untracked deferred `*_wasm.go` proof files. The clean
  committed tree has 203 files, causing 12 false bytecode-scope invalidations.
- Developer-worktree checks still report 23 current closures and zero
  invalidations, proving the discrepancy is location/scope contamination
  rather than a publication-code change.
- Retained no production or WASM change and removed the 139,996 KiB
  disk-backed workspace.
- Next make evidence paths relocatable, exclude WASM-only files from the native
  bytecode scope, rebuild the ledger from committed inputs, and guard both a
  dirty deferred worktree and a relocated clean export.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-parser-publication-shared-tree-reconciliation.md`.

## 2026-07-30 - Relocatable performance-evidence gates retained

- Added a shared active-repository resolver for retained `v12/...` provenance
  and used it in the scoreboard and five-sample evidence checker.
- Four regressions prove ordinary retained paths, relocated retained reports,
  genuinely external report rejection, and relocated measured Able sources.
- Native `bytecode-production` hashing now excludes `*_wasm.go`; a focused
  regression proves deferred WASM-only edits cannot select native bytecode
  closures.
- Rebuilt the checked baseline from 203 committed native files with tree hash
  `4c6b98e…`. All 23 closures remain current and the evaluated JSON/Markdown
  reports remain byte-identical.
- The developer worktree and a relocated 10,959-file source export both pass
  the 130-row scoreboard, five-sample evidence check, zero-actionable frontier,
  and zero-invalidation ledger. The export contains none of the three deferred
  WASM proof files.
- No runtime, compiler, interpreter, VM, language, dependency, benchmark,
  stdlib, fixture, or WASM source changed.
- Removed the 140,068 KiB disk-backed validation workspace.
- Next inventory the retained correction and unchanged deferred WASM boundary
  exactly, without repository mutation.
- Evidence:
  `docs/perf-baselines/2026-07-30-relocatable-performance-evidence-gates-retained.md`.

## 2026-07-30 - Post-relocatable-evidence release inventory

- Inventoried all 48 fully expanded pre-record dirty paths at published
  `e7a054032e189e9a606dad68c827be50af897417`, with an empty index and zero
  local, tracking, or remote divergence.
- The exact boundary is 14 retained shared-tree reconciliation,
  evidence-tooling, evidence-record, and handoff paths plus the unchanged 34
  deferred WASM files. Adding three inventory metadata files yields an exact
  17-path retained candidate.
- The 48-row TSV records Git state, dependency order, family, disposition,
  governing record, lines, bytes, SHA-256, and path. Every identity reproduces,
  and the deferred content/path set exactly matches the prior inventory.
- JSON, Go, JavaScript, Python, whitespace, source-size, scope, secret,
  scorecard, five-sample, frontier, ledger, canonical-stdlib, Git-index,
  remote, and cleanup checks pass.
- No production, dependency-version, compiler, runtime, interpreter,
  bytecode-VM, parser, canonical-stdlib, language, benchmark-measurement,
  fixture, frozen v10/v11, or WASM change occurred during the inventory.
  Nothing was staged, committed, or pushed.
- Next obtain explicit authorization before exact 17-path local consolidation.
  This protects the 34-path deferred complement and keeps any push
  independently gated.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-relocatable-evidence-release-inventory.md`.

## 2026-07-30 - Post-relocatable-evidence exact local consolidation

- The maintainer authorized exact staging of the verified 17 retained paths
  and one local commit; no push was authorized.
- Reproduced the exact candidate and 34-path deferred complement, refreshed
  the non-self retained identity, and staged only the NUL-delimited pathspec.
- Cached-path, whitespace, index-tree, manifest, scorecard, frontier, ledger,
  and complement checks passed before the single commit.
- The final worktree contains only the unchanged deferred WASM paths. The
  index is empty and local `master` is one commit ahead of `origin/master`.
- Next obtain explicit authorization before pushing the exact consolidation
  commit-to-branch refspec. This keeps remote publication independently
  reviewable.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-relocatable-evidence-release-inventory.md`.

## 2026-07-30 - Relocatable evidence correction publication

- Explicitly published only
  `9a4eac717e1a46e10195e17c562b389fa452dcfd` to `origin/master`.
- Pre-push checks proved the expected GitHub destination, exact remote parent,
  one-commit fast-forward, empty index, exact 17-path commit boundary, current
  evidence gates, and unchanged 34-path deferred complement.
- Post-push `ls-remote`, local tracking identity, and ahead/behind checks all
  agree at `9a4eac71` with zero divergence.
- The repository-wide GitHub banner remains 74 alerts at the same severity
  distribution. No deferred WASM path entered history, and no other ref or
  commit was published.
- Next run the scoreboard, five-sample, frontier, ledger, and focused
  relocation/scope tests from an exact clean archive with the canonical
  adjacent stdlib, then remove all disk-backed task state. This proves the
  published correction independently of the developer worktree.

## 2026-07-30 - Published evidence clean-tree reconciliation

- Exported exact published `9a4eac71`, tree `eed160e4`, and 10,966 files to
  disk-backed `/var/tmp`, with no deferred bytecode WASM proof file.
- The adjacent canonical stdlib reproduces 70 runtime sources, scorecard tree
  `6a412c87…`, and ledger tree `382d256e…`.
- The scoreboard, five-sample gate, four relocation tests, frontier, and five
  frontier tests pass: 130 rows and zero actionable groups.
- The ledger fails solely because its checked canonical-stdlib definition
  stores `/home/david/sync/projects/able-stdlib/src`, while the clean export
  uses an adjacent `/var/tmp/.../able-stdlib/src`. File count, include/exclude
  rules, and content tree are identical.
- All 23 closures select `scope-definition-drift:canonical-stdlib`; seven of
  eight ledger tests pass. A developer-path override restores 23 current
  closures, proving the remaining hidden path dependency.
- Retained no code and removed the exact 141,040 KiB workspace.
- Next canonicalize default external-scope identity, preserve complete content
  hashing, add relocation and real-drift guards, rebuild the ledger, and
  rerun both environments. This makes performance evidence portable between
  identical checkouts.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-relocatable-evidence-publication-reconciliation.md`.

## 2026-07-30 - Canonical stdlib scope identity relocatable

- Scope overrides now relocate reads without replacing the configured
  semantic root stored in the closure baseline.
- Two regressions prove identical content at separate roots stays current and
  changed relocated content selects all 23 canonical-stdlib-dependent
  closures.
- Rebuilt the baseline with root `../able-stdlib/src`; its SHA-256 changed
  from `15fcba70…` to `aaf867b3…`, while evaluated JSON and Markdown remain
  byte-identical.
- Both the dirty worktree and a clean published-tree export pass the 130-row
  scoreboard, complete five-sample evidence, four path tests, zero-actionable
  frontier, five frontier tests, 23-current-closure ledger, and ten ledger
  tests with one conditional skip.
- The clean export used the canonical adjacent 70-file stdlib with no
  developer path or scope override and contained no deferred bytecode WASM
  proof file.
- No production or WASM behavior changed. Removed the exact 140,948 KiB
  disk-backed workspace.
- Next inventory the retained correction and records against the unchanged
  34-path deferred boundary without repository mutation. This keeps future
  consolidation exact.
- Evidence:
  `docs/perf-baselines/2026-07-30-canonical-stdlib-scope-identity-relocatable-retained.md`.

## 2026-07-30 - Canonical stdlib scope release inventory

- Inventoried all 42 fully expanded pre-record dirty paths at published
  `9a4eac71`, with an empty index and zero local/tracking/remote divergence.
- The exact boundary is eight retained reconciliation, ledger, record, and
  handoff paths plus the unchanged 34 deferred WASM files. Adding three
  inventory metadata files yields an exact 11-path candidate.
- The 42-row TSV records Git state, dependency order, family, disposition,
  governing record, lines, bytes, SHA-256, and path. Every identity reproduces.
- Format, syntax, whitespace, source-size, scope, secret, stdlib, scoreboard,
  five-sample, frontier, ledger, selector, index, remote, and cleanup gates
  pass.
- No production or WASM behavior changed. Nothing was staged, committed, or
  pushed.
- Next obtain explicit authorization before staging only the 11-path
  NUL-delimited candidate and making one local commit. This protects the
  unchanged 34-path deferred complement and keeps publication separately
  reviewable.
- Evidence:
  `docs/perf-baselines/2026-07-30-canonical-stdlib-scope-release-inventory.md`.

## 2026-07-30 - Canonical stdlib scope exact local consolidation

- The maintainer authorized exact staging of the verified 11 retained paths
  and one local commit; no push was authorized.
- Reproduced both pathspec hashes, the final eight-file non-self identity,
  expected remote parent, empty index, and exact 34-path deferred complement.
- Staged only the NUL-delimited candidate. Cached and unstaged complements
  were exact, and all scorecard, repeated-evidence, frontier, ledger,
  selector, ledger-test, and whitespace gates passed.
- Created
  `e3a4e1e8000aeb09d80a3a1adc48548b9deeeb59`, parent `9a4eac71`, with
  exactly 11 paths.
- The immediate post-commit residual was precisely the unchanged deferred
  WASM boundary; the index was empty and local `master` was one commit ahead.
- Next obtain explicit authorization before publishing that exact commit.
  This keeps remote mutation separately reviewable.
- Evidence:
  `docs/perf-baselines/2026-07-30-canonical-stdlib-scope-release-inventory.md`.

## 2026-07-30 - Canonical stdlib scope correction publication

- Explicitly published only
  `e3a4e1e8000aeb09d80a3a1adc48548b9deeeb59` to `origin/master`.
- Pre-push checks proved the GitHub destination, exact `9a4eac71` parent,
  one-commit fast-forward, empty index, exact 11-path commit identity, green
  evidence gates, and unchanged deferred complement.
- Post-push `ls-remote`, local tracking identity, and ahead/behind checks all
  agree at `e3a4e1e8` with zero divergence.
- The repository-wide GitHub banner remains 74 alerts at the same severity
  distribution. No deferred WASM path, additional commit, or other ref was
  published.
- Next export exact `e3a4e1e8` to disk-backed `/var/tmp`, attach the canonical
  adjacent stdlib, rerun every evidence/path/frontier/ledger gate without an
  override, and remove the workspace. This proves the shared-history ledger
  is genuinely checkout-independent.
- Evidence:
  `docs/perf-baselines/2026-07-30-canonical-stdlib-scope-release-inventory.md`.

## 2026-07-30 - Canonical stdlib scope publication reconciliation

- Exported exact published `e3a4e1e8`, tree `877da26e`, and 10,971 files to
  disk-backed `/var/tmp`, with no deferred bytecode WASM proof file.
- The adjacent canonical stdlib reproduces 70 runtime sources, scorecard tree
  `6a412c87…`, ledger tree `382d256e…`, and semantic root
  `../able-stdlib/src`.
- Without a scope override or developer path, the scoreboard, complete
  five-sample evidence, four relocation tests, frontier, five frontier tests,
  ledger, selector, and ten ledger tests pass: 130 rows, zero actionable
  groups, 23 current closures, and zero invalidations.
- Every published evidence-tool and checked-artifact identity reproduces. No
  production or WASM behavior changed.
- Removed the exact 141,004 KiB workspace.
- Next inventory the four retained handoff/reconciliation paths against the
  unchanged 34-path deferred boundary without repository mutation. This keeps
  any later consolidation exact.
- Evidence:
  `docs/perf-baselines/2026-07-30-canonical-stdlib-scope-publication-reconciliation.md`.

## 2026-07-30 - Canonical stdlib scope publication release inventory

- Inventoried all 38 pre-record dirty paths at published `e3a4e1e8`.
- The exact boundary is four retained handoff/reconciliation paths plus the
  unchanged 34 deferred WASM files. Three metadata files produce an exact
  seven-path candidate.
- The 38-row manifest and all format, syntax, whitespace, source-size, scope,
  secret, stdlib, scoreboard, repeated-evidence, frontier, ledger, selector,
  index, remote, and cleanup gates pass.
- No production or WASM behavior changed. Nothing was staged, committed, or
  pushed.
- Next obtain explicit authorization before staging only the seven-path
  candidate and making one local commit. This keeps deferred work and remote
  publication separately gated.
- Evidence:
  `docs/perf-baselines/2026-07-30-canonical-stdlib-scope-publication-release-inventory.md`.

## 2026-07-30 - Canonical stdlib publication reconciliation published

- Explicitly published only
  `9c32f2777536da2c948327720acc75187973a6d9` to `origin/master`.
- Pre-push checks proved the expected GitHub destination, exact `e3a4e1e8`
  remote parent, one-commit fast-forward, empty index, exact seven-path commit
  identity, current evidence gates, and unchanged deferred complement.
- The push used only
  `9c32f2777536da2c948327720acc75187973a6d9:refs/heads/master`.
- Post-push `ls-remote`, local tracking identity, and ahead/behind checks all
  agree at `9c32f277` with zero divergence.
- The repository-wide GitHub banner remains 74 alerts at the same severity
  distribution. No deferred WASM path, additional commit, or other ref was
  published.
- The index is empty, and the current residual is three handoff files plus
  the unchanged 34-path deferred boundary.
- Removed the disk-backed publication workspace.
- Next refresh strict interpreter-free compiled CPU and allocation profiles
  across at least three unlike applications and select the largest shared
  generated-code/runtime owner. This returns the workstream to evidence-backed
  native-lowering progress toward Go performance.

## 2026-07-30 - Post-publication compiled profile admission closure

- Applied the checked invalidation gate before scheduling another compiled
  profile cohort.
- The current compiler identity already has a six-family strict profile
  refresh: ten CPU and three exact allocation profiles per application, all
  verifier-backed, with 96-package graphs that omit `pkg/interpreter`.
- The complete 65-row compiled ownership partition and the profile cohort
  expose no open exact owner with three-unlike-family breadth.
- All checked production, benchmark, row, and evidence identities reproduce.
  The 130-row scoreboard, five-sample gate, four path tests, frontier, five
  frontier tests, 23-current-closure ledger, empty selector, and ten ledger
  tests pass.
- `go test ./cmd/ablec` passes in 6.090 seconds.
- Retained no production or WASM change and admitted no duplicate measurement
  or A/B implementation.
- Removed the exact 127,280 KiB disk-backed test workspace and a six-file
  generated Python cache.
- Next audit the active non-WASM correctness backlog and bounded test signals
  for one reproducible v12 defect. Any changed checked scope will then select
  the exact performance evidence that must be refreshed.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-publication-compiled-profile-admission-closure.md`.

## 2026-07-30 - Implicit generic redeclaration Go fixture coverage restored

- The non-WASM correctness audit found one stale active-target exclusion:
  `errors/implicit_generic_redeclaration` skipped Go even though the specified
  diagnostic is implemented and unit-tested.
- Removed the stale manifest exclusion. Strict direct replay now emits the
  exact source-attributed diagnostic and reports `skipped: false`.
- Focused fixture/checker tests, all error fixtures, the full typechecker
  package, and fixture CLI tests pass.
- The default v12 runner passes all preflights, non-compiler packages, 34
  compiler batches, and the complete bytecode fixture corpus.
- An initial artificial 55-second package cap stopped cumulative packages;
  the runner's designed package timeout passed with no correctness failure.
- No production or performance scope changed. All 23 performance closures
  remain current, invalidations are zero, and the selector is empty.
- Removed the exact 2,660,460 KiB disk-backed test workspace.
- Next add a general fixture-inventory guard rejecting unclassified active Go
  exclusions. This prevents a stale manifest flag from silently removing
  canonical coverage again.
- Evidence:
  `docs/perf-baselines/2026-07-30-implicit-generic-redeclaration-go-fixture-coverage-retained.md`.

## 2026-07-30 - Canonical stdlib publication reconciliation local consolidation

- The maintainer authorized exact staging of the verified seven retained
  paths and one local commit; no push was authorized.
- Reproduced both 390-byte pathspec hashes, the final four-file non-self
  identity, expected published parent, empty index, and exact 34-path deferred
  complement.
- Staged only the NUL-delimited candidate. Cached and residual sets were
  exact, and all scoreboard, repeated-evidence, path, frontier, ledger,
  selector, ledger-test, and whitespace gates passed.
- Created
  `9c32f2777536da2c948327720acc75187973a6d9`, parent `e3a4e1e8`, with
  exactly seven paths.
- The immediate post-commit residual was precisely the unchanged deferred
  WASM boundary; the index was empty and local `master` was one commit ahead.
- No production or WASM behavior changed, and nothing was pushed.
- Removed the disk-backed consolidation workspace; no task-owned temporary
  file remains.
- Next obtain explicit authorization before publishing only `9c32f277`. This
  keeps remote mutation separately reviewable and protects the deferred
  boundary.
- Evidence:
  `docs/perf-baselines/2026-07-30-canonical-stdlib-scope-publication-release-inventory.md`.

## 2026-07-30 - Active Go fixture target-exclusion guard retained

- Added a target-lifecycle policy that treats `go` as active and `ts` as
  retired. Active exclusions now require an exact classified and reasoned
  allowlist entry.
- Added a general deterministic validator for all 176 fixture manifests. It
  rejects malformed or duplicate targets, unknown targets, invalid policy
  entries, unclassified active exclusions, and stale allowlist entries.
- Added seven policy tests and made the live inventory plus tests a default
  runner preflight.
- The current repository has zero active exclusions, one retired exclusion,
  and no allowlist entries.
- The full v12 runner passes every preflight, all non-compiler packages, all
  34 compiler batches, and the complete bytecode fixture corpus.
- The 130-row scoreboard has zero actionable frontier groups; the performance
  ledger has 23 current closures, zero invalidations, and an empty selector.
- No production or performance input changed. No compiler, runtime,
  interpreter, VM, stdlib, benchmark, language, dependency, or WASM source
  changed, and nothing was staged, committed, or pushed.
- Removed the exact 2,440,504 KiB disk-backed test workspace.
- Next audit conditional non-WASM Go test skips and harness opt-outs, retaining
  policy only for a reproducible unclassified semantic blind spot. This
  protects native-lowering correctness from silent loss of test coverage.
- Evidence:
  `docs/perf-baselines/2026-07-30-active-go-fixture-target-exclusion-guard-retained.md`.

## 2026-07-30 - Default parser fixture-corpus lane retained

- Classified all 140 explicit skip calls across 617 active non-WASM Go test
  files with zero unknown mechanisms.
- Reproduced one default-handoff blind spot: all 16 parser fixture families
  skipped under `-short` while the parser package still returned success.
- A non-short replay passed all 16 families and all 176 source fixture
  subtests in 0.125 seconds.
- The default runner now executes `./pkg/parser` once in full mode under a
  one-minute bound and removes it from the remaining short-mode package set.
  Its integrated result passed in 0.149 seconds.
- Every preflight, remaining non-compiler package, all 34 compiler batches,
  and the complete bytecode fixture pass remain green.
- The 130-row scorecard/frontier and 23-entry ledger remain current with zero
  actionable groups, zero invalidations, and an empty selector.
- No production or performance input changed. Nothing was staged, committed,
  pushed, or changed under the deferred WASM boundary.
- Removed the exact 2,440,980 KiB disk-backed workspace and 20,483-byte raw
  inventory file.
- Next audit silent early-return and build-tag test suppression. This is
  important because those controls can hide semantic coverage without a
  visible skip event.
- Evidence:
  `docs/perf-baselines/2026-07-30-default-parser-fixture-corpus-lane-retained.md`.

## 2026-07-30 - Silent Go test-suppression audit closed

- A temporary Go AST audit parsed 617 active non-WASM test files and 3,226
  top-level tests.
- All 21 top-level returns classify as child/helper process coordination,
  successful search before failure, or bounded polling success; none silently
  bypasses semantic assertions.
- All 33 environment-reading functions with returns are explicit child,
  fixture/batch/configuration, benchmark, or path helpers.
- The three test build constraints are either native-active or the documented
  `able_bytecode_box_reuse` diagnostic lane. That tag adds exactly two tests,
  both passing, and excludes no normal semantic test.
- Twenty of 27 active non-WASM packages own test files; the other seven are
  deliberate support/command packages. The default compiler listing has 836
  unique tests partitioned exactly into 34 shards.
- Focused child-process, search, polling, and tagged tests pass under
  one-minute limits.
- Retained no code. The unchanged runner/source identity retains its immediate
  prior full-suite pass, while the 130-row frontier and 23-entry ledger remain
  current with zero invalidations and an empty selector.
- Removed the exact 458,224 KiB disk-backed audit workspace.
- Next inventory the exact 18-path retained non-WASM candidate against the
  unchanged 34 deferred WASM paths without staging or publishing. This makes
  later consolidation safe and reviewable.
- Evidence:
  `docs/perf-baselines/2026-07-30-silent-go-test-suppression-audit-closure.md`.

## 2026-07-30 - Post-correctness release inventory verified

- Captured a 52-path pre-record snapshot using fully expanded untracked
  status.
- The exact manifest classifies 18 retained non-WASM paths and the unchanged
  34 deferred WASM paths with state, review order, governing record, line,
  byte, and SHA-256 identities.
- The three inventory metadata files produce an exact 21-path retained
  candidate. Its only complement is the published 34-path deferred WASM hold.
- All 52 manifest identities reproduce. JSON, Go formatting, JavaScript
  syntax, whitespace, final-newline, source-size, scope, secret-signature,
  canonical-stdlib, scorecard, frontier, ledger, selector, index, remote, and
  cleanup gates pass.
- The current runner source retains its complete full-suite result: every
  preflight and non-compiler package, all 34 compiler batches, and the
  complete bytecode fixture pass.
- Removed an empty generated Go workspace and 316 KiB of Python bytecode
  cache. A follow-up guarded cleanup dry run is empty.
- No production or evidence input changed during the inventory. Nothing was
  staged, committed, pushed, or modified inside the deferred boundary.
- The maintainer subsequently authorized exact 21-path staging and one local
  consolidation commit. No push or broad staging operation is authorized.
- After consolidation, next obtain explicit authorization before publishing
  only that commit. This matters because remote mutation remains separately
  reviewable and deferred WASM must remain outside shared history.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-correctness-release-inventory.md`.

## 2026-07-30 - Post-correctness consolidation published

- Pushed only
  `f3f3c92d512d313648563503904528e307b7c11e:refs/heads/master`.
- Local, tracking, and remote `master` now agree with zero divergence, and the
  index is empty.
- The published commit reproduces the authorized 21-path set, while the
  developer worktree contains exactly the unchanged 34 deferred WASM paths.
- An independent clean archive contains no deferred untracked file and no
  tracked deferred dirty identity.
- The clean export passes the 176-manifest fixture inventory, all seven policy
  tests, 130 fully sampled scoreboard rows, path-relocation and frontier
  tests, all 23 current ledger closures, and the empty selector.
- The canonical 70-file stdlib source identity reproduces; the unchanged
  runner retains its complete full-suite pass.
- No production or evidence input changed. The publication workspace stayed
  under disk-backed `/var/tmp`, and Python bytecode generation was disabled.
- Next create an exact non-mutating inventory of the five retained
  reconciliation/handoff paths and unchanged 34 deferred WASM paths. This
  closes publication without blurring the next consolidation boundary.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-correctness-publication-reconciliation.md`.

## 2026-07-30 - Post-correctness publication release inventory verified

- Captured a fully expanded 39-path pre-record snapshot.
- The exact manifest classifies five retained publication
  reconciliation/handoff paths and the unchanged 34 deferred WASM paths with
  state, review order, governing record, line, byte, and SHA-256 identities.
- The three inventory metadata files produce an exact eight-path retained
  candidate whose only complement is the deferred WASM hold.
- All format, syntax, source-size, scope, secret, fixture-policy, scoreboard,
  frontier, ledger, selector, Git-identity, and cleanup gates pass.
- Shared history remains converged at `f3f3c92d`; the index is empty.
- No production or evidence input changed. Nothing was staged, committed,
  pushed, or modified inside the deferred boundary.
- The maintainer subsequently authorized exact eight-path staging and one
  local consolidation commit. No push or broad staging operation is
  authorized.
- After consolidation, next obtain explicit authorization before publishing
  only that commit. This keeps remote mutation separately reviewable and
  deferred WASM outside shared history.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-correctness-publication-release-inventory.md`.

## 2026-07-30 - Post-correctness publication handoff published

- Pushed only
  `8be1779aeac40e7590b852626141d01ad57deba0:refs/heads/master`.
- Local, tracking, and remote `master` now agree with zero divergence; the
  index is empty.
- The exact eight-path commit excludes the unchanged 34 deferred WASM paths.
- A clean archive contains no deferred untracked file or tracked deferred
  dirty identity and passes the fixture-policy and performance-evidence
  gates.
- The canonical stdlib identity reproduces, and the exact runner source
  retains its earlier complete full-suite pass.
- No production or evidence input changed. All task state stayed under
  disk-backed `/var/tmp` with Python bytecode disabled.
- Next create an exact non-mutating inventory of the five retained
  reconciliation/handoff paths and unchanged deferred WASM boundary.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-correctness-publication-handoff-reconciliation.md`.

## 2026-07-30 - Publication handoff release inventory verified

- Captured a fully expanded 39-path pre-record snapshot.
- The manifest classifies five retained publication-handoff paths and the
  unchanged 34 deferred WASM paths with exact state, line, byte, and SHA-256
  identities.
- The three metadata files form an exact eight-path retained candidate.
- Fixture-policy, performance-evidence, formatting, syntax, Git, scope, and
  cleanup gates pass.
- Shared history remains converged at `8be1779a`; the index is empty.
- Nothing was staged, committed, pushed, or modified inside the deferred
  boundary.
- The maintainer subsequently authorized exact eight-path staging and one
  local consolidation commit. No push or broad staging operation is
  authorized.
- After consolidation, next obtain explicit authorization before publishing
  only that commit.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-correctness-publication-handoff-release-inventory.md`.

## 2026-07-30 - Performance direction model refresh retained

- The performance-direction audit exposed stale non-default architecture
  evidence despite current default scorecard, frontier, and ledger gates.
  Several inputs pinned the former frontier/ledger identities and four tests
  asserted obsolete 128-row aggregates.
- Rebased and regenerated the residual, compiled/bytecode budget,
  semantic-region, native-tier, cross-engine, structural-strategy,
  backend-ADR, semantic-ABI, and shared-runtime cutover chain on the current
  130-row frontier.
- Current selection remains closed: 10 meets, 120 misses, 273.420211 seconds
  target excess, no eligible residual/compiled/semantic-region mechanism, no
  hot-function/backend candidate, and one bounded shared-runtime family versus
  three required.
- Added all fast architecture checks to the default runner and set
  `PYTHONDONTWRITEBYTECODE=1` so future frontier drift fails closed without
  leaving repository cache artifacts.
- All architecture contract and exact-artifact tests pass, including the
  shared semantic ABI Go packages. The 23-entry performance ledger remains
  current with an empty selector.
- The updated default runner passes all preflights and non-compiler packages,
  all 34 compiler batches, and the bytecode fixture corpus.
- No production or performance-input behavior changed. Removed the 142,316
  KiB architecture cache, 2,400,964 KiB full-run cache, 272 KiB direction
  workspace, and generated Python cache.
- Next audit sustained multi-feature workload depth and add at most one
  source-equivalent application only for a material gap. This creates a
  legitimate new evidence identity without retrying a closed mechanism.
- Evidence:
  `docs/perf-baselines/2026-07-30-performance-direction-model-refresh-retained.md`.

## 2026-07-30 - Sustained multi-feature workload-depth audit retained

- Joined the 65 current applications to feature families, interaction
  priorities, operation evidence, repeated scorecard timing, and compiled
  target excess.
- The general policy classifies at least six of 11 discriminating families as
  broad and a five-process Go mean of at least 0.200 seconds as sustained.
- Exactly 23 rows are broad and ten are sustained, but none is both. The
  longest broad Go row is 0.011 seconds; the broadest sustained rows cover
  three families.
- Added a checked JSON artifact, five fast contract tests, and a default
  runner gate. Repaired stale pair/triple interaction assertions for Generic
  Slot Buffer and current frontier excess; both standalone suites now pass.
  The complete default runner passes. Retained no benchmark, profile, or
  production behavior change.
- Next implement one genuinely new sustained multi-feature application in
  Able, Go, Python, and Ruby, then refresh the affected evidence identities.
- Evidence:
  `docs/perf-baselines/2026-07-30-sustained-multi-feature-workload-depth-audit.md`.

## 2026-07-30 - Versioned Telemetry Pipeline application gate retained

- Added the 66th portable application in Able, Go, Python, and Ruby. Its
  sustained loop combines application-owned generic nominal storage, static
  Arrays, nullable and Result flow, interface dispatch, iterators, a capturing
  closure, nominal construction, control flow, and primitive arithmetic.
- The default compiled/Go contract performs 16.5 million updates; `--short`
  performs 160,000 updates for bytecode/Python/Ruby. Canonical and external
  Able sources are byte-identical, and all language outputs pass the public
  verifier.
- On CPU 12, five strict Able processes averaged 51.4180 seconds versus five
  Go 1.26.5 processes at 0.2201 seconds. Five bytecode processes averaged
  3.3040 seconds versus Python at 0.2023 and Ruby at 0.1258 seconds. Every lane
  had 5/5 successful verified runs with zero timeout or failure.
- The new row covers ten of 11 discriminating families and ten operation-depth
  entries. The sustained-depth audit now reports 66 applications, 24 broad,
  11 sustained, and one intersection, so the material depth gap is closed.
- The strict dependency graph omits `pkg/interpreter`. A 51.57-second
  main-only CPU profile puts 34.78 seconds (67.44%) beneath the erased local
  scorer and 35.46 seconds (68.76%) beneath `__able_binary_op`. The sampled
  allocation profile places 608,030,785 objects (73.26%) beneath the scorer
  and 633,056,182 (76.28%) beneath dynamic binary work.
- Generated Go converts native `*Sample` arguments into `runtime.Value` and
  loses native `i64` arithmetic inside the scorer. Retained exact evidence
  shows the same erased captured-callable boundary in Manifest Normalization
  and Binary Event Log, clearing the three-unlike-application gate.
- Three bytecode profiles contributed 9.63 seconds and repeated only closed
  dispatcher, call/member, raw-integer, frame/return, cast, and lookup leaves.
- Refreshed the 132-row scorecard, 327.778105-second frontier, 23-closure
  ledger, workload-depth audit, interaction contracts, and downstream
  architecture chain. The compiled iterator/control group is now an open
  candidate with exact breadth three; the ledger has zero invalidations.
- The complete default runner passes every preflight and non-compiler
  package, all 34 compiler batches, and the bytecode fixture corpus. Removed
  1.2 GiB of disposable task workspaces, caches, and generated binaries after
  retaining compact evidence.
- Retained no compiler, runtime, interpreter, VM, stdlib, language,
  dependency, or WASM code change.
- Next prototype the general statically monomorphic captured-callable carrier
  rule. Infer a concrete carrier only when every static use agrees, preserve
  erased carriers at dynamic/conflicting/escaping/host boundaries, add
  positive and negative compiler guards, and run balanced five-or-more-process
  baseline/candidate/Go cohorts across Versioned Telemetry, Manifest
  Normalization, and Binary Event Log. This is important because it removes a
  proven compiled/runtime boundary without naming an application, `Result`,
  container, or non-primitive nominal type.
- Evidence:
  `docs/perf-baselines/2026-07-30-versioned-telemetry-pipeline-application-gate.md`.

## 2026-07-30 - Statically monomorphic captured-callable carrier retained

- Fresh local lambdas now receive a native callable carrier only when every
  statically known direct or nested use agrees on one fully bound signature.
  Unknown, conflicting, stored, escaping, host, runtime-service, function, and
  iterator boundaries remain on the erased carrier.
- Versioned Telemetry, Manifest Normalization, and Binary Event Log lower
  their admitted callables to native `Sample`, `ManifestRecord`,
  `EventRecord`, and `i64` carriers. The rule contains no application,
  `Result`, container, or non-primitive nominal-name branch.
- Balanced public-verifier A/B cohorts improve wall time by 96.09%, 37.11%,
  and 57.08%; exact allocated bytes fall by 98.73%, 60.76%, and 84.97%.
  Every strict dependency graph remains interpreter-free.
- The standard scorecard now records the three compiled rows at 9.95x, 9.17x,
  and 9.28x equivalent Go. Manifest's short standard process measurement is
  noise dominated, while its ten-run monotonic cohort and independent exact
  allocation counters both show the retained improvement.
- Native, nested callback, generic union, future-binding, dynamic, stored,
  conflicting, and execution guards pass. The complete Go semantic lane
  passes all non-compiler packages, 34 compiler batches, and the bytecode
  fixture corpus.
- The refreshed frontier contains one `refresh-required` group. The
  performance ledger intentionally reports 12 invalidated compiled/cross-mode
  closures, so the downstream structural-strategy gate remains fail-closed.
  No stdlib, runtime, interpreter, VM, language, dependency, or WASM source
  changed.
- Removed the exact 3,831,624 KiB disk-backed task workspace after preserving
  the compact verifier-backed reports, plus 28,476 bytes of task-created
  Python cache.
- Next census post-carrier reach and refresh current strict CPU/allocation
  ownership across at least three unlike applications, then review and
  advance only supported closures. This matters because the eliminated owner
  cannot select the next optimization and stale evidence must not be promoted.
- Evidence:
  `docs/perf-baselines/2026-07-30-statically-monomorphic-captured-callable-retained.md`.

## 2026-07-30 - Post-captured-callable compiled closure reconciliation

- The retained callable rule has exact current-source reach in Binary Event
  Log, Manifest Normalization, and Versioned Telemetry only. Three unlike
  zero-reach controls generate byte-identically to the frozen baseline.
- All six strict controls pass their public verifier with 96-package graphs
  that omit `able/interpreter-go/pkg/interpreter`.
- Ten main-only CPU profiles, three exact allocation-counter processes, and
  one exact allocation-shape process per reached application all verify.
  Exact allocation means are 9,321,773 bytes/171,069 objects for Binary,
  1,397,200/27,078 for Manifest, and 430,788,165/13,325,303 for Telemetry.
- Retained no new production change. Checked arithmetic reaches only two rows
  and is already closed. Allocation/GC is the sole common ancestor, while the
  nominal descendants have different escaping return/storage lifetimes and
  must preserve Able identity and alias-visible mutation.
- The 132-row frontier now has zero actionable groups. All 23 closure-ledger
  entries are current with zero invalidations, and the downstream architecture
  chain is regenerated and green.
- Updated the structural-strategy and interaction-triple contracts to the
  current 278.369263-second aggregate target excess.
- Next generate all 66 strict modules and census remaining erased/native
  boundaries by semantic mechanism. Join that reach to timing excess and
  existing profiles, and admit only an exact safe boundary material in three
  unlike programs. This directly advances native Go lowering without
  repeating closed runtime or nominal routes.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-captured-callable-compiled-closure-reconciliation.md`.

## 2026-07-30 - Full compiled static native-boundary census closed

- Added a report-only generated-Go analyzer and bounded runner, then generated
  all 66 selected compiled applications with `--no-fallbacks`. Every row
  completed within the 60-second guardrail; the slowest generation took 23.459
  seconds.
- The pass analyzed 7,145,082 generated Go lines while deleting each
  disposable module after its compact result was retained. It separates all
  emitted compiled bodies from a conservative direct-call graph rooted at the
  compiled `main`.
- The direct-main lower bound finds erased/dynamic calls in 62 applications,
  bridge encodes in 62, control/error conversions in 50, runtime struct
  conversions in 38, native union conversions in 41, and native interface
  adapters in 22. It finds no direct runtime-value constructor.
- Exact review admits no production candidate. The broad sites are explicit
  host I/O/argument calls, already-closed control/call routes, required
  scheduler-service conversions, or successful native union/interface
  lowering. Callable shapes lack three-unlike-application breadth; repeated
  nominal error encoders are family-specific cold paths.
- Retained only the report tooling and its evidence contracts. No compiler,
  generated runtime, runtime, interpreter, VM, stdlib, language, dependency,
  benchmark, or WASM performance behavior changed.
- The four-test census evidence contract, analyzer and compiler package tests,
  runner syntax, and CLI contract all pass. Removed the exact 337,268 KiB
  disk-backed census workspace after preserving the compact evidence.
- Next run one current correctness and release-readiness tranche, then keep
  production performance mutation paused until a concrete evidence identity
  invalidates a closed owner. This protects the native-lowering gains while
  avoiding a benchmark-specific or semantics-weakening prototype.
- Evidence:
  `docs/perf-baselines/2026-07-30-full-compiled-static-native-boundary-census.md`.

## 2026-07-30 - Extern plugin toolchain release readiness retained

- Generated Go extern plugins now build with the exact valid released Go
  toolchain that built the Able host. This fixes canonical stdlib plugin
  package-identity failures without an application, benchmark, container,
  nominal-type, compiler-lowering, or bytecode-VM special case.
- Focused released/development toolchain guards and generated-extern
  integration tests pass. The complete canonical stdlib suite passes in
  tree-walker and bytecode modes; `../able-stdlib` is unchanged.
- Repaired the sustained workload-depth evidence contract to derive current
  bounds and generically test its Go-reference duration rule.
- Five-run cold-cache affected-row refreshes move Base64 bytecode from 3.9740
  to 2.9760 seconds and JSON from 1.2080 to 0.8800 seconds. Both beat Python;
  JSON also beats Ruby and Base64 is 1.09x Ruby.
- The current 132-row scorecard has 66 compiled and 66 bytecode rows. The
  frontier has ten guards, 122 misses, zero actionable groups, and 277.200421
  seconds of aggregate target excess. All 23 closures are current.
- `go vet`, `go build`, the architecture budget, ordinary runner, 640 release
  audit partitions, and generated-Go CLI integration all pass. The selected
  125-test timing replay peaks at 24.32 seconds with no individual violation.
- Removed 30,448,352 KiB of confirmed inactive predecessor caches and the
  exact 28,929,240 KiB isolated task workspace after preserving compact
  evidence.
- Next refresh the v12 release-consolidation inventory without mutating Git or
  worktree contents. This matters because no performance owner is currently
  admissible, and the extremely dirty but verified state needs a deterministic
  record/review-boundary map before release consolidation.
- Evidence:
  `docs/perf-baselines/2026-07-30-extern-plugin-toolchain-release-readiness-retained.md`.

## 2026-07-30 - Post-readiness release consolidation inventory retained

- Captured an immutable 148-path pre-record snapshot at converged local
  `HEAD`/`origin/master` `418886c7` with an empty index.
- Classified 107 retained paths across the performance-direction, workload,
  telemetry, captured-callable, reconciliation, boundary-census,
  release-readiness, and handoff stages.
- Held the byte-identical 34-path deferred WASM boundary outside the candidate,
  excluded seven `v12/__pycache__` files as generated local state, and found
  zero unmatched paths.
- Added the exact 148-row TSV manifest and Markdown/JSON records. The resulting
  post-record candidate contains 110 paths.
- All formatting, syntax, whitespace, final-newline, source-size,
  secret-pattern, Git, current-evidence identity, and deferred-boundary checks
  pass. No dirty maintained source reaches 1,000 lines.
- The non-mutating cleanup preview found 1,742,116 KiB across four guarded
  generated paths. No project path was deleted in this tranche.
- Retained no production, benchmark measurement, fixture, stdlib, dependency,
  frozen-workspace, or WASM behavior change.
- Next apply the guarded generated-local cleanup after verifying no selected
  cache has an active owner, then reproduce the 110-path candidate and
  unchanged 34-path deferred complement without staging. This is important
  because only reproducible local noise should be removed before a separately
  authorized release operation.
- Evidence:
  `docs/perf-baselines/2026-07-30-post-readiness-release-consolidation-inventory.md`.

## 2026-07-30 - Guarded generated-local cleanup retained

- Reproduced the exact four-target, 1,742,116 KiB preview from the release
  inventory.
- Verified that every selected path is repository-local, contains no tracked
  files, and has no active process or open-handle owner.
- Applied only the guarded cleanup script. It removed the two empty temp
  directories, the 1,742,064 KiB Go cache, and the 52 KiB Python cache. The
  follow-up cleanup preview is empty.
- The seven visible Python bytecode files disappeared. The post-cleanup
  pre-record worktree is exactly the 110-path release candidate plus the
  unchanged 34-path deferred WASM hold.
- Candidate path and content identities, all deferred hashes, formatting,
  JSON/Python/shell/JavaScript syntax, whitespace, source-size, current
  performance-evidence identities, cleanup-policy, Git, and empty-index gates
  pass.
- Adding the Markdown and JSON cleanup records yields an exact 112-path
  candidate. Retained no production, measurement, fixture, stdlib,
  dependency, frozen-workspace, or WASM behavior change.
- Next obtain explicit authorization before staging exactly those 112 paths
  and creating one local consolidation commit. This is important because the
  reviewed candidate can be consolidated without the 34-path deferred WASM
  complement only through an exact, separately authorized operation.
- Evidence:
  `docs/perf-baselines/2026-07-30-guarded-generated-local-cleanup-retained.md`.

## 2026-07-30 - Exact 112-path local consolidation authorized

- The maintainer authorized staging exactly the reviewed 112-path candidate
  and creating one local commit from `418886c7`.
- The operation uses the exact 7,868-byte NUL pathspec and forbids broad
  staging. Its staged set must contain all and only the candidate paths and
  have zero intersection with the 34 deferred WASM paths.
- The local commit consolidates the verified performance model and evidence,
  telemetry application, captured-callable carrier, compiled boundary census,
  extern toolchain correction, release inventory, and cleanup records.
- No push, reset, revert, history rewrite, broad `git add`, or WASM mutation is
  authorized.
- Next verify the exact one-commit divergence, parent, tree, 112-path boundary,
  empty index, and unchanged deferred complement, then obtain explicit
  authorization before publication. This is important because the protected
  WASM boundary and any unintended ref must remain outside shared history.
- Governing evidence:
  `docs/perf-baselines/2026-07-30-guarded-generated-local-cleanup-retained.md`.
