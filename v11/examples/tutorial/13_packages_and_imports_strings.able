package tutorial_packages_strings

private fn suffix() -> string { "!" }

fn shout(text: string) -> string { `${text}${suffix()}` }

fn make_label(prefix: string, n: i32) -> string { `${prefix}-${n}` }
