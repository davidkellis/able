{
  "name": "interpreter10",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "license": "EPL-2.0",
  "devDependencies": {
    "@types/bun": "latest",
    "tree-sitter": "^0.22.4",
    "web-tree-sitter": "^0.25.10"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test",
    "export:fixtures": "bun run scripts/export-fixtures.ts",
    "test:fixtures": "bun run scripts/run-fixtures.ts",
    "run:module": "bun run scripts/run-module.ts"
  }
}
